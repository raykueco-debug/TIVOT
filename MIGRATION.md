# 聖約第四騎士團 — 拆分與搬遷方案

> 本文件是「單檔 → 可擴充目錄」重構的藍圖與交接依據。
> 分兩部分:**第一部分已完成(資源外置)**,**第二部分待執行(程式模組化,採整檔重寫)**。

---

## 0. 為什麼要拆(決策背景)

1. **防拆解 / IP 保護**:單檔一被「另存網頁」,整套設計、數值、`GAME_CONFIG` 全裸露,別人下載即帶走全套。這是 EOS IP 一環、日後要商業化的作品該有的基本防護。
2. **可擴充**:要長期做舞台/角色/敵人/技能/武器擴張,單檔會愈改愈難。
3. **上線即目錄**:試玩版上線時就必須是目錄結構,不能是單檔。

---

## 第一部分:資源外置(✅ 已完成)

11 張 base64 圖片已從 `index.html` 抽出為獨立檔,`index.html` 由 ~2MB 降至 ~121KB。

### 目錄結構

```
index.html                    ← 系統本體
resources/                    ← 圖片資源,依「功能」分目錄
├─ inspector/   監察官         Freya_SI_01.jpg
├─ partner/     搭檔           Luna_SI_01 / Luna_CI_saint / Luna_CI_advent /
│                             Luna_CI_obe / Luna_CI_maxburst /
│                             Renee_CI_guard / Renee_CI_return
├─ enemy/       敵人           Faceless_EN_01 / Witch_EN_01 / Belinda_CI_boss
├─ weapon/      武器(預留,無圖)
├─ vfx/         特效(預留,無圖)
├─ background/  背景(預留,無圖)
└─ misc/        其他(預留,無圖)
```

### 命名規則

`角色_類型_編號.副檔名`
- `SI` = 立繪(Standing Illustration)
- `CI` = cut-in
- `EN` = 敵人立繪
- 例:`Freya_SI_01`、`Luna_CI_maxburst`、`Belinda_CI_boss`

### 分類原則(已拍板)

- 聖徒化 cut-in(saint / advent / obe / maxburst)四張都是 Luna → 歸 **partner**(Luna 是搭檔變身)。
- 蕾妮 cut-in(guard 即死防禦 / return 生命歸還)→ 歸 **partner**(蕾妮=搭檔)。

### 音效 / 音樂 / 視覺特效 → 不外置

- 音樂、音效:Web Audio API 即時合成,無檔案。
- 視覺特效:CSS `@keyframes`(38 組)+ JS 生成,無檔案。
- `weapon / vfx / background / misc` 為預留目錄,日後若有實體圖檔再放。

### 換圖方式

圖片鑰匙集中在 `index.html` 的 `ASSETS`。換圖 = 替換 `resources/` 對應檔案(同名同副檔名),或改 `ASSETS` 中路徑字串。路徑可填相對路徑或網址。

---

## 第二部分:程式模組化(⏳ 待執行,採「整檔重寫」)

### 目標結構(B 方案:完整功能模組化)

程式邏輯依 **六大功能** 各自分檔,而非單一 `game.js`:

```
index.html          ← 用 <script type="module"> 串接
style.css           ← 抽出的 CSS
config.js           ← GAME_CONFIG(所有資料:舞台/敵人/武器/技能)
state.js            ← 集中的共享狀態 + 存取契約(重寫核心,見下)
modules/
├─ combat.js        戰鬥面板(舒爾特核心、盤面、輸入、計時)
├─ saint.js         聖徒化(降臨/OBE/Maximum Burst/回血推進)
├─ weapon.js        武器(MG/散彈/狙擊,vfx/判定差異)
├─ partner.js       伙伴(Luna 變身、蕾妮即死防禦/生命歸還)
├─ inspector.js     監察官(芙蕾雅:好感度、對白、結算立繪)
└─ enemy.js         敵人(立繪、大絕排程、受擊特效、Boss)
resources/          ← 第一部分已完成
SPEC.md             ← 行為權威描述(驗收基準)
```

### 為什麼「整檔重寫」而非「在舊檔上改」(已拍板)

現有 `index.html` 是 17 版疊出、~2000 行,有數十個**模組級共享 `let` 變數**(`playerHp` / `saintMode` / `combo` / `boardIndex` / `enemyHp`…),幾乎每個函式都直接讀寫它們。

- **在舊檔上增量拆** = 一邊拆牆一邊住人,每步都可能踩到隱藏耦合且不易當場發現。
- **整檔重寫** = 拿「已知正確」的現有行為當驗收標準,重建乾淨結構去對齊。對齊比拆解好驗證。
- 心態:**框架一次性寫好,有偏差對照 SPEC 逐項修**;不追求一次每個數值都對,追求結構乾淨、行為對齊。

### 瀏覽器約束(決定架構)

多檔 JS 要能跑,採 **`<script type="module">` + 靜態託管**。
- 代價:不能雙擊 `file://` 開,必須經 http 伺服器或上傳靜態空間(GitHub Pages / Netlify / itch.io)。
- 這代價「上線託管」本就接受,故無需引入打包工具(Vite 等)。

### 驗收標準:手感等價(已拍板)

不只是「功能等價」(系統都在、規則對),而是 **手感等價**——連隱性時序、節奏、微妙推進都要跟現行版本一致。理由:現行版「是能往外丟的完成度」,其手感即成品手感,重寫不得弄跑。

⚠️ **重寫頭號風險**:程式裡有大量**不在 SPEC、但玩家有感的隱性行為**(cut-in 後敵停 3 秒、聖徒化推進手感、延時/按錯懲罰判定門檻、combo 節奏…)。SPEC 若只寫設計意圖,重寫版手感會跑。故 SPEC 必須含隱性行為(見交接鏈)。

---

## 第三部分:重寫的交接鏈

```
[對話 A]  丟現行 HTML → 產出「含隱性行為的 SPEC」
   │        指令要求 A 挖出:時序/延遲、聖徒化推進與回血手感、
   │        延時/按錯懲罰門檻、combo 節奏、cut-in 前後敵人行為,數值標出
   ▼
[本對話者]  拿 SPEC + 現行 HTML →
   │        (1) 產出「模組邊界圖 + 共享 state 契約」(SPEC 不含,需讀程式耦合)
   │        (2) 打包成「給重寫者的作業要點」
   ▼
[對話 C]  拿 作業要點 + SPEC + 現行 HTML(當行為基準) → 執行整檔重寫
```

### 給對話 A 的指令(產 SPEC 時附上)

> 讀完這份 HTML,產出一份 SPEC,但**不要只寫設計意圖**。把程式碼裡**實際會發生、但可能沒被寫進設計文件的隱性行為**也全部挖出來——特別是:各種時序/延遲(誰觸發後停多久)、聖徒化的推進與回血手感、延時/按錯懲罰的判定門檻、combo 節奏、cut-in 前後的敵人行為。數值都標出來。這份 SPEC 要當「重寫的驗收基準」,寧可囉嗦,不要漏掉手感相關細節。

### 回到本對話者時,請一併帶回

1. 對話 A 產出的 **SPEC**。
2. 拆圖片後的 **最新 121KB `index.html`**(畫模組邊界圖需看程式實際耦合;SPEC 講意圖,程式碼講真相)。

---

## 附:未來的線上功能(已設計定案,非本次重寫範圍)

重寫時 state 結構請預留,但**本次不實作**:

- **Ghost 對戰**:只記「每盤點擊頻率」(5 個數字),對戰時用頻率生成槍聲+隨機彈孔,無聖徒化/無雙槍,純舒爾特五戰三勝取兩盤。**不重播輸入軌跡**(刻意繞開輸入時序精準重播的技術債)。
- **排行榜**:各關卡完成秒數,有聖徒化有雙槍,秒數決勝。
- 兩者共用一個零維運後端(Supabase 類),**不做防作弊**(階段性取捨)。
- 開發順序:榜(前端假榜)→ 自我 ghost 對戰(純前端)→ 接後端。每階段可獨立出貨、可喊停。
