# 美術 → 程式 交接（2026-09-25，米夏九張重做，ver -1728 之後）

> Ray：「交接給程式」。⚠ 美術 session 一行程式都沒碰（鐵律 11）。下面**只有一件事**：
> `script/speakers.js` 的 `ART.misha.expr` 改九行。圖已入庫並推上（commit `49aab57`），
> 原 PNG 在 `_originals/si/misha_si_<key>_gpt_20260925.png`，舊 webp 在 `_recycle/`。
> 細節與理由在 `resources/si/_misha_program_worklist.md` **附四**；做完把本檔刪掉或標成已接。

## 九行照抄（`script/speakers.js` 約 1481～1502 行，現行 → 改成）

⚠⚠ 全部是**同名覆蓋** ⇒ 版號一律要跳（§5 快取）。
⚠⚠ `frown`／`close`／`frontshock` 是**重畫**（以 front 為底），頭的位置與舊圖不同 —— `top`/`bot`/`fx` 都要換，不是只跳版號。
   `frontshock` 頭轉正直視觀者，臉中心 0.444（front 是 0.393，差 52 px，是真的移了）⇒ 要從字串縮寫改成物件。
⚠ `…open` 那四張是「以新底圖為底只改嘴」，取景照抄各自的新底圖（同一組數字）。

```js
    stare:      { src:'resources/si/misha_si_stare.webp?v=2',      top:11, bot:1527, fx:0.450 },   // -1729 護弓照 draw
    frown:      { src:'resources/si/misha_si_frown.webp?v=3',      top:1,  bot:1535, fx:0.394 },   // -1729 以 front 重畫，刀 0.67
    draw:       { src:'resources/si/misha_si_draw.webp?v=4',       top:7,  bot:1535, fx:0.434 },   // -1729 臉照 front（嚴肅閉嘴）
    close:      { src:'resources/si/misha_si_close.webp?v=3',      top:0,  bot:1535, fx:0.389 },   // -1729 以 front 重畫，自然站姿
    frontshock: { src:'resources/si/misha_si_frontshock.webp?v=2', top:0,  bot:1535, fx:0.444 },   // -1729 以 front 重畫，直視觀者（頭轉正，fx 不同於 front）
    closeopen:  { src:'resources/si/misha_si_closeopen.webp?v=2',  top:0,  bot:1535, fx:0.392 },   // 照 close
    drawopen:   { src:'resources/si/misha_si_drawopen.webp?v=3',   top:6,  bot:1535, fx:0.434 },   // 照 draw
    frownopen:  { src:'resources/si/misha_si_frownopen.webp?v=2',  top:0,  bot:1535, fx:0.394 },   // 照 frown
    stareopen:  { src:'resources/si/misha_si_stareopen.webp?v=2',  top:11, bot:1527, fx:0.451 },   // 照 stare
```

| 鍵 | 現行 | 改成 |
|---|---|---|
| `stare` | 無版號，13/1526/0.449 | `?v=2`，11/1527/0.450 |
| `frown` | `?v=2`，11/1526/0.469 | `?v=3`，1/1535/0.394 |
| `draw` | `?v=3`，6/1534/0.434 | `?v=4`，7/1535/0.434 |
| `close` | `?v=2`，14/1509/0.469 | `?v=3`，0/1535/0.389 |
| `frontshock` | 字串縮寫（沿用 front 取景） | 物件，`?v=2`，0/1535/**0.444** |
| `closeopen` | 無版號，照舊 close | `?v=2`，0/1535/0.392 |
| `drawopen` | `?v=2`，6/1534/0.434 | `?v=3`，6/1535/0.434 |
| `frownopen` | 無版號，照舊 frown | `?v=2`，0/1535/0.394 |
| `stareopen` | 無版號，照舊 stare | `?v=2`，11/1527/0.451 |

## 兩行註解要改（不改會騙下一個人）

- 1475 行附近「`…open`／`frontgrit`／`frontshock` 是只換臉的表情差分 ⇒ 取景照抄底圖」——
  **`frontshock` 拿掉**（它現在是重畫、頭轉正）；`frontgrit`／`frontopen` 仍照 front。
- 1474 行「同名覆蓋過的六張（draw／wound／close／frown／talk／order）掛 `?v=2`」—— 已過期，改成「版號逐張看那一行」即可。

## 驗收（8123 或 8200 看畫面就好）

- 切 `close`→`closeopen`、`frown`→`frownopen`、`draw`→`drawopen`、`stare`→`stareopen`：臉不跳位。
- `front`→`frontshock`：頭會轉正（那是刻意的），身體不跳。
- 沒帶新版號的話畫面上是**舊圖**、沒有任何錯誤訊息 —— 看到「好像沒換」先查 `?v=`。

## 不必接、等 Ray 決定的
- `stare`／`stareopen` 刀長 0.77（規格上限 0.74）；刀長不合格剩 `side`／`talk`／`guard`／`back`。
- 閉嘴攻擊版 `drawclose`（Windows 那台的 `_originals/`）用途未定。
