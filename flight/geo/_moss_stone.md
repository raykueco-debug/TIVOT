# `flight/geo/moss_stone.webp` — 苔石牆材質（256×256，四邊無縫）

> Ray（2026-09-13）：「四邊可無縫平鋪、正面平視的石面…大塊的風化石灰岩砌石，
> 砌縫細而不深…苔痕與地衣約佔三成…底色冷灰白 ≈ (180,183,176)（我從 `Forest_Cliff_Day` 量的）
> …⛔ 不要方向性的紋理（它會被貼在牆與頂面兩種方向上）」→「讓 gpt 生個 pixel tile」

**產線**：ChatGPT 生 1254×1254 → `tools/tile_seamless.py` 做無縫＋校色 → 256×256 webp
**原圖**：`resources/_originals/geo/moss_stone_raw.png`（gitignore）
**採用版**：**v3 —— 照 Ray 自己寫的那一版提示詞**（見 §三）

---

## 一、驗收

| 項 | 值 | 判準 |
|---|---|---|
| 尺寸 | 256×256（同 `castle_wall`／`flight_ruins`／`rock_grey`） | —— |
| 平均 RGB | **(179,183,176)** | Ray 給的 (180,183,176) ✔ |
| **接縫 左右／上下** | **11.43 ／ 10.27** | **內部相鄰 11.06 ／ 12.71** —— 跨過接縫不比往旁邊走一個像素更跳 ✔ |
| **方向性** 上半−下半 ／ 左半−右半 | **1.67 ／ −1.08** | 滿分 255，等於沒有上下之分 ✔ |
| 苔／地衣覆蓋 | **29.2%**（G−B>10） | Ray 要「約三成」✔ |
| 平均彩度 | 0.049 | 低彩度的冷灰白 ✔ |

## 二、⚠⚠⚠ 圖像模型給的「無縫」不是真的無縫

**一定要在本機再做一次**，不要相信提示詞裡寫了 seamless：

| | 接縫 左右／上下 | 內部相鄰 | |
|---|---|---|---|
| GPT 原圖 v1 | 11.95 ／ 11.55 | 6.95 ／ 7.92 | ✘ 接縫是內部的 1.5~1.7 倍 |
| GPT 原圖 v2 | 16.10 ／ **21.53** | 7.16 ／ 8.04 | ✘ 上下差 2.7 倍 |
| GPT 原圖 v3 | 12.99 ／ 10.83 | 7.60 ／ 8.06 | ✘ 同樣有縫 |
| **處理後 v3** | **11.43 ／ 10.27** | 11.06 ／ 12.71 | ✔ |

⚠ **判準是「接縫 vs 內部相鄰」不是絕對值**：一張細節多的材質，內部本來就跳得多。
拿固定門檻去判，細材質永遠不及格、平材質永遠及格。

### 作法（`tools/tile_seamless.py`）—— border cross-fade

① 左邊那一帶與**右邊那一帶**交叉淡化（raised cosine，不是線性）② 把右邊那一帶**砍掉**。
砍完之後新的第 0 欄＝原圖第 (W−bw) 欄、新的最後一欄＝原圖第 (W−bw−1) 欄，
兩者在原圖裡本來就相鄰 ⇒ 必然連續。上下同理。**代價是圖會縮一個帶寬**（現行 22%）。

⚠⚠ **第一版我寫成「左右兩帶各自往對方靠」，接縫從 12 變成 35** ——
那只是把兩邊都弄糊，兩條邊界的值**仍然不相等**。**「看起來糊掉」不等於「接得起來」。**
⚠ 權重用 raised cosine 不用線性：線性在帶的兩端有折角，會留下兩條淡淡的直線。

## 三、⚠ 三版的差別（Ray 的提示詞贏在哪）

| | 苔覆蓋 | 苔長什麼樣 |
|---|---|---|
| v1（我寫的） | 16.6% | 碎小的斑，散在石面中央 —— **太乾淨** |
| v2（我補的「加量」） | 30.3% | 量夠了，但是**大團的軟邊雲塊**，讀起來像髒污不像地衣 |
| **v3（Ray 寫的）** | **29.2%** | **沿砌縫與石塊邊緣長起再往中央擴散**；疏密不均（有的石頭幾乎蓋滿、有的乾淨）；**不規則鋸齒狀輪廓** |

⇒ 差別不在「要多少」而在**「怎麼長」**：只寫覆蓋率，模型會給你一團軟邊的雲；
寫出**生長的路徑（先沿縫、再擴散）與邊緣的形狀（鋸齒不是柔邊）**，才會像地衣。
**同 §5 那條通則：規格要寫成「怎麼畫」，不要寫成形容詞或數字。**

## 四、程式端要接的（**還沒接，這裡只交檔**）

1. `RUIN_ART.muyak.tex` 由 `'castle_wall'` 改成 `'moss_stone'`
   （木雅克神殿的色票就是 `mossgrey`，而這張的底色正是從它的參考圖量的）。
   ⚠ `roofTex` 要不要一起換由程式端判斷 —— Ray 這一張是「石面」，沒說屋頂。
2. **新檔不必進 `RUIN_TEX_VER`**（那張表是同名覆蓋的版號）；
   ⚠ 日後**同名覆蓋這一張**才要加 `moss_stone:2`，否則瀏覽器會抱著舊的不放（§5）。
3. GL 那一路（`glRuinTexture`）吃的是同一個名字，不必另外處理。
