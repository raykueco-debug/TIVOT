# CLAUDE.md — 聖約第四騎士團 / Saint Install 模組化重寫

> 本檔是本專案的憲法。每個 session 開工前必讀。任何改動不得違反此處鐵律。
> 任務性質:把「單檔可循環 HTML 原型」**整檔重寫**成可擴充的模組化目錄,**行為與手感須與 `reference/index.html` 完全等價**。

---

## 0. 鐵律(不可違反)

1. **資料/程式分離**:所有遊戲內容(武器/敵人/搭檔/監察官/評價/數值/盤面)集中在 `config.js` 的 `GAME_CONFIG`。程式碼不得寫死任何內容數值——一律讀 config。
2. **共享狀態集中**:所有跨模組的可變狀態集中在 `state.js`。模組間不得各自持有散落的全域變數(這正是舊單檔的病灶)。存取規則見第 3 節。
3. **手感等價是驗收標準**:不是「功能都在」就算過,是連時序、延遲、推進手感、判定門檻、combo 節奏都要與 `reference/index.html` 一致。判斷有疑義時,**以 `reference/index.html` 的實際行為為準**,SPEC 為輔。
4. **reference 唯讀**:`reference/index.html` 是「已知正確」的行為基準,**永不修改**,只讀來比對。
5. **框架一次性建,偏差逐項修**:先把目錄骨架與 state 契約立好,再逐模組填。不追求一次每個數值都對,但每個模組完成都要對照 SPEC/reference 校驗。
6. **每個模組完成即 commit**(見第 6 節 Git)。
7. **⚠⚠ 一個量只能有一個計算點(單一真相)** —— 本專案**同一類錯誤已重犯三次**,列為鐵律。
   任何**解出來或量出來**的量(幾何、尺寸、時間、音量、門檻),**只准有一處計算**;
   其他地方一律**讀**它(CSS 變數 / 匯出常數 / 快取物件),**不得用「等價的式子」重算一次**。

   已發生的三次(全部都是「兩處各算一次,式子看起來等價,實際上只在某個比例下才相等」):

   | 版 | 症狀 | 兩個計算點 | 何時才相等 |
   |---|---|---|---|
   | `-343` | 楣被槍棺吃掉 | 門的行程 `--kerb-rise` vs clip 的行程「面板頂−楣高」 | 門頂＝面板頂時 |
   | `-352`/`-354` | 立繪忽大忽小 | 戰鬥用 `#top`/`#app` 的高 vs 劇情用 `#storyCast` 的高 | 兩個框同高時 |
   | `-364` | 楣的上緣被裁掉 | clip 起點「面板頂−楣高」 vs 楣的實際上緣(由 `layoutKerberos` 解出) | 同 `-343` |

   **作法**:算的那一支把結果**發佈出去** —— 寫進 CSS 變數(`--kerb-clip-top`)、
   匯出常數、或存進快取物件(`cam`);其他人只讀。
   **改動時的自檢**:動任何幾何/時間/音量常數之前,先 `grep` 那個數字或那條式子,
   **確認全專案只有一處**。找到第二處就是 bug,不是巧合。
   ⚠ 真的必須出現在兩邊時(例如 CSS 與 JS 都要用同一個數字,像 `bgmVol.bgm_crisis` 的
   0.62 與 `story.js` 的 0.62),**兩邊的註解要互指**,寫明「改一邊要改另一邊」。
8. **⚠⚠ 規矩寫成「腳本要怎麼寫」是不夠的 —— 要寫成**引擎的一支函式**,然後所有路徑都呼叫它。**
   ver -370 的教訓:「換場要清場(立繪是持續狀態)」早就寫進 §6.5,但那是寫給**腳本**的
   (第一拍要 `show:false` / `hide`)。城鎮探索是**新的程式路徑**,不經過 scene 渲染,
   於是整條規矩被繞過去,前一個地點的立繪就留在新的背景前面 —— Ray:「不是寫入原則了嗎?」

   **作法**:凡是「每次 X 都要做 Y」的規矩,把 Y 做成**唯一的一支函式**
   (例:`story.clearCast()`),規矩改寫成「**任何做 X 的地方都必須呼叫它**」。
   **新增任何一條進入/切換畫面的路徑時,先問:既有的演出規矩有哪幾條?我呼叫了嗎?**
   ⚠ 這與鐵律 7 是同一件事的兩面:7 說「一個**量**只有一個計算點」,
     8 說「一個**動作**只有一個實作」。

9. **⚠⚠ 旗標與狀態鑰匙：「插了以後被拔之前不動，要拔旗只有單一事件能拔」**
   （ver -563，Ray 原話，在「又變 stage3／直接變 stage5」連環案之後定為鐵律）。
   - **不准讓「鑰匙不存在」參與遊戲邏輯**。查詢層的預設值（`STAGE_DEFAULT`、
     `AFFECTION_DEFAULT`、`NAME_BEFORE`）只准給**沒跑主線**的情境（試玩版／試飛）
     當顯示用假定值 —— 主線一旦開始，所有主線鑰匙都必須**真的插著**
     （`newRun()` 收尾寫 `stage 0`，就是這一條的落地）。
   - **每一支旗/鑰匙都要能回答**：「誰插的？誰（唯一的那個事件）能拔/改？」
     答不出來就不要加。改值的守門看**值**（`===0` 才升 1），不看有沒有旗。
   - **快照存鑰匙的原始狀態**（null＝不存在，restore 原樣移除）——
     預設值只活在查詢的那一刻，永遠不准被物化進存檔（ver -561 的教訓）。
   - 這與鐵律 7/8 同族：7＝一個量一個計算點、8＝一個動作一個實作、
     9＝**一個狀態一個擁有事件**。

---

## 1. 目錄結構(目標)

```
index.html          <script type="module"> 掛載,串接以下
style.css           全部 CSS(原單檔 <style>)
config.js           GAME_CONFIG(所有內容資料)+ ASSETS(圖片路徑鑰匙)
state.js            集中共享狀態 + 存取器(見第 3 節)
audio.js            SFX(Web Audio 合成,IIFE 改模組)
modules/
├─ combat.js        戰鬥核心:Schulte 盤面、輸入判定、計時、傷害計算
├─ defense.js       三級防禦:大絕紅點生成/縮放/判定(Defense/Perfect/Counter)
├─ saint.js         聖徒化:降臨/推進/三結局(MaxBurst/OBE/生命歸還)
├─ weapon.js        武器:雙槍破防、Counter 反擊演算
├─ partner.js       搭檔:即死防禦、生命歸還觸發
├─ inspector.js     監察官:評價計算、對白、結算面板
└─ enemy.js         敵人:立繪載入、大絕排程、受擊特效、Boss(亂入)
resources/          圖片(已完成,見第 5 節)
reference/
└─ index.html       現行完整版(唯讀行為基準)
SPEC.md             行為規格
```

**注意**:SPEC 原提六模組(戰鬥面板/聖徒化/武器/伙伴/監察官/敵人),我在重寫時把「三級防禦」自戰鬥核心獨立為 `defense.js`——因為它是自成一體的紅點判定系統,獨立後 combat 更乾淨。這是唯一對 SPEC 模組劃分的調整,已納入下方邊界圖。

---

## 2. 模組邊界圖(誰依賴誰)

依賴方向由上而下,**不得出現反向或循環依賴**:

```
        state.js  ◄──────────── 所有模組讀寫狀態的唯一來源
        config.js ◄──────────── 所有模組讀資料的唯一來源
        audio.js  ◄──────────── 純輸出(被呼叫播音),不依賴他人
           ▲
           │(以上為底層,無業務邏輯)
   ┌───────┴─────────────────────────────┐
   │              combat.js               │  戰鬥主循環,協調者
   │  呼叫→ defense / weapon / saint /     │
   │        enemy / partner / inspector    │
   └───┬────┬────┬────┬────┬────┬─────────┘
       │    │    │    │    │    │
   defense weapon saint enemy partner inspector
       │         │              │
   (回報判定) (聖徒化狀態)   (即死防禦攔截)
```

**關鍵原則**:`combat.js` 是唯一的「協調者」,其他模組彼此**不直接互相 import**,一律透過 `state.js` 溝通或由 combat 調度。這樣切斷了舊單檔那種「人人讀寫 saintMode」的網狀耦合。

---

## 3. 共享狀態契約(state.js)

舊單檔約 40 個模組級變數散在全域。重寫時全部收進 `state.js`,分組如下。**標註「擁有者」= 唯一有權寫入的模組;其他模組只讀。** 這是解耦的核心。

### 3.1 戰鬥核心(擁有者:combat)
`boardIndex, N, cols, intervalLimit, intervalDeadline, cells, order, expect, combo, energy, boardStartTime, boardClean, transitioning, boardTimes, boardsCompleted, runStartTime, killTime`

### 3.2 生命/敵我(擁有者:combat;partner 可經攔截器改 playerHp)
`playerHp, playerMax, enemyHp, enemyMax, overkill, over, flawlessRun`
- ⚠️ `playerHp` 唯一的例外寫入:`partner.js` 的即死防禦(`tryDeathGuard`)在致死時把 playerHp 保留為 1。此為契約允許的攔截,須明確走 partner 的攔截函式,不得散寫。

### 3.3 三級防禦/大絕(擁有者:defense)
`threats, threatTick, ultCheckTimer, CHARGE_SECONDS, ULT_DAMAGE, ULT_SHOTS, ULT_GAP_MS, ULT_MIN, ULT_MAX, DELAY_PENALTY_SCALE, DELAY_TIME_DELTA, WRONG_PENALTY_SCALE`
- 後面那串大寫是「當前敵人的大絕參數」,由 `enemy.js` 設定敵人時寫入、`defense.js` 讀取執行。設定敵人是 enemy 的職責,執行判定是 defense 的職責。

### 3.4 武器/雙槍(擁有者:weapon)
`equippedWeapon, grenades, dualWield, dualTimer`

### 3.5 聖徒化(擁有者:saint)
`saintMode, saintTimer, saintUsedThisBattle, saintDamageDealt, saintReactTimer, saintPrevBoard, saintPrevUlt, enemyAtkSuppressUntil`
- ⚠️ `saintMode` 在舊檔被 31 處讀取(跨 combat/defense/enemy/weapon)。重寫後:**只有 saint.js 能寫 saintMode**,其他模組一律**讀** `state.saintMode` 來分支。這條契約若破,就退回舊單檔的病。務必守住。

### 3.6 評價/流程(擁有者:inspector)
`counterCount, counterDamage, perfectCount, sawExecution, sRankUnlocked, resultMode, currentFavor`
- `counterDamage/counterCount` 由 weapon 反擊時累加、inspector 結算時讀 → 允許 weapon 寫入這兩個計數(跨擁有者的唯一計數例外,須註明)。

### 3.7 亂入/Boss(擁有者:enemy)
`currentEnemyKey, curEnemyHitFx, intruderTriggered, inIntruderFight, deathGuardUsed`

### 3.8 增益(擁有者:combat)
`atkBuff, atkBuffTimer`

> **契約寫法建議**:state.js 匯出一個 `state` 物件 + 少量受控 setter(如 `applyDeathGuard()`、`enterSaint()`、`addCounter(dmg)`)。純讀走 `state.x`,跨擁有者的寫一律走具名 setter,禁止外部模組直接 `state.saintMode = ...`。

---

## 4. 函式歸屬對照(重寫時搬遷依據)

- **combat.js**: shuffle, goNextBoard, boardGridFor, loadBoard, buildGrid, fitGridSquare, markNext, tap, clearBoard, recordBoardTime, gunHitOnEnemy, floatDmg, startIntervalTimer, effIntervalLimit, resetIntervalDeadline, stopIntervalTimer, updateBars, updateStatus, hitDamage, triggerAtkBuff, clearAtkBuff, startGame, goHome, win, lose, stopAll, fmtTime
- **defense.js**: scheduleUlt, startCharge, updateThreats, stopThreatTick, releaseUlt, endCharge, spawnThreat, removeThreat, clearThreat, resolveThreat, flashDefense, resetEnemyTimers
- **saint.js**: saintAdvance, activateSaint, playSlash, startSaintReactTimer, clearSaintReactTimer, setReturnSwipe, startSaintMode, triggerMaxBurst, triggerOBE, activateLifeReturn, playSaintCutin, finishSaintMode
- **weapon.js**: activateDual, endDual, weaponCounter, addEnergy, updateUltBtn, updateEnergyClasp, refreshLoadoutLabels, openPickSheet, closePickSheet
- **partner.js**: currentPartner, tryDeathGuard
- **inspector.js**: computeEvaluation, getInspector, pickByThreshold, pickInspectorPortrait, pickInspectorDialogue, inspectorPanelHtml, combatStatsRows, showResultSequence, typeInspectorLine, showBanner, loadBestTotal, saveBestTotal
- **enemy.js**: enemyAttack, enemyDamage, showHitFx, triggerClaw, hitLayer, addFx, spawnBlood, spawnBite, spawnBullets, bulletSVG, shatterCell, ejectShell, triggerIntruder, loadEnemyPortrait, setEnemy, displayEnemyName, applyConfigToDOM
- **audio.js**: SFX(init/unlock/noise/env/gunshot/sniperShot/wrong/hit/clear/ultCharge/confirm/menuClick)
- **index.html / 啟動**: bindBtn, onRematchBtn, playCutin, cutin 綁定, 手勢綁定(showAura/hitThreatAt/滑動)

> 這是**建議歸屬**,重寫時若發現某函式跨模組更合理,可調整,但須維持第 2 節的依賴方向、不得製造循環。

---

## 5. 資源(已完成,勿重做)

圖片已外置至 `resources/`,依功能分:`inspector/`(Freya_SI_01)、`partner/`(Luna 立繪+聖徒化 cut-in、Renee cut-in)、`enemy/`(Faceless/Witch/Belinda)、`weapon/vfx/background/misc`(預留空)。命名 `角色_類型_編號`(SI=立繪/CI=cut-in/EN=敵人立繪)。`config.js` 的 `ASSETS` 已改為相對路徑;音效/特效為程式合成,無檔案。

**`resources/SI/` — 對話用立繪集中區（ver -305，Ray 指定「方便做差分」）**
四位女角在對白裡用的立繪一律放這裡,檔名 `角色_SI_變體.webp`,
一律 1024×1536。目前:

    Anya_SI_front / _side / _back
    Sorana_SI_front / _side
    Nouvelle_SI_front / _side / _back ＋ Nouvelle_Nun_SI_front（修女裝差分）
    Renna_SI_front
    Luna_SI_seat

- ⚠ **只放「對白裡會出現的立繪」**。戰鬥 cut-in（`Luna_CI_*`／`Renee_CI_*`／
  `Malzeno_CI_*`）留在 `partner/`,`Anya_CI_Search` 是探索 UI 也留著 —— 那些
  不是差分對象,混進來只會讓資料夾失去意義。
- ⚠ 換掉或新增任何一張,`flight/index.html` 的 `PORTRAIT` 與 `script/speakers.js`
  的 `SPEAKERS` **兩邊都要改**,而且 `eye/fx/top/bot` 四個取景值**必須重量**
  （見 §6.5「新增立繪要量什麼」）。沿用上一張的數字人一定會歪。
- ⚠ 露娜在對白裡目前借用 cut-in `partner/Luna_CI_exc.webp`（`speakers.js`
  標了 `unmeasured:true`）。`Luna_SI_seat` 是坐姿,還沒接進去。

**⚠⚠⚠ 回收區 ＝ 本專案唯一的「刪除」出口(ver -696,Ray：「以後不要刪我的東西,
在根目錄放一個回收區」)**
- 任何 session(程式的、美術的)要拿掉或**覆蓋掉**任何檔案,一律走 `tools/recycle.sh`
  —— **不准 `rm`,也不准用 `mv`／`cwebp` 就地蓋掉**。它只搬進 `_recycle/`,永遠不刪。

      tools/recycle.sh -m "為什麼" <檔案或資料夾>...   # 回收
      tools/recycle.sh --list                          # 看紀錄
      tools/recycle.sh --restore _recycle/<路徑>       # 放回原位

- 保留**原本的相對路徑**(`_recycle/resources/CI/x.png`),才知道它本來住哪、也才放得回去;
  同名再回收一次不覆蓋(加時間戳)。每一筆記進 `_recycle/RECYCLE_LOG.tsv`,
  **那份紀錄入版控** —— 「誰在哪一版拿掉了什麼」翻 git 就查得到。
- ⚠⚠ **它與版控不重疊,不是多此一舉**:git 只保護 **commit 過的**東西。
  ver -695 之後 Ray 回報一張自己畫的諾薇兒 OBE 不見了 —— 查下來那張圖**從來沒進過版控**
  (全機、`_originals`、git 全歷史都沒有),所以 git 一點忙都幫不上。
  回收區保護的正是**還沒 commit 的那一段**。
- ⚠ 內容只留本機(`.gitignore`),同 `_originals` —— 是「可回滾」不是「異地備份」。
- ⚠ **清空回收區是 Ray 的決定**,沒有任何 session 被授權清它。

**原圖備份的規矩(ver -357,Ray 指定「以後被做成 webp 的原 png 圖找個不會被讀的地方存好備份」)**
- 轉檔流程固定三步:①`cwebp` 出 `.webp` 到原位 ②`config.js`/`speakers.js` 指 `.webp`
  ③**原 PNG 移進 `resources/_originals/<同層資料夾>/`**,絕不留在會被載入的目錄裡。
- 為什麼是 `_originals`:底線開頭的資料夾**不會被遊戲載入**(同 `_master`/`_unused`),
  而且已在 `.gitignore` 裡 —— 不進版控、不上靜態空間,但本機留著可回滾。
- ⚠⚠⚠ **同名覆蓋的圖一定要加／改 `?v=N`**(ver -650)。瀏覽器會沿用舊的快取 ——
  檔名沒變、內容變了,它照樣拿舊的那一份。實測:美術把娜塔莉那兩張同名蓋回去之後,
  不帶 cache-buster 抓到的 `dead.webp` **像素指紋與 `dying` 完全相同**,
  於是換到 `dead` 那一拍畫面上還是舊圖 —— 讀起來就是「dead 立繪出不來」
  (Ray 連報三次,而 `src`／位置/程式全部是對的,查了四版才發現是快取)。
  · 路徑後面直接掛 `?v=2`(`config.js` 的 `sfx_saint` 早就這樣用)。
  · **一組差分要一起帶**:漏掉哪一張,哪一張就被快取住。
  · 這也是為什麼**新增比覆蓋安全** —— 換個檔名就沒有這個問題。
  · 自檢:改完圖之後在瀏覽器量一次像素指紋(8×8 平均 RGB),
    兩張不同的圖指紋一定要不同;相同就是快取。
- ⚠ **換圖(同名覆蓋)一定要重量取景值**:`top`/`bot`/`fx` 是**那一張圖**的數字。
  實測 ver -357 換的三張:`Luna_SI_seat_hand` 的 `bot` 由 1536 變 1489、`fx` 由 0.465 變 0.546
  —— 沿用舊值會歪掉一大截。量法見 §6.5「新增立繪要量什麼」。
- ⚠ **`_originals` 目前只在本機**(133 MB,gitignore)。它是「可回滾」不是「異地備份」——
  真的要防硬碟壞掉,要另外複製到雲端/外接;要不要改成入版控由 Ray 決定(repo 會多百餘 MB)。

⚠️ SPEC 提到 `imageBase` 走 `assets/inspector/freya/...` 舊路徑——重寫時統一改為 `resources/` 新結構,與已完成的目錄一致。

---

## 6. 執行約束與驗收

### 執行環境
- 多檔用 `<script type="module">`,**必須經 http 伺服器**(`python3 -m http.server`)或靜態託管,不能 `file://` 雙擊。上線走靜態空間(GitHub Pages / itch.io)。

### Git(重要 — 防止改壞丟工作)
- 開工第一件事:`git init` 並把 `reference/index.html` + 現有 resources commit 為初始版本。
- **每完成一個模組 commit 一次**,訊息註明模組名。改壞隨時可回。
- ⚠⚠ **改程式就順手把 `config.js` 的 `VERSION` 尾碼改成這次的 `ver -NNN`**
  (ver -626 立):它顯示在診斷 HUD(首頁連點團徽 5 下),是 Ray 在手機上確認
  「我現在跑的是哪一版」的**唯一**依據。-568 到 -625 這 57 版都沒動過它,
  於是 HUD 一直顯示 568,被讀成「快取卡住好久」——**版本號不動就等於沒有版本號**。
  ⚠ 它同時是**暖開機戳記的鑰匙**(`main.js` 的 `WARM_BOOT`):版本一變,上一版的
  戳記失效 → 下一次開機重跑完整讀取。改版後本來就該這樣。

### 驗收流程(沿用專案慣例)
1. 語法:Python regex 抽出各 JS(或直接 `node --check modules/*.js`)。
2. Runtime:Playwright 於 **390×844 viewport** 測試,對照 `reference/index.html` 逐項比對手感關鍵點:
   - 聖徒化推進手感(受擊 +1s / 格擋 +0.5s / 免傷不推進 / 無受擊 10s 回滿)
   - 三級防禦門檻(0.35 / 0.12 兩條界線)、散彈 Perfect 改傷、狙擊無 Perfect 帶
   - cut-in 後敵不發動 3 秒、聖徒化期間大絕更密集
   - Boss 雙發大絕(間隔 1s、2~4s 頻率)、延時懲罰半傷減 1 秒
   - combo 傷害斜率、MaxBurst 追加 20% 總傷、評價 S 解鎖亂入
3. 建議建一份 `ACCEPTANCE.md` 把上述關鍵點列成勾選清單,逐項對照 reference 打勾。
4. ⚠⚠ **瀏覽器測試的第一步永遠是「先點一次把開機讀取頁點掉」**（ver -570，Ray 定案）：
   等 COMPLETE → 點一下 → **聖光離場整段跑完、首頁穩定**之後,才准下任何程式化指令
   （`town.open`／`startScriptBattle`／`story.open`…）。開機交接期間搶跑會與
   聖光離場的收尾回呼競態 —— 症狀是「黑幕不掀」「首頁又蓋回來」「讀取頁卡 COMPLETE」,
   全是**測試自己造的假 bug**,玩家路徑到不了（ver -570 追過三輪才確認）。
   判斷穩定：`#home.on` 在、畫面是首頁、再等 1~2 秒。

### 開發順序(增量,每步可獨立驗)
1. 骨架:目錄 + `index.html` + `style.css` + `config.js` + `state.js`(空殼契約)。先能載入、顯示標題。
2. combat + enemy + defense:先讓「一般怪一場五盤」能打完、能結算。這是核心,先對齊手感。
3. weapon + saint:接雙槍與聖徒化三結局。
4. partner + inspector:即死防禦、生命歸還、評價與監察官結算。
5. Boss/亂入(enemy 內)、最佳成績存檔。
6. 全流程對照 reference 做 ACCEPTANCE 勾選,收尾。

---

## 6.5 對白演出通則(全域,ver -247 起)

任何畫面要演對白(戰鬥教學、飛行探索、日後的劇情段)一律照這一套,**不要各寫一份**。
基準實作:`modules/tutorial.js` + `style.css` 的 `#tutCast`(DOM 版)、
`flight/index.html` 的 `castEnter`/`drawTalk`(canvas 版)。

**站位與進場**
- 每個角色的站位(左/右)寫在角色資料裡(`side`),**不隨台詞變動** —— 同一個人每次都站同一邊,玩家才記得住誰是誰。
  - ⚠ **例外只能到「整幕」為止**(ver -360):一幕裡只有兩個人又剛好同側時,每換一個
    說話者就是一次滑出＋滑入,讀起來很忙 —— 這時可用 scene 的 `sides:{RENNA:'R'}`
    整幕覆寫。**不可以逐句換邊**(＝ver -288 被退回的發起位制:立繪朝向是畫死的,
    換邊要水平翻轉,髮旋與持物會左右顛倒)。
  - ⚠ 加覆寫前先看那一幕台上還有誰:覆寫成右而右邊已經有人(例如會客廳裡坐著的
    璐娜莉亞),兩個人會互相擠掉。
- 立繪從**自己那一側**滑入,450ms、ease-out。
- **說話的人先上場,接話的人輪到他那一拍才上場**。不要一開場就把所有人擺出來。
- 第三、四人上場時,**同一側的舊角先滑出、新角滑入**(輪轉換卡)。
- ⚠⚠ **兩個人同台就一定分站兩邊；非得同邊換人時,一律走抽牌輪轉**
  (ver -619,Ray：「人物要分站兩邊，如果同邊換人要用抽牌輪轉」)。
  - 「分兩邊」是**站位覆寫要負的責任**:把某人挪到另一邊時,先看**那一邊已經
    站著誰** —— 兩個都被指到同一側,就是「一個槽兩個人」,每換一句話就換一次人,
    讀起來是閃爍不是演出。實例:BOSS 那一場諾薇兒被指定站右,蕾娜(`cast.renna`
    預設也是右)就必須一起改成左(`talkSides:{nouvelle:'right', renna:'left'}`)。
  - 真的只剩同一邊可用時(第三、四人上場),**換人＝抽牌輪轉**:舊的滑出、新的滑入,
    不可以就地把 `src` 換掉 —— 就地換是「同一個人瞬間變成另一個人」。
  - ⚠⚠ **翻轉是這張畫的性質,不是站位的性質**(ver -625,Ray：「諾薇兒跟索拉娜左右是
    對稱的,可以水平翻轉;蕾娜原則右,碰到安雅就放左,因為蕾娜整體框細,受左右影響小」)。
    立繪朝向是畫死的,換邊要水平翻轉,而髮旋/單邊持物/不對稱制服翻了就錯 ——
    所以「這張可以翻」寫在**角色**上,**唯一的真相是 `script/speakers.js` 的
    `ART[key].mirror`**(預設不翻)。`config.tutorial.cast[key]` 只寫**站哪一邊**,
    不再自己記一份 mirror(鐵律 7:兩份必然走鐘,同一個人會「這邊翻、那邊不翻」)。

    | 角色 | 可翻? | 站位原則 |
    |---|---|---|
    | 諾薇兒 | ✔ | 本位左;要讓位時翻到右 |
    | 索拉娜 | ✔ | 本位右 |
    | 蕾娜 | ✘ | **原則右,碰到安雅就放左** —— 她的框細,換邊的影響小 |
    | 安雅 | ✘ | 本位右(不讓) |

    判定各一支,兩套 DOM 各一份(劇情頁 `story.castLayout` / 戰鬥對白
    `tutorial.applyMirror`):被擺到 `ART.side` 以外那一側**而且**可翻,才翻。
    翻轉之後**臉的錨點也要鏡射**(`fx → 1-fx`)—— 錨的永遠是臉不是圖框。
  - ⚠ **不能翻的人換邊就是換邊**(蕾娜):不要為了「不翻」而讓兩個人擠同一側 ——
    分兩邊優先於站位原則。
  - ⚠ **引導箭要落在台上那個人的另一邊**(生命歸還的上滑箭)。位置**現算**
    (`tutorial.stageSides()`),不要寫死「正中」或「左 1/4」—— 站位現在是資料,
    寫死的位置每換一次站位就要再改一次(-613 改過一次、-619 又一次)。
    ⚠ 判「台上有誰」讀**資料**不讀 DOM 的 `.in`:`.in` 是延後掛上去的滑入旗標,
      當場問一定問到上一個狀態(同「不要從畫面反推」)。

**明暗**
- 說話者原色；其餘 `brightness(.38) saturate(.75)`（與教學同值，不要另訂）。
**⚠⚠ 立繪的色調要跟著場景（ver -631／-632，Ray 指定寫進憲法）**

Ray：「人物色調要隨場景調整，安雅現在太亮」→「這邊立繪有點太暗了，調亮 30%」。

量**玩家現在看得到的那一層**的平均亮度 → 換算成一組濾鏡套在**立繪容器**上。
實作只有 `modules/tone.js` 的 `matchPortraits` 一支（鐵律 8），
係數全在 `config.tuning.portraitTone`（鐵律 1）。

- ⚠⚠ **「那一層」＝有插圖就是插圖，沒有才是背景**（`story.toneSrcEl()`）。
  插圖蓋著背景時拿背景去量，量到的是**看不見的東西**。插圖換掉要重算。
- ⚠⚠ **幅度要量過再訂，不要憑感覺鎖一個小範圍**。舊版鎖 ±12%，在很暗的場景
  等於沒調 —— 實測北方泊地教堂（戰損）平均亮度 **0.152**、安雅的立繪 **0.573**
  （差 3.8 倍），舊公式只給 `brightness(0.916)`，她就是一張貼紙。
- **形狀**：以 `ref` 為中性點的線性斜率（`gain`）＋上下夾（`min`/`max`）。
  中等亮度的場景幾乎不動（≈1.0），只有很暗／很亮才明顯偏。
  很暗的場景是被 **`min` 夾住**的，所以「太暗／太亮」多半調 `min` 不是調 `gain`。
  現行 `min:0.85`（教堂壓暗 15%）。
- ⚠ **套在容器上不是逐張立繪**：容器的濾鏡會與立繪自己的 `dim`／`dark` 自然疊加。
  代價是**兩者相乘** —— 現行 0.85 × 0.38 ≈ 0.32，非說話者已經很暗，
  `min` 再往下拉要先確認那一位還讀得出是誰。
- ⚠ 它與「非說話者壓暗」是**兩件事**：那是**誰在講話**的線索（句子屬性），
  這是**融進場景**（場景屬性）。不要拿其中一個去代替另一個。
- ⚠ 取樣縮到 16×16 再讀、同一張圖只算一次（快取）；跨網域污染 canvas 就放棄不調。
- ⚠ **壓暗必須是不透明的，不可以用透明度代替**（ver -277）。半透明會讓背景從人身上
  透出來，讀起來是「這個人快消失了」而不是「這個人在暗處」。
- ⚠ canvas 版**不能只靠 `ctx.filter`**：它在 Safari 18 以前整個不存在，而舊版的退路
  正是 `globalAlpha=0.45` —— iPhone 上看到的一直是半透明那一版。作法是把壓暗版
  **預先烘進離屏畫布**（畫一次立繪，再用 `source-atop` 蓋一層深灰，等於拿立繪自己的
  alpha 當遮罩），亮暗兩張都 1:1 貼上。順帶省掉逐幀對整張立繪套 filter 的成本。
  基準實作：`flight/index.html` 的 `buildDim`。

**依好感段位換台詞(ver -625)**
- 稿上寫「（T1）／（T2以上）」時,線上寫 `textByTier:{1:'…', 2:'…'}`。
- ⚠ 那兩個數字是**門檻不是等於**(同 `config.inspectors.dialogues` 的兩層查表):
  只寫 1 與 2 ＝「T1 一種說法、T2 以上另一種」,日後加一把 T4 的鑰匙,前面幾段自動沿用。
- ⚠ 看的是**這一句說話者自己的**好感段位 —— 那句話是她說的。
- ⚠⚠ 解析只有 `story.lineText()` 一支(鐵律 8):所有讀「這一句要印什麼字」的地方
  一律問它 —— 漏一處就會「打字打到一半換成另一句」。**不要把結果寫回 `line.text`**
  (那是資料模組上的常數,改了下次重播就跟著錯)。

**推進**
- **點畫面任何一處(按鈕除外)推進一句**,不自動跳拍。最後一句再點一下才收場。
- 框內給 `▼` 閃爍提示,否則玩家不知道要點。

**⚠⚠ 取景要跟著「畫面上真的是哪一張」走(ver -647,Ray：「娜塔莉說『安娜』的時候
位置跑掉了，此時應該就是用 dead 了，但圖還是 dying」)**
- 換圖是**延後**執行的(換表情 190ms、同側換人 203ms、首次 16ms,外加 `onload`),
  而取景的鑰匙(`slotExpr`)若在**呼叫的當下**就更新,那段延遲裡就會
  **拿新圖的取景去擺舊圖** —— 畫面上是「圖還沒換、人先跳走」。
  娜塔莉 dying→dead 的 `fx` 差 0.23、`top` 差 56,跳了 45px。
- ⚠⚠ 換圖有**兩段**延遲:① 排程的 `setTimeout` ② **圖自己的載入解碼**。
  規則是**取景在 `onload`／`complete` 那一刻才換** —— 新的像素真的畫上去了才換取景。
  ⚠ 只移到「設 `src` 的那一刻」是**不夠的**(ver -647 犯過):圖沒快取時 ② 還沒完成,
    畫面上仍是舊圖卻已套上新取景,於是又跳一次。
  ⚠ 載不到圖時它**不會更新** —— 取景與畫面上那張(舊的)仍然一致,那正是要的失敗模式。
  (圖沒變的那一支照舊立刻更新 —— 取景本來就一樣。)
- ⚠ 這與 §6.5「上場是延後執行的，所以撤場一定要把延後取消掉」是同一族:
  **凡是延後才生效的畫面狀態,任何「依它計算」的東西都要一起延後**,
  不要一半即時一半延後。

**同一張立繪＝同一個結果(ver -336,Ray 指定寫入原則)**
- 同一張立繪在**任何場合**都要算出同一個大小與同一個位置 —— 不因台上有幾個人、
  不因是誰在講話而變。玩家會把「她變小了」讀成畫面出錯,而不是讀成鏡頭語言。
- ⚠ 踩過兩次:①依人數等比縮小(ver -320 拿掉);②身高讓位只在兩人同台時做,
  於是同一張立繪在單人那一格與兩人那一格高低差 46px,Ray 回報「立繪縮小了」——
  **量出來尺寸其實一樣**,是位移讓人讀成縮小。ver -336 起身高讓位一律套用。
- 代價:單人時頭頂上方會空出 (最高身高 − 自己身高)×每公分像素。那是身高的誠實表現,
  接受它,不要用「單人就不讓位」去換 —— 那正是上面那個 bug。

**取景(ver -249 改版)**
- 逐角色一組 `{cm, eye, fx, top, bot}`,**不要共用一組數字** —— 每張插畫構圖與畫風都不同。
- ⚠⚠ **縮放鎖「身高」,不是鎖眼寬**(ver -266 改;-249~-265 曾經鎖眼寬,見下)。
  - 縮放 = `每公分像素 × 角色身高cm ÷ 這張圖裡人物的像素身高`。
  - 站位同樣鎖身高:頭頂 y = `頂線 + (最高身高 − 自己身高) × 每公分像素`。
  - ⚠ **每公分像素必須與縮放自洽**(= 最高角色的螢幕身體高 ÷ 她的身高),不能再用「真人單眼寬≈3cm」那個估值 —— 自洽時**四個人的腳才會落在同一條地平線上**(實測腳底 y 全部 972)。
- ⚠ **為什麼放棄鎖眼寬**:它把**畫風差異放大成體型差異**。四張插畫的身體都佔滿約 1520/1536 像素,但插畫家給的眼睛差很多(索拉娜 27、安雅 34);鎖眼寬 = 縮放 ∝ 1/eye,眼睛被畫小的人整個被放大 —— 實測螢幕身體高索拉娜 983 / 安雅 787,比值 1.249,而身高比只有 1.107。**索拉娜被畫得比應有的大 13%**,Ray 回報「索拉娜太巨大、安雅太小」。
  代價是臉的大小會有差(索拉娜眼寬 16.1、諾薇兒 20.1),那正是鎖眼寬原本要避免的 —— 要往回調就動 `CAST_EYE_MIX`(0=純身高、1=純眼寬),但別調回 1。
- ⚠ 側面圖**不能拿自己的眼寬當尺**(索拉娜 side 的眼睛被壓扁成 26px)。鎖身高之後 `eye` 已不是縮放的尺,但 `CAST_EYE_MIX>0` 時仍會用到,所以照舊量 front 的。
- ⚠ 頂線(最高角色的頭頂)**由左上角 HUD 的實際高度量出來**,不寫死 —— 那塊吃 safe-area,寫死在瀏海機上一定會撞到「大陸曆 1908」。
- ⚠ 橫向站位錨的是**臉的中心**(`fx`)不是圖框中心:這些插畫左右留白差很多。
- ⚠ **立繪不可越中線**(ver -265):左側的人輪廓右緣不得超過畫面中線,右側反之。夾的是**輪廓**不是圖框(留白佔圖寬 2~5 成),而且只能量**看得見的那一段**(立繪只畫到腰,裙襬腿部不在畫面上)。由 `measureBounds` 在載入時建逐列累積的左右界,改取景不必重量。

**⚠⚠ 對白播放中不放「時間地點」(ver -385,Ray：「時間地點都壓人物臉上了」)**
- 城鎮的地名／時刻(`#townInfo`)、飛行頁的大陸曆／時刻與地名(`#top`/`#place`),
  **只要在演對白就一律讓開**(淡出,不要硬切 —— 對白一段接一段,硬切會閃)。
  那一行的高度正好是立繪的臉。
- ⚠⚠ **「現在有沒有在演」由演出層宣告,不要從畫面反推**。
  `story.markTalking(true/false)`：`renderLine`／`flashLine` → true,`clearCast`／`close` → false;
  飛行頁則直接讀 `sayQueue`(drawTalk 用的同一個真相來源)。
  ⚠ 踩過:第一版寫成「現場量對話框看不看得見、台上有沒有人」—— 立繪的 `.on` 與對話框的
    `visibility` 都是**這一拍稍後**才套上／撤掉的(滑入 450ms、延遲打字、滑出動畫),
    當場量到的是**上一個狀態**,結果整個顛倒(對白中反而顯示、結束後反而藏起來)。
- ⚠ 這也是鐵律 8 的又一次:原本是「誰開關導覽,誰順便決定地名出不出現」,
  於是沒經過那兩個呼叫點的路徑(路人閒聊、店主對話收尾、戰鬥交棒回來)全部漏掉。
  **一條規矩要由一個地方宣告,不要散在各個呼叫點。**

**⚠⚠ 全畫面染色（`tintHold`，ver -664）**
Ray：「畫面變色，紫紅負片」（墓地那一幕，一路撐到進戰鬥）。
- 與 `shakeHold` **同族**：那一拍寫 `tintHold:'<名字>'`，推對話不會收掉；
  出口是同三個（進戰鬥／換場／離場），全部走 `story.stopTint()`（鐵律 8）。
- 名字對到 CSS 的 `.story-tint-<名字>`，配方在 CSS（鐵律 1）。
- ⚠⚠ **負片的配方是 `invert → sepia → hue-rotate → saturate`**：
  單靠 `invert + hue-rotate` 只是把原本的每個色相各轉一個角度 ——
  **畫面有幾種顏色就跑出幾種**，實測墓地（灰天＋白碑）翻出來是一片粉白。
  `sepia(1)` 先壓成單一色相，再轉到紫紅、拉濃，才會是「整個畫面同一個顏色」的負片。
- ⚠⚠ **只翻背景**（ver -665，Ray：「紫紅背景就好，人物跟槍棺不用」「對話框也不用」）。
  -664 那一版是整個舞台一起翻（連楣、槍棺、對話框都成了負片），已被退回 ——
  立繪維持原色反而更好讀：**世界變了，人沒變**。
- ⚠ 配方只寫一次（`--tint-nightmare`）：城鎮的背景本來就帶著去雜訊的
  `blur/contrast`，而 `filter` 是**整條取代**不是疊加 —— 各寫一份完整的式子必然走鐘。
- ⚠ 進場給 260ms 過渡 —— 瞬間翻轉是閃屏，對光敏感的人也不友善。

**⚠⚠ 跨句的演出要另開一個欄位,不要沿用一次性的那個(ver -638)**
Ray：「蕾娜的『！！』之前的畫面震動要持續 10 秒,點擊推進對話也要繼續」「直到進戰鬥停止」。
- `shake:true` ＝**這一拍**抖一下(一次性,`stopFx` 每推一句就收掉);
  `shakeHold:<毫秒>` ＝**跨句的狀態**,推對話不會收掉,一路抖到出口為止。
- ⚠⚠ 跨句的狀態一定要有**明確的出口清單**,而且全部走同一支收尾函式(鐵律 8)。
  持續震動的三個出口:**進戰鬥**(`line.battle`)／**換場**(`playScene`)／**離場**(`close`),
  全部呼叫 `story.stopShake()`;時間到只是保險。
- ⚠ 它的計時器**不可以放進 `fxTimers`** —— 那一組是給一次性演出用的,每推一句就被清空。
- ⚠ 自檢:任何「要撐過好幾句」的演出(持續震動、持續音、閃爍)都照這條 ——
  沿用一次性的欄位一定會在下一句被收掉,而那不會有任何錯誤訊息。

**演出的節奏(ver -351,Ray 指定「原則類的東西都要寫進去，確保每個 session 都遵守」)**
- ⚠ **插圖轉回一般對話時,要等畫面全亮才彈出角色與對話框。** 換圖/收圖走的是黑幕
  (淡黑→換→淡回),立繪與對話框若在黑幕還蓋著時就上,黑幕一收畫面上是「已經站好的人」,
  讀起來像少了一拍。實作:`modules/story.js` 的 `renderLine` 把整段放人出來的程式包成
  `reveal()`,走過黑幕就延後 `CG_FADE_MS×2` 再執行。
- ⚠⚠ **有立繪在台上的無台詞拍要「點擊」才推進**(ver -628,Ray：「角色立繪無對白時
  也要點擊才往下播,除非自動播放」)。那一拍是**演給人看的**(換表情、轉身要跑、
  站定不說話)—— 自己跑掉的話玩家還沒看清楚就沒了,而它又沒有字可讀。
  · 判定看**台上有沒有人**(`slot.L`／`slot.R`)不是看有沒有寫 `portrait` ——
    立繪是持續狀態,上一拍放上來的人這一拍還站著。
  · **自動播放／加速模式照跑** —— 那是玩家明講「不要等我」。
  · **台上沒人的純演出拍**(震動、空畫面、換插圖)照舊吃 `auto`:那些沒有
    「要看清楚的東西」,停在那裡只是空等。
- ⚠ **無台詞的立繪拍那一秒從立繪站定才起算**(`auto` 仍用在上面那個例外)。
  滑入要 450ms,若從這一拍第 0 毫秒起算,實際站定只剩半秒 —— Ray 回報「監察官立繪撤太快」
  就是這個。實作:`auto + (有人滑進來 ? SLIDE_MS : 0)`。
- ⚠ **說話的人的立繪圖層一定在另一個人之前。** 兩人輪廓允許交疊,交疊處誰在前就是
  「誰在講話」的線索;壓在別人身後講話會讀成她在後面自言自語。
  兩個槽是兄弟元素、DOM 序固定,所以要寫 inline `z-index`,光靠 class 做不到。
- ⚠⚠ **同一張插圖的差分切換用淡入,不走黑幕**(ver -628,Ray：「007_Anya_passout /
  007-2_Anya_awake 兩張的切換直接用淡入,不用轉黑」)。腳本那一拍寫 `cgSoft:true`。
  黑幕是「換一個地方」的語氣;**同一張圖上發生了變化**要的是疊上去淡入 ——
  中間插一片黑等於把那個變化切斷。
  ⚠ 作法是把新圖疊在第二層(`#storyCg2`)淡入、淡完交棒給主圖層,**不是**
    「先清空再淡入」(那會閃一格空白,跟黑幕一樣糟)。
  ⚠ **收圖(`cg:null`)不吃這一條** —— 那是「這張插圖結束了」,該走黑幕。
  ⚠ 預設仍是黑幕:只有明寫 `cgSoft` 的那幾拍走淡入。
  ⚠⚠ **疊上去的那一張要停在同一個取景**(ver -631,Ray：「passout 要下往上平移,
    awake 不用,直接疊在平移後的 passout 上」)。主圖層可能停在平移/放大的結果上
    (`pan-up` 跑完是 `object-position:50% 0%`),第二層用預設值就是「另一個構圖」,
    讀起來是跳了一下。作法:①`cgSoft` 那一拍**不重置平移** ②`cgCross` 把主圖層的
    **computed** 取景抄到第二層 ③交棒時把那組值寫成主圖層的 inline
    (animation 的 forwards 結果換掉 `src` 之後未必守得住)。
    ⚠ 下一次真的要重新平移時 `startMove` 會把那幾個 inline 清掉。
- ⚠ **場景與場景之間走黑幕**(黑幕淡入 → 讀取頁 → **在全黑之下**演出新場景第一拍 → 淡出)。
  第一拍要在黑幕還蓋著時演,否則會看到舊畫面殘留一格才換。
- ⚠⚠ **讀取頁自己的進出也是黑色淡入淡出**(ver -433,Ray 指定)。以前是
  `appendChild`／`removeChild` 硬切 —— 前一幕「啪」一聲被蓋掉、載完又「啪」一聲換掉。
  ⚠ 底色本來就是近黑(`#0a0812`),所以「淡入淡出」＝淡**這一整層**,不必另疊一片黑。
  ⚠ 淡入要**隔一幀**才拿掉起始的 `.al-fade`:同一幀加上又拿掉會被合併成一次計算,
    過場整個跳掉(同 `story.veil` 那條 `offsetWidth` 的理由)。
  ⚠ 黑幕(`#storyFade`)要等**讀取頁淡完**才掀,否則會看到「讀取頁疊在新場景上一起變淡」。
  ⚠ **開機那一頁不受這條管**:它的離場是 Ray 設計的聖光綻放,那是另一件事。
  ⚠ 飛行頁那一頁(`#boot`)以前是 `display:none` 硬切,ver -433 一併改成淡出;
    **淡完才 `display:none`** —— 只給 opacity 的話那一層還蓋在上面吃掉所有點擊。
- ⚠⚠ **所有切景都用淡入淡出**(ver -438,Ray 指定)。城鎮這一邊收在**一支**
  `sceneCut()`（`modules/town.js`）：淡到全黑 → **在全黑之下** `enter()` → 淡回來。
  ⚠ 走 `story.veil()`（唯一那一片黑幕,鐵律 8）,不要另貼一片。
  ⚠⚠ **淡出與淡入的擁有者是分開的**：`sceneCut()` 只負責淡出,**淡入交給 `enter()`**
    —— 只有它知道新的一景什麼時候擺好（背景是非同步載的）。兩邊都收就會有兩段
    淡入互相打架。
  ⚠ 涵蓋：走一步（`go`）、強制轉場（`forceGo`）、進城（`open`，`veil(true,0)` 立刻全黑
    再由 `enter` 淡回）。那 260ms 的空檔本來就在,只是以前什麼都沒演。
  ⚠ **主線 scene→scene 的 `next` 鏈不在這一條裡**：那一段的節奏是 Ray 一句一句調過的,
    而且需要黑幕的地方已經用 `{ load:… }` 那一拍明寫了（見上一條）。要改要另外說。
- ⚠ **新場景開場先用情境卡報地點**(半透黑罩+置中文字),立繪之後才出來。
- ⚠ **楣（下半那片槍棺的上緣）的圖層僅次於對話框**(ver -363,Ray 指定)。
  它是**畫框**:背景、插圖、立繪、情境卡、黑幕全部在它之下,只有對話框在它之上
  (`#kerb` z-6 < `#storyBubble` z-7)。退出鈕/跳段鈕另外抬到 8 —— 那兩顆要一直點得到。
- ⚠ **戰鬥裡的對話立繪與劇情頁的立繪是同一把尺**(ver -352,Ray：「戰鬥對話人太小,
  要與一般對話同比例同高度」):同一個 `castShow`、同一個**參考框**(`#app` 高 × 56%)、
  同一條頂線(退出鈕)、同一條身高讓位。戰鬥的框只有 50% 高,拿它自己的高去算 `pxCm`
  會讓同一張立繪在戰鬥裡小一成 —— **分母要用劇情頁那個框**,超出的部分讓 `#top` 裁掉。
- ⚠⚠ **照飛行畫面那一套:取景在 resize 時算成常數,之後每一句只「讀」,一個 rect 都不逐句量。**
  基準實作是 `flight/index.html` 的 `castMeasure()` —— 它把 `CAST_TOP` / `CAST_PX_CM`
  算好放全域,`castGeom()` 只是查表。DOM 版要達到同樣的穩定,**每一項都必須是常數**:
  視口高、瀏海高(`--notch-bar-h`)、鈕的尺寸(`.corner-btn` 44px / `top:10px`)。
  逐句量任何一個 rect(框的位置、鈕的位置、框的高度)都會漏 —— 那些值會在轉場、
  網址列收合、門開合時被抓到中間值,而立繪是**每一句**重排的。
- ⚠⚠ **參考框是「視口高 × 56%」,不是 `#app` 的高。** `#app` 走 `height:100%`,而開機時
  main.js 會取 100vh／100dvh 裡**較大**的那個來閃開 iOS 幽靈工具列 —— 實測 `#app` 883、
  視口 812,差 8.7%;而且那個值在開機過程中還會變一次。拿 `#app` 去乘,同一張立繪
  在戰鬥裡就比劇情裡大一截,而且**每次跑的結果還不一樣**。
- ⚠⚠ **頂線要「由 CSS 常數推」,不要量那顆鈕的即時 rect。** 量到的值取決於第一次排版
  剛好發生在哪一刻 —— 鈕若正好被藏起來(門在開、結算 banner 開著)或版面還在轉場,
  `height` 是 0,整組取景就被那一瞬決定,又被快取一整場(實測同一段教學跑兩次:665 vs 732)。
  鈕是 `.corner-btn` 44px、`top:10px`,加上 `--notch-bar-h` 就是它的位置,直接算。
- ⚠ **頂線夾的是那顆角落鈕的「上緣」,不是下緣。** 要閃開的是瀏海/動態島,而版面已經
  被 `#app` 的 padding 推到瀏海之下,鈕的上緣就是安全線;夾下緣等於把鈕高(44px,約一個臉)
  再讓一次 —— 手機上尤其明顯(鈕吃 safe-area,下緣落在 ~101px 而桌機只有 ~58px)。
  ⚠ 但**相機的頂線**仍用下緣:`castShow` 是對著那個框調出來的,一起改人就變大變小。
- ⚠ **取景要「整組」快取,一場之內是常數。** 只快取縮放不夠 —— 頂線也會動
  (iOS 的 `env(safe-area-inset-top)` 隨網址列收合在 47px↔0 之間跳),尺寸沒變、
  整個人上下跳,一樣被讀成「忽大忽小」。寬度變了(轉向)才重量。

**面盤的操作(ver -367,Ray 指定;劇情頁)**
- 跳段鈕在**控制面盤左下**(上排留給「離開」,面盤那一排是操作區)。
- **面盤點一下也推進**——推進層是整個舞台,不是只有上半。
- 手勢一律**起點在面盤才算**,起點在上半就是單純的「點一下推一句」:
  **按住下拉**＝加速(打字變快＋自動點擊,放開即停)／**往右滑**＝自動播放(再滑一次關掉)／
  **往左滑**＝開啟本場已播腳本。
- ⚠ 兩種模式(加速/自動)**不跨場**:那是「玩家現在的操作意圖」,不是劇本狀態,
  換場沿用會變成下一幕自己跑起來。
- ⚠ 自動推進掛在**「這一句唸完」**(打字機的回呼),不是固定秒數 ——
  長句與短句該停一樣久的是「讀完之後」,不是「出現之後」。

**⚠⚠ 一拍還沒演完,點下去就「把這一拍演完」,不推進(ver -430)**
Ray：「對話點擊太快(或者點到箭頭)會有立繪殘留。」
- 這條規矩本來就有兩個實例 —— **還在等 `delay`** → 先把對話框叫出來;
  **還在打字** → 先把字補完。ver -430 補上第三個:**黑幕底下還沒演的那一拍**。
- ⚠⚠ 成因:走黑幕的那一拍(換插圖/換背景),整段「誰上台、誰下台、誰高亮」被包成
  `reveal()` **延後** `CG_FADE_MS×2+140` 才跑(§6.5「等畫面全亮才彈出角色」)。
  那一秒既沒在打字、也沒在等 delay,於是 `advance()` 直接跳下一句,
  而 `renderLine` 開頭的 `stopFx()` 把那個計時器清掉 ——
  **那一拍的立繪指令整段沒有執行**,上一個人就留在台上。
- 作法:把還沒跑的那一支存進 `pendingReveal`,`advance()` 第一件事就是
  `if(flushReveal()) return;`。⚠ **換場/離場要「丟掉」不是「補演」**
  (`playScene`／`close`／`skipToNextGate` 把它設 null)—— 台上馬上要清空,
  補演等於把上一幕的人請回來。
- ⚠ 補演之後要收掉打字機:`reveal` 的尾巴會開始打**上一句**的字,而新的這一句若是
  空框/演出拍只寫 `textContent=''`、不會停那個 interval,舊句子會爬回新的框上。
- ⚠ **對話框是 `pointer-events:none`**,所以「點到 ▼ 箭頭」與點畫面是同一件事 ——
  不必為箭頭另做一個熱區,修好 `advance()` 兩者一起好。

**⚠⚠ 上場是「延後」執行的,所以撤場一定要把那個延後取消掉(ver -433)**
Ray：「角色對話結束需要清除畫面上所有立繪…感覺是馬上點到路人對話立繪會卡。」
- 成因:`ensureOn` 真正 `classList.add('on')` 的那一步是**延後**的 ——
  首次上場 `setTimeout(…,16)`、同側換人 `SLIDE_MS*0.45`、換表情 190ms,
  外加 `el.onload`(圖還在載時)。那幾個都跑在 `leaveSlot` **之後**。
  於是:對白最後一句剛把人放上來、玩家立刻點掉 → `clearCast()` 撤場 →
  **還在路上的 `apply()` 又把 `.on` 加回去** → 畫面上有人、資料上沒有人,
  沒有人會再撤她。
- 作法:`leaveSlot` 要 `clearTimeout(slotT[side])` **並且** `el.onload=null`。
  ⚠ 這是「凡是延後執行的上場,撤場就要能取消」的通例 —— 日後任何 `setTimeout` 出來的
    畫面狀態都照這條檢查一次。
- ⚠ 另外掛一道**驗收**(`verifyCastCleared`,Ray 指定「在播放結束後放一個檢查」):
  滑出跑完之後,若 DOM 還有人掛著 `.on` 而資料上那個槽是空的,就清掉並**記一筆 console**。
  ⚠ 判斷看**資料**(`slot[side]`)不是看畫面 —— 撤場後可能立刻有人被合法地放上來
    (店主 `castSolo`、下一段對白的第一拍)。
  ⚠ 它是驗收不是規矩:真的驗到東西就是上游有 bug,靜靜修掉會讓下一個同類 bug 查不出來。

**⚠⚠ 一段對白演完就清掉畫面上所有立繪(ver -430,Ray 指定寫進憲法)**
- **立繪是持續狀態**,沒人撤它就一直站著。所以「這一段講完了」＝**台上要空**:
  兩個槽都撤、對話框收掉、`story-talking` 關掉 —— 那三件事就是 `story.clearCast()`。
- ⚠⚠ **這條規矩收在引擎裡,不是寫給呼叫端**(鐵律 8)。收尾的出口只有兩個,兩個都做了:
  · 主線 scene → `playScene`(換場時清)／`close()`(離場時清)
  · 城鎮／旅店／店主／再挑戰那些臨時段落 → `endScene()` 的 `__adhoc` 分支(ver -430 補)
  ⚠ 以前 adhoc 那一支只收對話框,清場交給每個呼叫端的 `done` 自己記得做 ——
    當時四個呼叫端剛好都寫對了,但那是運氣,不是保證。**新增第五條路徑必漏。**
- ⚠ 呼叫端原本寫的 `story.clearCast()` **不要刪**:它是冪等的,而且讀起來就是那一段的收尾。
- ⚠ **例外只有「持續狀態的立繪」**:店主(`castSolo`)不是對白,它的收尾是
  `shopClose()`／`enter()`,不受這一條管。要分清楚「一段話講完」與「離開這個地方」。

**⚠⚠ 新畫面/新路徑上線前的檢查表(ver -374,鐵律 8 的落地)**
每加一條「進入或切換畫面」的程式路徑(城鎮節點、商店、日後的旅店/委託…),
上線前逐條確認。**這張表已經被漏掉三次**(清場、站位、抵達停頓),每次都是
「規矩寫在腳本層,新路徑沒吃到」:

0. **上一個地點的店舖畫面**收了嗎? → `shopClose()`(立繪＋左邊那張單子，ver -404)
1. **清場**了嗎? → `story.clearCast()`(立繪是持續狀態)
2. **上一段還在播的對白**中止了嗎? → `story.endAdhoc()`
3. **上一段的計時器**取消了嗎?(抵達停頓、auto、演出計時器)
4. **兩個角色同台有分左右**嗎? → 節點/scene 的 `sides` 覆寫(§6.5 固定站位)
5. **立繪與對話框是一起出現**的嗎?(框要等立繪站定,`delay: SLIDE_MS`)
6. **同一段對白只播一次**嗎?(用 flag 擋,不要靠資料上寫 `once`)
7. **取景**走的是同一把尺嗎?(`castStage` 那組常數,不要另算)

**腳本的寫法(同上,原則)**
- ⚠ **稿子裡括號的動作描述是舞台指示,不進台詞**(「（舉起義肢）」「（乾笑）」),
  除非 Ray 明說要唸出來。動作交給表情差分去演。
- ⚠ **立繪是持續狀態**:`show` / `cg` / `bg` / `bgm` 都是沿用的。開場那一拍若不要某人上場,
  **一定要明寫 `show:false`**(預設是 true) —— 這個坑踩過三次。

**新增立繪要量什麼(基準點)**
- **縱向基準 = 頭頂**(`top`)。每張圖都看得到,最穩。⚠ 髮飾/帽子/武器超出頭頂會污染它,量的是**人物最上緣**,所以誇張髮飾會把整個人往下壓。
  ⚠⚠ **法環/光圈/特效也算污染**(ver -635,Ray：「戰鬥中諾的 saint install 立繪太小,
    因為你把法環也納入總高了,抓臉的大小調整」)。諾薇兒的 SAINT INSTALL 頭上有一圈
    金色法環,照 alpha 上下緣量到的 `top:3` 是**法環頂** —— 鎖身高把她整個縮小 6%,
    `top` 偏高又把她往下壓了 73px×縮放。
    量法:**用顏色把人物挑出來**（頭髮的暗紅棕挑得掉金色法環）,腳底只看**畫面中央
    那一段**（避開兩側垂下的緞帶）。這張圖的正解是 76 / 1519。
- ⚠⚠ **近景／坐姿的立繪要用 `cm` ＋ `standCm` 兩個旋鈕**(ver -636,Ray：「娜塔莉跟
  安雅的立繪太失控了吧」)。取景公式假設「全身站姿」,近景與坐姿都會同時破壞
  **大小**與**高度**,而這兩件事各有各的旋鈕:
    · `cm`      ＝**這張畫該佔多少公分** → 管**大小**(縮放的分子)
    · `standCm` ＝**站姿身高** → 只管**頭頂高度**(頂線 ＋ (最高身高−standCm)×每公分像素)
  ⚠⚠ **只調 `cm` 一定失敗**:畫得越近 → `cm` 越小 → 頭擺得越低,正好相反。
    近景要 `cm` 小（頭才不會比別人大）＋ `standCm` 給她真正的身高（頭才回到該在的位置）。
  ⚠ 坐倒在地的角色(娜塔莉)反過來:`cm` 接近全身值(她那張圖是用全身的尺畫的)、
    `standCm` 壓低(她的頭本來就低)。⚠ 照真人比例(坐姿頭頂約 100cm)會把她整個推到
    對話框底下 —— 這個舞台只看得到站姿的上面約六成,要的是「看得見、又明顯比別人低」。
  ⚠ 這幾個值**沒有可靠的自動量法**(臉的自動偵測會被頭髮吃掉):看渲染結果調,
    而且**只動 `cm`／`standCm`,不要去動 `top`／`bot`** —— 那兩個是那張圖的客觀事實。
- ⚠⚠ **有些差分是「另一個尺」,要明寫 `rescale:true`**(ver -635)。
  預設拿**基本立繪**的像素身高當分母(ver -346:差分之間 alpha 上下緣差 ~1%,
  每換一次表情就縮放一次＝「同一個人忽大忽小」)—— 那條規則的前提是
  「所有差分都畫在同一個尺上,量到的差異是雜訊」。
  但有的圖**真的**畫得比較小(SAINT INSTALL 為了容納法環,人物只佔 1443px,
  而基本立繪是 1523px,差 5%),那就不是雜訊。`rescale:true` ＝ 這一張用它自己的身高。
  ⚠ 是**明寫的例外**不是自動判斷:門檻式的自動判斷會在雜訊邊界上跳。
  ⚠ 這也是為什麼 `top`/`bot` 一定要量**人物**:量錯了兩種模式都救不了。
- **縮放基準 = 這張圖的「每公分幾像素」**。全身頭到腳的圖直接用 `(bot−top)/cm`;⚠ **半身或胸像圖絕對不能照量 alpha 上下緣** —— 那不是全身高,照算會把人放大好幾倍。沒有腳可量時,拿**同一角色已量好的全身圖**對齊「頭頂→眼線」的距離換算。
- **橫向基準 = 臉中心**(`fx`),兩眼中心的中點 ÷ 圖寬。
  ⚠⚠ **`fx` 是「臉在**那一張圖**裡的位置」,不是螢幕位置 —— 所以差分之間**不能抄**
    (ver -645,Ray：「我看不懂 dead 的位置怎麼抓」)。引擎做的是
    `left = 畫面錨點 − 縮放 × fx × 圖寬`:**只要每張圖各自量對,臉就會落在同一個
    螢幕位置**,圖自己會左右挪。抄同一個數字反而會讓她在換表情那一拍橫向跳一格
    (娜塔莉兩張的臉本來就畫在 0.363 與 0.594)。
  ⚠⚠⚠ **同一個姿勢的差分,位置就直接沿用 —— 不要逐張量**(ver -649,Ray：「就用 dying 的
    位置放 dead，這麼簡單的事你繞一堆有的沒的」)。
    躺著／癱著的角色特別明顯:臉會跑到圖上完全不同的位置(娜塔莉 dying 0.363、
    dead 0.594,因為頭歪到一邊),**逐張量臉再想辦法對齊,是自己製造出來的問題**。
    · 作法:那一張的 `expr` **只寫 `src`**,其餘全部不寫 ——
      `frameOf` 是 `Object.assign({}, 角色, 這一張)`,不寫就是沿用
      (`fx`/`fxShift`/`top`/`bot`/`cm`/`standCm` 一併)。位置一模一樣,不必對齊。
    · ⚠ **逐張量只給「站姿、構圖不同」的差分用**(表情、動作)——
      那些的臉本來就該各自量。同一個姿勢換個角度的,沿用。
    · ⚠ -641~-648 為了「對齊」繞了一大圈(逐張量臉 → 算肩的欄位 → 兩段延遲的時序),
      全部是這一條沒想通造成的。**先問「這兩張要不要在同一個位置」,要就直接沿用。**
  ⚠⚠ **要整個人往左右挪,動角色層的 `fxShift`,不要去改 `fx`**(ver -645)。
    · `fxShift` 加在**那個角色的每一張圖**上 → 改一個數字,所有差分一起移。
    · **正數往左、負數往右**(`fx` 越大＝臉在圖上越右＝圖被推得越左)。
    · 加在**鏡射之後** → 「往右」在翻轉與否之下都是同一個螢幕方向。
    ⚠ **兩者不可以合成一個數字**:合了就分不出「量到的」與「手調的」,
      下次重量那張圖會把手調一起洗掉(這正是 ver -641~-644 手動同步兩張圖的原因)。
    ⚠ 兩套 DOM 各有一份實作(`story.castLayout` / `tutorial.placePortraitX`),
      而 `config.tutorial.portraitFrames` 的 `put()` 要把角色層的 `fxShift` 合進來
      —— 它與 `cm` 一樣住在角色那一層,`expr` 只帶 top/bot/fx。
- `eye` 只在 `CAST_EYE_MIX>0` 時才用得到,但仍建議量著備用。量法見 `flight/HANDOFF.md` F 節(要先拿已知角色校準尺,自動偵測暗像素會把頭髮當眼睛)。

**畫質**
- ⚠ 畫布**要開到裝置像素**(`canvas.width = W × DPR`,DPR 上限 2,再 `setTransform(DPR,…)`)。只開 CSS 像素的話立繪與 canvas 文字會被瀏覽器再放大一次 → 糊。
- ⚠ 立繪縮小超過 2 倍要走**逐次減半的預縮圖**(等同 mipmap),不能直接 `drawImage` 縮到位:關平滑是最近鄰(頭髮蕾絲閃成雜點=顆粒化),開平滑也只是雙線性,一樣漏取樣。縮好之後每幀 1:1 貼上。

**角色身高(全域通用,任何畫面的立繪都照這個比例)**

| 角色 | 身高 | 備註 |
|---|---|---|
| 索拉娜 | 176 cm | 立繪用 side(front 橫向太滿) |
| 蕾娜 | 169 cm | 立繪用 front(ver -263 到位) |
| 諾薇兒 | 165 cm | |
| 安雅 | 162 cm | ver -267 由 159 調高 |

- 兩人同台要挑**橫向佔比小**的那張圖(例:索拉娜用 side 不用 front,69% vs 78%),站位再拉開,才不會疊在一起。

**層次**
- ⚠ 立繪要在**操作面板與舵輪之下**。飛行頁因此畫在 canvas 上、排在 `drawPanel` 之前 —— DOM 疊層做不到這件事(裁切是「避開」不是「在下面」,圓形的舵輪會露餡)。
- 對話框與該畫面既有的橫向元件對齊(飛行頁:與方向計同寬、疊在其上),幾何抽成共用函式,兩邊別各寫一份。

## 6.5.1 劇本餵稿與驗稿(ver -342)

Ray 交劇本稿一律照 **`script/SCRIPT_FORMAT.md`** —— 稿子的最小單位是「拍」
(對白拍/演出拍/情境卡拍/閘門拍),演出用中文詞彙寫(`背景`/`插圖`/`平移 上`/
`推近 臉`/`抖`/`掃射`/`暗調`/`退場`/`等…再出框`/`自動…秒`),由我轉成
`script/mainScript.js` 的 line 物件。

- ⚠ 那份文件與 `modules/story.js` 的「演出層」註解是同一件事的兩面,**改一邊要改另一邊**。
- ⚠ 稿子轉完**一定要跑** `python3 tools/script_lint.py`:缺圖/缺音效/角色 id 打錯/
  scene 鏈斷掉/空拍沒有 `auto`,一次列出來,不必等演到那一句才發現。
- ⚠ **不要**在稿子裡寫站位、明暗、立繪大小、檔案路徑 —— 那些是算出來的(§6.5),
  寫死一定與既有規則打架。

## 6.5.2 劇情插入戰（ver -375）

腳本中間插一場架（公會那一場是第一個），一律走這條，**不要為了某一場另接一次戰鬥**。

- 腳本寫 `{ battle:'<id>' }`；`config.battles[<id>]` 說明這一場的規則
  （哪隻怪、能不能聖徒化／用搭檔技），怪本身的數值全在 `config.enemies[<key>]` 那張
  **敵人資訊標準卡**上（格式見 `script/SCRIPT_FORMAT.md` §8.5，Ray 定的）。
- ⚠ **卡上寫絕對值就存絕對值**（「延時懲罰 5 秒、攻擊力 5」）。不要在 config 裡把它
  換算成「基礎值 × 倍率」—— 那是把同一個量算兩次（鐵律 7），而且 `tuning` 的基礎值
  一改，所有換算過的怪就全部走鐘。程式端：`delayDamage()`/`wrongDamage()`/
  `effIntervalLimit()` 三支，絕對值優先、沒寫才回去走倍率。
- ⚠ **框是共用的，教學那一套不是**：`storyFramed()`＝`tutorialStoryRun || scriptRun`，
  共用的只有「不播櫻花過渡禎、進出走 Kerberos 之門、打完交還劇情」。教學專屬的
  （鎖攻擊力 2、敵人打不死、教學結算台詞）只看 `tutorialRun`，不要混進來。
- ⚠ **禁令擋在唯一的發動點**：`saint.activateSaint` 的 `state.noSaint`、
  `partner.tryActive` 的 `state.noPartner`。不要在手勢／鍵盤／按鈕各擋一次（鐵律 8）。
  即死防禦（被動）**不擋** —— 「不能用技能」指的是玩家主動發動的那些。
### ⚠⚠⚠ 戰鬥的分級與回檔（ver -697，Ray 定案 —— 這一節覆蓋 -430/-496/-589 的舊表）

> Ray：「遭遇戰，非劇情戰都用 1（原則一起處理）；劇情戰都用 2（每次手動設定回檔點）」
>       「原則上劇情戰要防卡死，所以必需手動回檔到主角仍然可以自由行動的地方。
>         遭遇戰防卡死就是死三次後送旅店」

**回檔的動作只有一個**：讀最新的那一筆快照（`save.loadLatest()`，與首頁「繼續」
同一支，鐵律 8）—— 旗標／時鐘／好感／道具／位置**整輪一起回去**。

- ⚠⚠⚠ **所以「回去之後那一段對白是初見還是二見」不必判斷**（Ray 問的正是這個）：
  快照在那一段之前＝整段重演（初見），在之後＝二見，**沒有第三種可能**（鐵律 9）。
  這件事之所以曾經「難以判斷」，是因為 ver -430 的「再戰＝跳回那一幕第 0 句」
  **只還原了播到哪裡** —— 旗標、好感、道具全部停在戰前。那不是「這一場還沒發生過」，
  實測就出過事（Ray 連報兩次「安雅的好感還在，也沒有觸發要求去教堂」）。
  **回檔一律讀完整快照，不准只跳位置。**

**存檔點（ver -698，Ray 交辦）—— 引擎自動落的有三種：**

| 何時 | 存什麼 | 實作 |
|---|---|---|
| 飛行每飛一段距離 | 帶座標的檢查點（＝戰敗復活點） | `main` 的 `FLIGHT_CKPT_DIST`（600） |
| **進城鎮／遺跡／特殊地點的入口** | 同上／城鎮那一筆 | `town.open` 收尾／`enterPlace` → `__tivotFlight.checkpoint()` |
| 有戰鬥的段落演完（＝戰勝即存檔） | 城鎮那一筆 | `town.enter` 的 act 收尾 |

**四種場次、四條回程：**

| 場次 | 死亡 | 戰勝 |
|---|---|---|
| **飛行戰**（遭遇／劇情） | 原地續飛（iframe 沒卸載，船還在遭遇座標）＋HP 回滿 | **補落一筆**飛行檢查點 |
| **特殊戰**（賞金獵人挑釁、抓賊…玩家自己走過去挑的） | **一次就後送旅店**，不計連敗、**不回檔** | —— |
| **劇情戰**（城鎮／野外／遺跡／腳本插入戰） | **回檔**（讀最新快照） | 段落收尾自動落點 |
| **遭遇戰**（非劇情戰） | **回這張地圖的入口**（進度照留） | 同上 |

**回檔點是誰落的 —— 這就是 1 與 2 的分野：**

| | 遭遇戰（非劇情戰） | 劇情戰 |
|---|---|---|
| 落點 | **引擎自動**：入口／有戰鬥的段落演完 | **腳本明寫** `checkpoint:true`（Ray：「我寫腳本時會註明回檔點」） |
| 防卡死 | **回入口**（Ray：「入口不會有戰鬥」）＋連敗三次抬回旅店 | **落點必須在「主角仍可自由行動」處** |

- ⚠⚠⚠ **「回入口」與「回檔」是兩件事**（ver -698 修 -697）：遭遇戰打輸，**進度照最新
  那一筆放回去**（戰勝即存檔 —— 已經清掉的格子不該白打），只有**位置**回到入口。
  所以是 `loadLatest({ noJump:true })` ＋ 明指節點，不是讓快照決定人在哪。
- ⚠⚠ **入口＝這座城資料上的入口**（`firstEntry`／`entry`，`town.entryNode()`），
  **不是**這一趟走進來的那一格：讀檔／跳關可以落在中間任何一格，而「入口不會有戰鬥」
  的保證只對入口成立。
- ⚠⚠ **入口那一格不可以有戰鬥**（資料規約，`script_lint.py` 提醒）——
  它是遭遇戰的復活點，有戰鬥就是「打輸回到這裡再打一次同一場」。
  ⚠⚠ **北方泊地目前違例**：城鎮戰的第一格就是入口（`entrance.acts[0]`）。
    靠「連敗三次抬回旅店」兜住，不會真的卡死 —— 但那是兜底不是設計。
    要真的守住這條，城鎮戰的地圖得留一格**沒有戰鬥的入口**（Ray 未定）。
    所以 lint 那一條是 **warn 不是 err**。
- ⚠⚠ **特殊戰寫在戰鬥卡上**（`config.battles[x].special:true`，鐵律 1）：
  戰敗**一次就送旅店**，不計連敗、**不回檔** —— 那一場「過了就沒了」，
  回捲等於把它變成沒發生過。
  ⚠ 「過了就沒了」指的是**打贏之後**：那一段對白的旗標是「演完才記」的，
    所以**打輸走回去還能再挑一次**（那才是「輸了可以再來」）。
    這是我的判讀，Ray 沒明說 —— 要改成「輸了也不能再挑」就得把旗標移到開打前記，
    而那會與城鎮「演完才記」的通則打架（§6.9），要另外決定。

- ⚠⚠ **劇情戰不吃「三次送旅店」**：被送走等於把那一段戲跳過去了。它的出路是回到一個
  **還能去買藥、換裝、練級**的地方再來一次 —— 那是**落點**的責任，不是引擎的。
- ⚠⚠⚠ **落在強制鏈中間的 `checkpoint` 等於沒有**：一走進墓地就是「對白→打→對白→打」，
  讀回來只會再走一次同一條必死路。墓地那兩場的正解是**上一段**（黑爪戰後 -653 那個
  記錄點）—— 讀回去人站在教堂，`enter()` 不會再把他拖進戰鬥。
  **寫 `checkpoint:true` 之前先問：讀回這一刻，玩家能不能走去別的地方？**
- ⚠ `tools/script_lint.py` 會提醒「這一段裡沒有 checkpoint」，但它**驗不出上一段有沒有**
  （跨段落、跨節點，靜態排不出先後）—— 那一句是提醒不是保證，仍要人確認。
- ⚠⚠ **「這一場是不是劇情戰」問段落**（`town.storyBattleAct()`）**不問敵人卡的 `story`**：
  北方泊地城鎮戰那一格的雜怪也是 `story:1`，卡上分不出來（ver -680）。城鎮沒開＝
  劇情插入戰，那一定是劇情戰。
- ⚠⚠ **連敗計數是全域的**（`progress.lossStreak`，鑰匙沿用 `flightLoss`）：
  **只有遭遇戰累加**，任何一場打贏就歸零（收在 `main` 的 `setStoryReturn` 入口，
  鐵律 8）、睡覺也歸零。
- ⚠⚠ **抬回旅店是「先回檔、再放人」兩件事，順序不能反**（`main.carriedToInn`）：
  ① `loadLatest({ noJump:true })` 回捲那一輪 —— 這就是 Ray 說的「死亡之前的那一段
  劇情必需再觸發」（旗標退回去了，走過去自然再演一次）；② 再 `town.open(旅店,
  {carried:true})`。**`noJump` 是為了不讓快照決定位置** —— 少了它，人會回到存檔當時
  的地點，等於沒被抬回去。
- ⚠⚠ **戰勝即存檔**：飛行打贏後要**再落一筆**檢查點（交棒進戰鬥那一刻已落過一筆，
  但那一筆的旗標停在戰前 —— 劇本遭遇的 `done` 是打贏才記的）。不補的話下一次回檔會把
  剛打贏的那一場退回去。城鎮那邊已經有了（段落收尾）。
- ⚠ 戰敗頁：`flight` 一顆「繼續」；`rollback` **兩顆**（繼續＝回檔／放棄＝主畫面）；
  `home`（出陣試玩版）維持「再度執槍」。右邊那顆的字面由「再戰」改成「繼續」——
  它不是立刻重打，是**從上一個記錄點繼續**。
- ⚠ 完全沒有存檔時 `loadLatest()` 回 false → 停在首頁：沒有可以回去的地方。

- ⚠⚠ **戰敗接 Game Over 那一頁**（ver -376），但**那一頁不是死路**（ver -430，Ray 定案）。
  「船戰死亡點擊繼續回到戰鬥前的飛行畫面進度；其餘戰鬥死亡點再戰回到該幕對話的開頭，
    點放棄回到主畫面。」三種場次三張臉，判定**只有一處**（`main.js` 的 `setLoseKind`）：

  | 這一場是誰叫起來的 | 鈕 | 按下去 |
  |---|---|---|
  | 飛行頁交棒的**船艦戰**（`flightBack`） | 一顆「繼續」 | 回飛行畫面，船還在遭遇發生的座標 |
  | **城鎮**插入戰（`storyResume`＋`town.isOpen()`，ver -496） | 一顆「繼續」 | **被抬回這座城的旅店**（`town.open(...,{carried:true})`）；**這座城沒有旅店＝功能未開**、**或那一段是劇情戰**（`town.storyBattleAct()`，ver -680）→ **回檔到前一次劇情** |
  | 劇情插入戰（`storyResume`，城鎮沒開） | 兩顆「放棄／再戰」 | 再戰＝那一幕的第 0 句；放棄＝主畫面 |
  | 出陣（試玩版） | 一顆「再度執槍」 | 回首頁（舊行為，不要動） |

  ⚠ **被抬回旅店的醒來拍**（ver -496，Ray：「如果在那之前還沒觸發旅店初見就優先跑
    諾薇兒一句『啊，醒了。』」）：`carried:true` 的那一次抵達，旅店**初見還沒看過**
    就演節點資料的 `wake`（一拍，取代該次進場對白），初見旗標**不記** ——
    下次正常走進來照演棺材那一幕（同打烊／傍晚插隊的作法）。初見看過＝安靜抵達。
    ⚠ 判 `town.isOpen()` 要在 `flightBack` **之後**：從城鎮出航的船戰城鎮也還開著
    （suspend 不 close），那一場的敗北要回飛行畫面。飛行連敗三場送回旅店那條
    也帶 `carried:true`（同樣是被抬回來的）。

  ⚠⚠ **城鎮裡的劇情戰戰敗也是回捲**（ver -680，Ray：「劇情戰戰敗是回捲至上一段
    劇情喔，不是直接送回旅店」）。「被抬回旅店」是給**城鎮戰**（那一格一格的遭遇）
    用的：那種輸了就是輸了，醒在旅店繼續逛。**劇本安排的那一場**輸了要做到
    「這一場還沒發生過」，所以回到上一個檢查點。
    ⚠ 判定問**段落**怎麼宣告自己（act 的 `storyBattle:true`）**不問敵人卡的 `story`**：
      北方泊地城鎮戰那一格的雜怪也是 `story:1`，那一格分不出來。
  ⚠⚠ **三顆鈕都走既有的交棒出口**（`combat.setStoryReturn`）——`{lose:'continue'|'retry'|'giveup'}`。
    那一支本來就知道「這一場是誰叫起來的、回去要接什麼」；另開一條回程必然與它走鐘（鐵律 8）。
  ⚠⚠ **功能未開的城鎮裡戰死 → 回檔到前一次劇情**（ver -589，Ray 指定）：
    「被抬回旅店」的前提是**這座城有旅店**（節點的 `inn:true`）。北方泊地那種還在
    蓋的城沒有旅店大廳 —— 舊寫法退回 `getLastInn()`／帝都旅店，於是在北方泊地打輸
    會醒在**另一座城**，而北方泊地的城鎮戰旗標還開著（實測就是這樣）。
    那不是「被抬回去」，是走鐘。正解是**回檔**：`save.loadLatest()`（與首頁「繼續」
    同一支，鐵律 8）—— 旗標／時鐘／道具整輪一起回到那一刻，「這一場還沒發生過」
    才成立（同本節劇情戰敗北回捲的原則）。
    ⚠⚠ **回檔要有得回：每次進城、每一段（含戰鬥）演完，都落一個檢查點**
      （ver -590，Ray：「在北泊劇情戰敗應該是回檔到祭司那邊吧」「每次進城跟一場
      戰鬥結束都要有存檔點」）。城鎮的段落**不經過劇情讀取頁**（ver -555 的自動
      檢查點掛在那裡），沒有這兩支的話在北方泊地打輸會退回**上一次讀取頁**
      （實測退到帝都船塢），把整段抵達的戲一起退掉。
      · 進城 → `town.setCheckpoint()`，落在 `enter()` **之後**（存檔要記人在哪一格）。
      · **有戰鬥的**段落演完 → `enter()` 的 act 收尾，落在**旗標之後**
        （早一步存那一場會再打一次）。
      ⚠⚠ **純對白的段落不落**（ver -687，Ray：「又出現劇情戰戰敗後沒有回捲劇情的
        狀況，安雅的好感還在，也沒有觸發要求去教堂」）—— -590 原本是「每一段演完
        都落一筆」，於是**回捲根本回不到哪裡去**：碼頭大道的請求、教堂的戰地醫院
        各自落了一筆，戰敗讀回來的是「請求已經提過、好感已經加過」的那一刻。
        Ray 的原話是「每次進城**跟一場戰鬥結束**」—— 那才是段落邊界。
      ⚠ 要在對白段落落點，**腳本自己寫 `checkpoint:true`**（§6.5.4.3 那一支）：
        「這裡是一個可以回來的地方」是**劇本的判斷**，不是引擎的預設。
        北方泊地第二天早上那一幕就在第一拍寫了它 —— 那是那一天的起點。
      · 兩處走**同一支** `saveSys.autoSave()`（劇情讀取頁也是它，鐵律 8）。
      實測：進北方泊地落一筆（旗標空＝司祭那一幕會重演）→ 演完再落一筆
      → 清掉西側再落一筆 → 死在中央大道，回到「西側、已清西側」那一刻。
    ⚠ 完全沒有存檔時（剛開新局就闖進去）停在首頁 —— 沒有可以回的地方。
    ⚠ 這條路**不能帶 `keepPages`**：要回的是別的地方，這座城要真的被收掉。
  ⚠ **「放棄」走 `story.leaveToHome()`**，不要自己 `combat.goHome()` —— 那一支還會
    **收掉城鎮**（`townCloser`）。漏了的話下次進城會接在舊節點上（ver -399 的舊傷）。
  ⚠ **有去處的那兩張臉要取消「自動回首頁」**（`resultAutoMs`）：那個計時器是給看戰績用的，
    而這一頁現在是要玩家做決定的岔路 —— 時間到了自己走人會把交棒狀態一起丟掉。
  ⚠ **「再戰」要擋在 `onLose` 分歧前面**（`story.resumeFrom`）：`res.lost` 兩者都是真。
  ⚠⚠ **結算頁不再「放太久自動回首頁」**（ver -433，Ray：「取消此機制」）。
    那個計時器（`config.transitions.resultAutoMs`）原本是給看戰績用的保險，
    但這一頁現在常常是**岔路** —— 時間到了自己走人等於幫玩家做了決定，
    而且會把交棒狀態一起丟掉。欄位留著當紀錄，已經沒有人讀它。
  ⚠ 主畫面的**「繼續」**（`#continueBtn`）是另一件事：進**最新的那一筆存檔**
    （`save.loadLatest()`，旅店睡覺存的就是它）。沒有存檔時整顆不出現。

- ⚠⚠⚠ **劇情戰敗北＝回捲到戰前**（ver -486，Ray 定案：「劇情蜈蚣戰敗北後應該
  回到蜈蚣戰前的狀態」「**所有劇情戰都依此原則處理**」）。重生之後要與「這一場
  還沒發生過」無法區分：**劇情再跑一次、遭遇再來一次、該擋的雜怪照樣擋**。
  背景：ver -430 起敗北不再走「Game Over → 讀檔」，§6.9「旗標跟著存檔回去」的
  保證對這條路失效 —— 所以**凡是在「開打之前／取段當下」就記下去的旗標，
  敗北時都要有人退**。目前的三份與退法：

  | 旗標 | 誰記的（何時） | 誰退的（敗北時） |
  |---|---|---|
  | 戰鬥內對白 `talkOnce` | **打贏才記**（`combat.win`／`storyBattleEnd`，ver -493） | 免退 —— 敗北時根本沒記（敗北重來每次都重播，打贏後永久停播；Ray：「所有劇情戰鬥都適用…結束以戰鬥勝利為條件」） |
  | 劇本遭遇 `done`（飛行） | **打贏才記**（`__flightResume({won:true})`，ver -487） | 免退 —— 敗北時根本沒記 |
  | 城鎮進場對白 | **演完才記**（town.enter） | 不必退 —— 記法本身就合這條原則 |

  ⚠⚠ **「是不是劇情戰」是一支明確的判定：`state.storyBattle`**（ver -493，Ray：
    「在戰鬥加上一個是否為劇情戰的判定，之後就讀那一個」；ver -495 起**敵人卡
    統一有 `story:1/0` 這一格**，Ray 指定）。優先序（判定只有一處，combat.startGame）：
    ① 發起端明確宣告（`startScriptBattle(id,{story})`，飛行交棒的 `scripted`：
      隨機遭遇 false、劇本遭遇 true）② 沒宣告（城鎮／腳本插入戰、舊交棒鑰匙）
      → 敵人卡的 `story` ③ 卡上也沒寫 → true。
    開場白要不要播、`talkOnce` 打贏要不要記，一律只讀 `state.storyBattle` ——
    不要再從別的旗標反推（-492 的 `talkIfNot` 已撤）。
  ⚠ 敵人卡同時統一加了 **`counterStagger:1/0`（反擊硬直，ver -495）**：預設 1 ＝
    被反擊（`weaponCounter` 真的開火：Counter 帶、散彈的橘圈改傷帶）時
    **延時懲罰計時歸零**；0 ＝不歸零。實作只有 defense 的 `staggerOnCounter`（鐵律 8），
    卡→state 的載入在 `enemy.setEnemy`（沒寫＝1）。

  ⚠⚠ **-486 的教訓（為什麼 -487 改成打贏才記）**：回捲版（記了再退）要跨兩個
    document 靠執行期掛鉤（main 呼叫 flight 的 rollback），而主頁與飛行頁的
    **程式版本會錯開**（飛行頁每次出航重載、主頁分頁可能開著好幾天）——
    舊 main 不認新掛鉤，回捲整個漏掉，實測就漏了兩次。
    **「打贏才記」不依賴任何跨頁呼叫**，版本再怎麼錯開都對。
    敗北後的 `scriptPending` 重掛在 `__flightResume` 的敗北分支（同頁內）；
    「送回旅店」那條路不經過它也沒關係 —— 下次出航重載本頁，pending 由旗標重算。
  ⚠ 持久 HP（§tivot_php_v1）敗北**不寫回**（-481 起）；**飛行戰鬥的敗北更進一步
    直接回滿**（ver -498，Ray：「死亡回檔時 hp 回滿，遊戲不要那麼硬核」——
    main 的 flightBack 敗北分支 `prog.clearHp()`，連敗三場送回旅店那條也吃到）。
    城鎮／劇情戰的敗北仍是「帶著進場前的殘量」。
  ⚠ **新增任何「開打前就記」的旗標時，先回答「敗北誰退它」** —— 答不出來就改成
    「演完／打贏才記」（城鎮那一套）。`allowLose:true` 的場次不回捲：
    那種輸法是劇本的一部分，不是重來。

  以上**除非**那一場明寫
  `allowLose:true`（＝劇本要它被打輸，輸了接著演）。可戰敗的場次要在戰鬥那一拍寫
  `onLose:'<label>'`，輸了就跳到帶那個 `label` 的拍（分歧）；贏的那一支用
  `{ goto:'<label>' }` 接回合流點。`script_lint.py` 會檢查兩者是否配對。這條**適用所有場次**，包含劇情
  帶起來的教學。教學真正的「劇情殺」是另一套（`tutorial.onPlayerDead()` 在傷害落地時
  就攔下來，根本走不到 `lose()`）。
  ⚠ 連帶：`inspector.settle` 的教學／插入戰結算頁要加 `!isLose` —— 那一頁是給打贏用的
  （戰績＋拾得），輸了看到一頁戰績是錯的。
  ⚠ 城鎮那一段對白的旗標是**演完才記**的（`modules/town.js`），所以輸了回頭再走一次
  還打得到 —— 開演就記的話，輸一次就永遠跳過那一段。
- ⚠ 戰鬥有自己的 BGM，交棒時要把**戰前那一首**一起帶著回來（`resume.bgm`），
  否則打完接回劇情整段都還在放戰鬥曲。城鎮另有 `TOWNS[x].bgm`，每進一個節點確認一次。
- ⚠⚠ **打完要換別首**就寫在**戰鬥卡**上：`bgmAfter:'<鑰匙>'`（ver -631，Ray：
  「黑爪戰完 bgm 換 Suspense6」）。沒寫＝照舊接回戰前那一首。
  ⚠ **只有打贏才換**：戰敗要再打一次，換了曲子等於幫這一段先畫了句點。
  ⚠ 寫在卡上不寫在腳本裡（鐵律 1）：「這一場打完之後是什麼氣氛」是那一場的性質。

### 結算頁的**評價者**（ver -432，Ray 交稿）

「第一次艦戰後開啟蕾娜評價，之後**除了打靶之外**蕾娜都會在結算畫面評價。
  評價獨立一個 script，分好感度跟 stage。」

- ⚠⚠ **預設就有評價，沒有的是特例**（ver -670，Ray：「之後的每一場戰鬥都要有
  蕾娜評價，沒有的是特例」）。-432 的「第一次艦戰後開啟」（旗標 `eval_renna`）
  與「不評的場次名單」（`EVAL_SKIP`）**都撤了**：
  · 旗標 —— 留著會讓沒跑過那一場艦戰的存檔整條看不到評價（Ray 回報的就是這個）。
  · 名單 —— 與卡上的 `noEval` 是同一件事的兩個真相（鐵律 7）。
  要某一場不評，在**那一場的戰鬥卡**寫 `noEval:true`（現在只有兩場打靶）。
  ⚠ 這一頁只有**劇情／城鎮插入戰**走得到（`settle` 的分流：教學在它之前就被接走、
    出陣走監察官那一套），所以「每一場」指的正是本篇。
- **內容在 `script/evaluation.js`**（鐵律 1）：`LINES[stage門檻][好感門檻][rank]`。
  ⚠ 兩層都是**門檻**不是等於（`pickByThreshold`，同 `config.inspectors` 的 `dialogues`）：
  現在只有 `1` / `0` 兩把鑰匙＝全部適用；日後第 3 章要換一批說法就加一把 `3`，
  第 1、2 章自動沿用舊的那一組。誰來評（`EVALUATOR`）、旗標、不評的場次也在那一檔。
- **什麼時候開啟寫在戰鬥卡上**（`config.battles[x].evalFrom:true`），不寫死是哪一場。
  ⚠ 旗標在**結算那一刻**才記，所以**那一場自己就評得到** —— Ray 的「第一次艦戰後」
  指的是「從第一次艦戰開始」，不是「下一場才開始」。
- **評分公式用試玩版那一套**（Ray 指定）＝ `config.rating` 的 `evaluate()`，S/A/B/C/D/E。
  ⚠ **有人評才給等第**：`scriptSettle` 本來刻意是 `grade-noRank`（教學那一頁的規格），
  但「評價」的意思就是評出一個字母 —— 沒有它，她那句話沒有著落。
- ⚠⚠ **她不是監察官那一套**：走 `showResultSequence` 的 `opts.speaker`
  （`{name, portrait, line}`），**不碰** `getInspector()` / `pickInspectorDialogue`。
  兩者共用的只有**這個版面**，不是那份資料（同「框是共用的，教學那一套不是」）。
  ⚠ `portrait` 是**直接路徑**（立繪住在 `speakers.js`，沒進 ASSETS），監察官那一支才是鍵。
- ⚠ 台詞裡的 **HUND** 照 Ray 的稿寫死，**不要換成 `{P}`**：那一層代換住在 `story.js`
  的 `subst`，結算頁的打字機根本不經過它，換了只會印出兩個大括號。

### 結算副標的用詞（ver -423／-432 補齊）

依敵人卡的 `kind` 換說法，對照表在 `i18n` 的 `result.winSubBy`（只有 `winSubOf` 一支在查）：

| kind | 中文 | 標了誰 |
|---|---|---|
| `harm` 禍魘 | 已淨化 | `centipi` 巨型蜈蚣 |
| `human` 人類 | 已擊敗 | `guild_hunter` 賞金獵人、`witch` 槍之魔女 |
| `ship` 船隻 | 已擊沉 | （還沒有） |
| `target` 靶 | 已擊破 | `dart_target` 固定立靶 |
| `beast` 動物 | 已獵殺 | （還沒有） |
| `slay` 聖徒系列 | 已擊殺 | `faceless`／`facelessgiant`／`trainee` |

⚠ 沒寫 `kind` 的怪退回 `winSub` 那一句，不會壞。
⚠ `slay` 是 ver -432 為**聖徒系列**另開的一類（Ray：「聖徒系列用擊殺」）——
  它們既不是禍魘也不是人類，用既有的兩類都不對。

### 戰鬥**裡面**的對話（ver -426／-429）

⚠⚠ **劇情殺 → 即死防禦 → 聖徒化，是一條有順序的鏈**（ver -619，Ray：「敵 hp 50%
以下時觸發劇情殺把主角三擊清零，一定要三擊，在三擊發生前讓蕾娜喊『小心！』。
主角 hp 被清零後發動即死防禦，然後才進聖徒化教學」）：

- **三擊**走**既有的** `config.tutorial.strike`（`combat.tutorialStrike`，鐵律 8）：
  前兩擊必留 1 HP、第三擊必致死 → 搭檔的即死防禦接住並播 cut-in。
  戰鬥卡的段落寫 `strike:true` ＋ `then:'<下一段>'`。
  ⚠ **不要用「一步把血設成 0」**（ver -599~-618 的 `drain:true`，已移除）：
    那不是三擊，也**不會**發動即死防禦 —— 玩家看不到自己怎麼倒下的，
    也看不到搭檔接住他。
  ⚠ 三擊的**擊數與失誤秒數都不算在玩家頭上**（`_scriptedHits`／`_scriptedAtk`）。
- **`then` 要等末擊落地之後才開始等 cut-in**：`afterCutin` 沒看到 cut-in 時 2.5 秒
  就放行，而末擊在 1.4 秒後才打出去 —— 從 0 秒起算會讓下一段壓在 cut-in 上面。
  長度問 `tutorialStrike()` 的回傳值，呼叫端不要自己乘一次（鐵律 7）。
- ⚠⚠ **生命歸還要在倒數槽 99% 那一拍發動**（Ray：「生命歸還在 OBE 後不能用」）：
  聖徒化的倒數槽**推滿＝OBE**，OBE 一走生命歸還就沒得用了。攔截在滿−1 的實作
  只有 `saint.saintAdvance` 一處，**要不要攔由 `tutorial.saintCriticalPending()` 回答**。
  ⚠ 舊版把它寫死成 `state.tutorialActive` —— 於是戰鬥卡的 `talk`（那一場**不是教學**）
    整條吃不到，槽推滿就 OBE，`php:99` 那一段永遠等不到。**「這是不是教學」不是
    「有沒有人在等這一拍」的同義詞。**


一場戰鬥自己的幾句話（船艦戰的反擊短教學是第一個）寫在
`config.battles[<場次>].talk`，掛在既有的節點上
（`battleStart`／`board:N`／`threat`／`defended`），`talkOnce:'<旗標>'` ＝**打贏過就不再講**。

- ⚠⚠ **對話的實作只有一支**：`modules/tutorial.js` 的 `openStep` 那一條（立繪、打字機、
  真暫停、退場時序）。**不要為了「這又不是教學」另寫一份** —— 另寫一份必然與教學走鐘
  （鐵律 8）。分流在 `fire()`：教學中走 `stepsLeft`，否則走 `talkLeft`。
- ⚠⚠ **但它不是教學**：鎖攻擊力 2、敵人打不死、教學結算、引導閘門、罵人插話
  那一整套一律只看 `state.tutorialActive`，`talk` 從頭到尾不碰它（同「框是共用的，
  教學那一套不是」）。
- ⚠ **紅點的生成帶由 tutorial 那一層統一回答**（`threatBand()`）：只要這一場會插對話，
  紅點就走中央帶（`config.tutorial.threatSpawn`）—— 左右是立繪、下方是對話框，
  落在那裡的紅點在講解時根本看不見。⚠ 兩種場次**共用同一組數字**（鐵律 7）：
  那組數字是對著**這個對話框**量的，不是「只給教學用」。
- ⚠ **開場的大絕是 0~3 秒內隨機排的**（`scheduleOpeningUlt`），而開場白要等
  `startDelayMs`（700ms）—— 所以「紅點比開場白先到」是常態不是例外，
  `talkFire` 必須有那條「先講開場白、這一段排隊接上」的分支。
- ⚠⚠ `talkOnce` 的旗標**打贏才記**（ver -493，`combat.win`／`storyBattleEnd`；
  且僅劇情戰 `state.storyBattle`）：敗北重來每次都重播、打贏之後永久停播
  （§6.5.2 的表）。-426 的「最後一段被取走就記」與 -492 的「勝敗都記」都已推翻 ——
  敗北不再走 Game Over → 讀檔（-430 起），「旗標跟著存檔回去」那個保證已失效。
- ⚠ 掛的位置在 `startGame` 的 `stopAll()` **之後**（`stopAll` 會 `tutorial.abort()` 收掉它）
  且 `loadBoard(0)` **之前**（晚掛就吃不到 `board:0`）。
- **拍上可以帶演出**（ver -429）：`se`（音效名，表在 story.js 的 `SE_FILES` —— 借過來用，
  不要抄第二份）、`shake`（畫面抖一下＋手上震動）、`blank:true`（主角的空白對話框）。
  ⚠ `blank` 那一拍**不動立繪**：台上的人維持原樣 —— 他不在台上，他說話不代表別人要換位。
    名字欄放**暱稱**（同 story.js）。
- ⚠⚠ **兩個人同台要看 `cast` 的 `side`**（ver -429）：蕾娜與諾薇兒在 `speakers.js` 裡
  **都是左**，直接搬進戰鬥對白會一直互相擠掉。`config.tutorial.cast.renna` 站**右**，
  與船塢那一幕（`sides:{RENNA:'R'}`）同一個安排 —— 玩家的空間記憶才連得起來。
- ⚠ **立繪鍵只列真的用得到的那幾張**（config.js 的 `tutPortraits`）：`ART` 每個角色有
  二十幾張差分，整批掛上 ASSETS 等於讓開機預載多抓幾十張圖。路徑與取景值一律**抄 `ART`**。
- 餵稿格式見 `script/SCRIPT_FORMAT.md` §8.65；`script_lint.py` 驗時機／角色／差分圖／音效／空台詞。

### ⚠⚠ 無傷 ＝ 等第下限 S（ver -626，Ray：「無傷基本讓他保證 S」）

`config.rating.flawlessFloor:'S'`，判定 `stats.hitsTaken===0`（＝與結算頁那個
「無傷」標籤同一個），實作只有 `inspector.evaluate` 一處。

- ⚠⚠ **不要寫成「折幾秒」**（ver -620 那一版已推翻）：定額折秒的份量被**場的大小
  稀釋** —— 同樣 10 秒在 300 血的場值 16.7 分、在 1500 血的城鎮戰只值 3.3 分（五倍差）。
  「保證 S」是一句**等第**的宣告，用等第表達才不會被場的大小改寫。
- ⚠ **無傷是很硬的條件**：全程不點錯、不逾時、每一發大絕都完美防禦或反擊 ——
  任何一項破功就掉血，`hitsTaken` 就不是 0 了。
- ⚠ **不必另外加「太慢就不給」的上限**：大絕每 4~8 秒一次，拖越久要連續完美的次數
  越多（拖到 200 秒約要連過 33 次）—— 這個條件**本身就隨時間變難**，慢慢磨是自限的。
  多加一條時間上限只是多一個要調的數字。
- ⚠ 分數一起抬到那一級的門檻：EXP 由分數算，等第與 EXP 不該互相打架（鐵律 7）。

### ⚠⚠ 折算成秒的那幾項：哪些算、哪些不算（ver -659，Ray 定案）

`config.rating.penalty` 把每一項乘上次數折成秒，加進「用時」再算分。判準是
**這一項是不是玩家控制得了的**：

| 項 | 每次 | 為什麼 |
|---|---|---|
| 點錯 `wrong` | +2 | 失誤 |
| 挨大絕 `ult` / 擋一半 `block` / 延時 `delay` | +3 / +1 / +1 | 失誤 |
| 完美反擊 `counter` | −0.5 | 打得好 |
| **完美清盤 `perfectBoard`** | **−1** | 「這一盤沒點錯也沒被打到」是**每一盤自己決定**的 |
| **MB 收尾 `maxBurst`** | **−10（一次性）** | 同上；⚠ 與處決**互斥**（擊殺的那一次是處決） |
| 處刑收尾 `execution` | **−15**（一次性） | 這一場有沒有發生過，不乘次數 |
| **overkill** | **0** | ⚠⚠ 見下 |

- ⚠⚠ **overkill 不折秒**（Ray：「OVK 還是不能算時間，否則不公平，因為 ovk 的量
  幾乎隨機」）—— ver -611 的「一格減 0.1 秒」已推翻：那個量取決於最後一擊溢出多少、
  敵人剩多少血，**不是玩家控制得了的**，折成秒數等於把隨機數寫進等第。
  ⚠ overkill 那一段**照樣計時**、**EXP 照樣給**（`exp.overkillExp`）——
    它是「打爽的」不是「打好的」。欄位留著寫 0 不刪，日後要改回來只動一個數字。
- ⚠ 完美清盤的條件就是既有的 `state.boardClean`（與清盤獎勵聖能**同一個**，鐵律 7）
  —— 不要另訂一套「完美」的定義。累加在 `clearBoard`，一路走 `stats.perfectBoards`
  → `SUM_KEYS`（連戰要併帳）→ `evaluate`。
- ⚠ MB／處決的秒數 ver -675 由 Ray 改定（原本處決是 −5）。惡夢化清空殘格也算 MB
  （Ray：「同 SI 的 MB」）—— 標記走 `state.markMaxBurst()`，與 `markExecution` 一組。
- ⚠⚠⚠ **MB／處決折得很重是刻意的，不要「平衡」掉它**（ver -676，Ray 定案：
  「處決的發生率本來就不高，而且聖徒／夢魘既然高風險高回報，那就要在評價上也
  表現出來」）。
  · **處決很難發生**：要在倒數槽推滿之前把整盤點完、而且那一擊剛好把敵人打死。
  · **聖徒化／惡夢化本身是高風險**：聖徒化推滿＝OBE（HP→1）、惡夢化抽乾＝熔斷，
    中途點錯或反應慢都會把你推向那一端。
  · 所以 −15／−10 在小場上看起來「太甜」（300 血的場等於 20 分）是**對的** ——
    那是拿風險換來的。要調難度動 `timeK`（那才是難度旋鈕），**不要**回頭削這兩項。
  · ⚠⚠ **「小場＝好賺」是誤讀**（Ray 補述）：小場多半是 BOSS 那種血戰，
    在那種場合開得出聖徒化／惡夢化，代表你已經被逼到**慘勝** ——
    不在評價上補回來反而不公平。血量少不等於輕鬆。
  · ⚠⚠ **劇情上它本來就該加分**：監察官（蕾娜）評的就是**主角對聖徒化的適性** ——
    用得出來、而且用得漂亮，正是她要看的那件事。這不是數值上的例外，
    是這套評價的本意。
- ⚠ **自檢**：新增任何一項折秒之前先問「這個量是玩家決定的嗎？」——
  答案是「看運氣」就不要折成秒，放進 EXP 或別的獎勵。

### ⚠⚠ 評價的失誤計數（ver -600 定欄位、**ver -619 才真的接上**）

`penUlt`（挨大絕）／`penBlock`（擋下一半）／`penDelay`（延時懲罰）由
`combat.enemyAttack` **唯一那個入口**累加，`inspector.evaluate` 折成秒數加進攻略時間。

- ⚠⚠ ver -600 定了欄位、結算也在讀，**但從來沒有人 `++`** —— 於是三項懲罰
  永遠是 0，只剩「點錯」在算。那就是「用滑鼠點都可以每場 S」的真正原因，
  `timeK` 一路被推到 550 也壓不住。**加欄位時要連「誰寫它」一起交代**（鐵律 9）。
- ⚠⚠ `enemyAttack(dmg, kind)` 的 `kind` 有**兩個用途**，ver -600 之後不再是同一個值：
  **計數**要分得出 `ult`／`block`，**演出**（音效、受擊特效）分不出來 ——
  敵人卡上只有 `ult`/`delay`/`wrong` 三格。所以演出讀 `fxKind`（`block`→`ult`），
  計數讀原值。-600 改了呼叫端卻沒加別名，格擋的敵大絕音與受擊特效整個掉了。
- ⚠ **腳本演出的擊數不算**（劇情殺三連擊）：`_scriptedAtk` 那一段。

## 6.5.3 武器與商店（ver -377）

### ⚠⚠ 主武器：迦尼米德雙槍（ver -699~-701，Ray 交卡）

> 「裝備欄加入雙槍　迦尼米德α「王之運」／迦尼米德β「運之王」。兩支算同一個武器，
>   但是各有一個掛件槽，可以掛強化護符」「固定武器不可更換，但可以在槍店強化」
>   「顯示強化等級就好…強化等級到 9」「現在強化一次就是 2」
>   「強化原則上走的是素材收集，打靶給強化是特殊事件」

- **資料在 `config.mainGun`**，⚠⚠ **不進 `config.weapons`**：那張表是**副武器**
  （可買可賣可換、有反擊數值）。主武器是**普攻**、而且不可更換 —— 混進去會立刻長出
  「賣掉主槍」「把主槍排進副武器順位」這種不該存在的操作。
- 「兩支算同一個武器」＝一張卡、一組數值，**只有掛件是逐支的**：`barrels` 兩筆
  只帶名字與掛件槽，不帶任何戰鬥數值。卡上的字是 **`固有武裝`**（Ray 指定，
  不寫「固定・不可更換」）。整張卡**不可點** —— 不可更換的東西不做成鈕。

**九階強化：水瓶座九顆星（ver -707，Ray 交卡）**

> 「升級機制：**非線性**。等級編號僅為排列順序，不代表升級先後。升級看素材是否足夠，
>   玩家自由選擇要升哪一項；部分關鍵素材由劇情控制產出。」
> 「命名：水瓶座九顆星。強化武器＝逐顆點亮蕾娜的星座；武力巔峰（爆擊）以主角雙槍
>   Ganymede 命名。」

- 資料在 `config.gunStars`（九筆，順序＝卡上的排列）。⚠⚠ **括號內的數值是內部參數，
  不顯示給玩家** —— UI 只印星名、名稱與效果那一句，所以數值欄位與 `desc` 是分開的
  兩件事，不要把數字寫進 `desc`。
- ⚠⚠ **累計加成只有 `progress.starBonus('<欄位>')` 一個查詢點**（鐵律 7）：
  九個效果散在 combat／weapon／defense／saint／inspector／partner，
  各自去翻 `gunStars` 的話，哪天改欄位名就會有一半沒跟上。

| 星 | 效果 | 接在哪（唯一的計算點） |
|---|---|---|
| Sadalmelik 王之運 | 爆擊傷害 | `combat.critDmgAt` |
| Sadalsuud 運之王 | 爆擊機率 | `combat.critRateAt` |
| Skat 疾走 | 破防值累積 | `combat.addEnergy` |
| Sadaltager 銀幣星 | 金錢掉落 | `inspector` 併完連戰的帳**之後** |
| Safina 方舟 | 無傷擊殺→回復已用的被動 | `partner.onEnemyCleared`（掛在 `finishEnemyOrAdvance`） |
| Albali 吞噬者 | 普攻攻擊力（**可多次**） | `combat.gunTuneMul` |
| Sadachbia 幸運星 | 掉落機率 | `inspector.rollLoot`（乘在機率上、夾到 1） |
| Ancha 交界點 | 反擊後增益秒數 | `combat.triggerAtkBuff` |
| Situla 源泉 | SI 期間連 3 發 → 延長 | `saint.onSaintTap` |

- ⚠⚠ **「源泉」退槽要走 `drainPlayer`，不能用 `saintAdvance(負值)`** ——
  `healPlayer` 開頭就 `Math.max(0, amount)`，負數會被整個吃掉、什麼都不會發生
  （ver -671 的惡夢化抽血踩過同一個坑，查了好幾版）。
- ⚠ 「方舟」的**無傷**用 `state.hitsTaken===0` —— 與結算頁那個「無傷」標籤、與
  `rating.flawlessFloor` 同一個定義（鐵律 7），不要另訂一套。
- **兩條路，各自獨立、可以疊加**：
  · **素材收集**（原則）→ 槍店「武器改裝」分頁，配方 `config.gunUpgrade.recipes[星id]`
  · **特殊事件**（例外）→ 腳本那一拍寫 `gunStar:'<星id>'`（北方泊地打靶＝`albali`，
    正是 ver -655 那個「普攻 +5%」）
- ⚠ 改裝頁把**九顆星全部列出來**：這個玩法的內容就是「知道要去收什麼」。
  選一列再按強化（非線性）；素材夠不夠只有 `modReady` 一支在判（鐵律 7）。
- ⚠ 舊存檔（-700 的線性 Lv1~9）**自動遷移成吞噬者的次數**：那時的效果就是 +5% 普攻，
  語意完全對得上（Lv1 是出廠，所以 lv−1 次）。
- ⚠⚠⚠ **`gunUpgrade.recipes` 的素材與價格仍是我擬的草案**，Ray 給的是九顆星的
  **效果**，沒給配方。「部分關鍵素材由劇情控制產出」那幾樣也還沒填。

**掛件槽（強化護符）**
- 護符就是**一般道具**（`cat:'charm'`）：商店買、劇情拿、遺跡開、怪掉落都走既有的
  道具管線，點槽就換（Ray 指定）。
- ⚠ **同一張護符不能同時掛兩支槍**：身上只有一個時裝到另一支＝**移過去**，
  有兩個才各掛一個。收在 `progress.setCharm`（鐵律 8）—— 日後劇情自動裝上那條路
  不必再判一次。
- ⚠ 掛件是**一輪內**的（護符是道具）：`newRun()` 清、`runSnapshot/runRestore` 帶（§6.9）。
- ⚠⚠ **`items.defs` 目前一張護符都沒有是刻意的**（Ray 的卡還沒到）：不要自己發明
  名字與數值。一張長這樣：
  `charm_xxx:{ name:'…', cat:'charm', price:…, charm:{ dmgMul:1.05 }, desc:'…' }`


⚠⚠ **本篇的整備頁 ＝ 點槍棺上的吊墜**（ver -422 改寫，`modules/gear.js`）。
  它與試玩版的**出擊整備**（`#prepSheet`）是**兩頁**：那一頁是出陣流程的一站
  （按「執槍」就開打），這一頁是城鎮／劇情裡的裝備管理。共用的是**資料**不是版面。
  三件事：①副武器三個順位（可拖曳）②切換模式的開關 ③搭檔卡（本篇只有諾薇兒）。
⚠⚠ ver -497（Ray 指定）再加兩件：**頁首顯示目前體力**（兩個分頁都看得到；
  真相＝progress 的持久 HP、上限＝`tuning.playerHp`，不另存）；**道具分頁按類別
  分頁**（頁籤照 `config.items.catOrder`，`item`＝使用類本來就排第一），回復道具
  （`defs[].use.hp`）長「使　用」鈕 —— 實作只有 `prog.useHealItem` 一支（鐵律 8：
  日後任何戰鬥外的使用入口都走它；滿血不消耗）。清單仍走 `bagListHtml`（同一份）。
⚠⚠ **副武器的編成只有一份真相**：`script/loadout.js`
  （類別順序／每個類別拿哪一把／切換模式）。戰鬥中那顆切換鈕**讀它**，
  整備頁**寫它** —— 不要在任何一邊自己記一份。**跨輪**（§6.9：武器與搭檔的選擇不清）。
⚠⚠ 切換模式（整備頁下方的開關，Ray 指定）：
  `rotate`（**預設**）按一下換下一順位；`fixed` 永遠從一順位開始、**連按 N 下切到第 N 順位**
  （350ms 內算連按），一次防禦判完就歸位（`weapon.onThreatResolved`，由 combat 串）。
⚠ **裝備清單不能用 `inv.grouped()`**：開局就有的那三把槍從來沒進過道具袋
  （config 的 `owned:true`）—— 要問 `inv.ownedWeapons()`（§6.5.3 的同一條）。
⚠ 從劇情層叫出來的全螢幕頁要抬到 `#storyStage`（8300）之上：整備頁 8450、它叫出來的
  道具欄 8460；`#prepSheet` 那一套走 `body.prep-over`（8400／8420）。
⚠ 槍棺上的兩顆功能鍵：**左邊吊墜＝整備、右邊齒輪＝選單**。兩顆都在推棺之後鎖住點擊。


- **武器的數值只有一份**：`config.weapons[]`。出擊整備的卡與槍店的規格表都由
  `config.weaponStatRows()` / `weaponDescText()` **算出來**（鐵律 7）。
  ⚠ 以前 `desc` 裡手寫了一份規格文字 —— 那是同一組數字寫兩次，改了數值文案就會騙人。
  現在 `flavor` 只留一句不含數字的風味。
- **武器沒有第二份道具定義**：`inventory.defOf()` 查不到 `items.defs` 就回頭問
  `config.weapons`（名稱／價格／說明）。武器的道具 id **就是** `weapons` 的鑰匙。
- **能不能拿去打**只有一支函式：`inventory.hasWeapon()`（開局就有的 `owned:true`
  ＋ 買來的）。出擊整備的卡疊、商店的「已持有」都讀它（鐵律 8）。
  ⚠ 卡疊要用「持有清單當指紋」判斷是否重建 —— 不然剛買的槍不會出現。
- **一家店長什麼樣**寫在 `config.shop.shops[key]`（頁籤、店主圖、只收哪一類、要不要
  比較數值）。`showShop` 只讀它，不為某一家店寫特例。
- **絕對值優先**同敵人卡：橘圈改打傷害寫 `perfectDmgPerHit`（卡上的絕對值），
  倍率由 `defense.js` 現算，只有那一處。
- ⚠⚠ **本篇與試玩版是兩套數值**（ver -378，Ray：「修改初始特殊武器數值，但是跟試玩版的
  『出陣』那個分開，**試玩版的不要動**」）。作法：`config.weapons[key]` **本體＝試玩版**
  （一個字不要動），本篇要調的欄位寫在 `story:{…}` 覆寫裡。
  **讀取一律走 `weaponOf(key, story?)`**（鐵律 8：不要在各模組自己 merge 一次），
  `story` 與否由 `state.storyMode()` 決定（＝`scriptRun || tutorialStoryRun`）。
  ⚠ 顯示端也分兩邊：商店與道具欄顯示**本篇**數值（那些地方只在城鎮裡），
  出擊整備頁顯示**試玩版**數值（那一頁就是試玩版的出陣流程）。
  本篇日後有自己的裝備畫面時，那一頁要傳 `true`。
- ⚠ 初始武器**不能賣**，所以卡上的價格存成 `value`（市價）**不是** `price`——
  `price` 一寫下去 `sellPrice` 就有值了。

### ⚠⚠ 北方泊地：自由探索期（ver -655，Ray 交稿）

黑爪那一段演完（`np_claws_done`）之後的狀態。

- **不可離港**：節點的 `sail.hold:{need, until, text}`（判定只有 `modules/town.js` 的
  `sailHeld()` 一支）。⚠ 它與 `sail.flag` 是**兩件事**：`flag` ＝「船還沒到手」
  （一去不回的前置），`hold` ＝「這一段劇情裡不准走」（會開會關的暫時狀態）——
  合併的話拿不到船那一段的台詞會跑出來。
  ⚠ 鐵律 9：`until`（`np_leave_ok`）**現在還沒有人插** —— 下一段劇情的稿還沒到，
  所以這一版是真的走不掉。名字先留好，插旗的事件由那一段自己認領。
- **兩家店的店主是這座城自己的人**：`config.shop.shops.np_grocery`／`np_gunstore`
  （功能與帝都相同，只有店主圖與**貨單的鑰匙**不同）。
  ⚠⚠ 貨單**分開記帳**：`script/shopstock.js` 的鑰匙就是那個鍵 —— 共用的話在帝都
  買空的東西飛到北方泊地也是空的，而那是兩家店。
  ⚠ 兩位店主的 speaker id 是 `GUNSMITH_NP`／`SHOPKEEP_NP`：**顯示名一樣（「店主」）
  但是不同的人**，art 不同就不能共用一個 id（同 §6.5.6 那條的反面）。
- **打靶（`np_range`）只有一條門檻**（25 秒）：Ray 的稿只有「成功／失敗」兩支台詞，
  所以只寫 `parSec`，不寫帝都那一場的 `prizeSec`/`prize`（＝「過關但沒獎品」的中間帶）。
  獎品不是道具而是**主槍的強化**，由腳本那一拍 `flags:['np_gun_tuned']` 記。
- **挑戰費的規則**（ver -656，Ray：「挑戰失敗就要再花錢挑戰，成功以後挑戰不用錢
  但也不會有強化事件」）：選項寫 `cost:200, costUntil:'np_gun_tuned'`
  —— 旗立起來就免費；**失敗不立旗**，所以要再挑戰就得再付一次。
  獎品的那幾拍用 `skipIf`／`onlyIf` 擋掉（見 SCRIPT_FORMAT §8.56）。
- ⚠⚠ **主槍的永久強化**（`tuning.gunTune`，Ray：「主槍普攻攻擊力強化5%」）：
  旗標立起來之後普攻傷害 **×1.05**（連擊加成一起放大），
  **只有 `combat.hitDamage` 一處在讀**（鐵律 7）。
  ⚠ 不可以快取成模組常數（像 `DMG_BASE` 那樣）：旗標是遊戲中途才立的，
  快取的話要重整頁面才生效。
  ⚠ 試玩版（出陣）吃不到 —— 那條路不跑主線，旗標永遠不存在。
- ⚠ 立繪的 `cm` **不要填超過現行最高的 178**：`CAST_TALL` ＝全體 `cm` 的最大值，
  每公分像素是拿它算的 —— 一個 NPC 的估身高不該把整個劇組一起縮小
  （實測填 180 全體縮 1.1%，而他自己頂到框）。

**兩個強制轉場（`gates`，ver -656）** —— 安葬死者 ／ 那一夜

城上寫 `gates:[…]`（`stage1` 是它的舊名，視為只有一項的清單）；判定與執行只有
`modules/town.js` 的 `stageGate()`／`clockGate()`（鐵律 8，與帝都 stage 0 的結尾同一支）。
一筆的欄位：`flag`（立起來就不再觸發，**立刻記**）／`need`（前置旗）／
`hour`（時間軸上那一個時刻）／`hourOfDay`（**今天**過了這個時刻）／
`onMove`（走一步就觸發）／`goto`／`enterAgain`／`clockTo`（推到**下一個**這個時刻）／
`stage`／`lines`／`sides`。條件全部是 and，由上往下取第一個成立的。

- ⚠⚠ **`hourOfDay` 寫成 `[起,迄]` ＝時段**（ver -664）：「隔日早上八點」不可以寫
  `hourOfDay:8` —— 那是「今天過了八點」，而**當晚八點也過了八點**，於是那一幕
  在前一晚就演掉了（實測 20:00 就跳出來）。寫成 `[8,18]`，只有真的走到隔天早上
  （睡醒或熬夜走到）才成立。迄不含（同營業時間的規約）。
- ⚠⚠ **`hour` 與 `hourOfDay` 是兩種時刻，混用一定出錯**（-656 踩過）：
  `firstHourAt(18)` ＝**開局那天**的 18:00（開局 11:00，所以是第 7 小時）——
  北方泊地是第二天以後的事，那個點早就過了，寫 `hour:18` 等於「立刻成立」，
  安葬一演完就被抓回旅店，中間整段自由探索消失。
- ⚠ `onMove` 讀的是 `backDir`（這一次抵達由 `pendingDir` 推出來的）：走過來才有，
  開城／強制轉場／讀檔都是空的 —— 那正是「一進行地圖移動」的定義。
- ⚠ `enterAgain`：玩家可能**已經站在目的地**（在旅店大廳待到六點），
  那時 `goto===nodeId`，不重新 `enter()` 一次的話那一格的 `acts` 沒有人叫得動。
- ⚠ 閘門的旗與那一段戲的旗是**兩支**（`np_burial` ／ `np_burial_done`）：
  一個回答「祭司來過了沒」、一個回答「那場戲演完了沒」（鐵律 9）。
- ⚠ `clockTo` 走 `clock.advanceToNextHour()`（已經過了就推到隔天），與
  `advanceToHour()`（推到今天、過了就不動）是**兩支**，不要合併。

**旅店（北方泊地）**
- ⚠⚠ **門的狀態是一張由上往下取的表**（節點的 `innDoors`，ver -666）：每一項
  `{need?, roster, asleep?, out?, answerBy?, say?}`，第一個 `need` 成立的就是現在的
  樣子（同 `acts` 的取法）。不寫＝這一章入隊的所有人（`girlsHere`）。
  ⚠ 它取代了 -656~-660 的 `innRoster`／`innAsleep`／`innStage1.answerBy` ——
    那三個各自表達門的一部分，狀態一多就對不起來（北方泊地兩天就兩種樣子）。
- ⚠⚠ **門有三種狀態，不要混用**：
  `awake` 燈亮／`asleep` **人在裡面但睡著了**（門在、燈熄、臉照畫、壓暗）／
  `out`（＝`empty`）不在房裡（臉都不畫）。
  北方泊地：那一夜的諾薇兒與安雅是 `asleep`（躺在那間房裡）；隔天早上諾薇兒是
  `out`（去了教堂）、安雅 `awake`。
- ⚠⚠ **`answerBy` ＝每一扇門都由同一個人應門**（Ray：「安雅跟諾薇兒的頭像也要上，
  但是點下去都是蕾娜應門」）。它只換「誰回話」，**不換門上的臉** ——
  `doorState`／`faceStyle` 照舊問真正的住客。換在**所有分支之前**，宵禁／
  今天約過了／她自己那一支照舊適用（鐵律 8）。
- `say:{WHO:'…'}` ＝這一個狀態下那個人的敲門詞（安雅隔天只回「........」）。

**⚠⚠ 劇情探索 ⇄ 自由探索（ver -666，Ray：「劇情期間不會在城鎮內遇到女角，
這個要插個 flag，分自由探索跟劇情探索。目前北泊都是插劇情探索旗，一樣視需求插拔」）**
- **劇情探索**＝女角不排外出行程（`rollOuting` 直接不排）：走到哪一格都不會碰到
  她們，餐飲街也不會因為誰在那裡而換店。**自由探索**＝帝都現在的樣子。
- ⚠⚠ **只有一支旗，而且它記的是「例外」**（鐵律 9）：
  · 城上 `storyExplore:true` ＝ 這座城**預設是劇情探索**（那是資料，不是狀態）
  · 旗 `free_explore_<地圖>` ＝「這座城已經開放自由探索了」，**插了才算**
  · 誰插的：act 上 `endStoryExplore:true`（演完才記）／誰拔的：act 上 `storyExplore:true`
- ⚠ 為什麼不是「進城時插一支劇情旗」：那樣**拔掉之後下次進城又會插回去**，
  於是要再加一支「已經拔過了」的旗 —— 同一個狀態兩支旗，必然有一支忘了立。
  把旗定義成**例外**就只需要一支。
- ⚠ 它與**安全區旗**是兩件事：那個管「有沒有遭遇戰」，這個管「碰不碰得到女角」。
  一座城可以是安全區但還在演劇情（北方泊地現在就是）。
- ⚠ **不是「排了再擋」**：排了就會被 `outingDebug` 印出來、也會被餐飲街的
  `whoOutAt` 讀到 —— 那是同一件事的兩個真相。
- ⚠ `innNoGuide:true` ＝這一格不跑一次性說明（玩家在帝都的旅店已經學過了）。
  寫在**節點**上不是全域關掉 —— 帝都那一次還是要教。
- ⚠ `noSleepUntil` ＝那支旗立起來就睡得著了；`innWake` ＝這一格的起床時刻
  （不寫＝ 07:00）。兩者北方泊地都**沒有用到**（ver -665，Ray：「那一夜演完之後
  不用睡，直接往下跑劇情到八點。因為沒畫夜間差分，現階段玩家跑出去夜遊也沒東西」）
  —— 那裡的睡覺一律擋著，時間由閘門的 `clockTo:8` 推過去。機制留著給別的城用。
- ⚠ `innStage1.rennaAlt:{need,lines}` ＝某支旗立起來之後敲門改講另一段（可多句，
  走劇情播放器）。
- ⚠ ver -656 曾經**不給 `innSpots`**（＝沒有那兩顆行動鈕）想擋住「睡覺跳過那一夜」
  —— 被 Ray 退回（-659）。正解是**擋在按下去那一刻**（`noSleep`），鈕要在。

**⚠⚠ 每次抵達都演的段落一定要給終點（`until`，ver -668）**
`acts` 的一項**不寫 `flag` ＝每次抵達都演**（`actDue` 只跳過「旗立了」的段落）——
那是「再訪」要的行為，但它**不會自己停下來**。`until:'<旗>'` ＝那支旗立了就不再演。
- ⚠ 與 `flag` 是兩件事：`flag` ＝「這一段演過了」（自己記的）、
  `until` ＝「別人那一段演完了」（別人記的）。
- ⚠⚠ **接續只接得上「有 `flag` 的下一段」**（ver -684，Ray：「第一次進教堂諾薇兒
  不會說不要催我，那是第二次以後」）：沒有 `flag` 的段落是**每次抵達都演的常駐句**
  （「再訪」），它本來就該等**下一次走進來**才講 —— 接在主線段落後面的話，
  第一次進教堂就會在戰地醫院那一段之後立刻補一句「不要催我」。
  **接續是給主線用的，常駐句不屬於那條鏈。**
- ⚠⚠⚠ **「還有下一段就原地接上」不可以接上剛剛演完的那一段**（ver -669）：
  沒有 `flag` 的段落演完之後 `actDue` 照樣回它自己 —— 於是它會**立刻再演一次，
  永遠不停**（那不是「重播」而是當場無窮迴圈，畫面就卡在那一句上，Ray 連報兩次）。
  判的是**物件本身**（`actDue(n) !== act`）不是「有沒有 flag」：日後若有兩段都沒有
  flag，還是接得上另一段。
- ⚠ 自檢：任何「條件成立就再跑一次」的接續，都要問「**這一次跑完，條件會不會
  還是成立**」——會的話就需要一個「不是剛剛那一個」的排除。
- 實例：教堂的「等等啦！不要催我！」`until:'np_grave_done'`（墓地那一幕演完就解除）
  —— 不寫的話會演到天荒地老（Ray 回報「卡在不要催我」）。

**⚠⚠ 「離開這一格之前要演的一段」寫在 `onLeave`（ver -664）**
Ray：「點擊離開旅店時 安：『……』…」。節點寫 `onLeave:{flag, need, sides, lines}`，
按下移動的那一刻先演完，**演完才走**（旗標演完才記，同城鎮所有段落）。
- ⚠ 與 `acts`（**抵達**時演）是兩個時機，不要混用：這一段的語意就是「你要走了」。
- ⚠ 出航也吃得到 —— 那一樣是「離開這一格」。

**⚠⚠ `nextFavor` 也掛得到「段落」上（ver -664）**
以前它只在**進場對白**演完時開機會（`armFavor` 只在那一支裡呼叫）——
而主線段落（`acts`）走的是另一條路，於是「應要求直接去教堂 +5」整條不會生效
（實測好感是 0）。現在 `acts` 的一項也可以帶 `nextFavor`，演完一樣開一次。
- ⚠ `throughConnectors:true` ＝**連接場景只是路**，走過去不算「去了別的地方」。
  Ray 說的是「若先進了其他**末端**地圖則失去此機會」，而兩個末端之間一定要
  經過連接場景 —— 不放行的話那個機會不可能達成。「連接場景」是算出來的
  （`connectorIds`），不列死名單。
- ⚠⚠ **沒有初見對白的旅店，大廳要一開始就在**（`inn.introDone()`，-656 修）：
  大廳是等 `introFlag` 才出現的，而那面旗是**進場對白演完**才立的 ——
  沒有 `lines` 的旅店永遠等不到，門與鈕整組不出現。現在城鎮那邊沒有 `lines` 就傳 null。

## 6.5.4 城鎮畫面上的互動元件（ver -387）

- ⚠⚠⚠ **ver -430 改：走進店裡出現的是「店主 ＋ 一顆大鈕」，點下去才開全畫面那張窗**
  （Ray：「龍息事件仍然被買賣視窗蓋著，把買賣窗變成一個大的按鈕，點下去開全畫面窗」）。
  - **為什麼推翻 -404 那張常駐單子**：它一直蓋著畫面左半 —— 任何要**指著畫面**的演出
    都被壓在底下（整備教學指的吊墜就在它底下，兩次都被回報）。而且它窄到只看得到
    一兩列商品，本來就得點標題展開才好用 —— 「常駐」換來的資訊量趨近於零。
  - `openMenu()` ＝把那顆鈕交還給玩家；`openSheet()` ＝開窗。窗**不帶 `dock:'left'`**
    ＝同一份 CSS 的置中全寬版（鐵律 8：不要為了全螢幕再寫一套版面）。
  - 窗開著時鈕收起來、窗關掉鈕自己回來（`onClose`）—— 兩者是**同一個入口的兩個狀態**，
    不要讓玩家關掉窗之後就沒得再開。
  - 鈕落在**左上、地名那一行之下**：右邊站著店主（`castSolo` 固定 `side:'R'`），
    下半是槍棺與吊墜 —— 左上是這一頁唯一沒有東西的角落。
  - ⚠ 鈕上寫**這家店在做什麼**（買賣／懸賞榜），不是店名 —— 店名已經在上緣那一行。
  - 以下 -404 那一段的其餘規則（店主立繪走 `castSolo`、不放常駐對話框、地名讓開）**照舊**。
- ⚠⚠ **店舖畫面 ＝ 右邊店主立繪、左邊選單，走進去就是**（ver -404，Ray：「把各商店的
  櫃台按鈕改成店主立繪…不用點擊，直接右店主左選單」）。ver -387 的櫃台鈕
  （`#townCounter` ＋ 節點上的 `counter:{x,y}`）與 `placeCounter` **整個撤掉了**。
  - 立繪走 `story.castSolo()` —— 與對白**同一支** `ensureOn`、同一把尺（§6.5：
    同一張立繪＝同一個結果）。店舖不准另算大小或站位；NPC 的 `side` 本來就是 `'R'`，
    所以「右店主」是既有規則的結果，不是那裡寫死的。
  - 選單走既有的 `showShop`／`showBounty`，只多一個 `opts.dock:'left'`：**靠左停、
    不壓黑、不吃掉其他地方的點擊**（右邊站著店主，底下的面盤還要能走出店門）。
    展開全寬＝**把 `dock-left` 拿掉**，回到同一份 CSS 原本那張置中的視窗（鐵律 8）。
- ⚠⚠ **店主不放常駐對話框**（Ray：「騰空間出來給選單」）。試過留一條全寬的招呼語：
  單子只剩 225px，而一張完整的商店單子要 306px（實測 390×844），怎麼壓都塞不下。
  那一句就是那 80px 的代價。
- ⚠ **點標題可以展開全寬**（Ray 指定）：窄單子看得到店主但欄位窄，要細看武器規格就展開。
  點的是**標題列**不是整張單子 —— 每一列與那排鈕都有自己的動作，整張都能點會誤觸。
- ⚠ **上緣的地名／時刻要讓開立繪的臉**（`body.town-shop`，同 §6.5 的 -385）。
  這是持續狀態不是對白，所以不借用 `story-talking`，另給一個 class；開關只有一支
  （`modules/town.js` 的 `setShopOn`，鐵律 8）—— 按下「交談」進入真正的對白時要先關掉。
  讓開不等於不給：地名與時刻改印在單子的標題下（`showShop` 的 `opts.info`）。
- ⚠ **單子的下界是「楣的上緣」**：`--kerb-clip-top` 由 `layoutKerberos` 解出來，
  ver -404 起**順手發佈到 `:root`** —— 單子是 body 底下的兄弟元素，讀不到
  `#storyStage` 上的那一份。不要在 CSS 裡用「56vh − 楣高」再算一次（鐵律 7）。
- ⚠ **一個地方只給一個入口**：走進去就開；玩家關掉之後**點畫面**再開回來，
  走的是同一支 `openMenu`（已經開著時它自己 return，不會疊第二張）。
- ⚠⚠ **店鋪有存貨**（ver -405，Ray：「給店鋪加入存貨數量，購買時可選擇購買數量，
  槍店武器存貨量都是 1，除非玩家賣給他才會入庫再賣」）。
  - 初始貨量是**內容資料**：`config.shop.stock[店] = [{id, n}]`。寫成單純的字串
    ＝**不限量**（那一項永遠買得到，也不記帳）。
  - 「現在還剩幾個」只有一個記帳點：`script/shopstock.js`（鑰匙 `tivot_shopstock_v1`）。
    程式一律問 `shopStock.list()/count()`，**不要直接讀 config**（鐵律 7）。
  - **賣給店家＝入庫**，之後買得回來 —— 貨單上原本沒有的東西也會擺出來賣。
  - 存的是**絕對值**（還剩幾個）不是差值：差值在 config 改動後會算出負數或憑空長貨。
  - ⚠⚠ 這是**一輪遊戲內**的狀態（§6.9）：`newRun()` 要清、`runSnapshot/runRestore`
    要帶 —— **同一張清單的兩面**，漏一支就是「讀了舊存檔卻帶著新一輪買空的貨架」。
  - ⚠⚠ **永遠帶著、賣不完的東西**（`items.defs[].always`，ver -661，Ray：「讓主角身上
  永遠有一個『薇拉馮德家的紋章』，數量無限，賣一次得錢 1000 元」）：
  它**不進道具欄的帳**（localStorage 一個字都不寫）——「永遠有」是道具**定義**的
  性質，不是持有狀態（鐵律 9：狀態要答得出誰插誰拔，這個沒有人插也沒有人拔）。
  所以 `newRun()` 不必清、存讀檔不必帶、賣掉不會變少。
  ⚠ 數量是 `Infinity`，UI 一律問 `qtyText()` 印成「∞」（鐵律 8：四個印「×n」的
    地方都問它，漏一個就會出現「×Infinity」）。
  ⚠ **賣掉不入店家的庫**：入庫那條是給「真的會變少」的東西用的。
  ⚠ 賣價寫 `sellValue`（**絕對值**）不寫 `price`：Ray 說的是「賣一次得 1000」，
    不是「市價 2000 打五折」—— 同敵人卡與武器卡的原則，`sellRate` 一改就走鐘（鐵律 7）。
- ⚠⚠ **買與賣是同一台購物車**（ver -662，Ray：「賣沒有做結帳啊」）：-496 只把買
  做成車，賣還是「選一項→按一下賣一個」，兩邊操作不一致。現在同一顆結帳鈕，
  差別只有**單價從哪裡問**（`unitOf`）與**上限是誰**（`capOf`：買＝店裡剩幾個／
  武器 1；賣＝手上有幾個，`Infinity` 夾成 99）。
  ⚠ 錢的上限**只有買才擋**（賣是進帳）。⚠ 賣的總價前面加「＋」：同一顆鈕在兩頁
    的金額方向相反，不標會讀成要付錢。
  ⚠ **結帳音買賣共用 `se_buy`，而且只在真的成交那一刻響**（ver -663，Ray：
    「賣也要有結帳音效喔」）—— 那是「結帳」的聲音，不是「買東西」的聲音。
    按下去先「喀」一聲的話，沒成交也會響，成交時還會兩聲疊在一起。
- ⚠⚠ **購買是購物車**（ver -496，Ray：「可以選要購買的商品數量一次結帳」，
    取代 -405 的「選一項→調數量→買下」）：每一列自己的 −/＋、底下一顆「結帳」
    整車一次付清。上限照舊取最小：**店裡剩幾個／武器的 1**（`hasWeapon` 是布林，
    第二把表達不出來）；**錢的上限擋在「＋」那一刻**（整車總價要罩得住，
    罩不住就按不動）—— 所以結帳鈕不會有「錢不夠」這一態，車空了才暗。
    ⚠ 「已持有就不能再買」**只限武器**：一般道具持有了照樣加購（-405 把 `owned`
    擋在整鈕上，連牛奶都買不了第二瓶 —— 那是 bug 不是規則，-496 修正）。
    ⚠ 換頁籤清空購物車：留著會把上一頁的總價帶進結帳鈕。
    ⚠ 結帳仍**先扣貨再扣錢**（`take` 回傳真的拿到幾個）—— 貨不夠只會少賣，
    不會發生「錢扣了東西沒拿到」。
  - ⚠ 「售完」照舊標在**那一列**上；整列變暗、控件不給。
- ⚠ **路人單句要標名字「路人」**（ver -405，Ray 指定）。字串在 `SPEAKERS.VOICE.name`，
  城鎮那邊問它，不要寫死（鐵律 7）。NPC 台詞已經不包引號了（見下），名字欄是唯一
  還能標明「這是一句話不是旁白」的地方。
- ⚠ **NPC 講話不要用「」包起來**（ver -404，Ray 指定）：對話框與名字欄本身就標明了
  這是誰在講話，再包一層引號只是噪音。路人單句（`chatter`，沒有名字）也一樣。
  句子**裡面**的引號（『永夜』、「禍魘」）是原文的一部分，照留。
- ⚠ **路人單句是「點一下出、再點一下收」**：收掉之前不出下一句。一直點就一直換句的話
  玩家永遠讀不完。狀態記成布林值（`chatterOn`），**不要從畫面反推** ——
  對話框的 `visibility` 是這一拍稍後才套上／撤掉的（同 §6.5 的 -385 那個坑）。
- ⚠⚠ **「回去」掛在來時方向的反向**（ver -405，Ray：「從哪個方向過來，回去就按方向」）：
  往上走進去的地方往下退得回來，往左走進去的往右退回來。記的是**這一次按了哪個方向**
  （`town.js` 的 `pendingDir` → `backDir`），不是節點資料上的欄位 —— 同一個地方可以從
  不同的路走到，寫死在資料上一定有一邊是反的。
  ⚠ 沒有記錄時（開城第一格、戰鬥交棒回來、讀檔）退回舊行為「掛在下」。
  ⚠ **出航先擺**：「下」那一格是它的既定用途，不能被「回去」擠掉。
- ⚠ **槍棺一上推，右上角的「選單」鈕就往外撤出**（ver -405，Ray 指定）。它錨在楣的
  下緣，門一動就跟著往上飄；而且那一刻它也不該能按。用 `:has(#kerb.rise)` 讀門自己的
  狀態，不另外掛 class（鐵律 8），曲線與門同一條。
- ⚠ 城鎮入口的**下方是「出航」**（`sail:{flag,blocked}`）：走的是同一套方向出口，
  不另做一顆鈕 —— 對玩家而言「往下走」與「出航」是同一個動作，只是目的地在城外。
  旗標由**主線**立（拿到船的那一幕），城鎮只讀。
- ⚠⚠ **營業時間**寫在節點的 `hours:[開,關]`（24 制，**上界不含**；不寫＝全天，旅店就是）。
  判定只有一支：`isOpenNow()`，時刻只有一個計算點：`clock.hourF()`。打烊時：
  - **走不進去**（ver -406，Ray 指定）：`go()` 攔下來，不移動、**不推進時鐘**，
    就地浮一句「店名／打烊描述＋營業時間」（`knockClosed`，走路人單句那一套）。
    走一趟空店要花 10 分鐘，而時間是資源 —— 那是實質的懲罰。
  - **目的地字格上就標「（已打烊）」**（`nameOfNode`，所以字格與蓄能提示都吃得到）。
  - 進場對白不播、店主與選單不出現、地名後面加「已打烊」。
- ⚠⚠ **字格的位置要在 `#townNav` 有 `.on` 之後才量**（ver -406 修）。沒有 `.on` 時整層
  `display:none`，量到 0×0 → 夾回畫面內那一段退回預設半寬，字格被擺在錯的地方
  （實測左緣 −23，第一個字被切掉）。以前字短剛好看不出來，加了「（已打烊）」就露餡。
  **這是量 rect 的通病**：量之前先確認那個東西真的顯示著。
- ⚠ **城鎮背景要些微糊化**（`#storyStage.town-on #storyBg`，ver -391，Ray 指定）：
  目的是**把 AI 製圖的顆粒磨掉**，不是做景深。量是 `blur(0.6px) contrast(1.03)` ——
  0.6 CSS px（DPR 2 下 1.2 裝置像素）剛好蓋住雜訊的尺度卻碰不到輪廓；對比補回低通吃掉的銳利度。
  ⚠ 試過 2.2px／1.2px 都被退回：「那是景深，不是我要的」。**只罩城鎮**，主線場景不糊。
  ⚠ 配 `transform:scale(1.01)`：blur 會往框外取樣到透明，不放大四邊會有一圈半透明軟邊。
- ⚠⚠ **時段差分的候選鏈只有一份**（ver -427）：`modules/story.js` 的 `bandNames()` ——
  `這個時段 →（退路時段）→ 無時段`，每個名字再試大小寫變體、再試 `.webp → .png`。
  ⚠⚠ **退路沿著明暗軸走，暗的時段不准退回白天**（ver -576，Ray：「攝政王廣場的
    夜間差分不正確」）。舊版一律 `這個時段 → _Day → 原名`，而 `Capital_Square`
    只有 Day／Dusk／midnight（**沒有 night**）—— 晚上八點到十二點站在廣場看到的
    是大白天。現行的退路表（`BAND_FALL`）：
      `Dawn→Day→Dusk`／`Day→Dawn→Dusk`／`Dusk→Day→night`／
      **`night→midnight→Dusk`**／**`midnight→night→Dusk`**
    ⚠ 同一個洞不只廣場：旅店與餐酒館沒有 midnight、雜貨舖與武器店只有 day/dusk。
      改在這一支全部一起好（鐵律 8）—— 不要在城鎮那邊為某個地點寫特例。
    ⚠ 這是**退路**不是替代品：該有的差分還是要交（退到 midnight 的夜晚廣場只是
      「不會錯得離譜」，不是「對」）。
  城鎮背景（`town.bgFor`）與**插圖**（`story` 的 `cg:`）都讀它。
  ⚠ 插圖自 ver -427 起**也吃時段差分**（Ray 指定，第一張是 `005_Kerberos_day/_dusk`）——
    腳本照樣只寫 `cg:'005_Kerberos'`，挑哪一張是算出來的。沒有差分的插圖不必改，
    候選鏈自己會退到原名。`cgNoTime:true` 可以明講「這張沒有差分」。
  ⚠⚠ **插圖的候選只解析一次，而且在預載那一段就解好**（ver -433，Ray：「插畫載入
    有問題」）。一張**沒有**時段差分的插圖會生出六個候選（`_Day`／`_day`／原名 ×
    `.webp`／`.png`）—— 以前是**顯示的當下**才逐個試，於是每次演到那一拍都要先吃
    4~5 個 404；那比「黑幕最多蓋 900ms」還久，黑幕先掀開了、插圖還沒到
    ＝ **插圖沒出現**。現在 `resolveCg()` 在預載時解出贏家、`cgResolved` 記著，
    顯示時只請求那一張（`cgCandidates`）。
    ⚠ 快取的鑰匙是**第一個候選**（已經把時段編進去）不是基底名 ——
      用基底名的話天亮之後還會拿出黃昏那一張，時段差分整個失效。
    ⚠ 一個都載不到就記 `null`（＝這張插圖不存在），下次不必再試一輪。
  ⚠ **腳本寫錯插圖名不會有人吭聲**：ver -433 抓到 `cg:'noue_fall'`（檔頭範例裡的假名字
    被抄進真腳本），六個候選全部 404，畫面上就是沒有插圖。`tools/script_lint.py`
    驗得出來 —— ⚠ 但**它需要 macOS 的 `jsc`**，在 Windows 上跑不了，
    只能手動對一次（`grep -o "cg:'[^']*'"` 對 `ls resources/illustration/`）。
  ⚠ 探測**就是**那一次載入（設 `el.src` 看 onload/onerror），不另開一輪 —— 那等於同一張圖抓兩次。
  ⚠ 為什麼要兩種副檔名：規約是 WebP（§5），但交件常常先是 PNG —— 載不到就整個時段沒背景。
  ⚠ 為什麼要大小寫變體：`clock.band()` 出的是 `Dawn/Day/Dusk/night/midnight`，
    但實際檔名兩種都出現過（`_Night` vs `_night`）。**macOS 不分大小寫，靜態空間分** ——
    在本機測不出來，上線才 404。

## 6.5.4.0 時間的規則（ver -435，Ray 定案）

> 「第一次進入城鎮的時間是 1908年6月13日 11:00，從這個時間點開始計時。
>   程式內的活動就用現在的計時方式，大地圖則是**一場戰鬥 1 小時、進入其他城鎮或
>   地圖 1 小時**，時間線性往前推進。」

- **起點只有一個計算點**：`script/clock.js` 的 `EPOCH = 1908-06-13 11:00`（鐵律 7）。
  存的是**開局起算的分鐘數**（`tivot_clock_v1`），不是絕對日期 —— 改起點才不必動存檔。
- **兩套花費，各管各的**：

  | 在哪 | 花多少 | 記在哪 |
  |---|---|---|
  | 城鎮（走一步／獨自坐坐／睡覺） | 現行那一套（一步 10 分鐘…） | `modules/town.js`／`modules/inn.js` |
  | 大地圖（一場戰鬥） | **1 小時** | `flight/index.html` 的 `toBattle` |
  | 大地圖（進城鎮／進地標） | **1 小時** | `doLand`／`enterPlace` |
  | 大地圖（單純飛） | **0** | —— |

- ⚠⚠ **飛行本身不花時間**（Ray 的「線性往前推進」）：時間跟著**事件**走，
  不跟著你繞了多少圈走。連帶：晝夜在飛行途中不再流動，過了那一小時才跳一格 ——
  那是規則改了之後的結果，不是壞掉。
- ⚠⚠ **飛行頁以前有一個自己的假時鐘**（`{minutes:8*60, date:14}` 寫死，再靠航行距離
  `mv*0.10` 推）—— 它與主遊戲完全沒有關係，所以城裡是 6/13 傍晚、飛上天卻永遠是
  「14 日 08:00」。ver -435 拆掉了：那一頁改成**讀主遊戲那一份**，
  `clock` 物件現在只是**主時鐘的投影**（`syncClock()`），`dist` 才是它自己的。
  ⚠ `EPOCH` 在 `flight/index.html` 有**第二份**（非 module 頁面 import 不到）——
    改一邊要改另一邊（鐵律 7 的但書，兩邊註解已互指）。
  ⚠ 推進**只有一支**：`clockAdvance()`。從戰鬥／城鎮回來要 `syncClock()` 重讀
    （`__flightResume`）—— 那段期間別人可能推過時間。
- **顯示**：城鎮的 `#townInfo` 是**上日期、下「地名＋時刻」**兩行（Ray 指定），
  整塊由 `+62px` 提到 `+16px`（62px 正好壓在立繪的**臉**上；16px 是兩顆角落鈕之間
  那條空的，只會壓到最高那位的**頭髮**）。飛行頁的 `大陸曆` 那一行也改成算出來的年月。
  ⚠ 多一行就多一份高度 —— 日期那一行要做小、行高收緊，提上去的量得蓋過長出來的量。

## 6.5.4.1 一天的流程：傍晚回旅店 ／ Stage 0 的結尾（ver -427，Ray 定案）

一輪城鎮探索有**兩個強制轉場**，都由**時鐘**推動，判定收在
`modules/town.js` 的 `eveningDue()` ／ `stageGate()`，執行都走 `clockGate()`＋`forceGo()`。

| 何時 | 誰說什麼 | 然後 |
|---|---|---|
| 走完城裡所有地點、**還沒到 18:00** | 諾：「時間差不多了，回旅店吧。」 | 強制移到旅店、時鐘推到 18:00 |
| **沒走完**、時間到或過 18:00 | 諾：「天色不早了，我們先回廣場旅店吧？」 | 同上 |
| 上面兩格成立時**人已經在旅店** | 走旅店自己的分支二：諾：「今天有點累了，我先去休息囉。」 | 那一支順便記掉傍晚的旗標 |
| 20:00~21:00 人在旅店（走進去、或坐坐坐到那一刻） | 蕾：「唉呀，你在等我嗎？真不好意思。」 | 好感 +1 |
| **到達或經過隔天早上 07:00**（睡覺／坐坐／通宵亂逛都算） | — | 強制移到**船塢**，接上蕾娜「28號碼頭的白帆三桅船……有了。」（⚠ ver -562 起**不再升段**，見下） |

> ⚠⚠ **S4 ＝北方泊地第二天早上在旅店醒來**（ver -674，Ray 指定）——
> 由那一格閘門自己的 `stage` 欄位設（`clockGate` 在演台詞之前就 `setStage`），
> 不要在腳本裡另外寫一拍。⚠ **S3 還沒定**（出航～北方泊地之間那一段）。
>
> ⚠⚠ **章節編號（ver -562，Ray 定案）**：開頭＝**S0** → 第一次進帝都＝**S1**
> （town.open 的 capital 分支，守門 `getStage()===0`）→ 第一次出航＝**S2**
> （main.js 的 `sailOut()`，守門 `===1`；兩條出航路都走它）。北方泊地之後**還沒定號**，
> 試玩版/飛行的測試預設與暫填（STAGE_DEFAULT、talks 的 sorana/anya `from`、
> FEATURE_FROM）**全部推到 5** 以防萬一 —— 定案後再對位。

- ⚠⚠ **三條路都會推時鐘**（走一步／獨自坐坐／回房睡覺），所以閘門收在**一支**
  `clockGate()`，由推完時鐘的人呼叫（鐵律 8）。城鎮那邊掛在 `enter()` 的收尾，
  旅店那邊走注入的 `host.onClock()` —— 寫在各個呼叫點一定會漏掉其中一條。
- ⚠⚠ **「隔天早上七點」是時間軸上的一個點，不是「現在幾點」**：用時刻去比，
  第三天早上會再成立一次。換算成**開局起算的分鐘數**再比
  （`clock.firstHourAt(7)`），「到達或經過」就都算 —— 而睡覺是一口氣跳十小時，
  「剛好等於」的機率是零。
- ⚠⚠ **`dayNo()` 算的是日曆日，不是「開局起算的 24 小時塊」**（ver -427 修）。
  開局是 11:00，用 24 小時塊算的話「第 2 天」要到隔天 **11:00** 才成立 ——
  實測 6/14 07:05 被算成第 1 天，第二日船塢那一整場戲不出來。**那就是舊版
  出航觸發不了的原因。** 現在船塢那兩段改由 `need:'stage1_open'` 開啟（＝上面那個閘門），
  日期不再是條件。
- ⚠ **時鐘不倒轉**：Ray 的稿寫「時間改為當天 18:00」，但觸發時可能已經 18:05
  （走一步 10 分鐘）。`clock.advanceToHour()` 只往前推、已經過了就不動 ——
  倒轉會讓「時間是資源」出現漏洞。
- ⚠ 兩條傍晚的觸發同時成立時（走完了**而且**過了 18:00）走**時間**那一句：
  那時「天色不早了」才是玩家看得到的事實。
- ⚠⚠ **傍晚那一格「讓過一次」才強制**（ver -430，Ray：「要等角色先把原有的場景對話
  講完才觸發，移動到下一個場景才強制觸發」）。ver -427 是「優先所有事件」——
  它會把那一次抵達原本要演的**進場對白整段吃掉**，玩家等於被搶走一段戲。
  現在：到期的那一次若這個地點還有自己的進場對白，就**讓它先講完**（記 `eveningHeld`），
  **下一次抵達**才不由分說地插隊。
  ⚠ **只讓一次** —— 不記旗標的話，一路走過還沒看過的地點會永遠讓下去，「強制」就名存實亡。
  ⚠ 主線段落（`acts`）本來就是這個行為（有 `act` 時 `ev` 就是 null），這一版只是讓
    進場對白享有同樣的待遇 —— 兩者現在一致，不必記兩套規則。
  ⚠ `eveningHeld` 是**這一趟城鎮探索**的狀態，`town.open()` 要歸零；它不進存檔
    （最壞情況只是多讓一次，而那一格本來就還沒觸發）。
- ⚠⚠ **Stage 0 的結尾先讓蕾娜講一句再轉場**（ver -438，Ray：「讓蕾娜在旅店先講一句
  『好囉，該出發囉』再淡入淡出轉到下一幕」）。台詞在資料上（`TOWNS[].stage1.lines`），
  `clockGate()` 演完才 `forceGo`。
  ⚠ 睡醒那一刻**黑幕還蓋著**（旅店的睡覺演出留下來的）—— 要先把畫面亮回來，
    她才有舞台；不亮的話那一句是在一片全黑裡講的。亮完再放人（立繪滑入 450ms）。
- ⚠ 強制轉場走**同一支** `enter()`（清場、背景、對白、大廳的整套收尾只有那一份）；
  不花時間、不看營業時間、不記「來時方向」（玩家不是自己走過去的）。
- ⚠ 「回**廣場**旅店」那三個字 ver -393 曾被拿掉（會把玩家指錯方向），
  ver -427 Ray 又寫回來 —— 這一版是**強制移轉**，玩家不必自己走，指不指錯已經不成立。

## 6.5.4.2 女角外出／餐飲街的分店（ver -575，Ray 定案）

> 「女主角們在早上 8:00 到下午 6 點可能會出門，出門時不顯示頭像，一天最多出門兩次。
>   出現區域是所有的連接用場景（非末端），以及各角色不同。」
> 「大城地圖已經規則化，餐飲街方向會出什麼場景取決於同行女伴，或者當下是誰在那個區域。」
> 「無女伴、且該場景無分配角色時就是酒吧。」

資料在 `script/town.js` 的 `OUTING`／`DINE`（鐵律 1），排程與查詢在 `modules/town.js`。

| 角色 | 專屬區域（連接場景與餐飲街之外） | 餐飲街開哪一家 |
|---|---|---|
| 蕾娜 | 行政廳・教堂・雜貨店 | 咖啡廳 |
| 諾薇兒 | 行政廳・教堂・雜貨店 | 餐廳 |
| 安雅 | 雜貨店 | 甜品店 |
| 索拉娜 | 雜貨店・武器店・賞金獵人公會 | 酒吧 |

- ⚠⚠ **這一組取代 ver -461 的 `rennaOut`**（只給蕾娜寫的那一份）—— 四個人各寫一份
  必然走鐘（鐵律 8）。舊欄位與 `rennaIsOut`／`maybeMeetRenna` 已刪。
- ⚠⚠ **「連接用場景」是算出來的，不是列出來的**（鐵律 7）：節點的 `exits` 裡有
  `back` 以外的方向＝它通往別的地方（`connectorIds()`）。大城地圖已經規則化，
  列表一寫死，日後加一座城就漏一次。**旅店不算** —— 她就住在那裡。
- ⚠⚠ **「一天最多兩次」是結構保證，不是事後數次數**：`hours` 切成 `perDay` 個等寬的窗，
  一個窗最多產出一次外出。數次數的話，跨日／離城再回來／讀檔那幾條路都要各記一次帳，
  漏一條就破功。連帶好處：兩次不會重疊、時段自然分散。
- ⚠ **行程的鑰匙是「這座城的這一天」**（`outKey`）：走出城再回來還是同一天就是同一份，
  在 `open()` 清掉的話「出城再進城」可以重擲。歸零收在 `close()`（回主選單／換城）。
  ⚠ **還不能排的時候不要記鑰匙**（`!townId || !st1Active()`）—— 記了就等於「今天排過了」，
  而 `stage1_open` 是白天中途才立得起來的，卡著鑰匙那一天整天沒有人出門。
- ⚠ 行程**不進存檔**：睡覺一定跨到隔天 07:00，醒來本來就要重排（同 `escortNou` 那一組）。
- ⚠⚠ **同一格同一時段不放兩個人**：不然「當下是誰在那個區域」答不出唯一解，
  而餐飲街要靠它決定開哪一家店。挑不到空的就那一次不出門。
- ⚠ **跟著玩家走的人不算「在外面」**（`outNow` 把同行者剔掉）：她在你旁邊，不在某個定點。
- ⚠⚠ **旅店的門燈只問一支 `inRoom(who)`**（出門與同行都是「不在房裡」，Ray：「出門時
  不顯示頭像」）。傳進 `inn.arrive` 的是**函式不是快照** —— 大廳裡的時鐘會走
  （獨自坐坐兩小時），快照會過期。ver -566 的 `nouIn`／`renIn` 兩個布林已改成這一支。
- ⚠ 四扇門是誰由 `st1.roster` 傳進來（＝`OUTING.who` 照 `from` 濾過）：
  inn.js 不再自己列一份名單（鐵律 7：她們入隊的章節只有一處在寫）。
  安雅與索拉娜的 `from` **暫填 5**，與 `flight/talks.js` 的 `PARTY` 同義，兩邊註解互指。
- ⚠ 碰到她走的是**路人單句那一套**（`flashLine` ＋ `chatterOn`）：再點一下收掉，節奏一致。

**餐飲街的分店**
- 判定只有 `dineKey()` 一支（鐵律 8）：① 同行女伴 ② 現在在那一格的人 ③ 都沒有＝酒吧。
- ⚠ 分店只換**背景與路人語**，不是另一個節點 —— 地名、出口、營業時間、店主
  都還是那一格的（地名見下）。
- ⚠⚠ **「哪一家店」是全域概念，「那一家店長什麼樣」是這座城的事**（ver -578）：
  `DINE.scenes` 只留店別的中文名；**背景檔名（完整基底名，不是後綴）、
  要不要吃時段差分（`noTime`，逐張）、路人語**寫在城的 `dining.scenes[店別]` 上。
  ⚠ -575 曾經拼成 `<節點bg>_cafe`，而 Ray 交件的檔名是 `Capital_Cafe` ——
  每座城的餐飲街是不同的四家店，用共同的後綴去拼一定會走鐘。
  ⚠ `noTime` 必須**逐張**：帝都三張新圖沒有時段差分（Ray：「營業時間不跨黃昏」），
  而**酒吧沿用既有的餐酒館原圖**、那一張有 Day/Dusk/night —— 用一個共用的
  `noTime` 參數表達不了，所以 `bgFor()` 改吃**已經展開好的候選檔名**
  （`bgCandsOf` 逐張叫 `bandNames`），不是基底名。
  ⚠ 城裡沒有那一家（`scenes` 沒那個鍵）→ 不換，照節點原本那一張；
  只寫 `{node:'tavern'}`（不給 `scenes`）＝那一格只是「碰得到人的地方」。
  北方泊地現在就是這樣（四張圖還沒交）。
- ⚠ 路人語：分店有自己那一組就用它，沒寫就回去用節點的（酒吧＝餐酒館的市井線）。
- ⚠⚠ **地名就是「餐飲街」，不接店別**（ver -580，Ray：「把餐酒館・餐廳改成餐飲街，
  點進去會進哪邊取決於你跟哪個角色在一起」）。-575~-579 曾經印成
  `帝都　餐酒館・餐廳` —— 那把「這是一條街」講成了「這是一家叫餐廳的餐酒館」，
  而且等於在箭頭上先劇透裡面是誰。**玩家看得出自己在哪一家靠的是背景與路人語**，
  那正是「點進去會進哪邊」這件事本身。`DINE.scenes[].name` 因此只剩除錯／文件用途
  （`outingDebug()` 印得出來），畫面上不出現。
- ⚠ 帝都那一格的節點 id 仍是 `tavern`（旗標、腳本、`nextFavor` 都指著它，
  改 id 等於把那些全打斷），只有 `name` 改成「帝都　餐飲街」。
- ⚠ **顯示名只有 `nameOf(id)` 一支在算**（鐵律 7）：上緣那一行、目的地字格、店舖鈕、
  閉門羹的名字欄全部問它 —— 四個地方各自讀 `n.name` 的話，哪天有一個要變就只有一個會變。
- ⚠ 背景走 `bgFor(候選檔名)`：**分店優先、載不到退回節點原本那一張** ——
  所以圖還沒交也不會變成空畫面。實測帝都四家：三張 `noTime` 的各只發一次請求
  （0 個 404），酒吧照舊吃 `Capital_Bistro_<時段>`。
- ⚠⚠ **城重建之後整組換背景**（ver -627，Ray：「stage5 之後北泊改用這一組差分」）：
    寫在城上的 `rebuild:{ fromStage, bg:{節點→基底名} }`，判定與取用只有
    `modules/town.js` 的 `bgCandsOf`／`rebuiltBg` 一處（鐵律 8）——
    背景、店舖、戰鬥交棒帶過去的那一張全部同步。
    · 節點的 `bg` 是**戰損版**（`_BF`，無時段差分），重建版吃既有的時段候選鏈。
    · **重建版優先、戰損版當退路** —— 圖沒交也不會變成空畫面（同分店那一條）。
    ⚠⚠ **基底名要逐格寫出來，不可以從 `_BF` 那個名字去推**：交件是
      `Northport_West`，戰損版是 `Northport_west_BF` —— **macOS 不分大小寫、
      靜態空間分**，字串推法在本機測不出來，上線整排 404。
    ⚠ `fromStage` 目前是**暫填號**（同 `STAGE_DEFAULT`／`FEATURE_FROM`／
      `talks` 的 `from`）—— 章節編號定案後這四處一起對。
- ⚠ 哪一格是餐飲街寫在**城**上（`dining:{node, scenes}`），不寫死節點 id。
  **帝都的餐酒館維持原樣**（`scenes` 沒開）：它有 stage 0 自己的戲與自己的一張圖，
  四張分店圖到齊之前換上去只會退回同一張、地名卻變了。
- 調機率／看行程用 `town.outingDebug()`（同 flight 的 `talkDebug`）。

**⚠⚠ 主線段落（`acts`）不受「回房休息」擋（ver -581，Ray：「帝都劇情第二天出航的
trigger 壞了」）**

- ver -459 的休息規則（「夥伴進了旅店休息就不再觸發她相關的**對話**」）原本連
  `acts` 一起擋；ver -566 又加了一條總則 —— `stage1_open` 之後夥伴預設在房內
  （`restingSet()` 的最後一段）。兩條合起來就是**矛盾**：船塢那一段的條件正是
  `need:'stage1_open'`，而它的台詞裡有諾薇兒 → **開啟它的那個旗標同時把它擋死**，
  第二天出航整段永遠演不到（-566 之後一直如此，直到 -581 才被回報）。
- 正解是分清楚兩者：休息擋的是**氣氛對白**（進場對白、傍晚提醒）；
  **主線段落是劇本，它說誰在場誰就在場** —— 更何況玩家是被那條主線強制搬到船塢的，
  用「她在房裡」把戲擋掉講不通。
- ⚠ **自檢**：新增任何「某某狀態下不觸發某類對白」的規則時，先問
  「**開啟主線那一段的旗標，會不會同時把它擋掉？**」—— 條件與封鎖用同一把鑰匙時，
  那一段就成了永遠到不了的地方，而且不會有任何錯誤訊息。

**宵禁與約會（ver -576，Ray 定案）**

> 「晚上九點以後女主角就不出門，約不出來。改放天晚休息了之類的台詞，到隔天七點以後才恢復。」
> 「出城鎮、回旅店以後就要解除約會，一天內同人不能約第二次，會拒絕。」

- **宵禁 21:00~07:00**（`OUTING.curfew`，跨午夜、上界不含）：判定只有 `isCurfew()` 一支，
  外出行程與旅店敲門都問它。敲門一律回 `innStage1.nightRest`，**四扇門共用**
  （`nightRestBy[誰]` 有寫就用專屬那一句 —— 蕾娜是「那麼晚了你還不睡嗎？」）
  —— 那是「現在是深夜」這件事，不是某一個人的狀態，好感再高也約不動。
  ⚠ 行程本來就排在 8~18 點，這一條現在攔不到外出 —— 但**規則要寫在規則上**：
  日後把 `hours` 拉長，宵禁不必跟著改。
- **解除約會只有 `endDate()` 一支**（鐵律 8），三條路都叫它：
  出城鎮（`suspend()`）／回主選單（`close()`）／**走進旅店**（`enter()` 看到 `inn`）。
  解除之後門燈自己亮回來（`inRoom()` 是現算的，不是快照）。
  ⚠⚠ **殘留事件帶起來的同行不算約會**（`escortLeftover`）：那一種有自己的收尾
  （ver -567 的 `nouTired`），被這一條收掉的話她那一段戲就演不到了。
- **一天內同一個人只能約一次**：`datedSet` 的鑰匙是 `dayNo()`，日期一變自己歸零
  —— 所以「出城再進城」還是同一天就約不了第二次。拒絕的台詞在 `innStage1.dateDone`。
  ⚠ `suspend()`（出城鎮）**只解除同行、不清帳**；清帳在 `close()`（回主選單＝這一趟結束）。
- ⚠ 兩者都排在敲門的**人的分支之前**（宵禁 → 今天約過了 → 各人的台詞）：
  它們是世界的狀態，不是某個人的心情。

## 6.5.4.3 城鎮戰（ver -583，Ray 交辦）

> 「現在製作戰鬥用的地圖，城鎮戰所以沿用原圖，但是末端只留教堂，其他末端不可進，
>   不用顯示箭頭，測試期間每張先放 B2G05 當怪，血量 300，攻擊模式同訓練用聖徒，
>   攻擊力同賞金獵人。」

資料在城上的 `siege:{from, until, keep}`（鐵律 1），判定在 `modules/town.js` 的 `siegeOn()`。

- **沿用原圖**：節點、背景一個都不改 —— 城鎮戰只是同一張地圖的另一個**模式**，
  不是另一組節點。
- ⚠⚠⚠ **戰鬥地圖與城鎮探索是兩個模式**（ver -584，Ray：「戰鬥期間要跟城鎮探索期間
  分開喔，兩個是不同的，只是背景跟城鎮圖沿用」）。城鎮戰開著時，這張地圖上
  **只有「走」與「打」**，探索的每一層一律不啟動：

  | 探索層 | 城鎮戰中 |
  |---|---|
  | 路人單句 `chatter` | 不出 |
  | 店舖（買賣／懸賞榜） | 不開 |
  | 旅店大廳（伙伴門／坐坐／睡覺） | 不開 |
  | 進場對白 `lines` | 不播、旗標不記（戰後正常走進來照演） |
  | 傍晚提醒 | 不催 |
  | 女角外出／碰面／約會 | 沒有人在逛街 |
  | 一次性操作提示 `tips` | 不教 |
  | 「走過了沒」`seen_*` | 不記 |
  | 走一步 10 分鐘 | **不花時間**（時間是**探索**的資源） |

  ⚠⚠ **每一層自己問 `siegeOn()`**（鐵律 8），不是在 `enter()` 一處判完再分派 ——
  日後新增任何一層探索機制，它自己會記得問；寫在呼叫端一定會漏。
  ⚠ 唯一照常的是 `acts`：城鎮戰的那幾場戰鬥就是掛在它上面的。
- ⚠⚠ **「末端」是算出來的**（鐵律 7）：`connectorIds()` 之外的就是末端。
  大城地圖已經規則化，列一張死名單日後加一格就漏一次。
- ⚠⚠ **擋在 `exitsOf()`，不是 `go()`**：Ray 要的是「不用顯示箭頭」——
  箭頭都不出現，玩家才讀得出「那邊過不去」，而不是「按了沒反應」。
  收在那唯一的出口表，箭頭／目的地字格／鍵盤／`go()` 一次全吃到（鐵律 8）。
- ⚠ 連接用場景一律留著，不然玩家會被關在某一格出不去。**出航不擋**
  （Ray 說的是「末端」，出航不是末端）。
- **每一格的戰鬥寫在節點的 `acts`**（`{ battle:'…' }` 那一拍，§6.5.2 的既有機制，
  引擎不必動）：`need`＝城鎮戰開著、`flag`＝這一格清掉了。
  ⚠ 旗標**打贏才記**（同所有城鎮段落「演完才記」）—— 打輸回頭再走一次還會遇到。
  ⚠ 一格一段，不是引擎生出來的：日後每一格要換不同的怪時，各改各的。
- **教堂是 Boss**（ver -586，Ray：「B2G01，教堂 boss 用這一隻，跟其他怪數值一樣就好」）：
  `np_boss` 的數值與雜怪**完全相同**，差別只有立繪（第二批 Gemini 第 1 張＝祭壇獸，
  身上嵌著教堂立面）、戰鬥背景用教堂那一張、以及戰鬥卡上的 `sessionEnd`。
### ⚠⚠ 惡夢化（Nightmare Install，ver -671／-672，Ray 交稿）

聖徒化的**鏡像**，實作在**同一支** `modules/saint.js`（鐵律 8）：同一套盤面／連擊／
追加傷害的規矩，方向相反 —— 聖徒化把血往上推（推滿＝OBE），惡夢化把血往下抽
（抽乾＝熔斷）。

- **熔斷有自己的 cut-in**（`CI_Anya_OBE`，ver -692）：它與聖徒化的 **OBE** 是
  **對稱的失敗結局**（推滿 ↔ 抽乾），所以照它的作法配一張全畫面 cut-in。
  ⚠ 字面留 `MELTDOWN`（Ray 的用語是「熔斷」）—— 檔名叫 OBE 只是沿用聖徒化那端的
  命名，兩者不是同一件事。⚠ **先關掉惡夢化再演**：狀態先收乾淨，演出只是演出。
- **盤面固定 16 格**（ver -690，Ray：「夢魘改成固定 16 格吧，跟 SI 一樣」）：
  與聖徒化同一套 —— 換成 16 宮格、時長固定 `16 × secPerCell` ＝ **12.8 秒**，
  收尾再把原本的盤面換回來（`saintPrevBoard`）。
  ⚠ -671~-689 是「沿用殘局、秒數隨殘格數變」，已被這一版取代。
  ⚠ **發動時要高光第一個該點的號碼**（ver -683，Ray 指定）：玩家眼前是打到一半的
  殘局，不指一下他得先自己找「剛剛點到幾了」，而倒數槽已經在抽血。
  ⚠ 走**既有的** `hintCurrentCell`（即死防禦之後那個「一次性續命導航」同一支，鐵律 8）
  —— **不要用 `markNext`**：那一支在 `hint:false` 的盤面上什麼都不做，而第二盤起
  全部是 false（實測：第二盤發動時 `next` 是空的）。
  ⚠⚠ **光加上 class 還看不見**（ver -684，Ray 連報兩次）：`.cell.next` 只改
  `border-color` 與一圈淡光，而 `#grid.saint .cell` 把**每一格**都設成金邊
  （id 選擇器權重贏），`#grid.saint` 自己又罩了一層 drop-shadow 把那圈淡光吃掉 ——
  DOM 上有 `.next`，畫面上完全看不出來。要一條**權重更高、而且換顏色**的規則
  （`#grid.ni .cell.next`：白框白光）。整盤都是金的時候，再加金光還是找不到。
- 清空殘格 → **回滿 ＋ 最後一擊追加期間總傷 20%**（同 MB）；期間清掉敵血一樣有 EXSECUTIŌ。
- **上滑自爆（夢境粉碎）**：一次清掉殘格，**沒有 MB、不回血、直接結束、HP 剩 1**。
  ⚠⚠ **傷害＝敵人最大 HP × `burstPct`(25%) × （期間清掉的格數 ÷ 16）**（ver -688，
  Ray：「用百分比好了，滿格 16 可以帶敵最大 hp 25% 傷害走」）。
  ⚠ 兩次改版的理由都寫著：
  · 最早是「把殘格逐一結算」—— 份量取決於**剩幾格**，玩家打得越好、殘格越少，
    自爆反而越弱，方向反了。
  · -685 改成「期間累積傷害 ×2」—— 方向對了，但**在大場等於沒有**
    （900 血的場只打得出百來點）。
  · -688 綁在**敵人最大 HP** 上：同一招在哪一場都是同一個份量。
  ⚠ 分母是**滿盤 16 格**不是「這一盤幾格」：9 格盤清完不該與 16 格盤等值。
  ⚠ **它打不死**（ver -673；下限 -689 由 10% 降到 **5%**，Ray：「最好讓夢境粉碎
  可以爆到她只剩 5% 血」）：敵血最低留 `burstFloor`。
  ⚠ 連帶**觸發點由 50% 壓到 30%**（同版，Ray：「把夢魘化的時間壓後」）——
  算一下就知道為什麼是 30：滿格是敵最大 HP 的 25%，加上期間點掉那十幾格的傷
  （約一成），30% 打完正好落在 5% 那條下限上；50% 觸發的話爆完還剩兩成多，
  讀起來就不是「一擊把她打到只剩一口氣」。作法是**先把傷害夾住**，不是打完再把血加回來 ——
  後者會先觸發「敵人死了」的那一整套演出再憑空復活。
  ⚠ ver -673 的「下一盤普攻 2 倍 ×5 秒」已由 Ray 拿掉（-685），連帶
  `combat.armAtkBuff` 那一支也移除（沒有人用了）。
- ⚠⚠ **發動時先把血灌滿再抽**：Ray 的稿有兩句打架（「玩家受擊 hp1」→ 而 NI
  「以現有的 hp 開始扣除」）—— hp 已經是 1 的話 NI 在發動那一瞬間就熔斷（實測）。
  灌滿再抽是唯一兩句都成立的讀法，也對得上「清空殘格 hp 全恢復」。
- ⚠⚠ **受擊要換算成「秒」再抽，不能直接扣敵人的攻擊力**（ver -691，Ray：「夢魘時
  被攻擊血掉得好像比聖徒化快很多？讓兩邊因受擊所減少的持續時間一致」）：
  那條倒數槽的單位雖然是血，量的卻是**時間** —— 直接扣攻擊力的話，一發大絕（22）
  在 12.8 秒的槽上吃掉 **2.9 秒**，而聖徒化的同一發只吃 0.67 秒（`saintPassiveHealSec
  ÷ saintAdvanceDivisor`），而且它會隨**敵人的攻擊力**變動（換一隻怪手感就不一樣）。
  ⚠ 兩邊用**同一個秒數**：`combat.enemyAttack` 的 `niMode` 分支把「這一發值多少秒」
  算出來交給 `saint.nightmareHit(sec)`，那一支再用**與被動抽血同一條斜率**換回槽量。
  ⚠ 點錯／逾時也走秒（`playerMax/DIVISOR` 是**聖徒化那條槽**的刻度，在惡夢化這條槽上
  代表的秒數完全不同）。格擋照舊減半。
- ⚠⚠ **抽血不可以用 `healPlayer(-x)`**：那一支開頭就 `Math.max(0, amount)`，
  負數被吃掉、什麼都不會發生（-671 就是這樣，血其實是被敵人打掉的，倒數槽一格
  都沒動）。也**不可以走 `enemyAttack`**：那是「被打到」，會記失誤、破無傷、震畫面。
  惡夢化的失血是它自己的代價 —— 專用的 `combat.drainPlayer()`，夾在 1（抽到 0 是陣亡）。
- ⚠ **那兩個閘門的雪鐵龍箭是紅色的**（`gate.tone:'red'`，ver -686，Ray：「夢魘教學
  雪鐵龍換成紅色，不然看不見」）：惡夢化的畫面本來就是金的（`#grid.saint` 把整盤
  設成金邊、還罩一層金色 drop-shadow），金箭頭疊上去讀不出來。
  ⚠ **三個地方要一起換**：容器的外光、chevron 的邊、標示字的顏色與字光 ——
    只改其中一個會變成「金字配紅箭」。顏色走既有的 `--danger`，不另定一個紅。
- **發動與自爆都是閘門**（ver -672，Ray：「不要劇情殺，直接介入，右滑雪鐵龍」
  「在熔斷前增加一個教學上滑雪鐵龍發動自爆」）：戰鬥卡寫
  `gate:{type:'right', action:'nightmare'}` ／ `{type:'up', action:'niBurst'}`。
  ⚠ 名字在 `tutorial.GATE_ACTIONS` 那張表（資料寫不了函式）。
- **`phplow:N`** ＝玩家血量**掉到** N% 以下（與 `php:N` 相反方向）—— 熔斷前的引導靠它。
- ⚠⚠ **聖徒化／惡夢化期間不再加密敵人的大絕**（ver -688，Ray：「把 boss 一進夢魘
  或聖徒就猛攻的設定拿掉」）：`saint.js` 那兩行 `setUltRate` 已移除，
  `tuning.saintUltMinMs/MaxMs` 留著當紀錄（沒有人讀）。
- ⚠⚠ **明晰之夢在惡夢化期間不發動**（ver -688，Ray：「如果是夢魘期間 hp 降到標準
  以下，要等夢魘退掉才會發動」）：那一段的失血是**它自己的代價**（倒數槽），
  不是被打到 —— 在那裡發動等於把代價換成獎勵。
  ⚠ 作法是 `partner.checkLowHpBuff` 看到 `niMode` **直接 return，不動上膛狀態**，
  由 `saint.finishNightmare` 在**設好結局血量之後**再叫一次它 ——
  門檻要對著結果判（熔斷／自爆＝1、清盤＝滿），不是對著過程判。
- ⚠ 安雅**沒有即死防禦**（Ray：「諾薇兒現在不是夥伴，所以不會有即死防禦」）：
  那是諾薇兒的技，換人就沒有了。她的被動是**明晰之夢**（Lucid Dream）。
  ⚠⚠ **觸發是「每隻怪第一次反擊成功」不是血量**（ver -693 由 Ray 改定：「娜塔莉戰
  如果先觸發 lucid dream 再進入 NI 劇情會卡住，或者同時……改成觸發單怪第一次反擊
  成功時發動，不算場，每隻怪都可以觸發一次」）——
  舊的「HP≤30%」與惡夢化**搶同一個時刻**：NI 的倒數槽本來就會把血抽到 1，
  於是「跌破 30%」與「進 NI 劇情」同時發生，兩段演出疊在一起就卡住。
  ⚠ **每隻怪一次**（不是每場一次）：`enemy.setEnemy` 是「換了一隻怪」的唯一時刻，
  重新上膛掛在那裡（鐵律 8）。反擊那一端掛在 weapon 的三個 `addCounter` 旁邊 ——
  那是同一個事件，而且**不能只掛在函式開頭**（那時還沒判定會不會開火）。
  ⚠ 效果（5 秒普攻加倍）與 `lowHpBuff` **共用同一個執行體**（`partner.fireBuff`）：
  換的是條件不是效果。
  ⚠⚠ **「挑戰」的選人畫面只列 `config.challengePartners`（蕾妮／馬季諾）**
  （ver -695，Ray：「挑戰的伙伴只留馬跟蕾妮可選」）：`partners` 現在也裝著本篇的
  搭檔（諾薇兒、安雅），而那兩位是**劇情給的**，不該出現在出陣的卡疊上。
  ⚠ 用**白名單**不是黑名單 —— 日後加一位本篇搭檔，預設不會漏進挑戰（同首頁那條
  白名單的理由，§6.9）。讀取只有 `weapon.PARTNER_KEYS` 一處。
  ⚠⚠ **蕾妮與馬季諾只存在於「挑戰」（試玩版），正篇不會有他們**（ver -694，Ray 指定）——
  所以馬季諾的高裝藥彈照舊是血量觸發，不必跟著本篇改。這與
  「本篇與試玩版是兩套數值」（§6.5.3 的 `weapons[].story`）是同一個原則：
  **動其中一邊之前先確認那一張卡是誰在用。**
- ⚠⚠ **沒有夥伴＝什麼技都沒有**（ver -681，Ray：「主角在沒有任何夥伴的狀況下
  不會有任何主被動技能，也沒有聖徒化」）：那兩場（`np_cemetery`／`np_nightmare`）
  的卡上寫 `noSaint:true, noPartner:true`。
  ⚠ 惡夢化與夢境粉碎照樣出得來 —— 它們由**腳本的閘門**直接呼叫，
    不經過那兩道守門（那兩道擋的是「玩家自己發動」）。

### ⚠⚠ 安全區旗（`safehouse_<地圖>`，ver -634，Ray 定案）

> 「只要插 safehouse flag 就不會有遭遇戰，黑爪戰後就插一個，拔 flag 才會遭遇。」
> 「flag 跟地圖，一插就是整個北泊。」
> 「特殊戰就先拔旗，打完再插，如帝都的賞金獵人跟打靶小遊戲。帝都其餘時間都插著。」

鐵律 9 的標準形狀：**插了以後被拔之前不動，而且只有單一事件能拔**。

- **旗名由 `townId` 推**（`safehouse_northport`／`safehouse_capital`），
  由 `modules/town.js` 的 `safehouseFlag()` 一支決定 —— **不要寫死在資料裡**：
  插旗端與查旗端各寫一個字串的話，打錯一個字就是「插了但查不到」，而且不會有
  任何錯誤訊息（鐵律 7）。
- **誰插的**：
  · 城上 `safehouse:true` → **進城時**插（帝都：其餘時間一直插著）
  · act 上 `safehouse:true` → **那一段演完**才插（北方泊地：教堂那一段，
    ＝黑爪打完＋戰勝那一段對白演完，正好是安雅跑掉、蕾娜追出去那一刻）
- **誰拔的**：`pullSafehouse:true` 的那一段 act（**特殊戰**）—— 開演前拔、演完插回去。
- ⚠⚠⚠ **劇情戰寫 `storyBattle:true`，旗一動都不動**（ver -679，Ray：「stage4 在北泊的
  兩場都是劇情戰，不該插戰鬥探索 flag」）。安全區旗擋的是**遭遇戰**（城鎮戰那一格
  一格的雜怪），不是劇本安排的那一場。
  · `pullSafehouse` ＝「這一段期間這裡真的不是安全區」（玩家自己走過去挑的特殊戰）
  · `storyBattle`    ＝「這是劇本，安全區旗管不著」
  ⚠⚠ **拿 `pullSafehouse` 去湊劇情戰會出事**：打輸的話那一段沒演完，旗就停在
    **拔掉**的狀態 —— 整座城當場退回城鎮戰模式（Ray 回報：第一場戰敗選繼續之後
    「整個城鎮就回到戰鬥探索了」）。-634 那句「狀態自己會收斂」只對特殊戰成立，
    對劇情戰不成立（劇情戰打輸還會再打，而中間整座城已經變了樣）。
  ⚠ 中途離開或打輸會停在「拔掉」的狀態：那一段的 `flag` 沒記，下次走進來照樣
    拔了再打，收尾時一起插回去 —— 狀態自己會收斂。
- **它擋什麼**：`actDue` 裡「這一段**有戰鬥拍**」＋「這張地圖插著旗」→ 不演；
  以及 `siegeOn()` 直接回 null（＝末端解封、店開回來、路人講話、時間開始走）。
  ⚠ 判「有沒有戰鬥」看**資料**（拍上有 `battle`），不另外加一個要記得寫的欄位 ——
    漏寫的下場是「安全區裡冒出一場架」，而那不會有任何錯誤訊息。
- ⚠⚠ 它**取代**了 ver -633 試過的 `siege.until` ＋ act 上的 `siege:true`：
  同一件事兩個開關就是鐵律 7 的病。現在城鎮戰只有 `siege.from`（開始）
  與安全區旗（結束）。
- ⚠⚠ **曲子有自己的結束點**：`siege.bgmUntil`（北方泊地＝`np_clear_church`）。
  「這張地圖還在戰鬥模式」與「怪清完了沒」是兩件事 —— 怪清完就該換回 Suspense6
  （ver -614 Ray 指定的節奏），而**遇敵**要到黑爪打完才停。
  沒寫 `bgmUntil` ＝兩者同一個結束點（其他城照舊）。
- ⚠ `progress.removeFlags()` 於 -634 重新啟用，**唯一的呼叫者就是這裡**。
  它**不是**用來做敗北回捲的（那條路 -495 拆掉了，已被「打贏才記」取代，§6.5.2）。
  ⚠ 不另立一支 `np_siege_done`：同一件事兩個旗標一定有一個忘了立（鐵律 7）。
  ⚠ 它與戰鬥卡上的 `sessionEnd` 是**兩件事**：那個管「閉棺與資源回滿」，
    這個管「地圖解封」，擁有者不同。

**⚠⚠ 整張戰鬥地圖算「同一場」（ver -585，Ray：「城鎮戰內打掉一個怪不用閉棺，
打掉 Boss 才閉，戰鬥地圖中移動期間算同一場，hp／聖徒化次數／主動技發動次數／
破防值算同一場」）**

- 戰鬥卡寫 `session:'<id>'` ＝屬於那一段連續戰鬥；`sessionEnd:true` ＝打贏它收段（Boss）。
  現在開著的那一段存在 `state.battleSession`（擁有者 combat，其餘只讀，鐵律 7）。
- 同一段之內：
  · **開棺只演第一次** —— 判定 `combat.battleNeedsGate()`，由 `main.js` 注入給
    `story.setGateSkip`（story 不 import combat）。一格演一次儀式，五格就是五次，
    那讀起來不是同一場戰鬥。
  · **「每場一次」的資源不回滿**：聖徒化（`saintUsedThisBattle`）、搭檔主動技
    （`partnerActiveUsed`）、破防值（`energy`）。HP 本來就延續（持久 HP，ver -481）。
  · **格與格之間只演「門」，在控制盤高度原地開／關**（ver -587，Ray：「雜怪 hp 清零後
    槍棺在原高度閉棺成為控制板，移動後遭遇下一個怪時直接原高度開棺不上彈，
    露出數字面盤」）：`story.playKerberosInPlace()` ／ `playKerberosShut()`。
    **不加 `rise`/`full`**（那兩個才是「上彈」），也不演解鎖與圓盤 ——
    完整的那一套是「一場戰鬥開始了」的儀式，一場之內不該演五次。
    ⚠ 戰鬥期間槍棺根本不在畫面上（舞台收著），所以「原高度」是這兩支自己擺的起始
      狀態 —— 擺在控制盤高度，玩家看到的就是面盤打開／闔上，中間沒有位移。
    ⚠ 閉棺回程**不走 `goHome` 的淡出**（`res.inPlace` 讓 main 分流）：那一片黑幕會
      把門的動作整個蓋掉，而且 `goHome` 會把首頁打開 —— 明明沒有要回首頁。
  · **中間幾場不結算**（ver -586，Ray：「結算也不要留，一場打完才結算」）——
    打掉一隻直接回戰鬥地圖，閉棺與結算頁都留給 `sessionEnd` 那一場。
    判定 `midSession()`：問的是**卡**不是 `state.battleSession`（Boss 打贏時
    段落已經被 `endSession()` 收掉，拿 state 判會把 Boss 也算成中間場）。
    ⚠⚠⚠ **凡是「以場為單位」的東西，終點都是結算**（ver -621，Ray：「無傷是算
    『場』不是算『敵』，所有以場為單位都是以結算為終點」）：用時、失誤次數、
    受擊數／**無傷**、overkill、敵人血量總和、EXP、金錢 —— 中間幾場先記帳
    （`inspector.bankSessionGain`），到收段那一場**一起併、一起評一次等第**。
    ⚠⚠ **併帳只有一處**：`inspector.settle` 的**開頭**，在分流到三條結算路徑
      （監察官／教學／劇情插入戰）之前（鐵律 8）—— 日後多一條結算頁也自動吃到。
      ver -601 的併帳只做在**監察官**那一頁，而城鎮戰／劇情戰走的是 `scriptSettle`，
      於是整段的戰績與錢**從來沒被領走過**（-621 才發現）。
    ⚠⚠ **`endSession()` 要在 `settle()` 之後**：它會 `clearSessionGain()`，
      而那筆帳正是結算頁要報的。-595~-620 收在 `win()` 開頭 ——
      帳在 `stats` 組出來之前就被清光了。
    ⚠ 拾得道具仍只在有結算頁的那一場給：那是掉落，不是帳。
    ⚠⚠ **段落中間要存檔就在那一拍寫 `checkpoint:true`**（ver -653，Ray：「黑爪戰後
      加一個記錄點」）。城鎮的 act 是**整段演完**才落點，而一個 act 可能很長
      （教堂那一段是「對白 → 打一場 → 戰勝 → 娜塔莉那一幕 → 插圖」）——
      中間想存就得有「這一拍存」的辦法。
      ⚠ 走的是**同一支** `story.setCheckpointHook`（＝`save.autoSave`，鐵律 8）：
        讀取頁、城鎮 act、腳本這三條路都存進同一格。
- ⚠⚠ 作法是**離場存、進場放**（`sessionSave`／`startGame` 尾端放回），
  **不是**在 `startGame` 開頭那排歸零裡挖特例 —— 那會讓「這一場到底重置了什麼」
  出現兩份答案（鐵律 7）。
- ⚠ 段落的收尾走**一支** `endSession()`：`sessionEnd` 那一場打贏、`goHome`
  （`keepPages` 例外 —— 那是借 goHome 當過場，收掉的話走一格資源就回滿）。

**⚠⚠ 戰鬥是這一段的最後一句時，舞台沒有人開回來（ver -585 修）**

- 打完回來走 `story.resumeFrom` 的 adhoc 分支。剩下有台詞時 `playAdhoc` 第一行就把
  `#storyStage.on` 加回去；**剩零句**時走的是「直接叫 `done`」那條捷徑 —— 那一條
  **漏了開舞台**。而 `goHome` 在黑幕全蓋那一刻一律 `$('home').classList.add('on')`，
  於是打完看到的是**首頁**，城鎮明明還開著（`isOpen()` 真、`getPosition()` 有值）
  卻整層 `display:none`。
- ⚠ 城鎮的 `acts` 常常整段只有一拍 `{battle:…}`（城鎮戰每一格都是），
  所以那是**常態不是邊角**。
- ⚠ 自檢：任何「把畫面交出去再接回來」的路徑，兩條分支（有剩／沒剩）都要問
  「誰把舞台開回來？」

## 6.5.4.4 敵人的顯形與淨化（ver -588，Ray 交辦）

> 「戰鬥中讓背景先出，怪快速淡入，怪 hp 歸零後淡出，看除了單純的淡入淡出
>   有沒有視覺效果更好的方法。」

兩端都**不是單純的淡入淡出**，各挑一個對得上世界觀的做法（CSS 在 `style.css` 的
`#enemyImg.enemy-rise` / `.enemy-purge`，掛法在 `modules/enemy.js`）：

- ⚠⚠ **戰鬥背景 ＝ 你站的那一格**（ver -592，Ray：「打完敵人應該會留在原背景，
  不要自動切背景」「敵人也是先出背景後再出現在背景上，背景不要閃動抖動變化」）：
  城鎮插入戰交棒的那一刻，`main.js` 把城鎮現在畫面上那一張（`town.currentBg()`）
  設進 `state.battleBg`，`enemy.setEnemy` 拿它蓋過敵人卡的 `bg` ——
  不然打完一場，上半會從卡上那張跳回節點原本那張，讀起來是換了個地方。
  ⚠ 覆寫存的是**檔名**（城鎮真的載到的那一個，含副檔名與時段）；卡上的 `bg` 是
    **基底名**（程式自己補 `.webp`）。兩種寫法差在這裡。
  ⚠ **每次交棒都明確設一次**（不在城裡就是 null）：靠上一場收乾淨會漏，
    漏了就把上一格的背景帶進下一場。
  ⚠ **平時背景不動**：怪的顯形、死亡、受擊特效只動 `#enemyImg`（一張去背立繪疊在
    `#top` 的底圖上）—— 底圖沒有 class 動畫，所以怪在抖、背景不抖。
    新增**這一類**效果時不要掛到 `#top`／`#app` 上。
  ⚠⚠ **例外：玩家受擊要整個畫面震**（ver -593，Ray：「玩家受擊時還是要有畫面抖動」）
    —— `#app.hitshake`（`screenShake()`，掛在 `enemyAttack` 這個所有扣血路徑的
    唯一入口，鐵律 8）。那是**鏡頭**在震：背景與怪一起動，讀起來才是「你被打到了」，
    而不是「那張圖在抖」。兩條規則管的是不同的時刻，不衝突。
    ⚠ 不共用 `camShake`（`#enemyImg` 的元素級抖動，沒放大）：整個 `#app` 位移
      四邊會露底色，所以 `appShake` 多一個 `scale(1.02)` 當過掃描。
    ⚠ 260ms 就好 —— 這是一記衝擊不是持續狀態，長了會妨礙點盤面。
- **降臨 `enemy-rise`（900ms，ver -640 由 420ms 的「顯形」改寫，Ray：「讓遭遇戰的怪
  出現時更有『降臨』的感覺」）**：它**降下來**，不只是被看清楚。三段：
  ① **從上方落下** —— 起手在上方 −7%、放大 1.16（離鏡頭遠）、失焦 22px、去彩、偏暗；
  ② **沉到定位**（62%）焦距與顏色一路回來；
  ③ **著地**（78%）**壓過頭一點**（scale .985）＋亮度衝到 1.45，最後 22% 回彈到 1
     —— 等速落到底讀起來是「貼上去」，有壓過再回彈才是「踩到地上」。
  配著地那一刻還有兩件事：**一圈由腳底往外擴散的聖光**（`.enemy-land`，`screen` 混合）
  與**鏡頭一震**（走 combat 的 `screenShake`，鐵律 8 —— 不要在 enemy 自己加 class）。
  ⚠ 失焦與去彩由重到輕是「從遠處來」的距離感，不要拿掉。
  ⚠⚠ 著地的時刻寫在 `enemy.LAND_AT`（702ms）＝ CSS 那一格（0.9s × 78%）——
    **改一邊要改另一邊**（鐵律 7 的但書，兩邊註解互指）。
  ⚠⚠ 著地的計時器要掛在**模組**上不是掛在 `rise` 那個閉包上：`loadEnemyPortrait`
    每次呼叫都做一個新的 `rise`，掛閉包等於前一次取消不掉 —— 實測換一次敵人
    就疊出**兩圈**落地光。
  ⚠ ver -588 那一版是「從背景裡解析出來」（本來就在那裡、你才剛看清楚），
    現在的語氣不同了；背景**先出**那一條照舊（`RISE_DELAY_MS`）。
  ⚠ 所以**背景要先出**：`setEnemy` 先擺 `#top` 的底圖，立繪延後 `RISE_DELAY_MS`
    才起 —— 沒有背景就沒有可以「浮出來」的東西。
  ⚠ 等 `onload` 才起（快取命中走 `complete`）：沒載到就播，前半段是在演一張空圖。
  ⚠ 每次先 remove 再 reflow 再 add，不然連戰換第二隻時動畫不重播；
    順手清掉上一隻的 `enemy-purge`（不清的話新的一隻頂著半截遮罩出場）。
- **淨化 `enemy-purge`（600ms）**（ver -591 改，Ray：「敵人死掉的效果不好，
  如果用垂直拉長抖動淡出的話呢？」）：**由腳底往上垂直拉長（1→2.2）＋
  橫向高頻抖動（±10px）＋ 淡出**。
  ⚠ `transform-origin:50% 100%` —— 從**腳底**往上抽。原點在中心的話是上下一起長，
    讀起來是變胖不是被抽走；立繪本來就靠下（`fit.pos` 多半 `center bottom`）。
  ⚠ 抖動與拉長寫在**同一個 `transform`**：一個元素只有一份 transform，
    分開寫後者會蓋掉前者。
  ⚠ 抖動用 **5% 一格**：格子太疏（10%）在 60fps 下會被內插成「左右搖擺」，
    要密到讀得出是顫抖。振幅**中段最大**、尾段隨淡出收小 ——
    一路抖到消失讀起來是雜訊，不是死亡。
  ⚠ 拉長走 ease-in：等速讀起來是「被拉開」，加速才是「被抽走」。
  ⚠⚠ ver -588 那一版（聖光漂白 ＋ mask 由下往上抹除）**已被 Ray 退回**，不要退回去。
  · **白光星芒飄散**（ver -594，Ray 加的）：撒 18 顆十字光斑（`.fx-star`：
    `::before`／`::after` 是橫豎兩道三段式漸層光條，`i` 是中心光核），
    往**上**飄散開來。
    ⚠ 光條要兩端尖中間亮（透明→白→透明）：等寬的白條讀起來是棍子不是光。
    ⚠ `mix-blend-mode:screen`：白光疊在暗背景上要用加亮混合，直接畫白色是白貼紙。
    ⚠ 方向**偏上**（`--dy` 一律負）：怪同時在往上拉長，光往上飄才是同一件事的兩面；
      四面八方散開會把方向感抵消掉。
    ⚠ 逐顆給不同的 `delay`／`life`：同時出現同時消失是一次閃光，錯開才像一直在飄。
    ⚠ 撒在**立繪站的那一塊**（30~70% × 28~74%）不是整個 `#top` ——
      撒到邊角會變成「畫面在發光」而不是「牠在散」。
- ⚠⚠ **降臨與淨化只給禍魘與聖徒系列**（ver -594 定淨化；**-657 Ray 加上降臨**：
  「只有分類為禍魘的敵人會有降臨跟擊敗淨化特效」；**-658 Ray 補**：「聖徒效果上
  等同禍魘」—— `slay` 吃同一套演出，但**結算副標**仍是「已擊殺」不是「已淨化」，
  同一格 `kind` 兩種用途各查各的表）：那是同一件事的兩端 ——
  禍魘從惡夢裡降下來、被淨化之後散成白光。人類／靶／船／獸／聖徒系列各有各的
  出場與死法，不該共用。**兩個演出共用同一份名單**（`HARM_KINDS`，鐵律 7），
  判定看**敵人卡的 `kind`**（結算副標讀的也是它）—— 不要另立一個「要不要播特效」
  的欄位。
  ⚠ 降臨那一支判的是**傳進來的那張卡**不是 `state.currentEnemyKey`：
  `setEnemy` 在寫那把鑰匙之前就可能叫到 `loadEnemyPortrait`，問 state 會問到上一隻。
  其餘 kind 目前沒有專屬死法（維持原行為），Ray 給了再照這裡加一支。
  ⚠ 掛在 `finishEnemyOrAdvance` 這個**匯流點**（鐵律 8）：自然清盤／按錯／逾時／
    聖徒化擊殺四條路都經過它。**連戰換敵那一條不掛** —— 那一隻是被「掠過」
    （既有的 `enemy-leave`），不是被淨化。
  ⚠ 冪等：overkill 期間 `enemyHp<=0` 會被判到好幾次，重複 add class 不會重播。

## 6.5.5 旅店大廳（ver -392）

旅店節點（`script/town.js` 的 `inn:true`）在進場對白之後多一層互動：
**伙伴門 ×4 ／ 獨自坐坐 ／ 回房睡覺**，實作在 `modules/inn.js`。

- ⚠ **進度由旗標推出來**（`inn_wait` / `inn_renna`），**不要另存一個狀態變數** ——
  那個變數存檔存不到，讀檔回來就對不上（§6.9 那張清單）。
- ⚠ **頭像不用新素材**：拿角色自己的立繪，用 `speakers.js` 量好的 `fx` 把臉挪到框中央
  （鐵律 7：取景值只有那一份）。睡了＝**不透明的壓暗**，不是透明度（§6.5）。
- ⚠ **大廳跟著導覽一起開關**（`body.town-nav`）：對白播放中那一排就不該在，
  不然按鈕與提示會壓在對話框上。與羅盤／目的地字格**同一個開關**（`showNav`），
  不要另寫一套（鐵律 8）。
- ⚠ 大廳的元件要**離場景區的下緣遠一點**：那一截被**楣**壓住（楣的上緣在場景區底
  往上約 60px），bottom 給 12px 的東西整行看不見。
- ⚠ `modules/inn.js` **不 import `town.js`**（會循環）：要用到城鎮那邊的
  `say`／`lock`／`play` 一律走 `inn.setup()` 注入。
- ⚠⚠ **「獨自坐坐」是長期要留的機制**（ver -409，Ray：「以後好感度往上坐一坐會有女角
  出來跟你互動，所以一定要留」）—— 不要因為「現在只用來等蕾娜」就把它收掉。
- ⚠⚠ **「現在站在旅店裡該發生什麼」只有一支 `runBranch()`**（ver -427，鐵律 8）：
  走進來、獨自坐坐坐完、蕾娜那一幕演完，三條路都問它。
  以前只有「走進來」問得到，而 `sitAlone` 自己**另寫了一份**蕾娜的判斷 ——
  於是「坐到過 18:00 諾薇兒不會去休息」整條漏掉（坐兩小時跨過傍晚，什麼事都沒發生）。
- ⚠ `runBranch(arrived)` 要分「**走進來**」與「坐完」：分支一（「時間還早，我想去城裡
  逛逛呢」）是**招呼**，只在走進來時講；分支二（「今天有點累了」）是**狀態轉移**，
  時間到了就講。不分的話每坐兩小時她就把那句招呼再唸一次。
- ⚠ 分支二的條件是「走完了」**或**「時間到 18:00」（與城鎮那一格傍晚是同一件事，
  用同一個 `eveningHour`）。只判 `allSeen` 的話，被強制抓回旅店那條路上
  （時鐘正好 18:00、地點沒走完）她會說「時間還早」—— 與剛剛那句話打架。
- ⚠⚠ **睡覺與坐坐是兩段演出**（ver -430，Ray 定案），不是「按下去就換數字」：

  | | 暗下去 | 中間 | 亮回來 |
  |---|---|---|---|
  | 回房睡覺 | **睡覺音檔多長就多長**（`se_sleep`，實測 9.9 秒） | 推時鐘到隔日 07:00、換時段背景、**建立那唯一的一份存檔** | 900ms 淡回旅店 |

  ⚠ **睡覺音播放期間 BGM 讓位**（ver -499，Ray：「播旅店睡覺音樂時原 bgm 要淡出，
    播完再淡入」）：`SFX.duckBgm`（淡到 0、不換曲）→ 整支睡覺音播完 `unduckBgm`
    淡回目標音量。兩支住在 audio.js（鐵律 8），與 `stopBgm` 是兩件事。
  | 獨自坐坐 | 700ms | 全黑**停 1600ms**、推時鐘、換背景 | 700ms（合計約 3 秒） |

  ⚠⚠ **淡出的長度問 `SFX.duration()`，不要在演出那邊寫秒數**（鐵律 7）——
    長度的真相在音檔身上，換一支音檔程式不必改。拿不到（還沒解碼）才走退路，
    而照 §6.6「音效不載完不放行」正常情況下不會走到。
  ⚠ **ver -433：淡出只走音檔長度的一半**（`SLEEP_FADE_RATIO`，Ray：「睡覺淡入淡出
    時間過長，減半，SE 不用改動」）—— 音效照樣整支播完（那是那一段的聲音），
    畫面不必陪它等：9.9 秒的漸暗讀起來是卡住，不是入睡。醒來那一段也由 900 減半成 450。
  ⚠⚠ **黑幕不能住在會被收掉的那一層裡**。舊版那一片在 `#innLobby` 底下，而
    `body.town-nav #innLobby.on` 才 display —— 演出第一行就 `lock(true)` 收掉導覽，
    於是**那片黑幕從來沒有亮過**，「暗去再亮起」一直只是乾等一秒（ver -430 才發現）。
    現在走 `story.veil(on, ms)`（`#storyVeil`，舞台最上層，唯一那一片，鐵律 8）。
  ⚠ **全黑要停一拍再亮**：暗→亮直接相接的話，換時段的背景是在觀眾眼前抽換的，
    讀起來像閃了一下，不像過了兩小時。
  ⚠ **被強制移走時黑幕交給 `town.enter()` 收**（睡到隔天七點＝ stage 0 的結尾）：
    新地點擺好了才亮，就是一次乾淨的剪接。「沒有特別劇情就淡入回旅店」是 Ray 的原話。
- ⚠⚠ **睡覺＝建立那唯一的一份存檔，不開存檔欄位面板**（ver -430，Ray：「不要給存檔
  欄位，只有單檔，睡覺就是建立唯一存檔」）。走 `save.storySave()`（存檔庫的 `main` 格）。
  ⚠ **不能借用 `quick`**（F4 那一格）：管理人隨手按一下 F4 就把玩家的進度蓋掉了。
  ⚠ 欄位面板（F5/F8）**留著** —— 那是 `body.testmode` 限定的開發梯子，玩家看不到。
  ⚠ 覆蓋不必確認：那正是「睡一覺＝存檔」的語意，問一次反而是在暗示有別的選擇。
  ⚠ 存檔要在**時鐘推完之後**：存的是醒來的那一刻，不是躺下的那一刻。
- ⚠ 「獨自坐坐」要**推進時鐘**（時間是資源）。
- ⚠⚠ **任何可能不會被呼叫的回呼，都不可以是唯一的解鎖點**（ver -667，Ray：
  「回房坐坐現在點了無效」）。旅店的 `busy` 是「這一層正在演東西」的鎖，由敲門／
  坐坐／睡覺的**回呼**解開 —— 而 `story.endAdhoc()`（換節點時一定會叫）
  **不會呼叫那個 done**。於是「敲蕾娜的門 → 對白還沒推完就走掉」會把鎖永遠留著，
  回到旅店之後兩顆行動鈕與敲門就全部按不動了，**而且沒有任何錯誤訊息**。
  ⚠ 解法是在**唯一的入口**歸零（`inn.arrive()`／`close()`），不是在每個可能被
    打斷的地方各補一次（鐵律 8）。
  ⚠ 這與 §6.5「上場是延後執行的，撤場要取消掉」是同一族的坑。
- ⚠⚠ **「這座城現在還不能睡」用 `noSleep` 擋在按下去那一刻**（ver -659，Ray：
  「旅店還是要有獨自坐坐，回房睡覺也要有，但是點了會跳『現在不是睡覺的時候。』」）。
  ⚠ -656 曾經整組不給那兩顆鈕（怕睡覺一口氣跳到隔天把當晚的戲跳過去）——
    被退回。**鈕要在**（藏起來玩家只會以為壞了），擋的方式是一句話，同「還沒六點呢」。
  ⚠ 它排在「太早」（`innEarly`）之前：那一條看時刻，這一條是劇情狀態，與幾點無關。
  ⚠ 名字欄空著＝旁白（主角自己的念頭）。
- ⚠⚠ **兩顆行動鈕不可以長得一樣**（ver -407，Ray：「兩顆都留，但擺開／改樣式」）。
  份量差很多：一個消磨兩小時、一個結束今天並存檔 —— 長得一樣會被讀成同一組選單。
  作法：①各帶一行**時間成本**的小字（時間是資源，按之前就該看得到代價）
  ②睡覺那顆走金色實心的「主要動作」樣式。**區別交給樣式，不要靠拉遠位置。**
- ⚠⚠ **鈕的位置會被讀成語意**（ver -408，Ray：「你這樣擺搞得好像要去跟蕾娜睡一樣，
  害我產生不必要的期待」）。-407 為了「擺開」把「回房睡覺」移到 0.620 —— 那正好
  在右側伙伴門欄的蕾娜那一格旁邊。**挪任何一顆鈕之前先看它會落在誰／什麼東西旁邊。**
  現在固定在櫃台正上方（0.490）。
- ⚠⚠ **一次性說明的遮罩是「說明」不是「鎖」**（ver -430，Ray：「教學雪鐵龍狀態下
  按鈕不作動」）。踩到的是兩件事，兩件都要修：
  1. **按下被指的那一顆不會收掉說明** → 遮罩永遠留在畫面上，之後每一次點擊都被它
     吃掉（實測：坐坐按下去時間有走、遮罩沒收，整個大廳從此按不動）。
     ⇒ **任何一顆鈕按下去都先 `closeGuide()` 再做事**。
  2. **沒被指到的那幾顆壓在遮罩底下** → 想按睡覺卻被指著坐坐的說明擋住。
     ⇒ 說明開著時（`#innLobby.guiding`）**所有可按的東西都抬到遮罩之上**；
       「指的是這一顆」交給 `.spot` 那圈金光，不是交給「別的按不動」。
  ⚠ 順手把**玩家剛做過的那一件**也記成看過（`learnTip`）—— 不然剛睡完醒來，
    還會被教一次「怎麼睡覺」。它與 `closeGuide` 是兩件事：那一支收的是「現在開著的
    那一則」，這一支記的是「他剛剛做的那一件」，常常不是同一則。
  ⚠ **`.spot` 不要寫 `position`**：兩種目標本來就都定位過了（鈕 absolute、門 relative），
    硬塞 `position:absolute` 會把門從那一欄的排版裡抽出來，敲門那一則一開門就跳位。
- ⚠⚠ **「還不能做」不要靠藏起鈕擋**（ver -408）：鈕不見了玩家只會以為壞了。
  「獨自坐坐」原本睡下之後（`slept`）就收起來，Ray 回報「又不見了」——
  消磨兩小時**本身就是一個行動**（城裡的店有營業時間、打烊了走不進去），
  所以兩顆鈕現在都是「初入對白演完就有，之後一直都在」。要擋就像諾薇兒那樣**開口擋**。
- ⚠⚠ **行動鈕、伙伴門、對話框是三套座標系**：鈕錨在**背景圖**上（`bgPoint`，會隨機器
  長寬比在圖上滑動），門欄錨在**畫面右緣**（CSS），對話框錨在**楣的上緣**
  （`layoutKerberos` 解出來的那組變數）。所以鈕擺好之後要**夾一次**：
  夾回畫面內 ＋ 不進門欄 ＋ **不壓到對話框**（下界＝框的上緣）。
  實測踩過兩次：睡覺鈕右緣 338 vs 門欄左緣 328（疊 10px）、坐坐鈕下緣 369 vs
  框上緣 333（Ray：「獨自坐坐壓到對話框了」）。
  ⚠ 夾之前要確認鈕真的顯示著 —— 沒顯示的元素量到 0×0。
  ⚠ 對話框可以量：`visibility:hidden` 的元素照樣有 box，所以框沒顯示時也夾得準。
  ⚠ **資料上的落點只負責「看起來對」**（坐在椅子上、放在櫃台上），
    保證不撞的是那個夾 —— 換一台長寬比不同的機器，圖上的同一點會落到不同高度。
- ⚠⚠ **「回房睡覺」從初入就按得到**（ver -405，Ray：「初入旅店除了坐坐之外也可以選擇
  直接去睡覺」），但**六點以前**按下去由諾薇兒擋回來（`innEarly`，front 立繪）。
  ⚠ 「太早」**不是靠藏起鈕**擋 —— 鈕不見了玩家只會以為還不能睡；她開口說「還沒六點呢」
    才知道為什麼。
  ⚠ 那個「六點」＝這座城 `evening.hour` 的**同一個數字**（由 `town.afterArrive` 傳進
    `inn.arrive`）—— 改集合時間只改那一處（鐵律 7）。
  ⚠ 蕾娜還沒回來就先去睡 → 記 `inn_missed`：她 20:00 才進門，人睡著了自然碰不到。
    不記的話 `stage()` 會卡在 `wait`／`none`，與「已經過了一夜」對不上。

**蕾娜的回店時刻**（ver -395）：`RENNA_BACK 20:00` / `RENNA_GONE 21:00`。
- 「獨自坐坐」是**把時鐘推到 20:00**，不是加固定時數（已經過了就不動 —— 時間只能往前）。
- 走出旅店再回來也算：20~21 點進去**撞見她**（好感 +1）；≥21 點她已經回房。
- ⚠ 「錯過」要**記旗標**（`inn_missed`），不能每次看時鐘算 —— 隔天早上時鐘又小於 21，
  不記的話她會「重新出現在大廳等你」。
- ⚠ 好感只在**一支**收尾函式裡加（`finishRenna`）：坐著等到她、或走出去撞見她，
  兩條路都走那一支（鐵律 8）。

**主角的名字與暱稱**（ver -395）：`{P}`＝名字、`{N}`＝暱稱，**顯示的那一刻才代換**。
- 預設 **凱勞諾斯／凱**（`progress.js` 的 `PLAYER_DEFAULT` / `NICK_DEFAULT`）。
  ⚠ 西文 `Keraunos` / `Ky` 是**檔名與程式用的 id**，與玩家輸入**脫鉤** ——
  不要拿玩家輸入的字去拼路徑。
- 輸入是一個**閘門拍**：腳本寫 `{ nameInput:true }`，沒有台詞、不出對話框，填完按確定才往下走。
- ⚠ `PLAYER_DEFAULT` 在 `flight/index.html` 有**第二份**（非 module 頁面 import 不到）——
  改一邊要改另一邊（鐵律 7 的但書，兩邊註解已互指）。
- ⚠ 暱稱是「一輪內」的資料：`newRun()` 要清、`snapshot/restore` 要帶（§6.9 那張清單的兩面）。

**傍晚的提醒**（`TOWNS[].evening`）：走完所有地點**或**時間過 18:00 → 諾薇兒提議回旅店。
- ⚠ 它**取代**那一次抵達原本要演的進場對白（Ray：「優先所有事件」），而那一段的旗標
  不會記 —— 下次再進來還是會演（同「打烊不播」的作法）。
- ⚠ **在旅店裡不演**：站在旅店裡說「我們先回旅店吧」是錯的。
- ⚠ 「走過了沒」另開一組旗標（`seen_<城>_<節點>`），**不能借用進場對白的旗標** ——
  有的節點沒有對白（中心區），有的對白被打烊擋掉，那些也算走到過。

## 6.5.6 選單／震動／主角與蕾娜的稱呼（ver -397 / -398）

**選單**（`modules/settings.js`，由槍棺右上那顆「選單」開啟）
- 回到主選單／分軌音量（音樂・音效・語音）／自動播放間隔／震動開關。
- ⚠⚠ **「回到主選單」不能只 `story.close()`** —— 劇情層底下是 `#app`，而 `#home`
  早就被別的路徑關掉了（`combat.startGame`、`openFlight`、飛行頁交棒…），
  掀開來就是**上一場戰鬥的盤面**（Ray 回報過）。要走**唯一那支**回主選單
  （`combat.goHome`，注入進來），劇情層在**黑幕全蓋的那一刻**才收。
- ⚠⚠⚠ **返回首頁＝殺光所有頁面**（ver -494，Ray：「返回首頁就要 kill 所有的 page
  再回去」）。實作只有一支：`main.js` 的 `killAllPages()`（注入 `combat.setPageKiller`，
  `goHome` 在黑幕全蓋的那一刻呼叫）—— 飛行 iframe **整個卸載**（`about:blank`＋拔 src，
  音訊／計時器／模擬隨 document 一起死；下一次出航就是一趟新的航行）、
  城鎮 `suspend()`＋`close()`（介面與狀態都收）、劇情舞台、整備頁、選單面板、
  買賣單子，外加 `flightBack`/`storyResume` 歸零。**藏起來不算殺**（ver -483 的
  extPaused 是暫停，這一條是死）。
  ⚠ 例外只有 `opts.keepPages`：**借 goHome 當過場**、onCovered 立刻開回某一頁的
    那兩條路（打完回飛行畫面 `openFlight({resume})`、戰敗「再戰」`story.resumeFrom`）
    —— 那不是回首頁，殺了船的座標／續播狀態就沒了。
  ⚠ 連敗三場送回旅店那條**照殺**：他是被抬回旅店的，追兵與船一起清場，
    town.open 在 onCovered 重開一座乾淨的城。
  ⚠ 新增任何全螢幕頁面時，把它的收場器補進 `killAllPages` —— 不補的下場就是
    「回到首頁了，那一頁還在背景活著」。
- ⚠ 按下去要**先確認**「尚未儲存的進度將會遺失」：旗標／時鐘／道具是即時寫的，
  但**演到哪一句**不是。面板用**換頁**不疊第二層（疊兩層很難點得準）。
- ⚠ 設定是**玩家的偏好**，`progress.newRun()` 不清（同靜音、語言，§6.9）。

- ⚠⚠ **分軌音量要涵蓋「所有會出聲的地方」，不是只有 `SFX`**（ver -436，Ray：
  「音量調節鈕是裝飾沒有效果」）。主遊戲那一邊是通的（`settings.apply()` →
  `SFX.setLayerVolume` → 每一軌一顆 gain 接進 limiter；BGM 走 `bgmTargetVol()`）——
  **漏的是飛行頁**：它是另一個 document，用自己的一組 `HTMLAudio`
  （`makeLoop` 的音樂與環境音、心跳、神楽鈴、Sturm、槍棺），從來不經過 `SFX`。
  而大地圖正是玩家待最久的地方，所以滑桿「看起來完全沒有作用」。
  ⚠ 作法：飛行頁自己讀**同一組鑰匙**（`tivot_vol_*`，§6.10 的跨頁慣例），
    `volOf()` 現讀不快取（設定可能在那一頁藏著時被改）。
  ⚠ 音效一律走**唯一的** `playSfx()`，音量在那一支乘進去 —— 呼叫端不必記得，
    日後多一支音效自動吃到（鐵律 8）。
  ⚠ **環境音／火車／海鷗算 SE 那一軌不是 BGM**：它們是「世界的聲音」。
  ⚠ 與靜音鈕（`tivot_mute_v1`）是**兩件事**，兩者相乘：一鍵全關 vs 分軌強弱。
  ⚠ **自檢**：日後新增任何一個會出聲的畫面，先問「它的聲音經過哪一條路？
    那一條有沒有乘上玩家的分軌音量？」—— 不經過 `SFX` 的一律要自己接。

**震動**（`modules/haptics.js`）
- 震的地方：受到敵人主動攻擊／反擊／破防窗口**每一發**／槍棺撞頂／劇情畫面震動／
  腳本上備註 `vibrate:true`。
- ⚠ **機槍反擊是「一條連續的震動，長度＝整串的持續時間」**，不是每發各震一下 ——
  手機會把密集的短震合併成一串黏在一起的抖動，讀起來比一條長震還糊。
- ⚠⚠ **iOS Safari 不支援 `navigator.vibrate`**：整支在 iPhone 上是 no-op，
  不是壞了。網頁拿不到 Taptic，只有原生殼才有。
- ⚠ 呼叫點只有一支（具名函式），不要在各處自己 `navigator.vibrate` —— 那樣開關會漏掉。

**主角的稱呼**
- ⚠⚠ **取名之前一律叫 `HUND`**（`progress.NAME_BEFORE`）：`{P}` 與 `{N}` 都代換成它。
  「還沒取名」與「取了名」是**兩個顯示**，不是「預設值」—— 預設值（凱勞諾斯／凱）
  是**輸入框裡的預填**，玩家按確定才成立（`isNamed()` 判鑰匙存不存在）。
- ⚠ `flight/index.html` 有第二份（非 module 頁面），改一邊要改另一邊。

**蕾娜的稱呼**
- ⚠⚠ **正名前後是兩個 speaker id**（Ray：「可以視作不同角色」）：
  `OFFICER`（監察官）用到她說出**「我以海森伯格之名請求妳」**那一句為止，
  下一句起改用 `RENNA`。兩者 `art` 同指蕾娜立繪，畫面上是同一個人。
- ⚠ 一幕的 `sides` 覆寫要**兩個 id 都寫**（`{ RENNA:'R', OFFICER:'R' }`）——
  只寫一個的話前半段會跑回她的固定站位。

### 一次性的操作提示（`TOWNS[].tips`，ver -429）

`need` 的旗標到齊了 → **下一次抵達、對白演完之後**彈一次雪鐵龍箭，彈過就記 `flag`。
第一個用例：整備教學掛在 `got_Shotgun_Dragon`（打靶 30 秒內拿到「龍息」）。

- ⚠ **為什麼不做成腳本裡的 `hint` 那一拍**：那一拍是**位置固定**的，而「拿到獎品」
  是有條件的（30 秒內、而且還沒有那把槍）—— 腳本沒有條件式的拍。掛旗標才對得上。
- ⚠ 旗標名**由武器 id 推**（`got_<id>`，`modules/inspector.js` 發獎品時記），
  不寫死是哪一把（鐵律 1）。
- ⚠ 與腳本的 `hint` **走同一支** `story.showHint`（鐵律 8）：遮罩、箭、抬層只有一份。
- ⚠⚠ **教學先、選單後**（ver -430，Ray：「武器店的裝備教學先彈出，裝備完才跳出武器店
  的選單」）。有到期的提示時，店舖只擺**店主**、那張單子**押後**：單子停在畫面左邊，
  正好蓋住要教的那顆吊墜，而且整備頁一開它還在底下佔著版面。
  ⚠ 「裝備完」＝**整備頁被收掉**，不是「點到了吊墜」。所以要等的是
    `gear.onceClosed()`（那一頁唯一的收場，鐵律 8），不是 `showHint` 的回呼。
  ⚠ **不能把玩家鎖在教學裡**：他可能直接把提示點掉、根本沒按吊墜 —— `onceClosed`
    查到那一頁沒開就立刻放行（同 `openHint` 「點提示本身也放行」的原則）。
  ⚠ 讓開的是**單子**不是整個店舖畫面：店主照舊立刻上場，不然玩家會以為走錯地方。
  ⚠ 兩條路（有提示／沒提示）都要把單子擺出來，而開單子的實作只有 `openMenu()` 一支。

## 6.5.7 鍵盤（ver -427，Ray 指定）

| 鍵 | 在哪 | 做什麼 |
|---|---|---|
| `W A S D` | 城鎮 | 移動（**按住 0.5 秒**，同箭頭：蓄能圈、目的地字格都照舊） |
| `空白` / `Enter` | 對白（劇情／城鎮／教學） | 推進一句（＝點畫面一下） |
| **按住**`空白` | 對白 | 加速（放開即停，同「按住下拉」） |
| `空白` / `W S` / `↑↓` | 選項面板 | 亮出游標／移動／確定 |
| `Esc` | 任何面板 | 關掉最上面那一層 |

- ⚠⚠ **鍵盤不是第二套實作，是同一套的另一個入口**（鐵律 8）：移動走 `startHold()`、
  推進走 `advance()`、加速走 `setFast()`、選項確定走**那顆鈕自己的 `click`**。
  另寫一條的話音效、動畫、回呼時序就會有兩份。
- ⚠ **「按住」用 `e.repeat`**：第一下推進，之後的自動連發轉成加速模式 ——
  一顆鍵同時是「點一下」與「按住」，與面盤那邊（點／下拉）是同一個語意。
  移動則相反：`e.repeat` **一定要濾掉**，每一發都重開一次長按的話進度圈永遠從頭算，
  結果是「按住 WASD 走不動」。
- ⚠ **視窗失焦要收手**（`blur` → `setFast(false)` / `cancel()`）：按著鍵切走的話
  `keyup` 收不到，回來會一路自己播完、或憑空走一步。
- ⚠⚠ **選項面板：第一下空白只「亮出游標」，不會選下去。** 玩家是一路按空白推對白
  進來的，選項一跳出來就被下一下空白選掉是很糟的意外（而且看不出選了哪一個）。
  亮出來之前畫面上沒有任何高亮，所以純觸控的玩家也不會憑空多一個「已選取」的框。
- ⚠⚠ **Esc 是一支通用的**（`main.js` 的 `bindEscClose`）：面板不必各自綁一次，
  也就不會有人漏綁 —— 每一層只要有一顆「關閉」鈕，那一支就按得到它。
  **閘門不關**（選項 `#choiceSheet`、取名 `#nameSheet`）：關掉會讓劇情卡在
  一拍沒有出口的地方。新增面板時在那張表加一列就好。
- ⚠ **教學的閘門不吃空白**：閘門要的是指定的操作（點計量表／右滑／上滑），
  用空白帶過去等於把教學跳掉。
- ⚠⚠ **主角的空白對話框（`blank:true`）是快進與自動播放的對象，不停**
  （Ray 指定）。以前那一拍直接 return，於是兩個模式都**卡在他那裡**等玩家點 ——
  而框裡根本沒有字可讀。停的長度另給（`BLANK_BEAT`）不照 `autoDelayMs`：
  那個間隔的語意是「一句**唸完**之後停多久」，而空框一出現就等於唸完了。

## 6.5.8 章節（管理人限定的跳關，ver -429）

首頁的「章節」鈕（`body.testmode` 限定）→ 直接跳到某一章的開頭。
內容是**資料**：`script/progress.js` 的 `CHAPTERS`（`flags`／`stage`／`clockHour`／
`enter`＋`town`／`node`），`main.js` 只負責演 —— 加一章不必動程式（鐵律 1）。

- ⚠ 每一章都先 `progress.newRun()`（＝「從頭開始」的**唯一**那一支，§6.9），
  再把「這一章開始時本來就該有的東西」放回去。
- ⚠ 起始時刻問 `clock.firstHourAt(h)`，**不要寫死分鐘數**（鐵律 7）——
  開局時刻改了，章節的起點要跟著改。
- ⚠ 入口寫成**代號**（`story`／`town`）不是函式：資料層不認識啟動層。
- ⚠ `flags` 只列**擋路的那幾個**（進場對白會不會重播、演出會不會插隊）。
  這是除錯工具，重播一段對白不致命；列太多反而難維護。

## 6.6 音訊通則(全域,ver -243 起)

**檔案位置與命名** —— `resources/audio/{bgm,se,vo}`,搬檔與改名走 `tools/audio_reorg.py`
(它會**一併**改 `config.js` 與 `flight/index.html` 兩邊,手改很容易只改了前者)。

    bgm_<場合>            背景音樂
    se_<分類>_<名稱>      音效(ui/weapon/enemy/saint/flight);循環音加 _loop
    vo_<角色>_<技能>      語音

命名一律用**用途**當主詞,不用來源或流水號。底線開頭的資料夾(`_master`/`_unused`/`_raw`)
不會被遊戲載入。

**⚠⚠ 響度:逐支拉平 ＋ 分層比例(ver -441 改,Ray 指定「全域音效 bgm 語音響度拉平,
語音 100% 音效 90% 音樂 80%」)** —— 量測用 BS.1770 K 加權的閘控積分響度(近似 LUFS),
**不要用 RMS**:RMS 低估人聲、高估低頻,舊表就是這樣把槍聲調得比語音還大。

- **每一支音檔**都拉到同一個目標(`tuning.loudness.targetLufs`,現行 −20 LUFS)。
- **層與層的關係只由三個數字決定**:`tuning.loudness.layer = {vo:1.00, se:0.90, bgm:0.80}`。
  ver -243 那套「VO −18／SE −22／BGM −28」的分級**已經取消**。

| 層 | 相對 | 乘在哪 |
|---|---|---|
| 語音 VO | 100% | `audio.js` 的 `_layerBase`(匯流那一節,乘一次) |
| 音效 SE | 90% | 同上 |
| 音樂 BGM | 80% | 同上(BGM 走 `bgmTargetVol()`) |

- 逐支的增益住在 **`config.js` 的 `tuning.fileGain`**,鑰匙是**檔名**(去副檔名、去 `?v=`、
  轉小寫)—— 因為**一支音檔只有一個響度**,而同一支在專案裡有好幾種叫法
  (`sfx_saint` 與 `se_saint_install` 是同一支;劇情層的 `SE_FILES` 根本沒有 ASSETS 鍵)。
  程式一律問 `fileGain(路徑或鍵)`(`sfxGain`／`bgmVol` 是它的別名)。
- 公式:`gain = 10^((targetLufs − 平均LUFS)/20) ÷ masterVolume`,
  **平均**＝耳機與手機模型兩次量測的平均(見下方「耳機對了不代表手機對了」)。
  語音要**過完 `voiceChain` 之後**才量。
- `peakCeilDb`(現行 +2 dBFS):增益推到峰值超過這條線就夾住 —— 匯流 limiter 的門檻是
  −6 dB／ratio 12,再推也只是被壓扁。被夾住的那幾支會低於目標,那是母帶的極限。
- ⚠⚠ **加新音檔一定要補一列 `fileGain`**:沒補＝增益 1 ＝以母帶的響度播出。
  ver -441 之前劇情那 20 幾支就是這樣(實測 `se_steps` −32 LUFS、`se_walk` −34,
  比目標低 12~14 dB)—— Ray 回報「跌倒音跟跑步音永遠不出來」,查了三版都當成
  預載沒趕上,**其實是聲音小到聽不見**。
- **量測跑 `tools/audio_scan.html`**:它現場列目錄(不會漏新檔)、增益 import config
  (不會與程式走鐘),逐支印出建議值。整體放大縮小只動 `masterVolume`,比例不變。
- ⚠ 飛行頁是另一個 document,`master`／`layer`／用得到的那幾支 `fileGain` 在
  `flight/index.html` **各存一份**(§6.10 的跨頁慣例,兩邊註解互指)。

⚠ 環境音(航行/停船那種循環床)與**音樂是堆疊不是輪播**。舊版把它們當成兩首 BGM
交叉輪播,換上真正的音樂之後環境音就整個消失了。

**檔案大小(ver -384)** —— 音效一律 **AAC(m4a)**,不要留 256 kbps 的 mp3。
- 立體聲 **96 kbps**、單聲道 **64 kbps** 就夠(短音效聽不出差別,實測整層省 53%:2.26→1.07 MB)。
- 轉檔:`afconvert -f m4af -d aac -b 96000 -s 0 in.mp3 out.m4a`。⚠ `-s 3`(VBR)會**忽略** `-b`。
- ⚠ 轉完要**改三個地方的引用**:`config.js` 的 ASSETS、`modules/story.js` 的 `SE_FILES`、
  `flight/index.html`;門的音效另有 `KERB_SFX_EXT` 的預設副檔名與一行寫死的齒輪音。
  (那一行以前就與別處不一致過 —— grep 副檔名,不要只改看得到的那個。)
- ⚠ 轉檔**不改響度**,所以 `tuning.fileGain` 那張實測反推的增益表不必重算。

**⚠⚠ 預載順序(ver -384;**-430 升為全域鐵則**,Ray：「全域都是優先預載音效
然後圖然後 bgm，音效不載完不放行」)** —— **音效全部 → 圖(立繪最先)→ 音樂**,
三段**串接**不併行,而且**音效那一段沒有時限**。

- 為什麼是這個順序:音效**要解碼**,慢網下最容易「點進去了還沒好」(開場的跑步聲/
  跌倒音消失過好幾次);圖沒載完不會壞(`<img>` 現抓即顯示);音樂最不急
  (`playBgm` 自己 `ensureBlob`,晚到只是晚幾百毫秒起播)。
  併行會搶頻寬 —— 那正是「音效讀曲跟不上」的成因。
- ⚠⚠ **「音效不載完不放行」的破口一律是「保底計時器」**。每一條路上都有一顆
  「卡住也要放行」的 `setTimeout`,只要它**從批次開跑就計時**,時間一到就等於
  跳過音效。作法:**保底掛在音效那一段之後**(`sfxP.then(()=>setTimeout(…))`),
  它只保護圖與音樂那兩段。
  ⚠ 不怕等太久:`SFX.load()` 對 404／解碼失敗一律 `catch → null`,
    真的無限等只有「請求整個停住」那一種。
- **這條適用每一條進畫面的路**(ver -430 一次收齊,以後新增畫面照抄):

  | 路徑 | 實作 | -430 改了什麼 |
  |---|---|---|
  | 開機讀取頁 | `main.js` 的 `startBatch` | 音效那段的 8 秒上限拿掉;12 秒保底改掛在音效之後 |
  | 飛行頁交棒的「開棺」 | `main.js` 的 `bootBattleGate` | **順序整個反過來**(原本是立繪→音效→音樂,ver -387 的特例) |
  | 劇情場景的讀取頁 | `story.js` 的 `preloadStory` | BGM 從「與音效一起等」移到最後;25 秒總上限改成只罩圖與音樂 |
  | 飛行頁自己的讀取頁 | `flight/index.html` 的 `preloadSfx` | 原本音效**排在讀取頁收掉之後 1.2 秒**才抓,改到最前面 |

  ⚠ 飛行頁的音效走 **HTMLAudio** 不是 WebAudio,所以「載完」＝`canplaythrough`;
    逐支給 8 秒保底(一支抓不到不能把整頁擋死),但**沒有總時限**。
- ⚠ 這條**覆蓋掉個別畫面的舊指定**。`bootBattleGate` 原本照 ver -387 的
  「彈出瞬間優先加載敵立繪、其次音效、最後音樂」—— 那個特例已作廢。
- ⚠⚠⚠ **「排第一」沒用,要先確認它**在那一批裡面**(ver -433)。**
  Ray：「為什麼不能把 se 放在預載第一位?se 永遠都出不來,一開始的 step 跟 fall
  在手機從來沒播過。」—— 順序**早就**是第一位了(ver -431),但那一批是從 `ASSETS`
  掃出來的,而 `se_steps`／`se_Fall` 那 20 幾支**根本不在 `ASSETS` 裡**
  (它們只登記在 `modules/story.js` 的 `SE_FILES`)。**清單裡沒有的東西,排第幾都一樣。**
  以前它們只在 `story.preloadStory` 那一道門才抓,慢網下第一次演到就來不及
  (`LATE_PLAY_MS` 1.5 秒沒等到就乾脆不播)。
  作法:`story.seSources()` 匯出那份清單,`main.js` 的 `startBatch` 與 `_sfxPaths`
  **聯集**進去 —— **不要把檔名抄一份進 `ASSETS`**(鐵律 7:兩張表一定走鐘)。
  ⚠ 自檢:改音效相關的東西時,先確認「這支檔案在開機那一批裡嗎」,
    不要只看順序。兩張表的差集用 `comm` 對一次就知道。

**⚠⚠ 全站同時只能有一首 BGM(ver -391,Ray：「BGM 播下一首就停上一首,不要重疊」)**
- 飛行頁自 ver -388 起是**內嵌 iframe**,它**有自己的一套 BGM**(另一個 document、
  另一組 HTMLAudio)。跳頁的時代舊頁一走音樂自然沒了;內嵌之後主選單／城鎮的曲子會
  **繼續播** —— 兩首疊在一起。
- 作法:**進飛行畫面時收掉主遊戲的 BGM**(`openFlight` 裡 `SFX.stopBgm`,唯一的入口,
  試飛／出航／打完回來三條路都經過它),回來時再把底下那一頁的曲子接上
  (城鎮 → `town.resumeBgm()`;首頁 → `bgm_home`)。
- ⚠ 環境音(航行／停船那種循環床)與音樂是**堆疊**的,那不算重疊(見本節開頭)。

**⚠⚠ `playBgm` 的同曲判斷要看「真的掛上去的那一首」(ver -391)**
- `_bgmSrc`＝**要播哪一首**、`_bgmPlaying`＝**真的已經掛上元素的那一首**。換歌的那
  0.9~1.9 秒之間兩者不同:`_bgmSrc` 一進來就是新的,元素上還是舊的(而且 `el.paused`
  仍是 false)。
- 舊寫法用 `_bgmSrc` 判同曲,於是換歌途中收到**同一首**的重複請求(`ensureBgm`／
  `riseCue`／場景切換都會)就當成「已經在播」直接 return —— 還順手
  `clearTimeout(_bgmTimer)`,而那個 timer 正是「等一下把新的掛上去」那一步。
  結果:舊的淡到 0(聽起來像停了)、新的永遠沒播。
- ⚠ blob 抓不到要**退回直接串流**(`el.src = src`),不要 `return` —— 那會讓舊的停在
  音量 0、新的不播,整段變安靜。

**⚠ 耳機對了不代表手機對了(ver -250)** —— LUFS 是**全頻**的量測,但手機喇叭
600 Hz 以下幾乎不發聲。同一層在耳機上齊平,到手機上可能整個散開:

| | 耳機落差 | 手機落差 | 最慘的一支 |
|---|---|---|---|
| 語音層(修正前) | 0.0 dB | **9.7 dB** | `vo_luna_dualwield` −18.0 → −25.2(掉到音效層以下) |
| 語音層(修正後) | 3.3 dB | 3.3 dB | −18.1 → −20.1 |

- **量測要量兩次**:原始的、以及**過一次手機喇叭模型**(600 Hz 三階高通)的。
  工具是 `tools/audio_probe.html`(瀏覽器開,WebAudio 解碼所以 mp3/m4a/wav 通吃)。
- **語音要走語音鏈**(`SFX.playVoice` → `tuning.voiceChain`):切 130 Hz 以下、
  壓 500 Hz 以下的渾濁段、抬 3 kHz 的子音。手機放不出來的低頻只會吃掉 limiter 餘裕。
- **增益對的是「耳機與手機的平均響度」**,不是只對其中一邊 —— 只對手機的話,
  低頻重的那幾支在耳機上會突出 4~7 dB,反而把原本對的耳機平衡打壞。
- ⚠ **不要再串一顆「限幅器」**:Chrome 的 `DynamicsCompressor` 內建自動補償增益
  (綁 threshold 與 ratio),多一節的結果是**更大聲、峰值更高**。實測把門檻從
  −1 降到 −4,整層反而從 −17.9 變 −16.0 LUFS。峰值交給既有的 SFX 匯流 limiter。

## 6.7 逐格貼地面的通則（ver -312，「聖王廳地板破圖」的結案教訓）

> ⚠⚠ **ver -445 起，城的底圖不再走這一套** —— 改成逐像素取樣，**完整規格見 §6.7.5**。
> **凡是要貼在地表的圖，先考慮併進取樣，不要再開新的格網。**
> 以下規則仍適用於**量體層的屋頂細分**與其他仿射貼圖。

任何「把一張圖用一堆小仿射貼到地面上」的東西（城的平面圖、量體屋頂、日後的
任何貼地貼圖）都照這幾條，否則會重演那個花了兩天的 bug。

1. **格距要在螢幕上等大** —— 所以它必須**隨緩衝尺寸等比縮小**。
   ⚠ `flight/index.html` 的 `QUALITY.cell` 原本是「畫質越低格子越粗」
     (11/14/18/24)，與緩衝變小相乘之後，q3 整座城只切成 **5×5 格** ——
     用 25 個平行四邊形逼近透視變形，誤差大到看得見形狀，而且**隨相機移動**。
     改成 11/10/9/7 之後解決，**每幀時間不變**（上限本來就由 `nmax` 擋著）。
2. **相鄰格的重疊量單位是「緩衝像素」，不是來源圖的像素。** 寫死圖像素的話，
   圖被縮小之後重疊會小於一個像素＝等於沒有重疊，接縫的抗鋸齒就連成一張網。
3. **縮小超過 2 倍要走逐次減半的預縮圖**（同 §6.5 的立繪規則）。canvas 的
   雙線性縮小時只讀 2×2 個 texel，每格各自抓到不同的取樣 → 每格亮度都不同。
4. **不要用螢幕空間的抹壓去假造厚度。** 抹的量隨距離變，近了糊成一片、
   遠了露縫，兩邊都不對。要厚度就走幾何（世界單位，投影自己會處理）。

### 火車／怪這類「畫在地形上的東西」要跟著像素化（ver -391）
- ⚠ 地形是**低解析緩衝放大**出來的像素塊，而 canvas 的路徑繪圖是全解析度、抗鋸齒的。
  兩者放在一起，一列邊緣乾乾淨淨的車貼在馬賽克山上會讀成**貼紙**。
- 作法（火車與怪同一套）：先畫進一張**與地形同解析度**的離屏緩衝
  （`pk = clamp(BW/W, 0.15, 1)`），再關掉平滑 1:1 放大貼回來。
- ⚠ 緩衝的外框要把**附屬物一起框進去**（火車的煙拖得比車身長很多），不然會被切掉。
- ⚠ 透明度留在離屏那一層處理，貼回來時 `globalAlpha` 是 1 —— 行為不變，只有解析度變。
- ⚠ 緩衝**只長不縮**（縮到剩一半以下才重配）：外框每幀都會變一點，每幀 new 一張 canvas
  是白花的配置成本。

### 回報畫面問題時的標準流程
- **先看 HUD**：`ver -312  60 fps 217×233 q3!  (942,642) 340° h700`
  —— 版本、緩衝尺寸、**畫質檔**、座標、航向、高度。
  ⚠ 開發者與使用者跑在不同畫質上是**真的發生過**的事（Ray q3／我 q2），
    低畫質下同一個 bug 嚴重得多。`?q=0..3` 可鎖畫質，比對前務必鎖住。
- **有規律圖樣就把那一層畫出來**：`?grid=1`（紅＝平面圖格、青＝量體屋頂子格）。
  不要請使用者形容或圈選 —— 那是我們的工作。
- **選指標前先問「這個症狀在指標上長什麼樣」**。用「破洞」指標去量「亮度型
  接縫」永遠是 0，會讓人得出「已經修好」的錯誤結論。

## 6.7.5 城市嵌入飛行地圖（ver -445 定案，Ray 拍板「A 案」，成功後指定寫入憲法）

> Ray：「或許不該在現有的方法上做改進，上網查一下最有效的做法」→ 討論後定案：
> **城的底圖在地形的射線取樣迴圈裡逐像素取樣**（Comanche/VoxelSpace 的正宗作法）。
> 城不是「貼在地上的一張盤子」，是**地面本身** —— 地形怎麼不會拉伸，城就怎麼不會。

### 一、為什麼是這一套（教訓，不要走回頭路）

- ⚠⚠ **Canvas 2D 的 `drawImage` 只有仿射，沒有透視。** 舊路「切幾百個小格逼近」
  修了六版（-226/-292/-296/-306/-311/-312）仍在 -444 破功：格數預算（q3 每軸 14 格）
  鋪滿整張圖含畫面外的大半座城，貼近時每格巨大 → 仿射吃不下 1/z → 拉伸。
  出航點一移近帝都（-432），這個潛伏的病就成了每一趟的必經畫面。
- **取樣路的紅利全是免費的**：透視逐像素精確、遮擋走同一條 ybuf、
  霧／雲海／日夜光照與地形同一套（Ray 的「遠距離用雲霧遮擋」因此不必另做）。
- **效能實測每幀 +0.33ms**（q3，12.63 vs 12.30）—— 內迴圈只多一次遮罩讀＋城區像素的取樣。

### 二、渲染端的三件套（都在 `flight/index.html`）

| 元件 | 做什麼 | 關鍵細節 |
|---|---|---|
| `buildCityPyramid` | 插畫 → 逐次減半的取樣金字塔（Uint32，ABGR 同 `COL`） | 開機先用 192px 縮圖建（**永遠有得取樣**，沒有「還沒載完」的空窗）；全解析飛近載到後換上（`full` 旗標，縮圖不准蓋回去） |
| `buildCityMask` | 地圖格 → 第幾座城（Uint8，0＝無） | 粗遮罩只管「這一格要不要試」，**精確邊緣交給插畫自己的羽化 alpha**（半透明處與地形色混合，城緣自然融進地貌）；要等 MAPW 與縮圖都齊（loadWorld 收尾） |
| 內迴圈取樣 | 射線打到城 → 反旋轉出 uv → 雙線性取樣 → 依 alpha 混合 | 插在**地貌細節之後、光照之前**（art 蓋掉林草，日夜與霧照地形走）；取樣層級**每個深度步進挑一次**（texel 世界尺寸 ≥ 2z/BW，§6.7 通則） |

- ⚠⚠ **反分級烘在金字塔裡**：插畫的色調是對著舊路校的（不吃 `GRADE_SAT`、
  只除 `CITY_BAKED_VAL` 乘 `CITY_TRIM`）。取樣路把城當地形畫會**再分級一次**，
  不反轉的話飽和度 0.70×0.68≈0.48，整座城洗成灰白（實測就是）。
  逆變換（飽和 ÷GRADE_SAT、亮度 ×TRIM÷BAKED_VAL）**只住在 `buildCityPyramid`**（鐵律 7）。
- ⚠ **舊格網還在但只當退路**（取樣器沒就緒的極早期）；它算的 `CZMIN` 照跑 ——
  量體層貼回時的遮擋裁切還靠它。
- ⚠ **「這座城有手繪圖」的判定要把金字塔算進去**（`planCity`）：只認全解析的話，
  載到之前程序生成的白色小房子會疊在插畫上（實測北方泊地遠看一片白點）。
  取樣器活著時全解析未到就**什麼都不補**：「只有地面的城」遠距離看不出差別。
- ⚠ **量體層（牆＋屋頂）照舊**是疊在上面的第二層，不歸取樣管；
  太近就淡出（`MASS_TOO_PX`，ver -444：z 一小牆會長成蓋滿畫面的暗板）。

### 三、加一座新城的流程（北方泊地＝範例，ver -444/-445）

1. **Ray 交正俯視插畫**（檔名可自帶座標，如 `Northport1516,150.png`）。
   規格照 §6.8：正俯視（不必反投影就 `unsquash:1.0`）、輪廓外才透明。
2. **`flight/build_city.py` 補一個 JOB**（src/dst/mx/my/planW/planRot），
   跑 `py flight/build_city.py <名字>` 出四件：`_plan` / `_plan_lo` / `_h` / `_mass`。
   ⚠ mx/my/planW/planRot **必須與 index.html 的 SETTLEMENTS 一致**（兩邊註解互指）。
3. **SETTLEMENTS 補一列**。⚠⚠ **整地一律照聖王廳**（Ray 指定「所有地圖的嵌入方式
   比照聖王廳」）：`podium:{ r:1.05, skirt:8, lift:0 }` —— 不整平的話量體與遮擋的
   深度判定會被起伏地形攪亂（薇拉馮德 -443 的教訓：落差 145 → 整片破）。
   港城自動吃「海不整平」的守門（雲海之下的格子不動，港灣不會被墊成斜坡）。
4. **PLACES 的名牌跟著城走**（同座標）；有城鎮內容才補 `town:` 鑰匙（降落鈕）。
5. **驗收**：遠中近三距離＋側面經過各拍一張，對照聖王廳（基準城）確認無拉伸；
   `?q=3` 鎖低畫質再看一次（低畫質下同一個 bug 嚴重得多）。

- ⚠ **碼頭／突出海面的結構**：插畫跟著地形表面走，所以「插畫說是陸」的海照常
  **填到地基高**，平台邊緣由 voxel 的立面渲染畫成岸壁（顏色自動取自表面色
  ＝碼頭邊緣色）。-444 的 `plateThick`／`noLandFill`（貼盤子時代的厚度與懸空）已退役。

## 6.8 立體模型製作原則(ver -312,「聖王廳地板破圖」的結案教訓)

做給飛行頁用的城/建築模型時照這幾條。**這些是製作端的限制,不是渲染端的**
—— 模型做壞了,渲染端再怎麼修都補不回來。渲染端的對應規則見 §6.7。

### 一、貼地的平面圖(plan)
- **輪廓之內必須完全不透明。** 內部只要有透明像素,地形就會從那裡透出來,
  而且在畫面上**與渲染的破洞長得一模一樣**,會害人查錯方向。
  ⚠ 驗收方法:把圖載進 canvas,數「四面都被不透明像素包圍、自己卻透明」的
    像素。現行 `holysee_plan.webp` 實測只有 **4 個**(等於全不透明),以此為準。
- 透明只能出現在**輪廓之外**。海岸線那類挖空由 `build_city.py` 逐像素烘進
  alpha,不要在渲染端逐格判(格子大小隨距離變,海岸線會抖)。
- **尺寸不必做大。** 渲染端會走預縮圖,但來源越大、每一格的縮小倍率越極端。
  現行 439×440 就夠(聖王廳 `planW` 550 世界單位)。

### 二、街廓量體(mass)
- ⚠⚠ **不要做「一整片跨越整座城」的大塊。** 渲染端有保險絲:一塊之內近端比
  遠端近 **3 倍**以上(`MASS_ZRATIO`)就**整塊不畫** —— 貼近時那一塊會整個消失。
  塊要切得夠碎,讓每一塊的深度跨度都小。現行聖王廳是 **24 塊**。
- 頂點上限 `POLY_MAX = 12`、簡化容差 `POLY_EPS = 1.5`(插畫像素)。街廓輪廓
  畫得再細也會被簡化掉,不必花力氣。
- `planTall` 與 `planW` **必須等比**。ver -213 把聖王廳整體縮到 50% 時
  `planTall` 也減半,否則高度/半徑比由 0.18 變 0.36,塔會變成一叢細竹竿。

### 三、atlas
- ⚠ `ATLAS_PAD` 目前只有 **1 個像素**。渲染端要做子格重疊時**擴不出去** ——
  實測往外擴一點就取到隔壁塊的圖(破洞 198→263,反而更糟)。
  **要讓渲染端能安全重疊,padding 至少要 2~4。** 這是製作端該給的餘裕。

### 四、地形與底圖的關係
- 地形頂穿底圖的正解是**整地**(`podium` 的 `r`/`skirt`,把 footprint 削平),
  **不是給底圖加厚度**。
  ⚠ 加厚度試過兩種都不對:螢幕空間的抹壓會隨距離爆掉(近了糊成一片、遠了露縫);
    幾何版填的是格網四邊形、不吃 alpha,而格網是正方形 ——
    結果是一塊圍著城的**方形台**(ver -311 做出來、-312 拿掉)。
- ⚠ 底圖的高度與整地的高度**必須同一個來源**。ver -298 之前兩邊各寫一個預設
  (44 vs 124),城的地板被埋在自己台座下 **80 個世界單位**,逐欄的地形深度裁切
  一路把它啃成斑駁。現在統一走 `settleBaseH/settleTerrainH/settleGroundH`。

## 6.9 「一輪遊戲」的邊界(ver -381,Ray 定案)

> 「所謂劇情只跑一次是指**一輪遊戲內**只跑一次,從頭開始、或者從之前的存檔開始的話就要跑劇情。」
> 「**story 才是正式版,出陣是試玩版**。」

- **邊界只定義在一支函式**：`progress.newRun()`（鐵律 8）。首頁的 `story` 鈕＝從頭開始,
  進去之前呼叫它。它清掉：
  `旗標 / 階段 / 好感 / 好感地板 / 玩家名 / 時鐘 / 道具 / 金錢 / 店鋪存貨 / 教學看過沒 / 飛行頁的交棒鑰匙`
- **不清**跨輪的東西：靜音、語言、管理人模式、最佳紀錄、武器與搭檔的選擇、飛行頁的免戰開關。
  那些是玩家的偏好、成績與工具開關,不是劇情進度。
- ⚠ **讀檔不走 newRun**：讀檔是 `progress.runRestore()`,把那個存檔的旗標放回去 ——
  該演的劇情自然會演,因為那個存檔本來就還沒演過。
- ⚠⚠ `newRun()` 與 `runSnapshot()/runRestore()` 是**同一張清單的兩面**：
  newRun 清掉的東西,存檔就要存得起來、讀得回去。
  **加了新的「一輪內」存檔，兩支都要加** —— 漏一支的下場是「讀了舊存檔卻還帶著新一輪的錢」。
- ⚠⚠ **玩家只有一份存檔**（ver -430，Ray：「只有單檔，睡覺就是建立唯一存檔」）：
  存檔庫的 `main` 格，由旅店「回房睡覺」寫（`save.storySave()`），由首頁的「繼續」讀
  （`save.loadLatest()`）。`quick`（F4）與 `slots`（F5/F8）是 `body.testmode` 限定的
  開發梯子 —— 所以 `main` 要**另開一格**，借用 `quick` 的話管理人按一下 F4 就蓋掉玩家的進度。
- ⚠ **「人在哪」不在那張清單上**（ver -430）：城鎮的所在節點存在**存檔紀錄自己**的
  `town:{town,node}` 欄位（`modules/save.js` 的 `capture`／`apply`，v3），不是 localStorage
  的一輪內鑰匙 —— 它是「這一筆存檔」的屬性,不是「這一輪」的狀態,所以 `newRun()`
  與 `runSnapshot()` 都不必動。
  ⚠ 它與 `story.getPosition()` 是**同一件事的兩面**：在城裡就有這個、在劇情裡就有那個。
    沒有它的話,旅店睡覺存的檔讀回來**沒有去處**（劇情播放器那時根本沒在跑）——
    首頁的「繼續」就等於什麼都不會發生。舊檔（v1/v2）沒這一欄,照舊只跳劇情位置。
- ⚠ 城鎮那些「只播一次」的段落,旗標是**演完才記**（`modules/town.js`）：
  中間可能插一場戰鬥,打輸了會被丟回首頁 —— 開演就記的話那一場永遠打不到。
- ⚠⚠ **首頁的可見範圍是「白名單」**（ver -623，Ray：「開始故事鈕只有管理人模式才開」
  「一般玩家只能挑戰」「還有教學原作跟 credit，除以上之外都只有管理人可視」）：
  一般玩家看得到的只有 **挑戰 / 教學 / 原作 / Credit**（＋語言鈕與版權那一行）。
  其餘一律 `body.testmode` 限定 —— **開始故事、繼續**、商店、後臺、試飛、story、章節。
  - ⚠⚠ 規則寫成**白名單**不是黑名單（鐵律 8）：
    `body:not(.testmode) #home button:not(#startBtn):not(#tutorialBtn):not(#creditBtn):not(#originalBtn):not(#langBtn){display:none}`
    —— 黑名單每加一顆新鈕就要記得補一條，漏掉就直接漏給玩家看；
    白名單是「沒被點名的一律不出」，**新鈕預設安全**。
  - ⚠ 管理人模式下這一條**整條不適用**，不是「再打開一次」——
    所以每顆鈕自己的顯示規則原封不動（例如「繼續」仍是「有存檔才出現」）。
  - ⚠ 「開始故事」與「繼續」是**同一條線**：沒有前者就存不出檔，後者也沒東西可進。
    只藏前者的話，管理人存過檔再關掉 testmode 會剩一顆孤兒鈕。
  - 「開始故事」本身的行為不變（ver -554）：`newRun()` → 主線第 0 句，
    不經章節工具；讀檔走「繼續」。

## 6.10 兩個頁面之間(主遊戲 ⇄ 飛行頁)

主遊戲(`index.html`)與飛行頁(`flight/index.html`)是**兩個 HTML**,飛行頁還是非 module 的
獨立頁面。跨頁的東西一律照這幾條：

- **狀態走 localStorage,鑰匙共用**（沿用好感／時鐘的既有慣例）。目前：
  `tivot_inventory_v1`／`tivot_money_v1`／`tivot_affection_v1`／`tivot_clock_v1`／`tivot_flags_v1`
- **交棒用「請求＋回程」兩把鑰匙,而且讀了就清**：
  `tivot_battle_req_v1`（怪撞上船 → 要打哪一場）、`tivot_flight_ret_v1`（打完回來站哪）。
  ⚠ 不清的下場：重整一次又打一場、或下次正常進飛行頁被拉回舊座標。
- **打輸也回得去(ver -430 改)**:船艦戰的戰敗頁只有一顆「繼續」,按下去走的是**同一條**
  回程(`flightBack` 那一段)—— iframe 從頭到尾沒卸載過,船就還在遭遇發生的座標上,
  什麼都不必還原。⚠ ver -382~-429 是「打贏才回得去,打輸 Game Over 回主選單」,
  Ray ver -430 改掉了(§6.5.2 那張表)。
  ⚠ 退出確認那顆鈕字面上是「回主選單」,所以按下去要先取消回程(`flightBack=false`)。
  ⚠ **劇本遭遇的 `done` 旗標是交棒那一刻就記的**,所以打輸回到天上不會再被同一隻攔一次。
- ⚠⚠ **共用樣式要抽成獨立檔,而且變數要列全**（ver -380）：
  `style.css` 是主遊戲專用的（它連 `html/body/#app` 的版面都管）,整份掛到飛行頁會把
  那一頁洗掉。所以 `#lootSheet` 那一套抽成 `css/lootsheet.css`,由 `modules/loot.js`
  **自己掛**（`import.meta.url` 推路徑,兩層都對;掛過就不再掛）。
  ⚠ 抽出去的那一份**用到的 CSS 變數要全部掛在自己身上** —— 宿主頁面不一定有那組 `:root`
  （飛行頁只有 `--gold`,而且色碼不同）。實測漏了 `--panel`,飛行頁的面板整個透明。
  自檢：`grep -o 'var(--[a-z-]*)' css/lootsheet.css | sort -u` 對照檔頭那一行。
- ⚠⚠ **飛行畫面開著時要把劇情層藏起來**（ver -428，Ray 回報「準備出航之後沒有成功
  進到飛行頁面，卡插圖」）。iframe 是 z-8200、劇情層是 8300 —— 從**城鎮／劇情**出航時
  劇情層是開著的，玩家看到的是那張插圖蓋在飛行畫面上。
  ⚠ **不要改 z-index 去解**：8200 < 8300 是刻意的（交棒那一刻槍棺要蓋在飛行畫面之上，
    收 iframe 才不會閃一格）。作法是 `body.flight-on #storyStage{visibility:hidden}`。
  ⚠⚠ 用 `visibility` **不可以用 `display:none`**：交棒時 `showKerbGate()` 是在收 iframe
    **之前**擺門的，那時這一層還藏著 —— `display:none` 量到 0×0，整組門的幾何就毀了。
  ⚠ **`goFlight` 那一拍要自己清插圖**（`stageCg=null` ＋ 撤 `.on`）：插圖是持續狀態，
    不清的話飛行頁一收（按返回／打完回來）畫面上就是那張插圖卡在城鎮前面。
    清場是**那一拍**的責任，不要求腳本記得寫 `cg:null`（鐵律 8）。
- ⚠⚠ **飛行頁是主遊戲的內嵌 iframe，不是跳過去的另一頁**（ver -388，`#flightFrame`）。
  ⚠⚠ **理由是音訊**：跳頁＝新的 document，音訊要**那一頁自己的**使用者手勢才解得開
    （iOS 一定要）—— ver -387 因此必須在進戰鬥前多一顆「開棺」，否則門的齒輪／開門音
    與戰鬥 BGM 整段是靜音的。內嵌之後戰鬥跑在**父頁**，開機那一點就一直有效，那顆鈕不見了。
    （同源 iframe 的手勢也會往上傳給父頁，所以怎麼玩都不會失效。）
  ⚠ 順帶解決：**打完回來不重載飛行頁**（它一直活著，船還在原處，沒有第二次讀取頁），
    而且回程鑰匙 `tivot_flight_ret_v1` 在這條路徑上用不到。
  ⚠ **不要再加第二條「跳過去」的路**。要新開一個全螢幕子畫面，照這個做法：滿版 iframe ＋
    `window.__tivotFlight` 那種掛鉤（子頁是非 module 的獨立文件，import 不到主遊戲的東西）。
  ⚠ 子頁自己有音樂與環境音，**進戰鬥要明確收掉**（`__flightPause`）：只把 iframe 藏起來
    不夠 —— rAF 停了它的 `updateBgm` 也停了，那幾個 `<audio>` 會維持當時的音量繼續播。
  ⚠ `src` **按下去才給**（lazy），不然每次開機都要多載一整個飛行頁。
- ⚠⚠ **在新的一頁真的蓋上去之前，不可以先把首頁收掉**（ver -576，Ray：「我按繼續
  總會跳挑戰的第一戰的圖出來」）。成因：`openFlightAt`（首頁「繼續」讀到飛行檢查點）
  自己先 `$('home').classList.remove('on')`，而 `openFlight()` 進來的第一件事是
  蓋 280ms 的黑幕再遞迴自己 —— 那片黑幕（`#storyVeil`）是 `#storyStage` 底下的
  `absolute`，首頁上那一層根本沒開，**黑幕是看不見的**。於是那 280ms 露出來的
  就是底下的 `#app` ＝ 開機時 `applyConfigToDOM` 擺好的**挑戰第一戰的敵人立繪**。
  ⚠ 作法：首頁自己是不透明的，讓它蓋著 —— 收首頁的動作留給
  `openFlight()`（它已經在 `flightFrame.classList.add('on')` 之後才收，
  而那個 iframe 有不透明底色 `#05060c`），只有那一處（鐵律 8）。
  ⚠ **自檢**：新增任何「從首頁進去某一頁」的路徑時，先問「我收首頁的那一刻，
  蓋在它上面的是什麼？」—— 答不出來就是會閃一格戰鬥盤面。
  （城鎮那一條沒事：`town.open()` 是同步的，`#storyStage` 不透明、同一幀就蓋上。）
- ⚠ **獨立開啟子頁仍然要能用**（開發時直接開 `flight/index.html`）：查不到父頁掛鉤就退回
  舊的 localStorage ＋ 跳頁那一套。`tivot_battle_req_v1` 是 `{battle, gate, geom}` ——
  `gate:'kerb'` ＝ 槍棺已經推到頂，主遊戲**由那個狀態接下去**且不走讀取遮罩
  （`bootBattleGate`，那條路徑上「開棺」那一顆還在，因為它真的需要）。
- ⚠⚠ **槍棺是「一扇門、兩個文件」，幾何只能有一處**（鐵律 7）：飛行頁那一半解出來的
  `fkerbGeom{Wd,Hd,top}` **交棒時要帶著**，主遊戲用 `showKerbGate(geom)` 覆寫自己的公式。
  兩邊各算一次的話（它的條件是「紋章圓心落在下半面板正中」，飛行頁是「推到頂蓋得滿畫面」）
  實測差 8.6%，交棒那一格紋章會忽然變大。
- ⚠ **推上去的是整扇門，不是一片鐵板**：紋章／箭／鉚釘／楣全部是門的子元素＝同一剛體
  （兩邊都是）。只推底圖的話上推的是一塊鐵板。
- ⚠⚠ **紋章、箭、鉚釘與左半扇是同一個剛體，開門與關門都是**（ver -389／-390）。
  ⚠ 關門那一拍會脫節是因為 **CSS 的過場吃的是「新狀態」的 transition**：
    `remove('open')` 之後 `#kerb.open #kerbPlate`（借了門的曲線）不再適用，紋章掉回
    自己那條帶回彈的曲線。所以關門第一拍要另外掛 `kerb-shut` 把門的曲線借過來，
    第二拍再與 `.lift` 一起拿掉。**凡是「兩個元素要一起動」，兩邊的 transition
    在每一個狀態下都要對得上，不是只在起點對上就好。**
  ⚠ 箭與鉚釘的基準過場**還帶著逐顆的 `--kerb-d` 延遲**（那是「解鎖時依次彈開」用的）——
    借曲線時**不要寫延遲**（＝延遲歸零）：同一剛體上的零件不能有時間差。
- ⚠ **進飛行畫面的「預載分流」**（ver -389，Ray 指定）：
    · **進入**（主選單試飛／城鎮出航）→ **跑**飛行頁的讀取頁（`contentWindow.location.reload()`）
    · **戰鬥結束回來**（`openFlight({resume:true})`）→ **不跑**，只把 iframe 顯示回來
  ⚠ 重載要走 `contentWindow.location.reload()` —— `src` 設成同一個字串**不會**重載。
- ⚠⚠ **「前往北方泊地」的地圖限制**（ver -429；-432 放行帝都；**-562 編號重排後改鎖
  `gameStage()===2`**）：擋在 `enterPlace()` 這**唯一的入口**（鐵律 8），日後多一條
  進地標的路也自動吃到 —— S2 正是「出航～北方泊地」這一段任務開著的時候。
  ⚠ 放行名單寫成**陣列**（`STAGE1_DEST`）不是單一字串：多開一個地方只加一項（鐵律 1）。
  ⚠ 帝都要放行的理由很實際：那是他剛出發的地方，擋掉等於回不了家。
  ⚠ **降落（`doLand`）不擋**：那是回帝都的路，擋掉會把玩家困在天上。
- ⚠ **從帝都出航的起點固定在 (1049, 622)**（ver -432，Ray 指定；`SAIL_FROM_CAPITAL`）——
  帝都在 (1005,600)，這是它東南邊的出港位。舊值 (1005,760) 在正南方一大段外海，
  出航第一眼看不到自己剛離開的城。⚠ 那是**地圖座標**，要乘 `MAP_SCALE`。
  ⚠ 打完仗回來會被 `restoreFlightPos` 覆寫成離開時的座標 —— 那是刻意的，
    這個常數只管「一趟新航行從哪裡開始」。
- ⚠⚠ **飛行頁的對白也吃表情差分了**（ver -432）：`who` 寫成 `renna/relief`。
  展開只有 `say()` 一個入口（`pkey`）：第一次用到就合成一筆 `PORTRAIT`
  （沿用本尊的 name/side/cm，換掉 src 與**那一張自己量出來的** fx/top/bot），
  於是 `castEnter`／`portraitArt`／`measureBounds`／`portraitLOD`／`drawTalk`
  一行都不必改（鐵律 8）。
  ⚠ 取景值與 `script/speakers.js` 的 `ART.renna.expr` 是**同一組**，改一邊要改兩邊（§5）。
  ⚠⚠ **`inParty()` 一定要先 `baseWho()` 切回本尊**：`PARTY['renna/relief']` 查不到，
    而查不到一律放行 ＝ 整組對話**悄悄繞過**「這個人在不在船上」那條硬條件。
  ⚠ 查不到差分就退回本尊（台詞照播，只是表情沒換），同 `story.js` 的 `missingExpr`。
- ⚠ **主角的空白對話框**在飛行頁寫成 `{who:'player', text:''}`：他沒有 `PORTRAIT` 條目，
  所以 `castEnter` 自然不動台上的人（§6.5：他不在台上，他說話不代表別人要換位）。
  ⚠ 名字欄要**退回 `playerName()`** —— 直接寫 `PORTRAIT[speaker].name` 會在這一拍爆掉。
- ⚠⚠ **加速（Sturm）與探索（感應）有解鎖章節**（`FEATURE_FROM`，ver -433，Ray：
  「現在出航是 stage 0，不應該有加速跟探索」）。兩者是**能力**不是操作 ——
  劇情還沒給，玩家就不該按得動。
  ⚠ 擋在**發動的那一點**（`pointerdown` 那兩個長按的起點，鐵律 8）：兩者的畫面回饋
    （充能條、冷卻完成的閃光）本來就是**用過才會出現**的邊緣觸發，所以擋住起點
    不會留下「亮著卻按不動」的死介面 —— 那是 §6.5.5 明令要避免的。
  ⚠⚠ **暫填 5（＝`STAGE_DEFAULT`，ver -562 由 3 推上來）**，理由同下面 `PARTY` 那一條：預設進度下行為不變。
- ⚠ **第 1 章船上只有蕾娜與諾薇兒**（`flight/talks.js` 的 `PARTY`，ver -432）：
  索拉娜與安雅暫填 `from:5`（ver -562 由 3 推上來），所以四人同台的那 23 組在第 1、2 章整批消失，
  第 1 章聽到的是新寫的 20 組蕾／諾對話。
  ⚠⚠ **`from:3` 是暫填的**（Ray 還沒定案她們哪一章入隊）—— 填 3 是因為 `STAGE_DEFAULT`
    就是 3，**預設進度下的行為完全不變**。定案了就改那兩個數字，其餘什麼都不必動。
  ⚠ 驗收用 `talkDebug()`：實測 stage1/2 各 20 組、stage3 共 43 組。
- ⚠ **`window.talkDebug=(m)=>talkDebug(m)` 這種「同名包一層」會無限遞迴**（ver -432 修）：
  `talkDebug` 是 `talks.js` 的頂層函式宣告，而那一頁是非 module 的一般 script
  —— 它本來就在 `window` 上，箭頭函式一覆寫，內文就解析回它自己。要包就換名字。
- ⚠⚠ **追擊的終端撲擊**（ver -502，Ray：「敵人靠近時會加速轉向玩家發起戰鬥」
  「不要讓敵錯身而過又在後面追半天」「一旦在正面碰上一定距離內就逃不了」）：
  `lungeDist`（1100）之內速度與角速度隨接近程度**連續**放大（門檻開關會在邊界抖），
  內緣 ×1.8／×2.8 —— 速度倍率要**壓過玩家極速**（最慢的蜈蚣 0.60×1.8＝1.08>1），
  「逃不了」才對每一種怪都成立。**錯身＝接戰**（`nearMiss` 600）：追擊中最近點
  落在 600 之內又開始拉開 → 當場交棒，不演背後看不見的長途追逐。
  ⚠ 劇本遭遇不走錯身那條（-455「看得到才進戰鬥」優先）。
- ⚠⚠ **隨機刷怪的抽選**（ver -500，Ray 的羽蛇卡）：`ENEMY_KINDS` 逐怪三格 ——
  `rarity`（稀有等級 → 權重 E30/D25/C20/B15/A10/S5，按合格者正規化）、
  `fromStage`（該 stage 的劇情之後才登場；羽蛇＝2）、`landOnly`（全陸域：
  生成點 HGT ≤ CLOUD_H ＝雲海＝海，不出）。實作只有 `pickEnemyKind` 一支；
  劇本遭遇（指定 kind）不經過抽選。蜈蚣／空賊的稀有度是**暫定 E**（卡還沒給）。
  戰鬥數值照舊在 config.enemies 的卡上（兩邊註解互指）。
  ⚠ 羽蛇卡的「戰鬥結束」段（Sturm／Deck_Chaos／著水）是 stage2 劇本的戲 ——
    觸發點與 Deck_Chaos 素材都還沒有，等 Ray 的 stage2 稿再接。
- ⚠ **遭遇不再有「進入戰鬥」那一格**（ver -389）：碰到遭遇距離就立刻彈棺，那一格只會被門
  壓在底下閃一眼。**免戰模式那一版留著** —— 那是開發用狀態（不會進戰鬥），沒有它就
  看不出遭遇有沒有觸發。
- ⚠⚠ **劇本遭遇**（ver -426）：主線指定「走到某處就一定被攔下」的那種遭遇，
  資料在 `flight/index.html` 的 `SCRIPTED_ENCOUNTERS`
  （`flag` 要有、`done` 還沒有、**離開** `outOf` 那個國界 → 在**正前方**生指定的怪，
  直接進 chase、`speedMul` 倍速、而且**不放棄追擊**）。
  ⚠ 「出某某國上空」用既有的**國界查表**（`regionAt`，與 `talkRegion` 同一份），
    不要另外拿座標畫一個圈（鐵律 7）。薩梅爾帝國＝region id 1。
  ⚠ **國界圖還沒載完不能判**：`regionAt` 那時一律回 0（無主之地），會被讀成
    「已經出去了」→ 一開場就在出發地上空刷怪。
  ⚠ **速度寫成「平時的幾倍」**（`speedMul`）不是絕對值：平時的速度是
    `ENEMY_KINDS[kind].speedK`，那才是「平時」的唯一定義。而且**只在 spawn 算一次**
    （存進 `enemy.spdK`），位移那一行只讀（鐵律 7）。
  ⚠ **不放棄追擊要明擋**：正面生成時 `dist` 一開始就等於 `d0`，一律大於
    `giveUpAt·d0` —— 不擋的話牠第一幀就「放棄」轉去橫向巡航。
  ⚠ **`done` 旗標在真的交棒那一刻才記**（`toBattle`）：免戰模式不記（開發時測得完），
    打輸走 Game Over → 讀檔，那個存檔本來就還沒有它（§6.9）。
    另外 in-memory 的 `scriptPending` 連免戰也清 —— 重整一次就能再測一遍。
  ⚠ 旗標的讀寫在這一頁**只有一份**（`flagOn`／`addFlag`），讀取頁的說明者也改讀它；
    不要各自 `JSON.parse` 一次（鐵律 7）。
- ⚠⚠ **槍棺的齒輪也是兩邊各一份**（ver -413）：`modules/story.js` 的 `KERB_GEAR` 與
  `flight/index.html` 的 `FKERB_GEAR`。主遊戲那顆大齒輪**就是「選單」鈕**，
  飛行頁那一份是純裝飾（那一頁沒有選單）。
  ⚠⚠ **ver -419：齒輪的大小與位置由「吊墜」推出來**（Ray：「齒輪位置大小依據吊墜調整，
    使兩邊看起來平衡」）。左邊掛吊墜、右邊嵌齒輪，那是**一對配件** —— 所以整組只留
    **一份設計數字**（`KERB_PEND.wantW` / `bandC`），另一邊鏡射過去：
      直徑 ＝ 吊墜寬 × `rel`；橫向 ＝ 鏡射吊墜的中心；縱向 ＝ 對齊吊墜的**身體中心**
      （圖的上面 12% 是鏈子，拿圖框中心會偏高）。
    小齒輪的直徑再由齒輪推（`smRel`），咬在團徽的 `smAngle`（60°＝右下）。
    ⚠ 小齒輪**不放正下方**：門的中縫在 0.506，正下方會跨在縫上、開門時被撕成兩半。
  ⚠⚠⚠ **ver -417 全部推翻重來：位置與大小是「設計決定」，不是解出來的。**
    Ray 直接在門的貼圖上標了齒輪該在哪、多大（`KERB_GEAR={cx:0.800, cy:0.040, d:0.121}`，
    **門圖上的比例**），並且把小齒輪整組拿掉（「兩個小齒輪太醜了」）。
    ⚠ 教訓：他要的是「**先以 UI 美觀做大小位置放置準則**」—— 我解了三版幾何最佳化
      （內接圓、避釘惰輪、一顆變兩顆），每一版都更「正確」也更醜，全部被退回。
      **UI 元件的位置與大小是設計問題，不是最佳化問題。**
    以下是被推翻的那一套，留著當紀錄：
  ⚠ ~~兩顆齒輪的位置與大小都是解出來的~~（ver -414，Ray：「以邊界為
    基準，應該讓大齒輪落在右上三角區的最中心，不要觸邊、不要觸釘」）：
      · 大齒輪 ＝ 右上那一塊（畫面右緣／楣的下緣／團徽的圓／鉚釘 圍成的三角區）的
        **內接圓**（Chebyshev 中心）——「最中心」與「不觸邊」是同一個解。
      · 小齒輪 ＝ 同時咬住團徽與大齒輪、**且避開鉚釘**的最小惰輪。
    只剩 `bite`（小齒輪咬進團徽幾 px，Ray：「邊要稍稍被團徽壓到」）是填的。
  ⚠⚠ **小齒輪不能擺在連線上**：2 點鐘那顆鉚釘（`r2`）是**貼在團徽環上**的
    （實測團徽圓心到它 145.9，而 er＋鉚釘半徑 = 147.7 —— 它與團徽本來就重疊），
    那條直線上根本沒有縫可以鑽。正解是**把惰輪撐大讓它滾到線的一側**：
    同時咬住兩者的圓心軌跡有兩支，半徑越大離連線越遠。由最小可行半徑往上加，
    第一個「離鉚釘與邊界都夠遠」的就是答案（實測單顆就夠，不必放兩顆）。
  ⚠ 轉速由**半徑比**決定（嚙合 ω ∝ 1/r，方向相反）—— 「越小轉越快」是式子的結果。
- ⚠⚠ **槍棺的吊墜也是兩邊各一份**（ver -412）：`modules/story.js` 的 `KERB_PEND` 與
  `flight/index.html` 的 `FKERB_PEND` 是同一組數字（含那條輪廓），改一邊要改另一邊。
  ⚠ 兩邊**都要掛**：遭遇時門是在飛行頁推上來的，交棒之後主遊戲那一半才接手 ——
    只有一邊掛的話，交棒那一格會憑空多出（或少掉）一個東西。
  ⚠ 「不碰到圓盤」要**對著圓、而且用吊墜的輪廓**去解最大寬度：這個吊墜是 X 形，
    最寬的地方在很上面，拿外框去閃外框會被壓成只剩一半大（實測 52px vs 89px）。
- ⚠ **槍棺的圖比例兩邊各一份**：`modules/story.js` 的 `KERB_META.top` 與
  `flight/index.html` 的 `FKERB.top`。飛行頁是非 module 的獨立頁面，import 不到那一份 ——
  **改圖重跑 `tools/kerberos_cut.py` 之後兩邊都要貼**（鐵律 7 的但書，兩邊註解已互指）。
- ⚠ 飛行頁有**自己的一套** stage／好感／玩家名讀取程式與**自己的時鐘**（`clock.minutes`,
  與 `script/clock.js` 是兩份）。鑰匙相同故不會走鐘,但那是既存的兩份真相,
  哪天要收斂就讓飛行頁改成 import 主遊戲那一份。

## 7. 未來功能(本次不實作,但 state 預留)

`state.js` 設計時預留、**本次不寫**:
- **Ghost 對戰**:只記每盤點擊頻率(5 數字),對戰生成槍聲+隨機彈孔,無聖徒化/無雙槍,純舒爾特五戰三勝取兩盤。不重播輸入軌跡。
- **排行榜**:各關卡完成秒數(有聖徒化有雙槍),秒數決勝。
- 共用零維運後端(Supabase 類),不做防作弊。
- 順序:前端假榜 → 自我 ghost → 接後端。每階段可獨立出貨。

## 8. 設計文件歸檔(docs/ — 規劃,尚未實裝)

Ray 提供的設計文件放 `docs/`,**內文一字不改**,歸檔時只在檔頭補「與現有程式的接點」。

- `docs/TIVOT_IMPL_SPEC.md` — 城鎮結構(節點式導航)、好感度 tier 1..5(棘輪不降)、
  結局判定(BE1~4 / HE1~5 / 離隊)、諾薇兒暴走(獨立風險系統)、蕾娜機制。
- `docs/TIVOT_AFFECTION_RULES.md` — 好感度**計算原則**：四人各自的來源(諾薇兒
  聖徒化 +1／安雅不用聖徒化 +1、<40 用了 −1／索拉娜評價越爛越加／蕾娜只吃 S
  且 +0.25)、時間即總資源、諾薇兒暴走螺旋、蕾娜道德鎖。與 IMPL_SPEC §2 配套。
- `docs/TIVOT_SCRIPT_ARCHITECTURE.md` — 對話/腳本系統的拆檔原則、資料結構、讀取邏輯。
  ver -326 歸檔（內文一字未改,檔頭補了「哪些已實裝、哪些還沒接」的對照）。
  已照它做出來的:`script/mainScript.js`(資料)＋`modules/story.js`(播放器)＋
  `script/speakers.js`(角色表)＋`script/progress.js`(stage/flags)。

⚠ **與 `GAMESPEC.md` 性質不同**:GAMESPEC 是現況實裝快照,docs/ 是還沒開工的規劃。
  判斷現行行為一律看 GAMESPEC 與 `config.js`,不要引 docs/ 當依據。

⚠ **養成層「一行都沒接」已經過時**(ver -298 更正)。`flight/index.html` 已有一套
  真的好感度儲存,要接的是它、不要另建:`AFFECTION{renna,nouvelle,sorana,anya}`、
  `AFFECTION_KEY='tivot_affection_v1'`(與主遊戲共用)、預設 10、
  `affection()`/`setAffection()`、`progTier()`、`talks.js` 的 `affBand()`(10~50 五段)。
  缺的是**棘輪(只升不降)**、以及蕾娜 +0.25 需要的內部 ×4 整數化
  (`setAffection` 現在有 `v|0`,小數會被截掉)。
  主遊戲的 `state.currentFavor`(固定 0)是更早的佔位,與上面那套**還沒接起來**;
  `config.js` 的 `dialogues[rank][好感門檻]` / `inspectors[].portraits[好感門檻]`
  查表(`pickByThreshold`)也還是只有 0 這一檔。

**監察官＝蕾娜(Renna Heisenberg)** —— Ray 定案。**芙蕾雅(Freya)是暫代版。**
  所以設計文件 §5 的蕾娜機制不是新增角色,是接在現行監察官那個位子上。
  正名要動:`config.js` 的 `inspectors` 鍵/`defaultInspector`/`ASSETS`/`castTable`、
  `i18n/` 三份的 `inspector.name`。`main.js` 已改走 `defaultInspector`(ver -256)。
  ⛔ **卡素材**:`resources/inspector/` 只有 `Freya_SI_01`,蕾娜立繪還沒有。

⚠⚠ **蕾娜(Renna)≠ 蕾妮(Renee)**。蕾妮是現行搭檔(即死防禦/生命歸還),是另一個人。
  ver -262 把監察官的西文名由 Regine 改為 **Renna** 之後,這兩個名字只差兩個字母、
  中文只差一個字 —— **全專案最容易寫錯的一組**。
  蕾妮已**實裝**(`config.js` 的 `partners.renee`、`Renee_CI_pas/act`、`vo_renee_*`);
  蕾娜還不存在。看到 `renee` 一律是搭檔,不要當成監察官順手改掉。
