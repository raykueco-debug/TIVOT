/* ══════════════════════════════════════════════════════════════════════
   evaluation.js — 蕾娜的結算評價（ver -432，Ray 交稿）
   ──────────────────────────────────────────────────────────────────────
   Ray：「第一次艦戰後開啟蕾娜評價，之後除了打靶之外蕾娜都會在結算畫面評價。
         評價獨立一個 script，分好感度跟 stage。」

   這個檔案**只放內容**（鐵律 1）：挑哪一句、怎麼演，在 `modules/inspector.js`。
   要加台詞、加章節、加好感段，動這裡就好。

   ── 一句評價長這樣 ────────────────────────────────────────────────
     LINES[stage門檻][好感門檻][rank] = { text:'…', expr:'差分鍵' }

   ⚠⚠ **兩層都是「門檻」不是「等於」**（沿用 `config.inspectors` 的 `dialogues`
     那一套查表法，`pickByThreshold`）：取「不超過現值的最高那一格」。
     所以現在只寫 `1`（第 1 章起）與 `0`（好感 0 起）兩把鑰匙 ＝ 全部適用；
     日後要讓第 3 章換一批說法，就加一把 `3`，第 1、2 章自動沿用舊的那一組。
   ⚠ `expr` 是 `script/speakers.js` 裡 `ART.renna.expr` 的鍵，**不是檔名** ——
     取景值（top/bot/fx）跟著那一份走，這裡不重複寫路徑（鐵律 7）。

   ── 為什麼評價的「誰」與「何時」也寫在這裡 ────────────────────────
   ⚠ `EVALUATOR` / `EVAL_FLAG` / `EVAL_SKIP` 是**這一套評價的設定**，不是程式常數。
     換人來評（日後別的監察官）、換旗標、多一場不評的，都只動這一檔。
   ⚠ 「什麼時候開啟」**不寫在這裡**：那是**某一場戰鬥**的性質，寫在戰鬥卡上
     （`config.battles[x].evalFrom:true`，第一場艦戰）—— 這裡只認旗標。
   ══════════════════════════════════════════════════════════════════════ */

/* 誰來評。⚠ 是 `script/speakers.js` 的 id：顯示名與立繪都查那一份（鐵律 7）。 */
export const EVALUATOR = 'RENNA';

/* 「評價已開啟」的旗標。⚠ 記在 `progress` 的 flags ＝ 進存檔、讀檔跟著回去（§6.9）。 */
export const EVAL_FLAG = 'eval_renna';

/* **不評的場次**（`config.battles` 的鍵）。Ray：「除了打靶之外」——
   打靶場是一直重打的計時挑戰，每打一次被評一次會很煩，而且那不是戰鬥。 */
export const EVAL_SKIP = ['range_trainee'];

/* ⚠ 第一批台詞用的是**試玩版的評分公式**（Ray 指定）＝ `config.rating` 那一套
   （`inspector.evaluate()`），S/A/B/C/D/E 六階。這裡不另訂分數，只認等第。 */
export const LINES = {
  /* ══ 第 1 章起 ══ */
  1: {
    /* ══ 好感 0 起（＝目前的全部）══
       ⚠ Ray 交的第一批稿，一字未改。台詞裡的 **HUND** 是她對主角的稱呼
         （`progress.NAME_BEFORE`，同結算失敗那一行「HUND 倒下了…」）——
         **不要自作主張換成 `{P}`**：那一層代換住在 `story.js` 的 `subst`，
         結算頁的打字機根本不經過它，換了只會在畫面上印出兩個大括號。 */
    0: {
      S: { text:'竟然……一個人就發揮了一艘船的火力……', expr:'surprised' },
      A: { text:'好驚人……這就是HUND的實力？',           expr:'surprised' },
      B: { text:'璐娜莉亞團長推薦你的理由，我總算明白了。', expr:'writing'   },
      C: { text:'嗯……看來還有不少地方需要觀察呢。',       expr:'writing'   },
      D: { text:'好危險……差一點就沒命了。',               expr:'relief'    },
      E: { text:'我還以為會死在這裡……',                   expr:'dying'     },
    },
  },
};
