/* ══════════════════════════════════════════════════════════════════════
   evaluation.js — 蕾娜的結算評價（ver -432，Ray 交稿）
   ──────────────────────────────────────────────────────────────────────
   Ray：「第一次艦戰後開啟蕾娜評價，之後除了打靶之外蕾娜都會在結算畫面評價。
         評價獨立一個 script，分好感度跟 stage。」

   這個檔案**只放內容**（鐵律 1）：挑哪一句、怎麼演，在 `modules/inspector.js`。
   要加台詞、加章節、加好感段，動這裡就好。

   ── 一句評價長這樣 ────────────────────────────────────────────────
     LINES[stage門檻][好感門檻][rank] = { text:'…', expr:'差分鍵' }

   ⚠⚠ **兩層都是「門檻」不是「等於」**（沿用 `config.inspectors` 的 `dialogues`
     那一套查表法，`pickByThreshold`）：取「不超過現值的最高那一格」。
     所以現在只寫 `1`（第 1 章起）與 `0`（好感 0 起）兩把鑰匙 ＝ 全部適用；
     日後要讓第 3 章換一批說法，就加一把 `3`，第 1、2 章自動沿用舊的那一組。
   ⚠ `expr` 是 `script/speakers.js` 裡 `ART.renna.expr` 的鍵，**不是檔名** ——
     取景值（top/bot/fx）跟著那一份走，這裡不重複寫路徑（鐵律 7）。

   ── 為什麼評價的「誰」與「何時」也寫在這裡 ────────────────────────
   ⚠ `EVALUATOR` 是**這一套評價的設定**，不是程式常數 —— 換人來評（日後別的
     監察官）只動這一檔。「哪一場不評」在戰鬥卡上（`noEval`）。
   ⚠ 「哪一場不評」**不寫在這裡**：那是**某一場戰鬥**的性質，寫在戰鬥卡上
     （`config.battles[x].noEval:true`，例如打靶）。
   ══════════════════════════════════════════════════════════════════════ */

/* 誰來評。⚠ 是 `script/speakers.js` 的 id：顯示名與立繪都查那一份（鐵律 7）。 */
export const EVALUATOR = 'RENNA';

/* ══⚠⚠ **預設就有評價，沒有的是特例**（ver -670，Ray 指定）══════════════════
   「之後的每一場戰鬥都要有蕾娜評價，沒有的是特例。」
   所以這裡**沒有**「什麼時候開啟」的旗標，也沒有「不評的場次」名單 ——
   要某一場不評，在**那一場的戰鬥卡**上寫 `noEval:true`（鐵律 1：那是那一場的性質）。
   ⚠ -432 的 `EVAL_FLAG`（`eval_renna`，第一次艦戰後開啟）與 `EVAL_SKIP`
     （打靶不評）都撤掉了：
       · 旗標 ——「之後的每一場」已經取代了「從某一場開始」，留著只會讓沒跑過
         艦戰的存檔整條看不到評價（Ray 就是這樣回報的）。
       · 名單 —— 與卡上的 `noEval` 是同一件事的兩個真相（鐵律 7）。
   ⚠ 這一頁只有**劇情／城鎮插入戰**會走到（`inspector.settle` 的分流：
     教學在它之前就被接走，出陣走監察官那一套）—— 所以「每一場」指的正是本篇。 */

/* ⚠ 第一批台詞用的是**試玩版的評分公式**（Ray 指定）＝ `config.rating` 那一套
   （`inspector.evaluate()`），S/A/B/C/D/E 六階。這裡不另訂分數，只認等第。 */
export const LINES = {
  /* ══ 第 1 章起 ══ */
  1: {
    /* ══ 好感 0 起（＝目前的全部）══
       ⚠ Ray 交的第一批稿，一字未改。台詞裡的 **HUND** 是她對主角的稱呼
         （`progress.NAME_BEFORE`，同結算失敗那一行「HUND 倒下了…」）——
         **不要自作主張換成 `{P}`**：那一層代換住在 `story.js` 的 `subst`，
         結算頁的打字機根本不經過它，換了只會在畫面上印出兩個大括號。 */
    0: {
      S: { text:'竟然……一個人就發揮了一艘船的火力……', expr:'surprised' },
      A: { text:'好驚人……這就是HUND的實力？',           expr:'surprised' },
      B: { text:'璐娜莉亞團長推薦你的理由，我總算明白了。', expr:'writing'   },
      C: { text:'嗯……看來還有不少地方需要觀察呢。',       expr:'writing'   },
      D: { text:'好危險……差一點就沒命了。',               expr:'relief'    },
      E: { text:'我還以為會死在這裡……',                   expr:'dying'     },
    },
  },
};

/* ══════════════════════════════════════════════════════════════════════
   **某一場專屬的評價台詞**（ver -597，Ray 交稿：北方泊地教堂的紫黑之爪）
   ──────────────────────────────────────────────────────────────────────
   鑰匙是**戰鬥卡的 id**（`config.battles` 的鍵）。查得到就用它，查不到才回去走
   上面那張依章節／好感的通用表。
   ⚠ 為什麼不塞進 `LINES`：那張表是**通用**的（第 1 章起、好感 0 起 ＝ 全部場次），
     把某一場的稿寫進去會把所有場次的評價一起換掉。
   ⚠ 一樣只認等第（S/A/B/C/D/E），公式仍是 `config.rating` 那一份（鐵律 7）。
   ⚠ `expr` 是 `ART.renna.expr` 的鍵，不是檔名。
   ⚠ Ray 的稿只給了 S~D 五檔；**E 沒給**就不寫 —— 查不到那一格就沒有評價那一段
     （`inspector` 回 null），不要自己補一句。
   ══════════════════════════════════════════════════════════════════════ */
export const BY_BATTLE = {
  /* 聖徒化教學戰（瓦礫中的紫黑之爪）。稿一字未改。 */
  np_claws: {
    S: { text:'難以置信……聖徒加護的你恐怕有接近團長級的實力。', expr:'shockedCalm' },
    A: { text:'聖徒化……那就是騎士團的王牌……？',               expr:'thinking'    },
    B: { text:'竟能將低階的聖徒之力運用到如此境界……',           expr:'thinking'    },
    C: { text:'低階的聖徒之力竟然也有這種威力……',               expr:'writting'    },
    D: { text:'你得好好感謝諾薇兒呢。',                          expr:'writting'    },
  },
  /* 禍魘娜塔莉戰（ver -671，Ray 交稿）。稿一字未改。
     ⚠⚠ S 那一句帶**好感 +5**（`aff`）：這是評價這一層第一次給好感 ——
       由 `inspector.pickEvaluator` 在挑到那一句時記帳（只有這一處，鐵律 8）。
     ⚠ 只加一次：`EVAL_AFF_FLAG` 記在 progress（進存檔，§6.9）—— 打贏之後
       重看結算頁、或這一場再打一次，都不會再加。 */
  np_nightmare: {
    S: { text:'……我決定這輩子都不會離開你身邊……', expr:'scarecute', aff:{ renna:5 } },
    A: { text:'說好了，一步都不許離開我！',         expr:'scarecute' },
    B: { text:'對不起，我最怕這種的了……',           expr:'awkward'   },
    C: { text:'對、對喔！神父本來就會驅魔嘛！',      expr:'meltdown'  },
    D: { text:'我要跟諾薇兒睡一間房……',             expr:'dying'     },
  },
};
