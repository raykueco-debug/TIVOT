# 給程式 session 的工單 — 索拉娜第二輪重製 28 張（ver -1650，美術 session）

> Ray：「把索拉娜圖跑完，重點是**面部畫風統一**，但**表情動作維持原圖**」
> 跑到 GPT 額度用完為止。

## 一、⚠⚠⚠ 唯一要做的：`speakers.js` 這 28 條路徑掛 `?v=2`

同名覆蓋，不掛就是玩家繼續吃舊的那一份，**而且畫面上不會有任何錯誤訊息**（§5 的 -650）。

```
angry armcross back blush confuse cringe die drink embarrass excite excite2 front
furiouscute furiousq guard guardtalk guardthink hug idea lauaghbig q read
readconfuse readhappy readsad readshock ready readysmile
```

⚠ 其中已經有 `?v=2` 的（`back` `cringe` `front` `readysmile` …）要改成 `?v=3`。
⚠ `q` 目前在 `speakers.js` 找不到對應的 expr 鍵 —— 圖交了，**要不要接是程式端／Ray 決定**。

## 二、✔ 取景值**不必動**（量過了，不是略過）

同一個姿勢的重製 ⇒ 依 §5 的 -649「同一個姿勢直接沿用，不要逐張量」。
實測 `top`/`bot` 的偏移 25/28 張在 ±13px 以內（佔畫布 <0.9%，屬雜訊）。

⚠ 三張偏移比較大，**要不要重量由程式端看畫面決定**（都是姿勢貼近下緣的那幾張）：

| 鍵 | bot 舊→新 | 位移 |
|---|---|---|
| `hug` | 1485 → 1514 | +29（1.9%） |
| `excite2` | 1487 → 1512 | +25（1.6%） |
| `readsad` | 1530 → 1506 | −24（1.6%） |

`fx`（臉的橫向中心）**一律不動** —— 構圖沒變，臉在圖上的位置就沒變。

## 三、驗收（四項，`resources/si/_sorana_check.py` 是唯一計算點）

| 指標 | 門檻 | 這 28 張 |
|---|---|---|
| 髮色色相 | < 220° | **186.7 ~ 217.4**（舊的是 216~270，多半偏紫） |
| 角落 alpha | ≤ 8 | 0 ~ 1 |
| 近白比例 | < 1.5% | **0.00 ~ 0.52%**（舊的最高 54.63%） |
| 與基底 A 的差異 | > 3 | 18.9 ~ 46.1 |

⚠ `furiousq` 的色相是 **90.2** —— 那是**指標讀到火焰不是頭髮**（那一張上緣整片是火焰特效）。
  並排看過：姿勢、表情、火焰全部保留，臉與髮色是對的。**指標誤報，不是不合格。**

## 四、還沒做完的 18 張

| 狀況 | 鍵 |
|---|---|
| **被內容判定擋**（連擋 2~3 次，照 Ray 的指示先跳過） | `amaze` `laugh` `remind` |
| **出圖了但沒有 alpha**（背景不透明） | `panic` |
| **完全還沒跑**（GPT 額度用完） | `battlecry` `shy` `side` `smile` `smirk` `sorry` `surprise` `talk` `tease` `think` `tire` `upset` `watch` `whisper`（14） |

⇒ 索拉娜 62 張的總進度：**-1556 那 16 張 ＋ 這一輪 28 張 ＝ 44 張已重製，還差 18 張。**

## 五、作法（下一輪照抄）

1. **一次餵兩張**：A＝`resources/_originals/SI_sorana_base/soranagpt_1.png`（先傳）、
   B＝線上那張同名舊圖。⚠ A 先傳是為了不被內容判定擋（§規約 §一）。
2. **提示詞就三行**（Ray 定的，一個字都不要加）：
   ```
   第一張是角色設定，第二張是構圖參考。
   臉、髮色、服裝、畫風 100% 照第一張；姿勢、表情、取景照第二張。
   100%保留原角色細節，alpha背景。
   ```
3. **被擋就照 Ray 的指示換下一張**（「連續被擋就換一張畫，先跑完不會被擋的，再來處理第二輪」）
   —— 原串重送一次、還擋就換串加一個短方向詞、再擋就跳過。
4. ⚠⚠ **收圖後一定要驗「與基底 A 的差 > 3」** —— 差 ≈ 0 代表抓到的是自己剛上傳的那張 A。
   本輪 `amaze` 第一次就是這樣（標題寫「拒絕」、抓到的是 A）。
