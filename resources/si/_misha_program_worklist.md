# 給程式 session 的工單 — 米夏差分十張（2026-09-23，美術 session）

> Ray：「讓 gpt 生米夏差分立繪先來個 10 張」→ 驗收「刀可以　轉 webp 交件」

## 一、`speakers.js` 的 `ART.misha.expr` 補十個鍵

現行 `ART.misha`（`script/speakers.js:1305`）是 `cm:176, eye:32, fx:0.393, top:0, bot:1535`，
`expr:{}` 是空的。十張都**逐張量過**（`top`/`bot` 取 alpha 上下緣，`fx` 走 -1578 的髮心量法）：

| 鍵 | top | bot | fx | 演什麼 |
|---|---|---|---|---|
| `stare` | 13 | 1526 | 0.449 | 直視前方，眼神銳利 |
| `frown` | 13 | 1523 | 0.469 | 雙臂抱胸、蹙眉 |
| `talk` | 8 | 1522 | 0.471 | 一手背後、一手攤開，說話 |
| `order` | 12 | 1525 | 0.432 | 右手伸直指向前方 |
| `guard` | 49 | 1501 | **0.344** | 壓低重心的戒備架式 |
| `draw` | 23 | 1516 | 0.460 | 拔刀，刀身出鞘一半 |
| `salute` | 9 | 1520 | 0.447 | 軍禮 |
| `side` | 16 | 1512 | **0.370** | 四分之三側身 |
| `wound` | 35 | 1499 | 0.403 | 負傷前傾、拄刀 |
| `close` | 14 | 1516 | 0.469 | 立正閉眼 |

⚠⚠ **`fx` 這一欄要用眼睛複核，不要照抄**（憲法 -1694 的教訓：工單給的 `fx` 只有一個是真的）。
· `guard` 0.344 與 `side` 0.370 是**兩個離群值** —— 那兩張本來就是壓低／側身，頭真的移了，
  但髮心量法會被**披風與手臂**拉偏。⇒ **看畫面決定**。
· 其餘八張落在 0.43~0.47，與底圖的 0.393 差 0.04~0.08 ——
  底圖那一張刀是**伸在身體外側**的，body 被推偏，兩者不該互抄。

⚠ `cm`／`standCm` 沿用角色層的 176，**不要逐張寫**（§6.5：那是角色的性質）。
⚠ `wound` 是前傾姿勢，若畫面上看起來偏大／偏低，調的是 `cm`／`standCm` **不是** `top`/`bot`（§6.5 的 -636）。

## 二、昨天那四張立繪 —— 已經 commit 了（.png）

`renna_si_hugangry` / `renna_si_hugshy` / `renna_si_holdnouvelle` / `torsten_si_back`
**`speakers.js` 早就指著它們、取景值也量好了，但檔案一直沒進版控** ——
別台機器 clone 下來整組不存在，而且畫面上沒有任何錯誤訊息。這一版補進去了。

⚠ **它們仍是 `.png`（每張約 2 MB）**。要轉 webp 的話是**程式端**四行路徑的事
（`speakers.js` 的 `.png` → `.webp`），美術這邊再轉檔 —— **兩件要一起做**，
只做一邊就是 404。⚠ 轉了可以省 8.4 MB → 約 1 MB（鐵律 12/13）。

## 三、⚠⚠⚠ 下一批米夏：刀要鎖得更死（Ray：「好幾張都跑掉」）

這一批的提示詞已經把比例量出來寫進去了（`chikushoudou` 實測 **柄 480 ／ 刀身 1119 ＝ 0.43**，
「長到雙手並排握得下」），**第一張 0.27 → 之後明顯拉長** —— 但 Ray 驗收時說
**「好幾張都跑掉」**，所以那個鎖**還不夠**。

下一批要逐件寫成「畫什麼」，不要只給比例（同 §5「不要顆粒感要寫成改畫什麼」）：

1. **柄**：長到**兩個拳頭並排握得下**；柄長 ＝ 刀身（連鞘）的 0.43 倍
2. **護手**：黃銅 **D 形弓**，從柄頭一路掃到鍔 —— ⛔ 不是圓盤鍔、不是十字護手
3. **鍔尖**：鍔的上方**一道 S 形向上捲的尖角**
4. **柄卷**：深色**菱形交叉**的卷繩（不是纏皮、不是光面）
5. **柄頭**：末端一枚黃銅圓帽
6. **鞘**：亮面黑漆、緩弧、長 —— ⛔ 不要畫直的

⚠ **每一張都要重貼這六條**（憲法 §5：逐次寫，中間隔幾張沒寫就會漂回預設）。
⚠ 建議下一批**把 `chikushoudou.webp` 每一則都重傳一次** —— 這一批只在第一則傳，
  後面九則靠「以這一串最上面那張為準」，而刀正是漂掉的那一樣。

---

## ⚠ 2026-09-23 晚：五張換成「刀鞘一致」的修正版（Ray 定：五張全用、talk 第一版）

刀的基準＝多數那五張（`stare`／`order`／`side`／`guard`／`salute`）：黑漆有太刀弧的鞘、
鞘口一圈黃銅、兩道黃銅箍（約 1/5 與 4/5）、黃銅鞘尾；柄照 `chikushoudou.webp`。
`draw` 另修了刀身弧度（原本直的、在鞘口折角）與握法（手指進 D 形護弓）。
GPT 修正、alpha 直出；原 PNG `_originals/si/misha_si_<鍵>_swordfix_src.png`；舊 webp 進 `_recycle/`。
（`close` 的 GPT 輸出是 1535 高，底部補一列透明回 1536。）

**`top`／`bot` 要用這一組**（角色本身幾乎沒動，差 1~7 px）；**`fx` 照上表不動**
—— 工具在 `draw` 量到 0.504 是被刀柄拉偏，不是頭移位。

| 鍵 | top | bot |
|---|---|---|
| `draw` | 22 | 1516 |
| `wound` | 34 | 1499 |
| `close` | 14 | 1509 |
| `frown` | 11 | 1526 |
| `talk` | 6 | 1525 |

⚠ 米夏的差分**還沒接進 `speakers.js`**（`expr:{}`），所以不必跳版號；
但若已經在某台瀏覽器載過舊檔，接的時候直接給 `?v=2` 最保險。

---

## ⭐ 2026-09-23 深夜：再 12 張（10 個新鍵 ＋ `order` 重畫 ＋ 背面）

### A. 表情差分 10 鍵 —— **只換臉、alpha 與底圖逐位元相同** ⇒ 取景**照抄底圖那一列**

GPT 每次都會把整張重畫（頭髮、衣褶、alpha 全部微變），所以不採整張：
**只把 GPT 版的臉（嘴；咬牙／微驚那兩張含眼眉）貼回原圖**，羽化 2.5 px，alpha 沿用原圖。
「微張嘴」那一族**刻意只取下半臉** —— `frownopen` 的 GPT 版把眼睛畫成金色，眼睛一律留原圖。
第二代編碼 q92。GPT 原檔 `_originals/si/misha_si_<鍵>_gptface_src.png`。

| 新鍵 | 底圖 | 演什麼 | 取景 |
|---|---|---|---|
| `frontopen` | 本尊 | 嘴微張 | 照本尊（角色層）|
| `frontgrit` | 本尊 | **憤怒咬牙、仍冷靜**（眉壓低、牙咬合） | 照本尊 |
| `frontshock` | 本尊 | **嘴閉、驚愕戒備**（瞳孔縮點、眉抽緊、眼上陰影；無冷汗）—— 「！！」那一拍用，Ray：初版「太清澈」已換 | 照本尊 |
| `closeopen` | `close` | 閉眼、嘴微張 | 照 `close` |
| `drawopen` | `draw` | 嘴微張 | 照 `draw` |
| `frownopen` | `frown` | 嘴微張 | 照 `frown` |
| `guardopen` | `guard` | 嘴微張 | 照 `guard` |
| `saluteopen` | `salute` | 嘴微張 | 照 `salute` |
| `stareopen` | `stare` | 嘴微張 | 照 `stare` |
| `sideopen` | `side` | 嘴微張 | 照 `side` |

（`order`／`talk`／`wound` 本來就開著嘴，不做。）

### B. `order` 重畫 —— 強透視（Ray：「order 的透視不自然」）
指人的那隻手朝鏡頭伸來、手放大、前臂透視縮短；臉與刀（一致性基準那一套）照原圖。
**整張新圖**，舊檔進 `_recycle/`。`top:8, bot:1527`；`fx` 照上表 0.432 不動（頭沒移）。

### C. 新鍵 `back` —— 背影回頭、嘴微張（Ray 交辦）
背部正對、越肩回頭；刀在左腰（從背後看在畫面左側，方向是對的）。
`top:12, bot:1496, fx:0.458`（工具量的；⚠ 回頭的臉偏左，**看畫面複核 fx**）。

---

# 附、米夏的**隨從**（`RETAINER`）—— 立繪交件（2026-09-24）

> Ray：「那是米夏的**隨從**　另外命名」

`speakers.js:59` 的 `RETAINER: { name:'隨從', art:null }`（ver -1707，古墓墓門那一段）
**現在有圖了**：

    resources/si/retainer_si_front.webp     1024×1536  q85 114 KB
    原圖 → resources/_originals/si/retainer_si_front_src.png

## 程式端要接的（一筆）

`script/speakers.js` 的 `ART` 加一筆，然後把 `RETAINER.art` 由 `null` 改成 `'retainer'`：

    retainer: { cm:???, eye:32, fx:0.451, top:4, bot:1534,
            side:'?', alt:null, base:'resources/si/retainer_si_front.webp', expr:{} },

- `top`/`bot`/`fx` 是 `tools/measure_si.py` 量的，**是那張圖的客觀事實，照抄**。
- ⚠⚠ **`cm`（身高）要 Ray 給** —— 同米夏 `cm:176` 那一條（那個也是估的）。
  不要憑畫面估：`CAST_TALL` ＝全體 `cm` 的最大值，填超過現行最高的 178
  會把**整個劇組一起縮小**（§6.5 實測填 180 全體縮 1.1%）。
  ⚠ 他看起來比米夏壯、年紀大，但「壯」不等於「高」—— 這是資料不是畫面。
- ⚠⚠ **`side`（本位站哪一邊）也要 Ray 決定**：墓門那一段台上同時有誰？
  他若與米夏（本位 `'L'`）同時在台上就必須**分兩邊**（§6.5：兩個人同台一定分站兩邊，
  同邊換人＝每句話抽牌輪轉＝閃爍）⇒ 那他要 `'R'`。
- ⚠ `mirror` **不要寫**（＝不翻）：他雙手位置不對稱（右手扶腰帶、左手垂下），翻了會換手。

## alpha ✔ 不欠（量過）

角落 α=0／透明 72.1%／**真正的邊緣斜坡近白 0.00%**（門檻 ≤1%）／斜坡中位數 2.8 px。
⚠ 直接量「半透明像素的近白」會得到 **14.4%，那是假警報** ——
這張圖的主體 α 是 **253 不是 255**（整個人都算「半透明」），而他穿**白軍服**。
**要量的是真正的邊緣斜坡（α<240）**，那一段是 0.00%。
⇒ 通則：**近白這個指標在「主體 α 不足 255」的圖上會整個失真，先看主體 α 再看數字。**

⚠ 外圍還有一圈**極淡的暖色光暈**（原圖暗角的殘留）：離主體 >10 px 的只有 **841 px、
平均 α 4.3**（≒1.6% 不透明度），>30 px 的只有 13 px。**看不見，不處理。**

## ⚠ 這不是米夏的差分，是另一個人

差太多，怕下一個人接錯：米夏是**銀白髮的青年、深藍金紋大披風、配畜生道**
（`speakers.js:1432`：安雅的**雙胞胎哥哥**）；隨從是**棕髮蓄鬚的中年、無披風、無刀**，
軍服是白底**金百合紋**的另一套剪裁。
⚠ 我一開始就是因為這個差異停手沒命名的 —— 接錯的話畫面上不會有任何錯誤訊息，
立繪照樣顯示，只是換了個人。

## 附二、2026-09-24 下午（Mac 那台）：米夏待修清單 —— **全部卡在「圖搬不回這台」**

Ray 這一輪追加的一件 ＋ 早上留下的三件：

| 件 | Ray 原話 | 規格 |
|---|---|---|
| ✔ **`draw` 護指沒接到護手**（**09-24 晚 Windows 已修**，見附三） | 「draw那一張的軍刀護指不對，應該要連到護手」 | 畜生道的**黃銅 D 形弓**要**從柄頭一路掃到鍔**（§三第 2 條）。現行 `misha_si_draw.webp` 的弓在柄頭處捲了一圈就停在半空，**下端沒有接回護手**。重畫時只改刀柄，其餘 100% 保留；`drawopen` 是換臉差分，底圖改了要一起重做 |
| `close` 自然站姿 | 「close 跟 closeopen 不要立正站好」 | 重心單腳、手搭刀柄；上一台選好的 `_originals/si/misha_si_close_natural_src.png` **這台沒有**（`_originals` 不入版控）—— 要重新向 GPT 要 |
| `closeopen` 重做 | 同上 | 以新的 `close` 為底、只換嘴 |
| 刀長不合格 7 張 | 「好幾張都跑掉」 | `_misha_sword_lock.md` §四那張表（`side`／`stare` 太長、`close`／`talk`／`frown`／`guard`／`back` 太短）；等 Ray 說要不要重畫 |

⚠⚠ **為什麼這一輪做不了**：Claude in Chrome 連的是 Windows 那台的 Chrome，GPT 產出的 PNG
下載會落在那台；這一台唯一的回程是「頁面文字」（≤5 萬字），只搬得動二值化的墨線圖
（小地圖圖示表就是這樣搬的），**搬不動 1024×1536 的 RGBA 立繪**。
試過用帶 `assets` 能力的 Artifact 當中繼（ChatGPT 分頁 `postMessage` → Artifact 存檔 → Mac 讀）
—— 發佈被自動模式擋下（判成可遠端寫入的介面），沒有繞過。
⇒ 三條路由 Ray 決定：① 放行那個中繼 Artifact ② 米夏那批在 **Windows 那台的 session** 做
③ 我在這台先把提示詞送出、圖下載到 Windows 的 `~/Downloads`，Ray 手動搬進 repo 再由這邊量刀長／交件。


## 附三、2026-09-24 晚（Windows 那台）：`draw` 護弓接到護手 —— ✔ 已交件（第三版）

Ray：「先做 draw 那張，護指接到護手」→ 退兩次：「仔細看圖 問題根本沒解決」「護弓要連到護手 手不可能握在護弓之上」。

⚠⚠⚠ **病不在弓的末端，在護手的位置**。原圖與前兩版的錯法：
· v0（原圖）：鍔（帶 S 形鍔尖的黃銅片）畫在拳頭上方，弓的下端在拳頭下方另捲一圈 —— 護手被拳頭切成兩截。
· v1：只把弓末端黏上去，結構照舊 ⇒ 退。
· v2：拳頭改握在柄上了，但 **S 形鍔尖還在拳頭右上方、鍔穿過拳頭** ⇒ 拳頭等於握在護手上 ⇒ 退。
· **v3（交件）**：沿刀的斜線由左上到右下 ＝ 柄頭 → 纏繩柄（拳頭握這裡，在弓裡面）→ **整片鍔含 S 形鍔尖都在拳頭右下方、刀身根部** → 刀身；弓從柄頭繞過指節外側接進鍔。

**有效的提示詞寫法（第三則才過）**：
1. 先逐條寫「原圖／上一張錯在哪」（S 形鍔尖在拳頭上方、鍔穿過拳頭、弓下端另捲一圈）。
2. **用畫面方位講順序**：「這把刀是柄頭左上、刀尖右下的斜線，沿線由左上到右下必須是 ①柄頭 ②纏繩柄 ③拳頭 ④鍔（S 形鍔尖從鍔背側伸出，**所以鍔尖也在拳頭右下方**）⑤刀身」。
3. ⛔ 清單：「拳頭上方除了纏繩柄與柄頭不可以出現任何黃銅護手零件」「鍔尖不可以出現在拳頭上方或旁邊」。
只寫「弓要接到鍔」或「鍔在柄與刀身之間」都不夠 —— 模型會把 S 形鍔尖留在拳頭上方，然後把弓接到拳頭下面。**要把鍔尖的位置獨立點名。**
同一串不附圖連送三則，一次都沒被擋。

**拼接**：GPT 每一版都整張微重畫（臉區約 10%）⇒ 只取刀柄＋拳頭框（x 0~360、y 110~400）內的改動像素，
膨脹 13 px、羽化 2.5 px，RGB 與 alpha 都在遮罩內取 GPT 版。臉區 0 px；邊界帶平均色差 5.2；框外不透明區改動 218 px（刀身沒位移）。

| 檔 | top | bot | fx（量測） | 交件 |
|---|---|---|---|---|
| `misha_si_draw.webp` | 22 | 1516 | 0.504（被刀柄拉偏，**沿用表上的 0.46**） | q92 第二代，alpha 零誤差、近白 0% |
| `misha_si_drawopen.webp` | 22 | 1516 | 同上 | 同一張遮罩（兩張的刀柄區原本只差 5 px） |

原檔：`_originals/si/misha_si_draw_guardfix_gpt.png`（GPT v3 整張）、`misha_si_draw_guardfix_src.png`／
`misha_si_drawopen_guardfix_src.png`（拼接後母版）。舊 webp、v1、v2 都在 `_recycle/`。

⚠⚠ **程式端要接（鐵律 11）**：同名覆蓋 ⇒ `speakers.js:1486` `draw` 的 `?v=2`→`?v=3`；
`speakers.js:1498` `drawopen` 現在**沒有版號**，要加 `?v=2`。`top/bot/fx` 都不用動。

⚠ **驗收教訓（兩次退件換來的）**：看刀柄要對著設定圖核**零件的順序與相對位置**（柄頭→柄→拳頭→鍔→刀身），
特別是 **S 形鍔尖在拳頭的哪一側** —— 它在拳頭上方就是錯，不管弓有沒有連上。3 倍裁切我兩次都「看過」，
兩次都只看了線有沒有接，沒看順序。
