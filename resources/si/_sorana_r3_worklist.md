# 給程式 session 的工單 — 索拉娜第三輪重製 **17 張**（2026-09-22，美術 session）

> Ray：「推上去，然後跑索」。作法照 `_sorana_r2_worklist.md` §五（A 先傳、三行提示詞、
> 被擋就重送一次、再擋就跳過）。

## 一、⚠⚠⚠ 唯一要做的：`speakers.js` 這 **15** 條路徑跳 `?v=`

同名覆蓋，不跳就是玩家繼續吃舊的那一份，**而且畫面上不會有任何錯誤訊息**（§5 的 -650）。

| 要加 `?v=2`（現在沒有版號，**13 張**） | `battlecry` `shy` `sorry` `surprise` `talk` `think` `tire` `upset` `watch` `whisper` `remind` `smirk` **`laugh`** |

⚠⚠⚠ **`tease` 已從這張表上拿掉**（ver -1670 後）：它的重製版被 Ray 退了
（「tease 修壞了 改回原本的」—— 臉被手擋掉、表情整個沒了），**檔案已還原回覆蓋前那一份**
（sha1 對過，與 `82919b0f^` 逐位元相同）。
⇒ 線上那一份與玩家快取裡的**本來就是同一個**，**加 `?v=` 等於白掛**，
  而且會害下一個人以為它重製過了（同下面 `side`／`smile` 那個坑）。
|---|---|
| **要改成 `?v=3`（現在是 `?v=2`，2 張）** | `side` `smile` |

⚠⚠ **`side` 與 `smile` 那兩個 `?v=2` 是上一輪白掛的** —— `git log --diff-filter=M`
查過，這兩個檔**在那之前從來沒有被覆蓋過**。這一次是真的覆蓋了，所以要跳到 3。
（順帶：這也證明 `_sorana_r2_worklist.md` §四「還差 18」那張表是對的 ——
**不要因為看到 `?v=2` 就以為它重製過了。**）

## 二、✔ 取景值**不必動**（量過了，不是略過）

同一個姿勢的重製 ⇒ 依 §5 的 -649「同一個姿勢直接沿用」。alpha 上下緣的偏移：

    dTop  -5 ~ +6 px     dBot  -19 ~ +6 px      （畫布 1536，全部 <1.3%）

⚠ 兩張的 `bot` 偏得比較大，**要不要重量由程式端看畫面決定**：
`battlecry` **−19px（1.2%）**、`whisper` **−31px（2.0%）**
（同 -1650 那三張 `hug`／`excite2`／`readsad` 的處理）。`fx` 一律不動。

## 三、驗收（四項，`resources/si/_sorana_check.py` 是唯一計算點）—— **11 張全過**

| 指標 | 門檻 | 這 11 張 |
|---|---|---|
| 髮色色相 | < 220° | **194.4 ~ 215.1** |
| 角落 alpha | ≤ 8 | **0** |
| 近白比例 | < 1.5% | **0.00 ~ 0.34%** |
| 與基底 A 的差異 | > 3 | **10.9 ~ 36.4** |

⚠ 另外做了 §規約 §三 要求的**臉部 100% 裁切並排**（11 張一起看）：臉模、髮色、
眼睛形狀、金飾與鈴鐺全部對得上基底 A，沒有 -1556 那種吊眼／大圓眼的變形。
透明像素佔 53~66%，是真 alpha 不是不透明底。

## 四、⚠ 還剩 **1 張**：`panic`（`laugh` 已於 ver -1670 後 交件）

> Ray（2026-09-22）：「更不對了　算了　**要這樣走的話我直接 sd 臉給你更快**」
> → 他真的出了：`reference/` 丟進一張 1024×1536 白底 SD 稿，
>   原稿已取進 `resources/_originals/si/sorana_si_laugh_SD_raw.png`。

| 鍵 | 狀況 | 誰處理 |
|---|---|---|
| **`laugh`** | ✔ **已交件（ver -1670 後）** —— Ray 出 SD 頭，美術這邊做 alpha ＋ 合成 | 完成 |
| **`panic`** | ⚠ 沒有被擋，但角落 alpha 246~252 ＝ 它自己畫了背景 | **重跑一次拿圖 → 走同一條 matting**（不必 Ray 出臉） |

**⇒ 索拉娜 62 張：61 張已重製，只剩 `panic`。**

### `laugh` 是怎麼做的（下一次拿到 SD 稿照這條走）

⚠⚠⚠ **先逐像素比一次「他到底改了哪裡」，不要預設整張重畫。**
實測他**只重畫了頭**：`rows 0..247, cols 449..724`，**2.47% 的像素**，
其餘與現行 webp 完全相同。

⇒ 所以**不是整張重去背**：

1. `tools/si_matting.py --backend toonout` 解出整張的 alpha ＋ 前景色
2. **只取頭那一塊**，其餘沿用現行 webp 的像素與 alpha（那是已上線、已被接受的那一份）
3. 接縫：把改動框外擴 16px、高斯 σ=6 羽化 —— 帶內是**未改動的像素**，
   所以兩邊描述的是同一件事，混起來不會有階梯（實測帶內可見像素 RGB 平均差 1.53、alpha 平均差 0.91）

**風險只落在頭上，身體不可能退步** —— 這是這條路最大的好處。

### ⚠⚠ backend 要用 **`toonout`**，不是工具預設的 `birefnet-matting`

白髮白衣這種題材兩者差很多（頭部框內）：

| | 專案四項的近白 | 頭部框內近白 | 斜坡寬 | 色相 |
|---|---|---|---|---|
| `birefnet-matting`（工具預設） | 0.66% | 5.29%（272 px） | 2.93 | 190.7 |
| **`toonout`** | **0.06%** | **0.84%（20 px）** | **1.79** | 191.9 |
| 現行 GT（舊髮） | 0.01% | 3.14%（160 px） | 3.01 | **225.9 ⚠** |

⚠ 憲法 -1516 說 ToonOut 的弱項是**極細的飄髮**，所以特地去看了左髮緣那根細碎髮
（3× 深色棋盤）：**完整留著**，而且主髮團外緣比 birefnet 乾淨。

### ⚠⚠ 順手抓到的：**舊的那張 laugh 髮色本來就不及格**

`色相 225.9`，門檻是 < 220 —— 它是全庫最後一個髮色離群值。
新的是 **189.5**，落在 `smirk` 198.5／`tease` 192.1 那一帶。
⇒ 這張不只是「補上」，是把那個離群值一起修掉。

### 交件驗收（`_sorana_check.py`，**驗的是 webp 不是 PNG**）

    色相 189.5   角α 0   近白 0.03%   與A差 32.6   ✔ 四項全過

編碼 `q85 / alpha_q100 / method6`，196,480 bytes（舊的 208,318）。
⚠ 取景值**不必動**：新圖 bbox `top 0 / bot 1529`，`speakers.js` 寫的就是 `top:3, bot:1529`。

---

## 五、⚠⚠⚠ 合成備案：**跑得完，但產出不合格**（2026-09-22 實測，留著當紀錄）

`_ext_worklist.md` §六那條「只重生頭部」，這一天完整跑過一遍：

- ✔ **不踩內容判定** —— 送上去的只有頭，**第一次就出圖**（全身版連擋 12 次）
- ✔ **接縫與取景沒問題** —— `dTop/dBot = 0/0`，四項量化驗收全過
  （色相 200.8／角α 0／近白 0.01%／與 A 差 32.4）
- ✘ **臉不合格** —— 這才是重點。Ray 看了兩版：
  · 第一版（提示詞寫「重繪」）→「**臉部畫風沒鎖好**」「**變成 gpt 臉了**」
  · 第二版（改成「修改我上傳的這一張」＋否定句，量化全過）→「**更不對了**」

⚠⚠⚠ **教訓：四項量化指標量不到「臉像不像」。**
那四項（髮色色相／角落 alpha／近白／與 A 的差異）在第二版**全部通過**，
而 Ray 一眼就退。憲法 §規約 §三早就寫著「**驗收一定要看臉的 100% 裁切**，
那四個量化指標完全量不到臉」—— 這一天又應驗一次，而且是**指標全過、人眼退件**。

⇒ **合成備案的瓶頸不是技術，是「臉由誰畫」**：Ray 手上有 SD 的臉，
那條路比用提示詞去盧一個模型快得多，也準得多。
**日後遇到「內容判定擋死」的張數，先問 Ray 要不要自己出臉，不要自己硬跑。**

⚠ 留在庫裡的兩個中間產物（下次要用直接拿）：
`resources/si/soranagpt/_head_ref.png`（A 的頭部特寫，400×320）
`resources/si/soranagpt/_laugh_headcrop.png`（laugh 的頭＋手，BOX `(430,0,760,350)` → 990×1050）

### ⚠ 合成的兩個技術坑（真的要再走這條路時看這裡）

1. **配準**：模型不照給定尺寸輸出（要 990×1050，它給 1217×1292，取景還更近）。
   用**邊緣圖的正規化互相關**粗搜＋細搜解 `scale/dx/dy`（那一張是 1.010／0／+16，2× 空間）。
2. ⚠⚠⚠ **羽化遮罩一定要再乘上原圖的 alpha** —— 新頭是**白底**，直接貼進半透明的髮緣
   就是**整顆頭外圍一圈白霧**。作法：`mask = 羽化 × clip((alpha-200)/55, 0, 1)`。
   實測原圖半透明近白 9.02% → 合成後 9.27%（＋0.25pp，等於沒變）。

## 六、⚠ 這一輪踩到的坑（下一個人先看這裡）

1. **`_sorana_check.py` 的 `BASE` 指到 `_originals/`，而 `_originals` 沒進版控**
   ⇒ 換一台機器就跑不動。版控裡的那一份在 **`resources/si/soranagpt/`**（`soranagpt_1~4`）。
2. **`javascript_tool` 有 45 秒上限** —— `__send()` 自己就睡 20 秒，再串長等待就 timeout，
   **而且送出其實已經成功了**，只是回傳被砍掉。⇒ `__send` 與 `__grab` 分兩次呼叫。
3. **換新對話之後 `ref` 會失效，而且同一個編號會被重新指派給別的元素** ⇒ 每串重新 `find`。
4. ⚠⚠ **判「被擋了沒」看 `newImageTurns`（那一回合的讚鈕），不要看頁面文字** ——
   拒絕訊息會留在頁尾不消失。`_grab.js` 的 `__state()` 是唯一計算點。


---

# 七、⭐ battlecry 那一批 —— **六張表情差分**（ver -1670 後，Ray 丟 `resources/si/alpha/`）

Ray 用 SD 在 battlecry 那具身體上重畫了**六張臉**，一句「把這裡的圖都做同樣處理」。

## ⚠⚠⚠ 但「同樣處理」不等於同樣的作法 —— **先量他碰到輪廓沒有**

| | laugh | battlecry 這六張 |
|---|---|---|
| 他重畫的是 | **頭髮**（輪廓變了） | **臉**（純內部像素） |
| 改動落在半透明區的像素 | 有 | **0 個**（α<200 的 0、α<16 的 0，六張都是） |
| ⇒ alpha | **要重解**（ToonOut matting） | **一個位元都不要動** |

**實測輸出的 alpha 與現行 `sorana_si_battlecry.webp` 逐像素相同（max 差 0）。**
⇒ 輪廓、髮絲、近白全部是那份已上線、已被接受的 alpha，**不可能退步**。

⚠⚠ **所以這一批沒有跑 matting** —— 跑了反而更糟：我先照 laugh 那條做過一次，
ToonOut 解出來的髮緣 alpha 與現行那份在接縫帶差到 **平均 6.7／最大 228**，
等於為了一張沒動過的輪廓去冒險。**不要因為「上一次這樣做」就照抄。**

混合不靠框、靠 alpha 本身：`w = clip((α−200)/55)` —— 不透明處用 Ray 的 PNG、
半透明處用現行那份解好的前景色 F（白底 PNG 在那裡是白的，用了就是白邊）。
⇒ **沒有任何空間上的接縫。**

## ⚠ 這一批用 **q92** 不是慣例的 q85

因為它是**第二代編碼**（身體那一片是把現行 webp 解碼再壓一次）：

    q85  261 KB  未改動區 PSNR 35.0 dB      ← 看得出劣化
    q92  317 KB  未改動區 PSNR 38.6 dB  ✔   ← 與現行檔 313 KB 幾乎同大小
    q96  359 KB  未改動區 PSNR 40.8 dB      ← 不值得

⚠ 這是**這一批的特例**，不是改慣例：從 PNG 母版第一次壓的照舊 q85。

## ⚠⚠ 程式端要接：`speakers.js` 加 **5 個新鍵** ＋ `battlecry` 跳版號

取景值**直接沿用 `battlecry`**（六張的 alpha bbox 全部是 `(7,1503,0,1023)`，
與現行那張一模一樣 —— 身體根本沒動，憲法 -649「同一個姿勢直接沿用」）：

    battlecrylook:        { src:'resources/si/sorana_si_battlecrylook.webp',        top:6, bot:1523, fx:0.668 },
    battlecrylookaside:   { src:'resources/si/sorana_si_battlecrylookaside.webp',   top:6, bot:1523, fx:0.668 },
    battlecrylookserious: { src:'resources/si/sorana_si_battlecrylookserious.webp', top:6, bot:1523, fx:0.668 },
    battlecrylookup:      { src:'resources/si/sorana_si_battlecrylookup.webp',      top:6, bot:1523, fx:0.668 },
    battlecryserious:     { src:'resources/si/sorana_si_battlecryserious.webp',     top:6, bot:1523, fx:0.668 },

⚠⚠ **`battlecry` 本身又被覆蓋了一次**（這是第二次）——
它**本來就在 §一 那張「要加 `?v=2`」的表上**，而那一條**還沒被接**，
所以**仍然只要加 `?v=2` 就好**，不必跳到 3。
⚠ 但如果 §一 那一批**已經上線了**才讀到這一段，那就要跳 **`?v=3`**。
**判準：看線上那一份 `speakers.js` 現在寫的是什麼，不要照這份文件的字面抄。**

## 六張臉各是什麼（給接腳本的人）

| 鍵 | 表情 |
|---|---|
| `battlecry` | 張口大喊，視線朝前（**取代**現行那張） |
| `battlecrylook` | 開口笑，看向鏡頭 |
| `battlecrylookup` | 開口，往上看 |
| `battlecrylookaside` | 閉口，別開視線 |
| `battlecrylookserious` | 閉口，嚴肅，視線偏下 |
| `battlecryserious` | 閉口，嚴肅，視線朝前 |

驗收（`_sorana_check.py`，六張全過）：色相 191.7~194.0／角α 0／近白 0.34~0.35%／與A差 11.1。
⚠ 「與A差 11.1」比別張低是**正常的**：battlecry 是側身構圖，與正面基底 A 的差本來就小
（現行那張是 11.2）。門檻是 >3。

母版：`resources/_originals/si/sorana_si_<鍵>_src.png`；投放夾 `resources/si/alpha/` 已回收
（raw 不可留在會被載入的目錄，憲法 §5）。


---

# 八、⭐ 索拉娜背諾薇兒 **2 張**（ver -1670 後，Ray 丟 `resources/si/alpha/`）

Ray 走他自己那條路拿到的：**挑一張露肉最少的 SI 當參考 ＋ 最短提示詞（「a女背b女」）
＋ 回本機 SD 修臉**。服裝與肢體都鎖住了 —— 這是本機生成做不到的（他的原話：
「只有 gpt 能鎖服裝跟正常的肢體動作，本機辦不到」）。

| 鍵 | 內容 |
|---|---|
| `carrynouvelle` | 索拉娜背著諾薇兒，兩人都閉眼（諾薇兒睡著） |
| `carrynouvellescream` | 同一張，**諾薇兒睜眼**（差分只在她的臉，0.49%） |

## ⚠⚠ 程式端要接：`speakers.js` 加 2 個新鍵

    carrynouvelle:       { src:'resources/si/sorana_si_carrynouvelle.webp',       top:7, bot:1519, fx:0.648 },
    carrynouvellescream: { src:'resources/si/sorana_si_carrynouvellescream.webp', top:7, bot:1519, fx:0.648 },

**都是新檔，不必 `?v=`。**

- `top`／`bot` ＝ alpha 上下緣（客觀事實，兩張相同）
- `fx 0.648` ＝ **索拉娜**兩眼中點 663.5/1024（睫毛實測 625.2 與 701.7，鼻梁上，已目視確認）。
  ⚠ 不是諾薇兒的臉 —— 說話的是索拉娜。

## ⚠⚠⚠ `top` 是**諾薇兒的頭**，不是索拉娜的 —— 但**不要為此改 `cm`**

這正是 §6.5「法環污染 `top`」的同一類問題：`top:7` 是諾薇兒的髮頂，
**索拉娜自己的髮頂在 y=162**。照預設算，她會比別張小約 10%。

要她維持 176cm 的視覺大小，`cm` 得帶到 **196.1** ——
⚠⚠ **但那會超過 `CAST_TALL`（全體 `cm` 的最大值，現行 178）把全劇組一起縮小**。

⇒ **照既有前例走：`soranaanya_si_backcarry`（索拉娜背安雅）就是這樣處理的**
——`{ top:0, bot:1534, fx:0.820 }`，**沒有 `cm` 覆寫**，接受整組畫小一點。
`speakers.js` 第 631 行那段註解早就寫著這個結論：

> `cm` 帶到 200 以上，而那會超過 `CAST_TALL` 178 把全劇組一起縮小（§6.5）。
> 先照預設跑，要調就動 `cm`／`standCm` 這兩個旋鈕（不要動 top/bot）。

**所以先不要填 `cm`。** 真的覺得小了，動 `cm`／`standCm`，**不要動 `top`／`bot`**。

## 作法（雙人新圖 ＝ 第三種情況）

前兩種見 §四（頭髮／輪廓 → 跑 matting）與 §七（臉／內部 → alpha 不動）。
這一批是**全新的圖，沒有 GT** ⇒ **整張跑 ToonOut**。

⚠⚠ **兩張差分共用同一份 alpha**：差異只有諾薇兒的臉（改動像素落在 `α<200` 的 **0 個**），
所以 `scream` 用主圖的 alpha ＋ 只換 RGB（`w=clip((α−200)/55)`）。
**實測兩張 alpha max 差 0** —— 差分之間的剪影必須一模一樣，不然換表情那一拍輪廓會跳。
（各自跑 matting 的話兩張會差到 max 35。）

編碼 **q85**（第一代，從 PNG 母版壓）：167 KB／170 KB。

## 驗收

    角α 0    近白 0.47% / 0.44%    與A差 36.8 / 36.7    ✔

⚠⚠⚠ **`_sorana_check.py` 的「色相」那一欄對雙人圖無效** —— 它量的是
「頭頂往下 11% 那一帶」，而這張圖的頭頂是**諾薇兒的褐髮**（量出 120.1／138.0，毫無意義）。
改量**索拉娜自己的髮帶**（`y162..340, x560..830`，排除近白、要求有藍調）：

    新的這兩張   215.2 / 214.4
    校準 laugh    199.1      battlecry 203.4      smirk 206.3
    校準 backcarry（舊的雙人圖） 274.9

⇒ 比最近重製的那一批**偏藍一點點，但在門檻 220 之內**，而且遠好於舊的 `backcarry`。
要不要調由 Ray 看畫面決定。

母版：`resources/_originals/si/sorana_si_carrynouvelle{,scream}_src.png`。
投放夾內容已清、資料夾留著（README 還在）。


---

# 九、⚠⚠⚠ 背諾薇兒那一組：**有兩個世代，不要混用**（ver -1670 後，第二批）

Ray 丟了三張新差分（`shock`／`jealous`／`smirk`），並指定 `carrynouvelle`
**改用他自己去好背的那一版**。逐像素比對之後發現：**那不是同一次生成。**

| 世代 | 誰 | 索拉娜長什麼樣 | 成員 |
|---|---|---|---|
| **B** | Ray 自己出的 alpha（`sorana_si_carrynouvelle.png`） | 髮色偏銀白、髮束較碎 | `carrynouvelle`／`shock` |
| **A** | 我 ToonOut 去背的那一版 | 髮色偏藍、髮束較整 | `scream`／`jealous`／`smirk` |

兩個世代的身體差 **5.35%**（不是位移，最佳位移 0/0 之後還是 5.35%）——
差異集中在**索拉娜的頭髮與臉**，其餘是整圈的細邊。

⚠⚠⚠ **所以「同一份 alpha」要按世代分**，不能全部共用一份：
每一張差分只與**自己世代的底**共用 alpha（實測各自的改動像素落在 α<200 的：
`jealous` 0、`smirk` 0、`shock` 300）。

⚠⚠ **`shock` 那 300 px 是刻意吃下去的**：它在索拉娜**右側髮尖**
（rows 170..332, cols 729..778，α 中位數 130），代表那一版她的髮尖動了 1~2 px。
拿 B 的 alpha 去套會在那裡差一線 —— **但那遠比換成我的 alpha 好**（見下）。

## ⚠⚠⚠ ToonOut 在**諾薇兒的褐髮**上會留白邊，而近白指標抓不到

Ray：「注意諾的頭髮白邊」—— 查證屬實，而且**四項驗收全部是 0.00%**。

原因：近白的判準是 `min(RGB) ≥ 235`，而那圈殘留是**淡粉灰（約 200）**，
壓根到不了 235。⇒ **那個門檻對彩色頭髮無效**，它只抓得到白髮／白衣的白霧。

**新指標（邊緣亮度超出）**：半透明邊緣像素的 V 減掉**鄰近不透明像素**的 V。
越負代表髮絲越自然地收細；接近 0 或偏高就是邊緣被白污染。

    你的 PNG（B 世代）        -56.6   ✔
    我的 ToonOut（A 世代）    -36.3   ⛔ 差 20/255，5x 棋盤上看得見一圈淡白
    對照 laugh（索，白髮）     -44.6

⚠⚠ **試過修，修不掉**（留著當紀錄，不要再試一遍）：

    ml 40/6（現行）      -35.9
    ml 120/12           -35.8   ← 加迭代完全沒用（同憲法 -1516 的結論）
    unpremultiply       -74.5   ← 過頭，而且 α→0 會炸
    混合 w=(α-0.30)/.35 -42.5
    混合 w=(α-0.15)/.35 -48.6
    混合 w=(α-0.05)/.30 -59.0   ← 數字對了，**5x 目視仍看得到淡白**

⇒ **殘留不只在前景色 F，也在 alpha 的軟斜坡本身。**
**結論：深色／彩色頭髮的圖，Ray 自己出的 alpha 比本機 ToonOut 好，優先用他的。**
本機那條仍然適用**白髮／淺色**（索拉娜自己那 61 張都沒問題）。

## ⚠ 還沒解決的：`scream`／`jealous`／`smirk` 是 A 世代

它們的諾薇兒髮緣**帶著那圈淡白**，而且索拉娜與 `carrynouvelle`（B）**長得不一樣** ——
在劇情裡切換表情時，她的頭髮會跟著變。**兩條路，等 Ray 決定**：

1. 他把那三張也**出成 alpha** 給我（最乾淨，與 B 一致）
2. 或者**重生成**在 B 的底上

## 程式端要接：`speakers.js`

    carrynouvelle:        { src:'resources/si/sorana_si_carrynouvelle.webp',        top:5, bot:1520, fx:0.648 },
    carrynouvelleshock:   { src:'resources/si/sorana_si_carrynouvelleshock.webp',   top:5, bot:1520, fx:0.648 },
    carrynouvellescream:  { src:'resources/si/sorana_si_carrynouvellescream.webp',  top:7, bot:1519, fx:0.648 },
    carrynouvellejealous: { src:'resources/si/sorana_si_carrynouvellejealous.webp', top:7, bot:1519, fx:0.648 },
    carrynouvellesmirk:   { src:'resources/si/sorana_si_carrynouvellesmirk.webp',   top:7, bot:1519, fx:0.648 },

⚠ `top`／`bot` **兩個世代不同**（B 是 5/1520、A 是 7/1519）—— 照抄，不要統一。
⚠ `fx 0.648` 兩個世代都一樣（B 的睫毛實測 629.8／697.4，A 是 625.2／701.7，中點都是 663.x）。
⚠ `cm` **不要填**（理由見 §八）。全部是新檔，**不必 `?v=`**。

---

# 十、蕾娜 `snivel`（ver -1670 後，Ray 直接丟在 `resources/si/`）

他自己去好背的（近白 **0.00%**、角α 0、半透明 21,275 px）—— **我沒有重去背**，只轉 webp
（`q85 / alpha_q100 / method6`，134 KB，**轉檔後 alpha 與原 PNG max 差 0**）。

    snivel: { src:'resources/si/renna_si_snivel.webp', top:9, bot:1526, fx:0.548 },

⚠ `top:9` 確認是她的**髮頂**不是髮飾（row 9 的內容在 x 539..551，正在頭頂中央；
銀葉髮飾在側邊，沒有超出頭頂）。
⚠ `fx 0.548` ＝ 兩眼中點 560.9/1024（睫毛實測 524.7／597.2，已目視確認在鼻梁上）。
她在擦眼淚、頭是低的 —— **`top`／`bot` 照量的填，要調大小就動 `cm`／`standCm`**。
新檔，不必 `?v=`。母版進 `resources/_originals/si/`。


---

# ⛔ `amaze` 也退件了（ver -1670 後，緊接在 `tease` 之後）

> Ray：「amazed 也被修壞了 恢復」

`82919b0f` 那一版（與 `tease` 同一個 commit、同一個「A 只給頭」的作法）**兩張都被退**。
已還原回 `82919b0f^`（sha1 `a9ab440af928` 逐位元相同），四項照樣過：
色相 218.1／角α 0／近白 0.07%／與A差 33.3 ✔

⚠⚠ **連帶：`amaze` 也要從「加 `?v=2`」那張表上拿掉**（同 `tease` 的理由）——
檔案回到覆蓋前那一份，與玩家快取裡的本來就是同一個，掛版號是白掛的。

⚠⚠⚠ **這一輪 `82919b0f` 交的兩張全退** ⇒ **「A 只給頭」那個破判定的作法，
出圖是過了，但畫出來的臉 Ray 不收**。下一個人要用那一招之前先知道這件事：
**它解的是「送得出去」，不是「畫得對」。**


---

# ⛔ `die` 也退件（ver -1670 後）

> Ray：「sorana_si_die 也修壞了 還原」

`95d52262`（第二輪重製 28 張）那一版**把她的膚色洗白了** —— 並排一看很明顯。
已還原回 `95d52262^`（sha1 `e06777d433dc` 逐位元相同）。

## ⚠⚠⚠ 這一張與 `tease`／`amaze` **不一樣：它要跳 `?v=3`**

    speakers.js:650   die: { src:'resources/si/sorana_si_die.webp?v=2', ... }

`?v=2` **已經上線了**（第二輪那一批的版號早就接進去了）⇒ 玩家的快取裡存的是
**重製版**那一份。把檔案換回去而不跳版號，他們會繼續看到被洗白的那一張。

⇒ **`die` 要改成 `?v=3`。**

⚠⚠ **判準：看線上 `speakers.js` 現在寫的是什麼，不要照「有沒有被退件」推論。**
· `tease`／`amaze`：`?v=` **還沒接** ⇒ 還原之後**不必跳**（掛了是白掛）
· `die`：`?v=2` **已經接了** ⇒ 還原之後**一定要跳 `?v=3`**
同一件事（退件還原），兩種相反的處理，差別只在**那個版號有沒有出門過**。

## ⚠ 還原之後四項有一項不過（照實報）

    色相 238.2（門檻 <220）  角α 0  近白 0.00%  與A差 34.0

色相偏藍紫 —— 而第二輪重製的目的正是「髮色回冰藍白」。
**所以這張是「膚色對、髮色不對」換掉「髮色對、膚色不對」**。
要兩件都對得重畫一次（Ray 出 SD 臉那條路），等他決定。


---

# 十一、⭐⭐ 索拉娜第四輪 —— **19 張**（ver -1670 後，Ray 走「SD 臉」那條線）
> Ray：「把 alpha 的圖都給辦了」

## ⚠⚠⚠ 這一批有 **13 張換成 1024×1600 的新畫布**（原本是 1024×1536）
畫布一變，`top`／`bot` 就一定要重量 —— 它們是**那張畫布的像素**。全部用 `tools/measure_si.py`
量的（專案自己那支，鐵律 7；`fx` ＝頭部那一段的 alpha 重心，不是我自己寫的睫毛偵測）。

## 程式端要接：`speakers.js`

| 鍵 | 畫布 | top | bot | fx | 線上現在 | **要改成** |
|---|---|---|---|---|---|---|
| `amaze` | 1600 | 6 | 1590 | 0.562 | （沒有） | **?v=2** |
| `angry` | 1600 | 13 | 1593 | 0.525 | ?v=2 | **?v=3** |
| `armcross` | 1600 | 7 | 1593 | 0.545 | ?v=2 | **?v=3** |
| `back` | 1536 | 5 | 1527 | 0.567 | ?v=2 | **?v=3** |
| `carrynouvellejealous` | 1536 | 5 | 1520 | 0.648 | — | **新鍵** |
| `carrynouvelleshock` | 1536 | 5 | 1520 | 0.648 | （沒有） | **?v=2** |
| `confuse` | 1536 | 7 | 1522 | 0.511 | ?v=2 | **?v=3** |
| `cringe` | 1600 | 6 | 1591 | 0.455 | ?v=2 | **?v=3** |
| `cry` | 1600 | 11 | 1579 | 0.552 | ?v=2 | **?v=3** |
| `crybig` | 1600 | 8 | 1587 | 0.534 | （沒有） | **?v=2** |
| `cryclose` | 1600 | 11 | 1578 | 0.552 | — | **新鍵** |
| `determine` | 1600 | 12 | 1587 | 0.575 | ?v=2 | **?v=3** |
| `drink` | 1600 | 6 | 1592 | 0.452 | ?v=2 | **?v=3** |
| `eat` | 1600 | 7 | 1592 | 0.599 | ?v=2 | **?v=3** |
| `embarrass` | 1600 | 7 | 1593 | 0.576 | ?v=2 | **?v=3** |
| `excite2` | 1600 | 10 | 1577 | 0.688 | ?v=2 | **?v=3** |
| `front` | 1600 | 8 | 1592 | 0.672 | ?v=2 | **?v=3** |
| `shy` | 1536 | 7 | 1531 | 0.586 | （沒有） | **?v=2** |
| `talksmile` | 1536 | 6 | 1522 | 0.512 | — | **新鍵** |

⚠⚠⚠ **版號不是一律 `?v=2`** —— 照「線上現在寫什麼」決定（`die` 那一條的同一個判準）：
· 線上已經掛 `?v=2` 的 **12 張** ⇒ 要跳 **`?v=3`**
· 線上沒有版號的 **4 張**（`amaze`／`crybig`／`shy`／`carrynouvelleshock`）⇒ 加 **`?v=2`**
· **3 個新鍵**（`carrynouvellejealous`／`cryclose`／`talksmile`）⇒ 新檔，**不必版號**

⚠⚠ **`amaze` 回到要跳版號了**：稍早我把它從表上拿掉（Ray 退了重製版、已還原），
**但這一批他又交了一張新的 `amaze`（1600 畫布）** ⇒ 那一條作廢，現在要 `?v=2`。

⚠⚠⚠ **兩張 carry 的 `fx` 不要照 `measure_si.py` 的 0.600**：
那支工具取「頭頂往下 8%」的重心，而這兩張**頭頂是諾薇兒** ⇒ 量到的是她的頭。
說話的是索拉娜 ⇒ 用 **0.648**（睫毛實測 ＋ 目視確認在鼻梁上，與已交的
`carrynouvelle`／`scream` 同一個值）。同 §6.5「法環污染 `top`」那一族的坑。

⚠ `cm` 一律不要填（同 §八／§九 的理由）。

## 作法

| 張數 | 情況 | 作法 |
|---|---|---|
| 16 | 新畫布／新姿勢／碰到輪廓 | **整張跑 ToonOut** |
| 3 | 只改內部（`back`／兩張 carry 差分） | **沿用現行 alpha**（實測 α max 差 **0**） |

⚠⚠ 兩張 carry 這次是生在 **B 世代**（Ray 自己去背那一版）上的 ——
`jealous` 改動只有 0.67%、集中在臉那一框；`shock` 碰輪廓 18 px。
⇒ 兩張都沿用 **Ray 的 alpha** ⇒ **諾薇兒的髮緣沒有白邊**（那正是 ToonOut 的弱項）。
⚠ 但 `scream`／`smirk` 還是 A 世代 —— **那兩張與這三張混用時索拉娜的頭髮會變**，仍未解。

## 驗收（`_sorana_check.py`）

16 張全過。**3 張近白超標，逐張複核過，結論是不用重做**：

    cry 2.29%（231 px）   cryclose 1.89%（192 px）   shy 2.16%（222 px）

⚠ 那 200 來個像素**集中在她的白髮與白衣邊緣**，4× 深色棋盤複核：髮絲收得乾淨、
沒有霧，與已驗收的 `laugh` 同級。憲法 -1516 早就寫著「**近白這條門檻對白髮白衣角色
天生偏嚴**，看比例之前先看像素數」。

## ⚠ 一張被裁：`embarrass`

右緣 **0 px 餘裕**（其餘 18 張都有 2~20 px）。畫面上會少掉右邊那一小截。
**要不要重生成由 Ray 決定** —— 我沒有改它。


---

# 十二、⭐ 蕾娜・公主抱 **3 張**（ver -1670 後）

> Ray：「alpha 有三張圖 辦了／通通參考 `alpha/4d82534b-1679-4b8e-863e-1ed460fa2910`」

主角抱著蕾娜的公主抱，三張是**同一個姿勢的表情差分**：

    renna_si_hug           張口、看著主角
    renna_si_hugclose      閉眼／半閉
    renna_si_huglookaway   別開視線

## ⚠⚠⚠ 查到一件事：那三張 1600 的是 **1536 那張的「垂直拉伸」版**

Ray 一併放進來的 `4d82534b-….png`（1024×1536，**他自己去好背的真 alpha**）
與那三張 1024×1600 的關係，實測是**同一批像素重取樣**：

    直接對齊（只比上面 1528 列）  平均差 20.87
    把 1536 那張垂直拉到 1592     平均差 **0.56**   ← 就是它

⇒ 1600 那三張的**人被拉高了 4.2%**（1592/1528）。那是失真，不是重畫。

**作法**：把三張**反拉伸回 1536**（內容 906×1592 → 906×1528，貼回 (93,1)），
再與 **Ray 自己那份 alpha** 合成（`w=clip((α−200)/55)`：不透明處用新圖的臉，
半透明處用他的前景色）。
⇒ **alpha 與他的逐位元相同（max 差 0）、近白 0.00%**，比例也還原了。

⚠ 這是今天「深色／彩色頭髮優先用 Ray 的 alpha」那一條的最佳情況 ——
他連 alpha 都給了，我只要對齊就好，完全不必跑 matting。

## ⚠ 上一批那 13 張 1600 的**不是**拉伸（驗過了）

拿 `front`／`eat`／`determine` 與被它們覆蓋掉的舊 1536 檔比，
縮回去之後平均差 **55~65**（拉伸的話會是 0.x）⇒ 那些是真的新圖，沒問題。
⚠ 但它們的內容高寬比也從 1.50 變成 1.56~1.57（同樣 +4%）——
**是畫出來就那樣，還是產圖端把 1536 拉到 1600，我分不出來**（沒有對照的原稿）。
要確認的話問 Ray 產圖端輸出的原始尺寸。

## 程式端要接：`speakers.js` 加 **3 個新鍵**（蕾娜，新檔不必版號）

    hug:          { src:'resources/si/renna_si_hug.webp',          top:3, bot:1528, fx:0.380 },
    hugclose:     { src:'resources/si/renna_si_hugclose.webp',     top:3, bot:1528, fx:0.380 },
    huglookaway:  { src:'resources/si/renna_si_huglookaway.webp',  top:3, bot:1528, fx:0.380 },

⚠⚠⚠ **`fx` 不要用 `measure_si.py` 的 0.513** —— 那支取「頭頂往下 8%」的重心，
而這三張**畫面上有兩顆頭**（主角也在），量到的是兩人的中間。
說話的是蕾娜 ⇒ 用 **0.380**（她的兩眼中點 389.5/1024，已目視確認在鼻梁上）。
同 §九 背諾薇兒那一族的坑：**多人構圖時那支工具的 `fx` 一律要手動覆寫。**

⚠ `cm` 不要填（同前）。母版與他那張 alpha 原稿都在 `resources/_originals/si/`。

---

# 十三、⭐ 索拉娜 realpha 九張（2026-09-23 晚）—— Ray 的 SD 臉 ＋ GPT 原 alpha

> Ray：「索拉娜昨天我手動 sd 的那幾張因為尺寸有變 alpha 通道跑掉了要重做」
> → `resources/si/realpha/` 放 `73ca64df^` 的 GPT alpha 版 → Ray 重跑 SD 臉丟回來 →「全辦了，尺寸不同的先跳過」

**作法**：逐像素比「新 RGB」vs「GPT alpha 疊白底」—— 九張改動**全部在頭部**
（半透明區只有 `cry` 25 px／`front` 2 px，髮緣色差 ~45）⇒ 照 `alpha/README.md`
**alpha 一個位元都不動、只換 RGB**（`w=clip((α−200)/55)`）。q85。
驗收：`_sorana_check.py` 九張全 ✔（近白 ≤0.07%）；頭部 2× 深色底目視無白邊無接縫。

⚠⚠ **畫布回到 1024×1536**（取代第四輪那 13 張裡的 9 張 1024×1600）⇒ `top`／`bot` 一定要改。
`fx` **照線上不動**（-1578 的規矩；實測新值與線上差 ≤0.03，只有 `determine` 0.575 vs 0.547 ——
那是自動量測被手臂拉偏，不是頭移位）。

## 程式端要接：`script/speakers.js`（＋ `flight/index.html` 有 `amaze` 那一份）

| 鍵 | 畫布 | top | bot | 版號 |
|---|---|---|---|---|
| `amaze` | 1536 | 3 | 1527 | ?v=2 → **?v=3**（flight 那一份一起） |
| `angry` | 1536 | 12 | 1530 | ?v=3 → **?v=4** |
| `armcross` | 1536 | 3 | 1529 | ?v=3 → **?v=4** |
| `cry` | 1536 | 4 | 1515 | ?v=3 → **?v=4** |
| `crybig` | 1536 | 7 | 1524 | ?v=2 → **?v=3** |
| `determine` | 1536 | 10 | 1523 | ?v=3 → **?v=4** |
| `eat` | 1536 | 1 | 1529 | ?v=3 → **?v=4** |
| `embarrass` | 1536 | 5 | 1529 | ?v=3 → **?v=4** |
| `front` | 1536 | 5 | 1529 | ?v=3 → **?v=4** |

## ✅ 後三張（同晚補完）—— Ray 的 SD 稿是 GPT 圖**從左上角裁掉右／下緣**（無縮放，對位誤差 0.3~0.4）
重疊區照同一規則換 RGB，被裁掉的那幾列沿用 GPT 原圖（裡面幾乎沒有不透明像素）。改動同樣只在頭部。
畫布**照 GPT alpha 原尺寸**（不是 1536）：

| 鍵 | 畫布 | top | bot | 版號 |
|---|---|---|---|---|
| `cringe` | 1024×1535 | 3 | 1527 | ?v=3 → **?v=4**（flight 那一份一起） |
| `drink` | 1024×1535 | 4 | 1527 | ?v=3 → **?v=4** |
| `excite2` | 1026×1532 | 9 | 1510 | ?v=3 → **?v=4**（flight 那一份一起） |

原檔：SD 稿 → `_originals/si/sorana_si_<鍵>_sdface_src.png`；合成母版 → `_originals/_realpha_work/`；
舊 webp 與 GPT alpha 抽出檔 → `_recycle/`。

---

# 十四、`sorana_si_carrynouvelleshock2.webp`（2026-09-23 晚，Ray 交辦）

> Ray：「把 carrynouvelleshock 中諾薇兒的臉換成 carrynouvellejealous 諾薇兒的臉，保留 alpha 通道」

＝**索拉娜驚訝（shock）＋ 諾薇兒閉眼（jealous）**。兩張同一代、同畫布、alpha 逐位元相同，
差異只在兩張臉 ⇒ 用「差異 ∩ 諾薇兒頭部區 − 索拉娜的藍白髮／深膚」做遮罩，2 px 羽化換 RGB。
**alpha 與 shock 逐位元相同**（maxdiff 0）；索拉娜的臉與頭髮沒動。
⚠ 諾薇兒左耳多出一個**十字耳墜**—— 那是 jealous 版本來就有的（臉連耳朵一起換過來），不是雜物。
第二代編碼 q92。母版 `_originals/_realpha_work/carryshock2.png`。

## 程式端要接：新鍵（不必版號）
    carrynouvelleshock2: { src:'resources/si/sorana_si_carrynouvelleshock2.webp', top:5, bot:1520, fx:0.667, cm:196, standCm:178 },
取景**全部照抄 `carrynouvelleshock`**（同一張畫布、同一個姿勢；fx 0.667 是線上那一族的值 ——
工具量到的 0.600 是被頭頂的諾薇兒拉偏，-1670 那條）。
