# 給程式 session 的工單 — 索拉娜第三輪重製 **13 張**（2026-09-22，美術 session）

> Ray：「推上去，然後跑索」。作法照 `_sorana_r2_worklist.md` §五（A 先傳、三行提示詞、
> 被擋就重送一次、再擋就跳過）。

## 一、⚠⚠⚠ 唯一要做的：`speakers.js` 這 11 條路徑跳 `?v=`

同名覆蓋，不跳就是玩家繼續吃舊的那一份，**而且畫面上不會有任何錯誤訊息**（§5 的 -650）。

| 要加 `?v=2`（現在沒有版號，11 張） | `battlecry` `shy` `sorry` `surprise` `talk` `think` `tire` `upset` `watch` **`whisper`** **`remind`** |
|---|---|
| **要改成 `?v=3`（現在是 `?v=2`，2 張）** | `side` `smile` |

⚠⚠ **`side` 與 `smile` 那兩個 `?v=2` 是上一輪白掛的** —— `git log --diff-filter=M`
查過，這兩個檔**在那之前從來沒有被覆蓋過**。這一次是真的覆蓋了，所以要跳到 3。
（順帶：這也證明 `_sorana_r2_worklist.md` §四「還差 18」那張表是對的 ——
**不要因為看到 `?v=2` 就以為它重製過了。**）

## 二、✔ 取景值**不必動**（量過了，不是略過）

同一個姿勢的重製 ⇒ 依 §5 的 -649「同一個姿勢直接沿用」。alpha 上下緣的偏移：

    dTop  -5 ~ +6 px     dBot  -19 ~ +6 px      （畫布 1536，全部 <1.3%）

⚠ 兩張的 `bot` 偏得比較大，**要不要重量由程式端看畫面決定**：
`battlecry` **−19px（1.2%）**、`whisper` **−31px（2.0%）**
（同 -1650 那三張 `hug`／`excite2`／`readsad` 的處理）。`fx` 一律不動。

## 三、驗收（四項，`resources/si/_sorana_check.py` 是唯一計算點）—— **11 張全過**

| 指標 | 門檻 | 這 11 張 |
|---|---|---|
| 髮色色相 | < 220° | **194.4 ~ 215.1** |
| 角落 alpha | ≤ 8 | **0** |
| 近白比例 | < 1.5% | **0.00 ~ 0.34%** |
| 與基底 A 的差異 | > 3 | **10.9 ~ 36.4** |

⚠ 另外做了 §規約 §三 要求的**臉部 100% 裁切並排**（11 張一起看）：臉模、髮色、
眼睛形狀、金飾與鈴鐺全部對得上基底 A，沒有 -1556 那種吊眼／大圓眼的變形。
透明像素佔 53~66%，是真 alpha 不是不透明底。

## 四、⚠ 還沒做完的 5 張 —— **四張被擋死、一張 alpha 出不來**

> Ray（2026-09-22）：「**換提詞被擋就換下一張換 session 跑**」——
> 以下每一張都走完了憲法 §5 那三段（原串重送 → 換串＋換一個短方向詞 → 再換串），
> **不是沒試，是試到該停了**。

| 鍵 | 今天試了幾次 | 用過的短方向詞 | 結果 |
|---|---|---|---|
| `smirk` | **4** | 自信的女戰士／挑釁的女戰士 | 每一次都是內容判定 |
| `tease` | **4** | 爽朗的女戰士／頑皮的女戰士 | 同上 |
| `amaze` | **3** | 驚訝的女戰士／震驚的女戰士 | 同上 |
| `laugh` | **3** | 開朗的女戰士／開懷大笑的女戰士 | 同上 |
| `panic` | **3** | —（沒有被擋） | **圖每次都出得來，但角落 alpha 246~252 ＝ 它自己畫了背景** |

⚠⚠ **`panic` 與另外四張是兩種病，不要混為一談**：
· 那四張是**內容判定**（連圖都沒有）
· `panic` 是**它不給透明背景** —— 加了「背景完全透明，四個角落必須是透明的」
  與「輸出透明背景的 PNG，角色以外全部透明」兩種寫法都一樣。
  ⇒ 這一張**不要再硬盧提示詞**：它已經是 §憲法 -1516 那條「本機 alpha」的適用對象
  （`tools/si_matting.py` ＋ `matting_eval.py` 的四項門檻）。⚠ 那條路需要
  `.venv-matting`（ToonOut／BiRefNet），**這台機器還沒建**（建法在憲法 §5）。

**⇒ 索拉娜 62 張的總進度：44 ＋ 13 ＝ 57 張已重製，還差 5 張。**

⚠⚠⚠ **這五張要不要繼續，是 Ray 的決定，不是美術的** —— 憲法 §5 的第三段
（「換掉那一張」）原本是給**補齊情緒範圍**用的；但這五張**已經接進腳本**，
換一個姿勢等於改掉那幾拍的演出。三條路：
1. **留著舊圖**（畫面不會壞，只是那五張還是舊的臉模）
2. **換一個姿勢重畫**（腳本那幾拍的演出會變，取景要重量）
3. **`panic` 單獨走本機 alpha**（先建 `.venv-matting`）

## 五、⚠ 這一輪踩到的坑（下一個人先看這裡）

1. **`_sorana_check.py` 的 `BASE` 指到 `resources/_originals/SI_sorana_base/soranagpt_1.png`，
   而 `_originals` 沒進版控** —— 換一台機器就跑不動。
   ⇒ 版控裡的那一份在 **`resources/si/soranagpt/`**（四張 UUID 檔名的 PNG，就是
   `soranagpt_1~4`，依檔名時間排序）。這一輪是把它們複製回 `_originals/SI_sorana_base/`
   才跑得起來。**下一台機器照做一次就好。**
2. **`javascript_tool` 有 45 秒上限** —— `__send()` 自己就睡 20 秒，再串一個
   25 秒的等待就會 timeout，**而且送出其實已經成功了**，只是回傳被砍掉。
   差一點誤判成「沒送出去」而重送（那會白燒一次額度）。
   ⇒ `__send` 與 `__grab` 要分成兩次呼叫，中間用 browser_batch 的 `wait` 湊時間。
3. **換一個新對話之後 `ref` 會失效**，而且**同一個 `ref` 編號會被重新指派給別的元素**
   —— 每開一個新串就重新 `find` 一次檔案輸入欄，不要沿用上一串的編號。
