# 拉芬斯達爾 Ravnsdal — 城市設計（北歐・1900）

> **城名（Ray 2026-09-12 交辦「取個 19 世紀北歐內陸風的名，不要跟現實城市撞名」）**
> **Ravnsdal ／ 拉芬斯達爾** —— 地圖 id `ravnsdal`、檔名前綴 `Varn_`。
> · `ravn` ＝ 北歐語（丹麥／挪威）的**渡鴉**；`dal` ＝**谷** ⇒「渡鴉谷」。
> · `-dal` 是**內陸**地名的後綴（海邊會用 `-vik`／`-havn`／`-nes`）——
>   照 Ray 要的「內陸風」。⚠ 這座城其實是**港市**：名字留著它從谷地聚落長成港口的來歷，
>   那是可以用的設定，不是矛盾。
> · **撞名檢查**：查過沒有叫 Ravnsdal 的城鎮。相近但不同的是
>   Ravnkilde（丹麥，312 人的村）、Ravnsnæs（丹麥，584 人）、Ravnholm（哥本哈根北郊的一區）、
>   Ravnedalen（挪威 Kristiansand 的一座公園）—— 都不是城、也都不是這個拼法。
>   ⚠ 北歐的小地名多如牛毛，我只能保證「不撞到任何叫得出名字的城市」。

> Ray（2026-09-12）：「設計一個城市，**用帝都當藍圖**，**市鎮中心、碼頭、大教堂都先留白**」
> →「**北歐風 1900 年，不要有任何旗幟跟國標**」

---

## 一、拓樸：**照抄 `capital`，一格不改**（12 格・11 邊・環 0）

大城地圖已經規則化（憲法 §6.5.4.2），所以節點 id、出口、樞紐與末端的分佈全部相同，
只換 `name` 與 `bg`。同聖索菲亞那一套。

| 節點 id | 向數 | 出口 | 中文名 | `bg` |
|---|---|---|---|---|
| `square` | 3 | `{ up:'midtown', left:'oldtown', right:'uptown' }` ＋ `sail`（下方＝出航） | 拉芬斯達爾　港口廣場 | `Varn_Square` |
| `midtown` | 3 | `{ left:'lookout', right:'church', down:'square' }` | 拉芬斯達爾　中心區 | `Varn_Midtown` |
| `church` | 末 | `{ back:'midtown' }` | 拉芬斯達爾　大教堂 | `Varn_Church` ⬜ |
| `lookout` | 末 | `{ back:'midtown' }` | 拉芬斯達爾　瞭望台 | `Varn_Lookout` |
| `oldtown` | **4** | `{ left:'gunstore', right:'square', up:'station', down:'guild' }` | 拉芬斯達爾　舊街區 | `Varn_Oldtown` |
| `gunstore` | 末 | `{ back:'oldtown' }` | 拉芬斯達爾　武器店 | `Varn_Firearm` |
| `station` | 末 | `{ back:'oldtown' }` | 拉芬斯達爾　火車站 | `Varn_Station` |
| `guild` | 末 | `{ back:'oldtown' }` | 拉芬斯達爾　賞金獵人公會 | `Varn_Guild` |
| `uptown` | **4** | `{ left:'square', right:'tavern', up:'inn', down:'grocery' }` | 拉芬斯達爾　上街區 | `Varn_Uptown` |
| `tavern` | 末 | `{ back:'uptown' }` | 拉芬斯達爾　餐飲街 | `Varn_Bistro` |
| `grocery` | 末 | `{ back:'uptown' }` | 拉芬斯達爾　雜貨舖 | `Varn_Grocerie` |
| `inn` | 末 | `{ back:'uptown' }` | 拉芬斯達爾　旅店 | `Varn_Hotel` |

⬜ ＝ **Ray 指定先留白**（見 §三）。

- ⚠ **節點 id 是 `oldtown`，背景檔名是 `Varn_Oldtown`** —— 帝都那一格的 id 也是 `oldtown`，
  但它的 bg 叫 `Capital_Downtown`。**id 與檔名本來就不同名**，這裡我讓它們一致，
  但**不要**回頭把帝都那邊「順手統一」。
- ⚠ **入口是 `square`，不可以有戰鬥**（§6.5.2：它是遭遇戰的復活點）。出航掛在它的下方。
- ⚠ `tools/map_layout.py` 的 `POS` 可以**直接複製 `capital` 那一組**（節點 id 一樣，方向驗證自然過）。
- ⚠ 大城市不上迷霧：`mist:0`（**要明寫**，沒寫就是有霧）。

---

## 二、風格：**北歐・1900 年**

### ⛔⛔ 硬禁令（Ray 指定）

> 「**不要有任何旗幟跟國標**」

· ⛔ 不要旗子、不要旗桿、不要三角旗與串旗、不要船旗。
· ⛔ 不要國徽、市徽、盾徽、紋章、王冠、雙頭鷹、十字旗那一族。
· ⛔ 招牌上不要任何國家或官方的標誌 —— 商家招牌用**器物剪影**（鑰匙、麵包、酒桶、火槍）。
· ⚠ 行政廳也一樣：正面可以有大鐘與石雕花飾，但**不掛任何徽章**。

> ⛔⛔ **城內的地面不要有輕軌／電車**（ver -1324，Ray：「城內地面不要有輕軌」）
· ⛔ 石板路上**不要鐵軌、不要嵌在石板裡的溝槽、不要轉轍器**。
· ⛔ 連帶：**不要跨街的架空接觸網與吊掛纜線**（軌道沒了就不該有接觸網），
  也不要電車專用的桿子與吊架。⚠ 街燈、燈柱橫桿、招牌鐵件照舊。
· ⛔ 也不要汽車與柏油路 —— 這是 **1900 年**：馬車、石板、雪。
· ⚠ **火車站是例外、但只到棚內為止**：鐵軌只出現在**月台車棚裡面**；
  前庭與街上是完整的石板鋪面。

> ⛔⛔ **車站不要有火車頭朝向鏡頭**（ver -1324，Ray：「車站不要有火車頭朝外」）
· 月台上可以停**側向**的客車廂（車身與鏡頭平行），**車頭不入鏡**、也不要噴向鏡頭的白煙。
· ⚠ 原因是讀圖：正對鏡頭的火車頭會變成畫面的主角，而那一格是**車站廣場**，
  主角是站體與前庭。

### ✅ 1900 年的北歐要畫什麼

| 要 | 怎麼畫 |
|---|---|
| **木造倉庫街** | 沿水邊一排排三四層的木造貨棧，山牆正對街、頂端有**吊貨的伸臂與滑輪**；外牆漆成赭紅、赭黃、暗綠，木板橫向密鋪 |
| **民族浪漫主義的石造建築** | 粗面花崗岩砌的底層、圓拱窗、厚重的塔樓與洋蔥／圓錐頂；線條沉穩、裝飾少 |
| **Jugendstil（新藝術）** | 只出現在比較新的臨街立面：帶曲線的窗框、鍛鐵陽台、植物紋的浮雕 |
| **屋頂** | 深色石板瓦或銅皮（生銅綠）；屋脊高陡（落雪用），老虎窗成排 |
| **街面** | 花崗岩方石鋪地、有車轍；鑄鐵**煤氣路燈**、繫馬環、排水溝 |
| **氣候** | 冷。空氣裡有海霧或煤煙，遠處被霧吃掉一階；屋簷與石縫有殘雪或水漬 |
| **1900 年的證據** | 電線與電報線橫過街道、早期的路面電車軌道（車可有可無）、玻璃櫥窗、鑄鐵柱廊 |

⛔ 不要：中世紀半木構（Fachwerk）、城牆與雉堞、南歐的赤陶瓦與拱廊、
現代玻璃帷幕、柏油、汽車、霓虹。
⛔ 絕對不要東方元素。

### ⚠⚠ 船是**水空二用、著水靠岸**（ver -1163，Ray 的世界觀設定，程式端轉來）

> Ray：「這個世界的船 90% 都是**水空二用**，在**河港**停泊，因為維持浮力要燃料、
> 懸停怕風不穩，所以**正規都是著水靠岸**。」

所以這座城是**河港**，不是空港：

- 停泊的船**浮在水上、繫在岸邊** —— 船體吃水、有吃水線與水面倒影，桅杆／浮空裝置在上。
  ⛔ **不要畫成離地懸停。**
- ⛔ 不要降落塔、繫留桅（mooring mast）、跑道那一類空港元件。
- 分工因此有了理由：**客貨上岸走河港、內陸轉運走鐵路**（＝ `station` 那一格）。
- ⚠ 落點對得上：拉芬斯達爾 (577,243) 距最近水面 12 格（南方那條河）。
  程式端量過全圖，**有出航的城都靠著水**（東方泊地 2／聖索菲亞 2／夏爾村 5／
  薇拉馮德港 12／北方泊地 14／帝都出港位 12）。

⚠ **`Varn_Square_day` 實測已經合規**（第一張就畫對了）：船浮在水面上、沿岸壁並排繫著，
有吃水線與倒影，沒有懸停也沒有繫留桅 —— **不必重跑**。

### 色調
低彩度的冷色：石灰白、花崗岩灰、赭紅木、銅綠、深藍灰的天。
暖色只從**窗內的燈光與煤氣燈**來 —— 那是這座城唯一的溫度。

---

## 三、留白：**只剩大教堂一格**（ver -1162，Ray 改定）

> Ray：「**大教堂留著，行政廳改成俯看全鎮的瞭望處，碼頭改成火車站**」

| 格 | 現況 |
|---|---|
| **大教堂 `church`** | ⬜ **仍然留白** —— 宗教建築的形制是世界觀的事 |
| ~~行政廳 `cityhall`~~ → **瞭望台 `lookout`** | ✔ 已定案，見 §四 |
| ~~碼頭 `dock`~~ → **火車站 `station`** | ✔ 已定案，見 §四 |

### ⚠⚠ 節點 id 跟著改了（`cityhall`→`lookout`／`dock`→`station`）

照抄帝都的**拓樸**（出口、樞紐與末端的分佈）一格不改，但**這兩格的內容
已經不是那個東西了** —— 留著 `dock` 當火車站的鑰匙，下一個讀這份資料的人
一定會以為那裡有船。這座城**還沒進 `script/town.js`**（實測 grep 是空的），
所以改 id 是零成本；`OUTING.nodesBy[城id]` 本來就是逐城一份，不受影響。

⚠ 這與聖索菲亞那條「不要順手統一 id」**不衝突**：那邊改的是**名字**
（`oldtown` 顯示成「舊街區」），這邊改的是**那一格是什麼**。

### ⚠ 「碼頭改成火車站」不影響出航

出航掛在 `square` 的**下方**（`sail`），本來就不是走 `dock` 那一格
（§6.5.4「城鎮入口的下方是出航」）。所以這一格換成火車站，
出航一行程式都不必動。
⚠ 順帶：這座城因此是**內陸感的港城** —— 廣場那一側仍有水面與繫纜柱
（飛空艇停泊處），貨運走鐵路。兩者不打架。

---

## 四、逐格美術簡報（**十一格**，留白的大教堂不在內）

⚠⚠ **中心區那一格的伏筆照舊有效**：左右兩條側街仍然「**往深處沒入海霧**」。
左邊現在通瞭望台（上坡）、右邊通大教堂（**仍未定案**）—— 霧裡沒有承諾任何形狀，
所以大教堂之後定案時中心區**不必重畫**。

| 格 | 向數 | 這一格長什麼樣 |
|---|---|---|
| **港口廣場** `square` | 3（＋出航） | 城的入口：一塊花崗岩鋪成的方形廣場，一側就是碼頭的水面與繫纜柱；中央一座**鑄鐵的飲水槽或無雕像的紀念石柱**（⛔ 不要人像、不要徽章）；三面是四層樓的石造街屋，底層是鑄鐵柱廊的店面；煤氣燈成列；地上有電車軌道與車轍 |
| **中心區** `midtown` | 3 | 全城最寬的一條石板大道：兩側是四五層的粗面花崗岩街屋，底層是鑄鐵柱廊的店面；路面是完整的石板鋪面（⛔ **沒有電車軌道、沒有架空電線**，見 §二的硬禁令），行道樹（菩提，已落葉）等距排開；煤氣燈成列往深處縮小。⚠⚠ **左右兩條側街要往深處沒入海霧**（左邊通瞭望台、往上坡；右邊通大教堂，**那一格還沒定案**）—— 霧裡只看得到一團模糊的量體與一點燈光，**不要畫出可辨識的塔或尖頂** |
| **舊街區** `oldtown` | **4** | 最老的一區：狹窄的石板巷，兩側是傾斜的木造貨棧，山牆頂端伸出吊貨臂與滑輪；巷子上方橫著電線與晾衣繩；四個方向各一條走得進去的路（其中一條往下是地勢較低的巷口） |
| **武器店** `gunstore` | 末端 | 店面在石造街屋的底層：厚重的木門與鍛鐵門環，**櫥窗裡陳列著獵槍與彈帶**；招牌是一支火槍的鐵剪影（⛔ 沒有文字國標）；牆上掛著鹿角 |
| **賞金獵人公會** `guild` | 末端 | 半地下的入口、往下幾階；門旁一整面貼滿紙的木製告示板，紙角被風掀起；門上一盞防風玻璃燈；⛔ 告示板上不要徽章，只有手寫的紙與圖釘 |
| **上街區** `uptown` | **4** | 地勢較高的新街區：Jugendstil 的臨街立面、曲線窗框與鍛鐵陽台、成排的行道樹（已落葉）；街道較寬、有分隔的人行道；遠景看得下去整片赭紅屋頂與港灣的桅杆 |
| **餐飲街** `tavern` | 末端 | 一條窄街，兩側是地下室的酒館入口與亮著燈的玻璃櫥窗；戶外沒有座位（太冷），只有靠牆的立桌與煤氣暖爐；門口掛著酒桶與麵包的鐵剪影招牌 |
| **雜貨舖** `grocery` | 末端 | 底層的店：大片玻璃櫥窗，裡面堆著麻袋、罐頭、繩索、油燈與桶裝鹹魚；門口一台磅秤與幾隻木箱；招牌是一把鑰匙與一個桶的鐵剪影 |
| **旅店** `inn` | 末端 | 三層的木石混造：底層是粗面石、上兩層是赭紅木板；門廊有雨庇與一盞大燈；二樓窗內透出暖光；門邊一塊掛著鑰匙剪影的鍛鐵招牌 |
| **瞭望台** `lookout` | 末端 | **全城唯一看得到整座城的地方**：石板路走到山脊的盡頭，一圈低矮的粗面花崗岩護牆與一支鑄鐵煤氣燈；護牆之外整座城在下方展開 —— 一片赭紅與暗綠的屋頂、幾根工廠與蒸汽船的煙囪、遠處港灣的桅杆與一道跨過河口的鐵橋，更遠被海霧吃掉一階。⚠ **鏡頭壓低、站在護牆這一側往外看**，地平線在畫面上三分之一；⛔ 不要瞭望塔、不要建築物當主體 —— 主體是**那片俯瞰的景**。⚠ 這是末端：**只有身後一條回去的石板路**，護牆之外是崖，走不下去 |
| **火車站** `station` | 末端 | 石造的站體正面：粗面花崗岩的底層、三道圓拱的入口、上方一面沒有指針以外任何裝飾的大鐘（⛔ 鐘面不要文字、不要徽章）；入口之後看得進去一截鑄鐵與玻璃的月台頂棚，棚下停著一列蒸汽機車、車頭冒著白汽；站前一塊小廣場，有馬車、堆著的行李與郵袋、一盞高的煤氣燈；地上有電車軌道繞過站前。⚠ 這是末端：**畫成「路走到站體正面就停了」**，站體把後面整個擋住 |

## 四之二、⚠⚠ 有店的五格改成**室內**（ver -1253，Ray 交辦）

> Ray：「武器店 公會 餐飲街、雜貨店、旅店都要重畫／**有店的都要在店內，不是在店外**
> ／餐飲街有三分支，咖啡店、餐廳、酒吧。」

§四 那張表裡這五格的舊敘述（門面、櫥窗、招牌剪影）**已作廢**：

| 檔名 | 改成 | 同名覆蓋？ |
|---|---|---|
| `Varn_Firearm` | 槍店**店內**：左側長木櫃台（綠呢、清槍工具、銅殼子彈）；後牆一排直立槍架；地上木箱與火藥桶；深處**結霜的小窗**透冷白天光，櫃台一盞煤油燈是暖的 | ✔ 覆蓋 |
| `Varn_Guild` | 公會**室內**：半地下、**低天花板**與粗木樑；左側整面木製懸賞板（貼滿手寫紙，⛔ 不要看得懂的字）；厚重長木桌（地圖、空酒杯、插在桌面的短刀）；鑄鐵柴爐透橘紅火光；深處**貼近天花板的一排小窗** | ✔ 覆蓋 |
| `Varn_Grocerie` | 雜貨舖**店內**：左側木櫃台（黃銅盤秤、牛皮紙與麻繩、錢盒）；後牆整面木格層架；地上敞口麻袋、鹹魚桶、麻繩；天花板垂下**魚乾與洋蔥**；深處大玻璃窗透冷白天光 | ✔ 覆蓋 |
| `Varn_Hotel` | 旅店**大廳**：**中央偏左的木製接待櫃台**，⚠ 櫃台**上方留一塊乾淨牆面**；後牆一排掛黃銅鑰匙的小木格；矮桌與扶手椅、鑄鐵柴爐（全圖最暖）；⚠ **右側空出來**（只畫往上的木樓梯與走廊牆）；左後方大窗外是冷灰的街與飄雪 | ✔ 覆蓋 |
| `Varn_Bistro` | **酒吧店內**（＝餐飲街的預設）：地下室、低矮磚拱；左側長吧台（深色木、黃銅扶杆、倒掛玻璃杯、橫躺酒桶）；高腳凳與長桌長凳；**整間暖橘燈光，只有深處樓梯口漏下一道冷白天光** | ✔ 覆蓋 |
| `Varn_Cafe` | **咖啡廳店內**：左側大理石小吧台（**黃銅咖啡壺與磨豆機**、白瓷杯盤）；小圓桌與曲木椅；淺灰泥牆配深色木護牆板、新藝術曲線燈具；**左後方一整片大玻璃窗**，天光整片灑進來 —— **這張最亮** | 🆕 新增 |
| `Varn_Restaurant` | **餐廳店內**：⚠⚠ **19 世紀的一般餐廳，不是高檔餐廳**（憲法 §5 的全域規矩）：左側木頭出餐檯（白瓷厚盤、大湯鍋與長柄杓、記菜小黑板）；中段素木長桌配長凳、桌面擦得發白；淺灰泥牆＋**素木護牆板**（有磨損與釘孔）；樸素的鑄鐵煤油吊燈；深處通廚房的門透出暖光與蒸氣 | 🆕 新增 |

### ⚠⚠ 餐飲街是一格三張圖

`DINE.fallback` 就是 `bar`，所以**節點的 bg ＝ 酒吧**（同帝都 `Capital_Bistro`）：

```js
dining:{ node:'tavern', scenes:{
  cafe:      { bg:'Varn_Cafe',       noTime:true, chatter:[…] },
  restaurant:{ bg:'Varn_Restaurant', noTime:true, chatter:[…] },
  bar:       { bg:'Varn_Bistro',     noTime:true, chatter:[…] },
} },
```

⚠ **沒有甜品店**（`dessert`）—— 安雅那一支在這座城不存在，`scenes` 查不到那個鍵
→ **不換、退回節點原本那張**（＝酒吧）。這是 §6.5.4.2 的既有行為，不是壞掉。

### ⚠⚠⚠ 前五張是**同名覆蓋** ⇒ 程式端要動 `ASSET_VER`

背景的檔名是組出來的（`bandNames`），路徑上寫不了 `?v=`，所以逐檔的版本表在
`config.js` 的 `ASSET_VER`（§5 ver -905）。**這五個鍵各給 `2`**：

    ravn_firearm / ravn_guild / ravn_grocerie / ravn_hotel / ravn_bistro

不加的下場是**玩家的瀏覽器一直拿舊的那張店外圖，而且畫面上沒有任何錯誤訊息**。
⚠ `ravn_cafe`／`ravn_restaurant` 是**新增**，不必加（新增比覆蓋安全）。

### ⚠⚠ 室內圖的構圖有硬規定（UI 會蓋掉一大半）

- **下半約四成被槍棺蓋住** → 重要的東西一律在**畫面上半**。
- **右三分之一會站店主立繪**（`castSolo` 固定 `side:'R'`）→ 櫃台一律在**中央偏左**。
- **左上角是選單大鈕** → 那一小塊不要有重要細節。
- ⛔ **畫面裡不要有人** —— 店主是另外疊上去的立繪。
- 旅店額外兩條（§6.5.5）：**右緣留給四扇伙伴門**、**櫃台正上方留乾淨牆面**給那兩顆行動鈕。

---

### ⚠ 每一格的向數是那張圖要畫得出來的（§6.5.4）
- `oldtown` 與 `uptown` 是 **4 向**：圖上要看得到四條真的走得進去的路。
- `square`／`midtown` 是 3 向。
- 其餘各格是末端：**畫得出「這裡走到底了」** —— 瞭望台是崖邊、火車站是站體擋住去路。

---

## 五、時段差分

### ✔ 已完成（ver -1259/-1261）：**室外六格 × 四時段 ＝ 24 張**

`Square`／`Midtown`／`Oldtown`／`Uptown`／`Lookout`／`Station` 各有
`_day`／`_dawn`／`_dusk`／`_night`。

⚠⚠ **程式端要改兩件事**：
1. **那六格拿掉 `noTime:true`**（四張都在了，留著等於只吃不帶時段的那一張 ⇒ 404）。
2. 舊的**無尾綴** `Varn_<X>.webp` 已走回收區 —— 不要再引用。

### 室內七格**維持單張、維持 `noTime:true`**

`Firearm`／`Guild`／`Grocerie`／`Hotel`／`Bistro`／`Cafe`／`Restaurant`。
理由照憲法 §5「**有室外光才有差分**」，與貝利薩爾同一把尺：那邊 52 張裡
只有看得到天的四格（`Exterior`／`SunkenCourt`／`RoofFall`／`DrainCliff`）做四差分，
其餘 36 張室內與地下都是單張。

⚠ `Church`（大教堂）仍留白 —— 圖交了再決定要不要差分（它是室外，照理要四張）。

---

## 六、進度

| 段 | 狀態 |
|---|---|
| 城名 | ✔ **Ravnsdal 拉芬斯達爾**（id `ravnsdal`／前綴 `Varn_`） |
| 拓樸 | ✔ 照抄 `capital`，12 格・11 邊・環 0 |
| 風格 | ✔ 北歐・1900、⛔ 無旗幟無國標 |
| 十一格美術簡報 | ✔ 寫好了（ver -1162 補上瞭望台與火車站） |
| 留白 | ⬜ **只剩大教堂** —— 等 Ray 給方向 |
| 重修 | ✔ **ver -1324**：`Station`／`Midtown`／`Square` 各四時段共 **12 張**重出 —— 清掉輕軌、架空線與朝外的火車頭（`ASSET_VER` 已補 12 列） |
| 背景圖 | ✔ **31 支**：室外 6 格 ×4 ＝ 24（ver -1259/-1261）＋室內 7 張（ver -1256）；⬜ 留白 1（大教堂） |
| 槍棺小地圖 | 未開始（走 `tools/map_compose.py` 那條，spots 算得出來） |
| 大地圖平面圖 | ✔ `flight/city/Ravnsdal_topdown.png`（交件單 `flight/city/_ravnsdal_plan.md`） |

### 交件清單（ver -1163）

`resources/background/Varn_<X>.webp` —— 1536×1024 原生、webp q85、**11 支**：

    Square  Midtown  Oldtown  Uptown  Firearm  Guild  Bistro  Grocerie  Hotel  Lookout  Station

⚠⚠ **檔名不帶時段尾綴**（不是 `_day`）：那 11 格程式端要寫 `noTime:true`，
而 `noTime` ＝**只試不帶時段的那一個名字** —— 帶了 `_day` 就整排 404
（同聖索菲亞那 12 張的作法）。時段差分交件時再一起拿掉 `noTime`、改成帶尾綴的四張。

⚠ `Varn_Church` **還沒有**（留白）—— 程式端掛 `bgPending:true`。

### 顆粒量測（平坦區高頻，相鄰差分 RMS）

| 格 | | 格 | | 格 | |
|---|---|---|---|---|---|
| Lookout | 6.28 | Hotel | 8.04 | Oldtown | 9.67 |
| Guild | 7.39 | Grocerie | 8.95 | Square | 9.81 |
| Firearm | 8.18 | Bistro | 10.94 | Midtown | 12.64 |
| Station | 12.82 | Uptown | 13.09 | | |

校準點：`Sofia_Square` 4.31（同類的城鎮圖，已驗收）／`Fallen_Altar_day` 6.13。

⚠⚠ **數字比聖索菲亞高，但不是顆粒**（§5 ver -906 的同一個結論）：
100% 裁切並排看，石面是整塊平面＋硬邊陰影、鐵件有乾淨的深色描邊、殘雪是有形狀的色塊，
**沒有斑點雜訊**。高出來的來自**這一組本來就密**的資訊 —— 鵝卵石鋪面的石縫、
成百的窗、電車架空線、老虎窗。那是有形狀有邊界的資訊，不是雜訊。

---

## 七、⚠⚠ 飛行地圖的平面圖與城鎮內容**對不起來**（ver -1220，Ray 交辦）

> Ray：「Ravnsdal 的飛行地圖跟城鎮內容不同，**城鎮內容有雪**，讓美術改個色」

**量出來的落差**（「雪」＝亮且低彩的像素：`max(rgb)>200` 且 `max−min<30`）：

| | V | S | 雪 |
|---|---|---|---|
| `Varn_Square`（入口） | 0.556 | 0.270 | **6.1%** |
| `Varn_Lookout`（俯瞰全城） | 0.524 | 0.220 | **6.3%** |
| `Varn_Uptown` | 0.547 | 0.259 | **5.0%** |
| `Varn_Midtown` | 0.539 | 0.288 | 2.7% |
| 其餘七格 | 0.31~0.45 | 0.17~0.23 | 0.5~4.3% |
| **`flight/city/ravnsdal_plan.webp`（成品）** | 0.489 | 0.236 | **0.0%** |

⇒ 城裡在下雪／積雪，**從空中看下去卻一片雪都沒有**。瞭望台那一格更尷尬：
它畫的就是「站在護牆這一側往下看整座城」——那正好是飛行地圖那張圖的同一個景。

### 建議的改法：**改那一張俯視圖，不要改十一張城鎮圖**

- 要改的是 `flight/city/Ravnsdal_topdown.png` **一張**；反過來要把雪從城鎮圖拿掉是 **11 張**，
  而且會把規格 §二要的「北歐・冷、屋簷與石縫有殘雪」整個拆掉。
- 改法＝**在俯視圖上加雪**（等價於 Ray 說的「改個色」）：
  · 屋頂受雪的那一面與屋脊 → 冷白（積雪），背陽面留原本的赭紅／銅綠
  · 街道、廣場、碼頭鋪面 → 車轍與人行的地方露出濕深色，其餘壓成冷灰白
  · 城緣的空地／園圃 → 大面積的雪白
  · 水面（河港）**不結冰**（船要進得來）—— 深冷藍，岸邊一圈碎冰即可
  · ⛔ 一樣不要旗幟與國標（§二的硬禁令照舊）
- 交件後程式端跑 `py flight/build_city.py ravnsdal`，再照 §6.7.5 步驟 5 量**成品**。

### 目標數字（給下一次量測用）

- **雪的比例**：成品 `ravnsdal_plan.webp` 要落在 **4~6%**（＝三個室外樞紐的量）。
- **色調**：⚠ §6.7.5 步驟 5 那條「落在北方泊地 0.401/0.135 與帝都 0.422/0.235 之間」
  是對**沒有雪**的城訂的 —— 雪城本來就該更亮更不飽和，**這一條不適用**。
  暫訂 **V 0.50~0.55 / S 0.15~0.18**（亮度對齊城裡的室外格、彩度因為積雪往下），
  實際以 Ray 看了之後的判斷為準。
- ⚠ `build_city.py` 的 JOB 有 `sat`／`val` 兩個旋鈕，`SETTLEMENTS` 還有 `tone` ——
  **不要用它們去「兜」出雪**：那只會把整張圖洗白，屋頂的受雪面與背陽面還是同一個色。
  雪是**畫上去的**，不是調出來的。

---

# ⚠⚠⚠ ver -1487：**改名「雪都瓦恩霍姆」＋ 擴建五格**（Ray 交辦）

> Ray（2026-09-18）：「**拉芬改名雪都瓦恩霍姆，加入圖書館 餐飲街 甜品店 餐酒館 咖啡廳**」

## 一、改名

| | 舊 | 新 |
|---|---|---|
| 顯示名（`TOWNS.ravnsdal.name`） | 拉芬斯達爾城 | **雪都瓦恩霍姆** |
| 各節點的 `name` 前綴 | `拉芬斯達爾　…` | **`雪都瓦恩霍姆　…`**（12 格全部） |

⚠⚠ **地圖 id（`ravnsdal`）與檔名前綴（`Varn_`）建議不動** —— 這是**美術的建議，Ray 決定**：
· 改 id 要一起動：旗標（`safehouse_ravnsdal` 那一族）、腳本裡的節點指向、
  31 張背景檔名、`ASSET_VER` 的鑰匙、`script/bg_index.js`、`tools/map_layout.py` 的 POS ——
  而**其中任何一處漏掉的症狀都是「那一格整個空白，畫面上沒有錯誤訊息」**（§6.5.4 的 -910）。
· 前例就在憲法 §6.5.4.2：帝都的餐酒館改名成「餐飲街」時，
  **「節點 id 仍是 `tavern`…只有 `name` 改」** —— 理由一模一樣。
· 代價只有一個：檔名前綴與新名對不上，下一個人會困惑 ⇒ **這一段就是給他看的**。

⚠ **西文拼法還沒定**：美術建議 **`Varnholm`**（`-holm` ＝北歐語的「小島／河洲」，
  Stockholm／Bornholm 那一族的後綴，與「雪都」的港市設定合得上）。
  ⚠ **撞名檢查還沒做**（上面那一節對 Ravnsdal 做過一次）—— 要用在畫面上之前補做。
  ⚠ 只有**畫面上會出現中文**（`name`），所以西文拼法目前只影響文件與日後的檔名。

## 二、擴建：餐飲街**改成走得進去的節點**（東方泊地那一套）

現在是 **ver -1263 之前的舊分店機制**（`dining.scenes`：一格三張圖，
誰在店裡就換哪一張）。東泊已經照 Ray 的「進去多加三條路線」改成節點了，這一座還沒。

**新的 17 格**（原 12 ＋ 圖書館 ＋ 餐飲街底下三家）：

| 節點 id | 掛在哪 | 中文名 | `bg` | 圖 |
|---|---|---|---|---|
| `library` | **`midtown` 的 `up`**（它現在空著） | 雪都瓦恩霍姆　圖書館 | `Varn_Library` | ✔ **本輪交件** |
| `tavern`（改） | `uptown` 的 `right`（原樣） | 雪都瓦恩霍姆　餐飲街 | **`Varn_Dining`**（室外街景） | ✔ **本輪交件・四差分到齊** |
| `bar` | `tavern` 的 `up` | 雪都瓦恩霍姆　餐酒館 | `Varn_Bistro` | ✔ 已有（原本是 tavern 的 bg） |
| `cafe` | `tavern` 的 `right` | 雪都瓦恩霍姆　咖啡廳 | `Varn_Cafe` | ✔ 已有 |
| `dessert` | `tavern` 的 `down` | 雪都瓦恩霍姆　甜品店 | `Varn_Dessert` | ✔ **本輪交件** |

```
tavern:  { back:'uptown', up:'bar', right:'cafe', down:'dessert' }
bar / cafe / dessert:  { back:'tavern' }
midtown: { left:'lookout', right:'church', down:'square', up:'library' }
library: { back:'midtown' }
```

⚠⚠ **`dining:{...}` 那一段要整個拿掉** —— 它與新的節點是**同一件事的兩個真相**（鐵律 7）：
留著的話「誰在店裡」還會去換 `tavern` 那一格的背景，而那一格現在是室外街景。

⚠ **`Varn_Restaurant.webp` 已經有，但 Ray 這一次沒點名餐廳** ——
  要不要也開一格（東泊有），**問 Ray**。不開的話那張圖就閒置（不必回收，日後可能用得上）。

⚠⚠ **`noTime` 逐格看**（同上面 -1293 那一段）：
  · `Varn_Dining` 是**室外**＝四差分 ⇒ **不要寫 `noTime`**
  · `Varn_Library`／`Varn_Dessert`／`Varn_Bistro`／`Varn_Cafe` 是**室內單張** ⇒ **要寫 `noTime:true`**

## 三、⚠ `tools/map_layout.py` 的 `POS`：**這座城整個沒有** ⇒ 小地圖做不了

美術這一邊已經備好圖示表（`resources/map/_icons_eastport.png` ＋ `_icons_extra.png`
有圖書館／餐酒館／餐廳的符號），**POS 補上就能合成**。17 格的建議版面
（相對位置＝出口方向，已逐條驗過）：

```
                 library (5,2)
station (2,4)  lookout (3,4)  midtown (5,4)  church (7,4)   inn (8,4)   bar (10,4)
gunstore(0,6)  oldtown (2,6)  square  (5,6)  uptown (8,6)   tavern(10,6) cafe(11,6)
               guild   (2,8)                 grocery(8,8)   dessert(10,8)
```

驗算：`midtown.up→library` 在上 ✔／`tavern.up→bar` 在上、`right→cafe` 在右、
`down→dessert` 在下 ✔／其餘 12 格照舊。

## 四、本輪交件（ver -1487）

```
resources/background/ravnsdal/
  Varn_Dining_{day,dusk,night,dawn}.webp   ← 餐飲街（室外街景・雪）**四差分到齊**
  Varn_Dessert.webp        ← 甜品店（玻璃展示櫃・塔式蛋糕・白桌巾小圓桌・窗外雪景）
  Varn_Library.webp        ← 圖書館（頂天書架・鑄鐵螺旋梯・綠罩台燈・高拱窗・鑄鐵火爐）
```

⚠ 畫風底圖：餐飲街用 `Varn_Uptown_day`、甜品店用 `Varn_Cafe`、圖書館用 `Varn_Restaurant`
—— 都是**同一座城**的圖，所以筆觸、雪的畫法、燈光色溫一致。
⚠ 三張都明寫了「**不要任何旗幟與國標**」（這座城的硬規定）與「不要文字」。

## 五、還缺的（**要 Ray 定才動**）

| 件 | 狀況 |
|---|---|
| `Varn_Church` 大教堂 | 上面那一節就標 ⬜ **Ray 指定先留白**，這一輪沒動 |
| `Varn_Bistro` 的 dusk／night | 憲法 ver -1378：**酒吧是三差分**（`[8,24]`）。現在只有單張 —— 但那一條也說「**營業時間是資料，美術先確認資料再決定交幾張**」，而這座城的 `hours` 還沒設 ⇒ **等程式端設好 `hours` 再補** |
| 甜品店／圖書館的差分張數 | 同上：憲法 -1378 **沒有點名**這兩類（只點名餐廳／公會／槍店／雜貨店＝只有 day，酒吧＝三張，旅店＝四張）⇒ 先交單張，`hours` 定了再說 |
| 三家分店的路人語 `chatter` | 內容，Ray 還沒給（§6.5.4.2：沒寫就回去用節點那一組，**不要自己編**） |


---

# ⚠⚠⚠ ver -1488：**前綴改成 `Varn_`** ／ 餐酒館不差分（Ray 定案）

> Ray（2026-09-18）：「**前綴改掉**，**bistro 完全室內不差分**」

## 一、檔名前綴 `Ravn_` → **`Varn_`**（37 張）

⚠ 上一節美術建議「前綴不動」，**Ray 否決了** —— 以他的決定為準。
拼法取 **Varnholm**（`varn` ＋ `-holm`，Stockholm／Bornholm 那一族的後綴）。
⚠ **撞名檢查仍然沒做**，要用在畫面上之前補做。

⚠⚠ **地圖 id（`ravnsdal`）與資料夾名沒有一起改** —— Ray 說的是「前綴」。
  id 是內部的鑰匙（旗標、腳本、POS 都指著它），改它是另一件事，**要改請明講**。

### ⚠⚠⚠ 過渡期：**舊檔名先留著，不要立刻回收**

照 `HANDOFF.md`「東泊那三件」的第 3 項：
> 「**無尾綴的舊檔先留著**，等 1 做完才走 `tools/recycle.sh` 退役 ——
>   先退會讓那幾格當場變空背景。」

所以這一版 `resources/background/ravnsdal/` 裡**兩套檔名並存**（`Ravn_*` 37 張
＋ `Varn_*` 37 張）。**程式端把下面三處的字串改完之後**，再走 recycle 退役 `Ravn_*`。

### ⚠ 程式端要改的三處（**美術不動，鐵律 11**）

| 檔 | 處數 | 改什麼 |
|---|---|---|
| `script/town.js` | **27** | `TOWNS.ravnsdal` 各節點的 `bg`：`Ravn_…` → `Varn_…` |
| `tools/bg_index.py` | **1** | 第 38 行的對照表 `('Ravn_', 'ravnsdal')` → `('Varn_', 'ravnsdal')`，**改完重跑一次**產出 `script/bg_index.js` |
| `modules/town.js` | 4 | **只是註解舉例**（`Ravn_Square_Day.webp`／`Ravn_Station.webp`），不影響功能；順手改比較不會誤導 |

⚠⚠ 漏掉任何一處的症狀是**那一格整個空白，畫面上沒有任何錯誤訊息**（§6.5.4 的 -910）
—— 所以才要留過渡期的舊檔：**兩套並存時漏改只是「還在吃舊圖」，不是空背景。**

## 二、餐酒館 `bar`：**完全室內，不差分**（Ray 定案）

`Varn_Bistro.webp` **一張就是全部**，不補 dusk／night。
⚠ 這**推翻**憲法 §5 ver -1378 對酒吧那一條（`[8,24]` ⇒ 三差分）**在這座城的適用** ——
Ray 的理由是「完全室內」（那間酒館沒有對外的窗，光不隨時段變）。
⇒ 節點要寫 **`noTime:true`**，不寫就白吃 dusk／night 的 404。
⚠ 同理，`cafe`／`dessert`／`library`／`gunstore`／`guild`／`grocery` 也都是單張 ⇒ 全部要 `noTime:true`。
⚠ **`tavern`（餐飲街）是室外街景、四差分 ⇒ 千萬不要寫 `noTime`。**

## 三、⚠ 這座城**還欠的只剩兩件**（ver -1488 實測盤點，不是憑印象）

掃 `script/town.js` 的 16 格 × 目錄裡實際有的檔案：

| 件 | 狀況 |
|---|---|
| ~~旅店 `Varn_Hotel` 的四差分~~ | ✔ **本輪補齊**（`_dawn` `_day` `_dusk` `_night`）。⚠ **四張全畫、不是補三張** —— `Day` 的退路是 `Dawn`（`BAND_FALL`），少了 day 那張白天會吃到清晨那一張。無尾綴那張留著當萬用退路 |
| **大教堂 `Varn_Church`** | ⬜ Ray 早先指定**先留白**，現在借 `Varn_Midtown`（節點的 `bg` 就是它）。要畫再說 |

**⇒ 現在只剩大教堂一件。** 其餘 15 格全齊：室外六格＋餐飲街各四差分／**旅店四差分**／
室內七格各一張（武器店・公會・雜貨舖・餐酒館・咖啡廳・甜品店・圖書館）。

⚠ `band_audit` 對 `Varn_Hotel_dawn` 報「細節被抹平 0.52」—— **肉眼複核過是誤報**：
  構圖相關度 0.994（很高），清晨那張本來就是**無硬影的漫射光**，梯度弱、分數自然低。
  書架／鑰匙櫃／地毯花紋一項都沒少。⚠ 同一支對 `night`（更暗）沒報 ——
  **差別在對比不在亮度**，這是那支工具的已知性質（憲法 §5：數字只用來挑可疑的，肉眼下判斷）。
