# 槍棺小地圖 — 待做的兩座城（ver -1488，程式端 → 美術）

> ## ✔ **ver -1490：兩座城都交件了**（美術）
> ```
> resources/map/map_eastport.webp   ＋ _spots_eastport.json   ＋ _icons_extra.png/_icons_extra2.png
> resources/map/map_ravnsdal.webp   ＋ _spots_ravnsdal.json   ＋ _icons_ravnsdal.png（16 格專用表）
> ```
> · 東泊：沿用既有的 13 個圖示 ＋ 補充表的餐廳／咖啡廳／甜品店（第三節那三格 ⬜ 已補）
> · 雪都：**整張新的 16 格圖示表**（照第三節的建議符號，屋頂都加了一道積雪的短橫）
> · 紙：雪都是**冷灰調**、東泊是**暖褐調** —— 並排一眼分得出是兩個地方（第二節①的要求）
> · ⚠⚠ **程式端要接**：`_spots_*.json` 照抄進 `town.js` 的 `map.spots`（兩座城都是**新增**，
>   沒有舊座標要換，也不必加 `?v=`）。
> · ⚠ 這一輪合成器多了一個 `spread:'raw'`（**用 POS 的實際欄號等比展開，不要壓成均勻**）——
>   16 格 8 欄的城壓成均勻的話，`station` 與 `lookout` 會疊在一起。**格數多的圖（古墓 34 格）
>   仍然要壓**，兩種都要留著。


> Ray（2026-09-18）：「**把東泊拓樸給美術**，雪都改名瓦恩霍姆，並加入餐飲街三分支同東泊」

畫風、分工、驗收一律照 **`_map_spec.md`**（那是權威，這一份只補「這兩座城要什麼」）。
**拓樸已經定案、佈局圖已經產好** ⇒ `_map_spec.md` §「先排小地圖再畫場景」那條的前置條件
到齊了，可以開畫。

---

## 一、要畫的兩座城

| 城 | id | 格數 | 佈局圖（**權威**） | 現況 |
|---|---|---|---|---|
| **東方泊地** | `eastport` | **16 格・15 邊・環 0** | `resources/map/_layout_eastport.png` | 圖示表有 13 個，**差 3 個**（見下） |
| **雪都瓦恩霍姆** | `ravnsdal` | **16 格・15 邊・環 0** | `resources/map/_layout_ravnsdal.png` | 圖示表**整張還沒有** |

⚠⚠⚠ **兩張佈局圖都是 `tools/map_layout.py` 從 `script/town.js` 讀出來的**，不是手畫的
（憲法 ver -909）。**拓樸以它為準，不要照舊版記憶畫** ——
· `_layout_eastport.png` 在 9/17 之前是 **13 格的舊檔**（餐飲街那三格 -1318 就進資料了，
  `POS` 一直沒補 ⇒ 工具報「POS 少了這幾格」而**不出圖**，所以留在磁碟上的是舊檔不是壞檔）。
  **-1488 已補、已重產。**
· `ravnsdal` 的 `POS` 以前**整個不存在** ⇒ 小地圖做不了。**-1488 已補。**

⚠ 兩座城的拓樸現在是**同一個形狀**（都是 16 格・5 個抉擇點・11 個末端，餐飲街都是四向樞紐）
—— 但**畫面上要看得出是兩個地方**（東泊＝地中海港都，雪都＝北歐雪港）。

---

## 二、美術要交的兩樣（**每座城各一套**）

照 `_map_spec.md` §「形狀交給人／幾何交給程式／畫法交給模型」：
**墨點、連線、草書地名的位置全部由程式算**，模型只出「畫法」的兩樣東西。

### ① 空白羊皮紙 `<城>_paper.png`
1536×1024、撕邊外 alpha 透明、**上面什麼都不要畫**（不要邊框裝飾、不要指北針、不要文字）。
可以沿用既有的那一張（`resources/_originals/map/map_tomb_paper.png`）——
但雪都建議另出一張**冷調**的，跟東泊分得開。

### ② 圖示貼紙表 `_icons_<城>.png`
⚠⚠⚠ **格式是硬的**：`tools/map_compose.py` 把整張切成 **6 欄 × 6 列**、
**照閱讀順序**（左→右、上→下）配給節點。所以：
- **一格一個符號、置中、白底黑墨**，格與格之間留空（程式會逐格裁到最小外框）
- **順序要與下面那張表一字不差**（多一格少一格都會整排錯位）

**畫法**（`_map_spec.md` §「鋼筆簡筆」＋憲法 §5「小地圖 ＝ 隨手筆記」）：
- **約 5 筆（3~6 筆）**畫完，單線、粗細有變化、**直線也要自然歪斜**
- **不上陰影、不畫透視、不畫材質、不畫版畫感**
- 判準：縮到 40px 還讀得出來；要瞇眼才看得出來＝畫太多了
- ⚠⚠ **抽象＝換成剪影，不是畫小**：提示詞逐格寫「這個符號由哪幾筆組成」，
  只寫「極簡」模型的預設一定是插畫

---

## 三、圖示順序（**照抄，不要重排**）

### 東方泊地 `_icons_eastport.png`（16 格）

前 13 個**已經有了**（`_icons_eastport.png` 現況），只是排成 4 欄 —— 要**重排成 6×6**
並補上最後三個：

| # | 節點 | 中文 | 現況 |
|---|---|---|---|
| 1 | `square` | 港口廣場 | ✔ 噴泉 |
| 2 | `midtown` | 中心區 | ✔ 雙塔 |
| 3 | `church` | 主教座堂 | ✔ 尖塔十字 |
| 4 | `cityhall` | 海關署 | ✔ 列柱山牆 |
| 5 | `university` | 大學 | ✔ 鐘樓 |
| 6 | `oldtown` | 舊城區 | ✔ 兩棟斜屋頂 |
| 7 | `gunstore` | 武器店 | ✔ 長槍 |
| 8 | `dock` | 南門驛站 | ✔ 帆船與棧橋 |
| 9 | `guild` | 賞金獵人公會 | ✔ 盾牌打叉 |
| 10 | `uptown` | 上城區 | ✔ 兩棟屋 |
| 11 | `tavern` | **餐飲街** | ✔ 高腳杯 |
| 12 | `grocery` | 雜貨舖 | ✔ 木桶 |
| 13 | `inn` | 旅店 | ✔ 掛招牌的屋 |
| **14** | `restaurant` | **餐廳** | ⬜ **要畫**（例：一個盤子＋一把叉一把刀，三筆） |
| **15** | `cafe` | **咖啡廳** | ⬜ **要畫**（例：一個杯子＋杯耳＋兩道熱氣，四筆） |
| **16** | `dessert` | **甜品店** | ⬜ **要畫**（例：一塊三角蛋糕＋上面一顆圓，三筆） |

⚠ 既有那 13 個的畫法**就是對的標準**（單線、歪斜、五筆上下）—— 新的三個照它畫。

### 雪都瓦恩霍姆 `_icons_ravnsdal.png`（16 格，**整張要畫**）

| # | 節點 | 中文 | 建議符號（約 5 筆） |
|---|---|---|---|
| 1 | `square` | 港口廣場 | 一個廣場方框＋中央一根柱 |
| 2 | `midtown` | 中心區 | 兩棟陡屋頂（北歐坡度要陡） |
| 3 | `church` | 大教堂 | 一個尖塔＋十字 |
| 4 | `lookout` | 瞭望台 | 一座塔＋頂上一盞燈 |
| 5 | `library` | **圖書館** | 一本攤開的書（兩道弧＋中縫） |
| 6 | `oldtown` | 舊街區 | 一棟屋＋一段石階 |
| 7 | `gunstore` | 武器店 | 長槍（同東泊那一支） |
| 8 | `station` | 火車站 | 一節車廂＋兩個輪＋一道煙 |
| 9 | `guild` | 賞金獵人公會 | 盾牌打叉（同東泊） |
| 10 | `uptown` | 上街區 | 兩棟屋（比中心區矮） |
| 11 | `tavern` | **餐飲街** | 高腳杯（同東泊） |
| 12 | `bar` | **餐酒館** | 一個酒瓶＋一個杯 |
| 13 | `cafe` | **咖啡廳** | 杯子＋熱氣 |
| 14 | `dessert` | **甜品店** | 三角蛋糕 |
| 15 | `grocery` | 雜貨舖 | 木桶 |
| 16 | `inn` | 旅店 | 掛招牌的屋 |

⚠ 這座城**在下雪**：屋頂可以加一道積雪的短橫（**一筆就好**，不要畫雪花）。

---

## 四、交件之後程式端做什麼（美術不必管，寫在這裡是為了讓你知道**不必量座標**）

    python3 tools/map_compose.py eastport --paper <紙.png> --icons _icons_eastport.png
    python3 tools/map_compose.py ravnsdal --paper <紙.png> --icons _icons_ravnsdal.png

→ 出 `map_<城>.webp` ＋ `_spots_<城>.json`，程式端再把 `map:{img,spots}` 接進
`script/town.js`。**墨點與連線是算出來的，不可能多也不可能少**（鐵律 7）。

⚠⚠ 所以**圖示表上不要畫墨點、不要畫連線、不要寫地名** —— 那三樣都是程式畫的，
畫上去會疊成兩份。

---

## 五、⚠ 交件之後**一定要重跑** `python3 tools/bg_index.py`

這一輪的真實教訓（-1488）：雪都那三批新背景（`Ravn_Dining_*`／`Ravn_Library`／
`Ravn_Dessert`）檔案都在、`curl` 也是 200，遊戲裡卻**三格都停在前一格的背景** ——
因為 `script/bg_index.js`（基底名 → 區域資料夾的索引）**沒有重跑**，
查不到就退回根目錄，而圖在 `ravnsdal/` 子夾裡。

⚠⚠⚠ **那個症狀不是「空白」是「上一格的圖」**，畫面上沒有任何錯誤訊息，
從截圖上看起來就像「美術還沒交」。**交了新背景就重跑那一支**，一行指令的事。

> ## ✔ **2026-09-24：鏡湖與卡耶爾山谷交件了**（美術，ver -1712 之後）
> ```
> resources/map/map_lake.webp     (1000×1024) ＋ _spots_lake.json    ＋ _icons_lake.png   （10 格・4×3）
> resources/map/map_canyon.webp   ( 700× 830) ＋ _spots_canyon.json  ＋ _icons_canyon.png （ 5 格・3×2）
> ```
> · 兩張都是 `tools/map_compose.py` 合成（紙沿用 `_originals/map/map_tomb_paper.png`，同野外那幾張）：
>   `map_compose.py lake   --cols 4 --rows 3 --order inlet,shingle,fallbase,cave,grove,eastshore,deadfall,northshore,boathouse,causeway`
>   `map_compose.py canyon --cols 3 --rows 2 --order entry,corridor,bones,bridge,altar`
> · 圖示表由 ChatGPT 出（逐格寫明幾筆），**本機以「亮度<200」二值化**成黑白貼紙表再餵合成器
>   —— 這一趟的圖是從另一台 Chrome 經頁面文字（逐列轉場點 RLE）搬回來的，原始 RLE 存
>   `_originals/map/map_{lake,canyon}_icons_rle.txt`（沒有彩色原檔，合成器本來就只吃墨線）。
> · ⚠⚠ **程式端要接（兩座都是新增，沒有舊座標、不必 `?v=`）**：`script/town.js` 的
>   `lake` 與 `canyon` 各補一個 `map:{ img, spots }`，`spots` 照抄 `_spots_<id>.json`：
>
>       lake:   map:{ img:'resources/map/map_lake.webp',
>                     spots:{ inlet:[0.431,0.915], shingle:[0.431,0.7212], fallbase:[0.131,0.7212],
>                             cave:[0.131,0.5273], grove:[0.131,0.3335], eastshore:[0.431,0.5273],
>                             deadfall:[0.731,0.5273], northshore:[0.431,0.3335],
>                             boathouse:[0.731,0.3335], causeway:[0.431,0.1396] } }
>       canyon: map:{ img:'resources/map/map_canyon.webp',
>                     spots:{ entry:[0.1871,0.8952], corridor:[0.1871,0.6542], bones:[0.1871,0.4133],
>                             bridge:[0.6157,0.4133], altar:[0.1871,0.1723] } }
>
>   兩張圖都**沒寫 `mist:0`** ⇒ 照預設有霧（荒野走過才亮），與夏爾森林同。
