# 交接：驗證「本機 matting」能不能取代 GPT 去背（ver -1503）

> **給 3070 那台的 session。** Ray：「用 gpt 去背成本太高，他的 token 應該要更有效使用，
> 我要的是 **gpt 的 alpha 邏輯的本地工具**」「我去 3070 裝 claude app 你去那邊跑吧，
> 那邊 SD 已經建置好了」

**先讀憲法 §5 的去背那一段**（`CLAUDE.md`，搜「你的 alpha 永遠都是不及格」）—— 那是
這件事的由來，而且**是我（美術 session）搞砸的紀錄**，不要重蹈。

---

## 一、要證明什麼

**一句話**：`matting` 能不能做出與 GPT 重繪等價的 alpha。

Ray 的判斷是「GPT 重繪是完美 alpha 的唯一解」。他說得有道理 —— GPT 的邊緣乾淨，
是因為它**重畫了前景色**：髮絲邊緣那個像素，它畫的是「頭髮真正的顏色」，
不是「頭髮混了白底之後的顏色」。

**matting 在結果上等價，機制不同**：影像合成方程式 `I = αF + (1−α)B`，
背景 B 已知是白，matting **同時解出 α 與真正的前景色 F**。
解出來的 F 就是那根髮絲真正的顏色 —— 與 GPT 重畫出來的是同一個東西。
而且它**不重畫，所以角色 100% 不可能變**（LayerDiffuse 那條的弱點正是這個，
Ray 試過：「印象中不理想」）。

## 二、⚠⚠⚠ 我上次是怎麼失敗的（不要再做一次）

我交了 78 張白霧的立繪。**不是 matting 失敗 —— 是我根本沒做 matting**：

| | 我上次做的 | 應該做的 |
|---|---|---|
| α | 亮度門檻 ＋ 連通性硬判 | 訓練過的模型 |
| **F（前景色）** | **完全沒解**（邊緣像素的顏色原封不動） ← **白霧的來源** | 前景估計（`pymatting` 的 `estimate_foreground_ml`） |

⚠⚠ **而且我的驗收是假的**：我確實照憲法疊了深色棋盤，但是**縮成 220px 縮圖**看的。
縮到那個尺寸白霧根本看不見，78 張全部放行，Ray 一眼就看出來。
⇒ **看 alpha 一律 100% 裁切看邊緣。**

## 三、Ground truth（這是關鍵，不要用自己的判斷當驗收）

**Ray 自己交的 22 張立繪已經有正確的 alpha**（產圖端直接輸出的），全部已入版控：

    resources/SI/Cecilie_SI_*.webp     9 張
    resources/SI/Laurie_SI_*.webp      5 張
    resources/SI/Nemo_SI_*.webp        6 張
    resources/SI/Sorana_SI_drink.webp  1 張
    resources/SI/Sorana_SI_shy.webp    1 張

**評測流程**：

1. 讀那 22 張 RGBA → **合成到純白底**（`out = rgb*α + 255*(1−α)`），存成 PNG
   —— 這就是「我們手上會有的輸入」
2. 丟給待測工具去背
3. **與原始 alpha 逐像素比對**

**三個指標 ＋ 過關線**（Ray 面前先寫死，不要事後才訂）：

| 指標 | 怎麼算 | 過關線 |
|---|---|---|
| **近白比例** | 半透明像素（8<α<200）中 `min(RGB)≥235` 的比例 | **≤ 1%** |
| α 平均絕對誤差 | `mean(|α_pred − α_gt|)` | ≤ 3（0~255 尺度） |
| 髮絲區 IoU | `α>16` 的二值 IoU | ≥ 0.97 |

**對照基準**（已實測，`resources/_HANDOFF_ART_20260919.md` 有）：
· Ray 交的（產圖端 alpha）：近白 **0.0%**
· 我上次那 78 張：近白 **18~65%**  ⛔
· 我用 unpremultiply 修過的：近白 **6.3%**  ⛔ 仍然不及格

⚠⚠ **最後一定要挑最難的幾張 100% 裁切貼給 Ray 看**（蘿芮的長捲髮、賽西莉的長髮、
尼莫的銀髮），**不要用縮圖**。數字過了但眼睛看得出白邊，就是沒過。

## 四、要測的工具（依序）

| 工具 | 授權 | 備註 |
|---|---|---|
| **BiRefNet** `ZhengPeng7/BiRefNet` | **MIT** ✓ | 2024 SOTA，髮絲最強；有 `-matting` 權重 |
| **anime-segmentation** (SkyTNT `isnetis`) | **Apache-2.0** ✓ | 專為動漫角色訓練 |
| **＋ `pymatting.estimate_foreground_ml`** | MIT ✓ | **解白霧的那一步，不可省** |
| ~~RMBG-2.0~~ (BRIA) | ⛔ **non-commercial** | 效果好但**這遊戲要上架**（憲法鐵律 12），**不准用** |

⚠⚠ **授權要逐個確認**：這個專案最終要打包上架賣（憲法鐵律 12），
non-commercial 的模型一律不能進產線，效果再好都不行。

⚠ 那台 SD 已經建好 → 如果 matting 三個指標都過不了，**才**考慮 LayerDiffuse 那條
（Flux 版 ＋ 低 denoise ／ ControlNet 鎖角色）。但 Ray 已經試過 SD 版說不理想，
**不要從那裡開始**。

## 五、⚠⚠ 過關之後的事（傳檔會卡住，先想好）

要重做的是 **78 張**：四主差分 72 ＋ 阿瑞尼斯 6。

⚠⚠⚠ **白底原稿在 `resources/_originals/SI_ext/*_white.png`，而 `_originals/` 是 gitignore 的**
—— **3070 那台 clone 不到**。三條路，由 Ray 決定：

1. 從**已交的 webp** 反推白底 ⛔ **不行** —— 那些 webp 就是我做壞的那一版，
   邊緣已經被污染，反推出來的白底會把錯誤帶進去。
2. 把 78 張白底原稿**臨時推上版控**（約 100MB），跑完再回收。
3. 走區網／USB／雲端硬碟直接傳。

⚠ 換上新 alpha 是**同名覆蓋**（那 78 張**已經接進 `speakers.js`**，程式端 ver -1507／-1509）
⇒ 要跟程式端說一聲，處理快取（憲法 §5 的 `?v=`／`ASSET_VER`）。

## 六、⚠ 這件事的意義（值得做的理由）

· **GPT**：一張要燒額度、要排隊，而且額度是**畫新圖**用的
  （Ray：「明天可能又畫不了幾張圖就要停工了」）
· **matting**：3070 上每張 **1~3 秒**、電費以外零成本，78 張三分鐘跑完，
  **而且之後每一批新圖都免費**

⇒ 這不只是救那 78 張，是把「去背」這一項從產線的成本裡永久拿掉。
