# 美術 → 程式 交接（2026-09-22 晚，ver -1670 之後）

> Ray：「把這些狀況交接給 code」
> ⚠ 美術 session 一行程式都沒碰（鐵律 11）。下面全部是**程式端要做的**。
> ⚠ 細節與實測數字在 `resources/si/_sorana_r3_worklist.md` 與
>   `resources/background/_tomb_fill_worklist.md`，這一份只列「要改哪裡」。

---

## 一、`config.js` 的 `ASSET_VER` —— 古墓七張，**同名覆蓋**

七格全部改畫成寬廣場地（Ray 交辦）。**不跳版號玩家的快取裡還是舊的窄圖，
而且畫面上不會有任何錯誤訊息**（§5 的 -650／-905）：

    'tomb_vaultw':        2,
    'tomb_bonepit':       2,
    'tomb_gallery3':      2,
    'tomb_landing3':      2,
    'tomb_crypt':         2,
    'tomb_lowaltar':      2,
    'tomb_lowaltar_off':  2,

⚠ **節點資料一個字都不用動** —— 七張的基底名全沒變，`lowaltar` 的 `bgWhen` 也照舊。

⚠ 通道數是照 `exits` 逐格畫的（工單的硬規格「出口數 − 1」）：
`gallery3` 3 出口 → 畫 2 條；`crypt` 1 出口 → **畫 0 條**（死胡同）；
`landing3` 那一條是**石階**不是門洞。

---

## 二、`script/speakers.js`

### ⚠⚠⚠ 2.0 先讀這一段：**索拉娜第四輪（19 張）覆蓋掉下面 2.1 的一部分**

Ray 在同一個晚上又交了 **19 張**（其中 **13 張換成 1024×1600 的新畫布**）。
**逐項的 top／bot／fx ＋ 版號在 `resources/si/_sorana_r3_worklist.md` §十一那張表** ——
那一張是**最新的**，與 2.1 衝突時以它為準。

三件最容易做錯的：

1. **版號不是一律 `?v=2`**：線上已經掛 `?v=2` 的 12 張要跳 **`?v=3`**；
   線上沒有版號的 4 張（`amaze`／`crybig`／`shy`／`carrynouvelleshock`）加 **`?v=2`**；
   3 個新鍵（`carrynouvellejealous`／`cryclose`／`talksmile`）不必版號。
2. ⚠⚠ **`amaze` 回到要跳版號了**：2.1 說它被退件、從表上拿掉 —— 那之後 Ray
   **又交了一張新的 `amaze`（1600 畫布）**，所以現在要 `?v=2`。
   （`tease` 仍然**不要**跳 —— 它沒有新圖。）
3. ⚠⚠⚠ **13 張的 `top`／`bot` 一定要照新表改**：畫布由 1536 變 **1600**，
   舊的 `top`／`bot` 是**另一張畫布的像素**，照舊值算出來的取景一定歪。

### 2.1 索拉娜的版號（**15 條**）

### 2.1 索拉娜的版號（**15 條**）

同名覆蓋，不跳版號等於沒換圖。清單在 `_sorana_r3_worklist.md` §一：

- **加 `?v=2`（13 張）**：`battlecry` `shy` `sorry` `surprise` `talk` `think` `tire`
  `upset` `watch` `whisper` `remind` `smirk` `laugh`
- **改成 `?v=3`（2 張）**：`side` `smile`
  （它們現在的 `?v=2` 是上一輪白掛的，這次才是真的覆蓋）

⚠⚠⚠ **`tease` 與 `amaze` 都不在表上**（ver -1670 後 Ray 兩張都退件）：重製版被退、檔案**已還原回覆蓋前
那一份**（sha1 逐位元相同）。線上與玩家快取裡本來就是同一個 ⇒ **加 `?v=` 是白掛的**，
而且會害下一個人以為它重製過了。

⚠ `battlecry` 在這一輪被覆蓋了**兩次**（第三輪重製 ＋ 六張表情差分的基底），
但它本來就在「加 `?v=2`」那張表上且尚未被接 ⇒ **仍然只要 `?v=2`**。

### 2.1b ⚠ `die` 要跳 **`?v=3`**（與上面那幾張不同）

Ray 退了 `sorana_si_die` 的重製版（膚色被洗白），檔案已還原回 `95d52262^`。
但 **`die` 的 `?v=2` 已經上線**（`speakers.js:650`）⇒ 玩家快取裡是重製版，
不跳版號他們會繼續看到被洗白的那一張。**改成 `?v=3`。**

⚠⚠ 判準：**看線上 `speakers.js` 現在寫什麼**，不要照「有沒有退件」推論 ——
`tease`／`amaze` 的版號還沒接，所以還原後**不必跳**；`die` 接了，所以**一定要跳**。

### 2.2 新鍵（**8 個**，都是新檔，**不必 `?v=`**）

    // battlecry 表情差分 ×5 —— 取景值直接抄 battlecry（六張的 alpha bbox 全是 (7,1503,0,1023)）
    battlecrylook:        { src:'resources/si/sorana_si_battlecrylook.webp',        top:6, bot:1523, fx:0.668 },
    battlecrylookaside:   { src:'resources/si/sorana_si_battlecrylookaside.webp',   top:6, bot:1523, fx:0.668 },
    battlecrylookserious: { src:'resources/si/sorana_si_battlecrylookserious.webp', top:6, bot:1523, fx:0.668 },
    battlecrylookup:      { src:'resources/si/sorana_si_battlecrylookup.webp',      top:6, bot:1523, fx:0.668 },
    battlecryserious:     { src:'resources/si/sorana_si_battlecryserious.webp',     top:6, bot:1523, fx:0.668 },

    // 索拉娜背諾薇兒 ×5（⚠ 兩個世代的 top/bot 不同，照抄不要統一）
    carrynouvelle:        { src:'resources/si/sorana_si_carrynouvelle.webp',        top:5, bot:1520, fx:0.648 },
    carrynouvelleshock:   { src:'resources/si/sorana_si_carrynouvelleshock.webp',   top:5, bot:1520, fx:0.648 },
    carrynouvellescream:  { src:'resources/si/sorana_si_carrynouvellescream.webp',  top:7, bot:1519, fx:0.648 },
    carrynouvellejealous: { src:'resources/si/sorana_si_carrynouvellejealous.webp', top:7, bot:1519, fx:0.648 },
    carrynouvellesmirk:   { src:'resources/si/sorana_si_carrynouvellesmirk.webp',   top:7, bot:1519, fx:0.648 },

    // 蕾娜
    snivel: { src:'resources/si/renna_si_snivel.webp', top:9, bot:1526, fx:0.548 },

⚠⚠⚠ **背諾薇兒那五張：`cm` 千萬不要填。**
`top` 是**諾薇兒的頭**不是索拉娜的（她的髮頂在 y=162）—— 同 §6.5「法環污染 `top`」。
要索拉娜維持 176cm 的視覺大小，`cm` 得帶到 **196.1**，而那會**超過 `CAST_TALL`(178)
把全劇組一起縮小**。照既有前例 `soranaanya_si_backcarry` 走：**不填 `cm`**，
接受整組畫小一點。`speakers.js` 第 631 行那段註解早就寫著這個結論。
⚠ 覺得小了就動 `cm`／`standCm`，**不要動 `top`／`bot`**。

⚠ `fx` 全部是**索拉娜**的兩眼中點（睫毛實測後目視確認在鼻梁上），不是諾薇兒的臉。

---

## 三、⚠ 還開著、要 Ray 決定的（程式端先不要動）

1. **`carrynouvellescream`／`jealous`／`smirk` 是另一個世代** —— 索拉娜的頭髮與
   `carrynouvelle`／`shock` 長得不一樣（兩個世代身體差 5.35%，不是位移）。
   在劇情裡切這幾張表情時**她的頭髮會跟著變**。要嘛 Ray 把那三張也出成 alpha，
   要嘛在新的底上重生成。**接腳本時先避開混用這兩組。**
2. **四張插圖還沒接線**：`resources/illustration/` 的
   `26-nouvellefaint`／`27-rennanouvelle`／`28-rennaholdhand`／`29-rennaprincesshold`
   （Ray 自己改的名，還是 PNG、未轉 webp、未接）。
3. **`resources/si/nouvelle_si_faint.png` ＋ 兩張 UUID 插圖**（`db5b640d` 一起 commit 的）
   仍未命名未接線。可能是 `prologue_fall` 那一幕缺的 `pain`。
4. **`adit` 的出口與祭壇圖**：`exitIf:{ left:'tomb_altar_on' }`（點亮才開），
   Ray 已定案 **`_off` 要實心牆、`_on` 才裂開** —— 美術正在重做那一對，交件後會再通知。

---

## 四、⚠ 兩條踩過的坑（程式端也會用到）

1. **近白指標（`min(RGB) ≥ 235`）抓不到彩色頭髮的白邊。**
   ToonOut 在諾薇兒的褐髮上留了一圈淡粉灰（約 200）的邊，**四項驗收全是 0.00%**，
   Ray 一眼看出來。要量得用「**邊緣亮度超出**」＝半透明邊緣像素的 V 減鄰近不透明像素的 V
   （Ray 的 alpha −56.6／ToonOut −36.3）。⇒ **深色／彩色頭髮優先用 Ray 自己出的 alpha。**
2. **量顆粒要量交件的那個 webp，不要量 PNG 母版。**
   webp 編碼本身就會把細雜訊壓掉，而既有的校準點全部是 webp ——
   基準不同的比較會得出相反的結論（我今天就報錯過一次）。
