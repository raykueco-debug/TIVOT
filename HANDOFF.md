# HANDOFF — Saint Install 模組化重寫 · 進度交接

> 每輪開工前讀本檔 + 憲法/規格。實況以 `git log` 與 `DECISIONS.md` 為準;本檔為人類可讀的進度總覽。
> **最後回寫:`ver -386`(2026-08-25)。** 本檔後半是逐版的交接,由新到舊往下加;
> **要快速上手請先讀正下方那一段「★★ 這一輪(-375 ~ -385)的總結」**,
> 再讀「★必讀 · 目前狀態(ver -341)」（餵稿規格見 `script/SCRIPT_FORMAT.md`）,
> 最後依需要跳到對應版本。
>
> 目前狀態:CLAUDE.md §6 開發順序第 1~5 步已完成;**第 6 步(ACCEPTANCE 對照 reference)仍未動**,
> `ACCEPTANCE.md` 至今不存在。`-208` 之後的產出集中在 `flight/`、**全站通用系統**
> (音訊分層、對白演出、讀取畫面)與 **`-318` 起的劇情播放器**。

---

# ★★ 這一輪的總結（`ver -375` ~ `-385`，2026-08-24/25）

一句話：**城鎮探索長出了「內容 ＋ 機制」**——公會、武器店、船塢三個節點；
劇情插入戰（含可戰敗分歧）、武器持有與買賣、一輪遊戲的邊界、飛行⇄戰鬥的來回。

## A. 這一輪確立的原則（都已寫進憲法，位置列在這裡方便查）

| 原則 | 在哪 |
|---|---|
| 劇情插入戰：卡上寫絕對值就存絕對值；框共用、教學那一套不共用；禁令擋在唯一發動點 | `CLAUDE.md` §6.5.2 |
| **戰敗一律 Game Over 回主選單**，除非那一場明寫 `allowLose`（＝劇本要它被打輸） | `CLAUDE.md` §6.5.2 |
| 武器數值只有一份（規格文字用算的）；持有制只有一支 `hasWeapon`；店家長相在 config | `CLAUDE.md` §6.5.3 |
| **本篇／試玩版兩套數值**（`weapons[].story` ＋ `weaponOf(key, storyMode())`） | `CLAUDE.md` §6.5.3 |
| **對白播放中不放時間地點**；「有沒有在演」由**演出層宣告**，不要從畫面反推 | `CLAUDE.md` §6.5 |
| **音效 → 圖 → 音樂** 三段串接；音效一律 AAC，不留 256 kbps mp3 | `CLAUDE.md` §6.6 |
| **「一輪遊戲」的邊界只定義在 `progress.newRun()`**；存檔是同一張清單的兩面 | `CLAUDE.md` §6.9 |
| 兩頁之間：交棒兩把鑰匙讀了就清；共用樣式抽成獨立檔且**變數要列全** | `CLAUDE.md` §6.10 |
| 敵人資訊標準卡 / 可戰敗分歧 / 空畫面拍 / 武器卡（Ray 的餵稿格式） | `script/SCRIPT_FORMAT.md` §8.5–8.7 |

⚠ 這一輪**沒有**新增鐵律 —— 每一條新原則都可以歸到**鐵律 7（一個量一個計算點）**
或**鐵律 8（一個動作一支實作）**底下。三次被 Ray 抓到的錯（`-374` 站位、`-385` 地名、
`-378` 兩套數值）也都是同一個形狀：**規矩散在呼叫點上，而不是收在唯一的一支**。

## B. 現在的帝都長什麼樣

```
                     行政廳 ─ 中心區 ─ 教堂
                               │
    船塢                  攝政王廣場                   旅店
     │                    │   │   │                    │
   舊街區 ── 槍店      左 ─┘   │   └─ 右          上街區 ── 雜貨舖
     │  └ 賞金獵人公會        （下＝入口）              │  └ 餐酒館
```

- 有內容的：廣場／中心區／教堂／行政廳／舊街區／賞金獵人公會／武器店／船塢／
  上街區／雜貨舖／餐酒館
- **只有殼的：旅店**（沒背景、沒稿）

## C. 三條「還沒接起來」的路

1. **出城 → 飛行**：城鎮沒有出口通往飛行頁（Ray：「還沒做到那邊」）。
2. **再入城的位置**：`town.open()` 一律從攝政王廣場開始，沒有記住離開時站在哪。
   要做就是一把 `tivot_town_pos_v1`，⚠ 記得同時加進 `newRun()` 與 `runSnapshot()`（§6.9）。
3. **合成表**（素材 → 武器）：武器改裝那一頁是空的。

## D. 缺素材／等 Ray 定案

| 項目 | 狀況 |
|---|---|
| 飛行頁三隻怪（蜈蚣／羽蛇／空賊）的**敵人卡** | 現在一律借巨型聖徒跑流程，卡到位只改 `enemy` 一欄 |
| 同上的**戰鬥立繪與天空背景** | 空賊有 `viewex.png`（1536×1024，還沒轉 webp）；另兩隻只有 489×291 方位圖 |
| **旅店**背景與稿 | 無 |
| `Nouvelle_SI_pray` | 酒館第一句會回退基本立繪 |
| **時段背景**（Dawn/Dusk/night/midnight） | 全城鎮只有 `_Day` |
| 槍店店主那 8 句知識、打靶結算頁「已淨化」的用詞 | **我寫的**，等 Ray 過目 |
| `resources/SI/28e1e4e3-….webp` | 蕾娜掩嘴的差分，等命名 |
| `se_ginclick` / `se_SailorShout` | 還沒照 §6.6 量響度 |

## E. 工具

- `tools/script_lint.py` —— 現在**連城鎮節點一起驗**（出口、背景、shop 貨單、
  `battle` 指到的場次、`goto`/`onLose` 的 label 配對）。改完稿一定要跑。
  目前剩 2 個已知錯誤：`prologue_fall` 的孤兒插圖、`capital.inn` 沒有背景（＝上面 D 的缺口）。
- `tools/measure_si.py` —— 立繪取景值量測（top/bot/fx），新增立繪照這支量，不要沿用別張。

---

# ★必讀 · 目前狀態（`ver -341`）

## 這一段時間做出了什麼（`-318` ~ `-341`）

三塊東西，彼此獨立但接在同一條流程上：

| 東西 | 在哪 | 一句話 |
|---|---|---|
| **劇情播放器** | `modules/story.js` ＋ `script/mainScript.js`／`speakers.js`／`progress.js` | 背景／插圖／立繪／對話框／演出拍／預載；資料與播放分離 |
| **劇情版教學戰** | `modules/tutorial.js` ＋ `i18n/zh.js` 的 `tutorial.story` | 同一套教學流程換一份台詞（諾薇兒帶），不出結算頁 |
| **Kerberos 之門** | `modules/story.js` 的 `playKerberos` ＋ `resources/vfx/` | 劇情進戰鬥的轉場：上推→撞頂→解鎖→旋轉→開門，門縫露出戰鬥畫面 |

**完整流程**：首頁「story」→ 標準讀取頁 → 地宮第一幕 → Kerberos 之門 →
教學戰（諾薇兒帶，教到破防為止）→ 門開回劇情 → 璐娜莉亞登場 → 收場。

## 現在可以怎麼跑

- 首頁 **story** 鈕。`MAIN_ENTRY` 目前是 `'dungeon_chase'`（暫時；主線接起來要改回）。
- 開發時跳段：`(await import('/modules/story.js')).jumpTo({scene:'dungeon_lunaria', line:0})`。
- 版本／畫質在診斷 HUD（首頁連點團徽 5 下）。

## ⚠ 這一段時間反覆咬人的六件事

1. **「同某某畫面」＝去讀那個畫面的程式碼**。立繪站位錯了三次，都是因為抄了
   「另一個地方的近似值」（0.38/0.62）而不是飛行畫面真正在用的 **0.24/0.76**。
2. **同一張立繪必須永遠算出同一個結果**（已寫進 CLAUDE.md §6.5）。踩過兩次：
   依人數縮放、身高讓位只在兩人同台時做（位移 46px 被讀成「變小了」）。
3. **換圖是非同步的**。淡出→換 src→淡入，中間一定要等 `onload`
   （`complete && naturalWidth` 是快取命中的退路）。立繪、背景、插圖各踩過一次。
4. **滿版插圖上任何會動的遮罩／縮放都會被讀成畫面在抖**。淡入、對焦落定、斜切揭幕
   三種都失敗，最後用**黑幕**——訊號不碰到插圖本身。
5. **transform 不會疊加**。旋轉與位移、推近與上移，都要寫在同一個 `transform` 裡；
   分成兩條規則的話後面那條會整個蓋掉前面。
6. **持續狀態要明寫**。立繪的 `show`、插圖的 `cg`、背景的 `bg` 都是沿用的 ——
   `hide` 之後不寫 `show:true` 就永遠回不來。

## 待辦（依優先序）

1. **`MAIN_ENTRY` 改回主線**（現在寫死 `dungeon_chase`），或把地宮這一幕接進
   `prologue_*` 那條鏈。
2. **`ACCEPTANCE.md` 仍不存在** —— CLAUDE.md §6 第 6 步的收尾清單。這是戰鬥那側
   欠最久的一項。
3. **諾薇兒的差分在劇情頁還沒逐張量**：`speakers.js` 的 `ART.nouvelle.expr` 已經
   有 top/bot/fx（`-326` 量的），但**戰鬥教學**與**劇情頁**用的是同一組值 ——
   目前沒發現偏差，若日後補新差分要照 §6.5 重量。
4. **監察官正名蕾娜**：卡在素材（`resources/inspector/` 只有芙蕾雅）。
   ⚠⚠ **蕾娜 Renna ≠ 蕾妮 Renee**。
5. **缺素材**：彈殼落地音（掃射那一拍原稿有，`se/` 裡沒有）、
   Kerberos 的門聲以外沒有其他缺口。
6. **`#storyLoad`（劇情自己那顆簡版預載頁）已停用**，DOM 與 CSS 還留著 ——
   確定不用之後可以刪。
7. **`{ load:'sceneId' }` 機制已就緒**但目前腳本裡沒有用到（開場那一次已經走完
   整條 scene 鏈）。場景鏈變長時再插。

## 這一段時間的檔案地圖

    modules/story.js      劇情播放器（＋Kerberos 之門的演出與幾何）
    modules/tone.js       立繪與背景的融合（葉節點，量背景亮度 → 濾鏡）
    modules/tutorial.js   教學（原版＋劇情版兩份台詞；立繪走飛行畫面那一套）
    script/mainScript.js  主線 scene 鏈（資料）
    script/speakers.js    角色表＋立繪取景值（**唯一一份**，config.js import 它）
    tools/kerberos_cut.py 由 Ray 的分層切出門的五件素材＋印出幾何常數
    resources/vfx/        門的五個 webp（約 480 KB）
    resources/_originals/ 所有原始檔（8.5MB 分層、wav 母帶）—— **不入版控**

---

> **飛行模組（`flight/`）的交接在 `flight/HANDOFF.md`** —— 那是獨立的一套
> （地形 raycaster、聚落、敵人、手繪城市貼圖），與本檔的戰鬥模組不共用程式碼。

---

## 一、開工必讀(依序)
1. `CLAUDE.md` — 專案憲法(鐵律、目錄結構、模組邊界、§3 狀態契約與擁有者制、§4 函式歸屬、§6 開發順序)。
2. `DECISIONS.md` — 已定的刻意偏離 reference 決策 **D1~D4**(D1 戰敗優先致死鏈、D2 MB 回血 50%、D3 統一改血 API、D4 saintComboStep 1.0)。
3. `SPEC.md` — 行為規格。**§4 已修正為規則制 rank**;**§三 已補「戰鬥層級模型(場/局/敵/盤)」**(見下方三、備註)。
4. `reference/index.html` — 唯讀行為基準,**戰鬥**行為有疑義以它為準。
   ⚠ `reference/Kerberos.png` 是 Ray 後來放的門的合成圖(＝`Kerberos_All.png`),
     與這支 html 無關,只當美術對位基準。
5. `docs/TIVOT_SCRIPT_ARCHITECTURE.md` — 劇情/對話系統的規劃(ver -326 歸檔)。
   已照它做出來的:`script/mainScript.js`(資料)＋`modules/story.js`(播放器)
   ＋`script/speakers.js`(角色表)＋`script/progress.js`(stage/flags)。
6. `script/SCRIPT_FORMAT.md` — **Ray 餵稿的規格**(ver -342):寫稿格式、演出詞彙表、
   現有素材清單、一定要交代清楚的六件事。收到稿之後跑 `tools/script_lint.py` 驗。
7. `git log`(本檔最後回寫於 `ver -374`)。

---

## 二、模組狀態表

| 模組 | 狀態 | 摘要 |
|---|---|---|
| `state.js` | ✅ | 集中狀態 + 具名 setter(applyDeathGuard/enterSaint/exitSaint/markExecution/addCounter/addPerfect/initEnemyHp)。 |
| `config.js` | ✅ | GAME_CONFIG + ASSETS(resources/ 相對路徑)。 |
| `audio.js` | ✅ | SFX(Web Audio 合成,模組化)。 |
| `combat.js` | ✅ | 戰鬥核心 + 統一改血 API(`healPlayer`/`setPlayerHpRatio`,D3)+ D1 致死鏈 + 雙槍 tap 分支 + 對 defense/saint/weapon/partner/inspector/enemy 的注入協調。win/lose 算 totalTime/avg → `inspector.settle`。**`startIntruderFight`(Boss 亂入＝重開新場,注入 enemy)**。 |
| `defense.js` | ✅ | 三段防禦(Counter/Perfect/Defense)+ `setUltRate` 擁有者管道 + `addPerfect` 計數。多發大絕(ULT_SHOTS/GAP)讀取端已就緒。 |
| `enemy.js` | ✅ | 立繪 / 受擊特效 / `setEnemy`(含 witch Boss 大絕/懲罰/彈痕 config 寫入 state)。**`triggerIntruder` 已接實體**(Boss 遭遇 cut-in → enterFight → 呼叫注入的 `startIntruderFight`)。 |
| `saint.js` | ✅ | 聖徒化三結局(MB / OBE / 生命歸還)、`lifeReturnAbort` 執行體、`markExecution`。 |
| `partner.js` | ✅ | 被動即死防禦(`tryDeathGuard`)接致死鏈;主動技框架(`tryActive(context)`,支援 `'any'` 情境與 `oncePerBattle`)。**第二搭檔馬季諾**:主動「前線補給」(`supplyRefill`,一般盤面發動即進雙槍破防窗口(`weapon.startDualWindow` 經注入)、不吃破防值、不另播雙槍 cut-in、每場一次;聖徒化期間不可發動——context:'board' + saintMode 雙重擋門)+ 被動「高裝藥彈」(`checkLowHpBuff`,HP 跌破 50% 瞬間發動 10 秒普攻加倍、可跨盤;邊緣觸發,回門檻上重新上膛;檢查掛 combat.updateBars,聖徒化中不判定;發動插 cut-in)。`currentPartner` 讀 `state.pickedPartner`(換人即換技)。 |
| `weapon.js` | ✅ | 反擊武器 `weaponCounter`、雙槍破防窗口(`activateDual`/`endDual`)、**副武器選擇畫面**(`openWeaponSheet`:全螢幕橫式卡疊上下滑動、抽換輪轉動畫、點卡直選播擊發聲、底部鈕=選定並返回)、`reset`/`stopTimers`。**搭檔選人畫面**(`openPartnerSheet`:全螢幕卡疊左右滑動——未選卡墊於現選卡後、箭頭切換、抽換輪轉動畫、技能描述卡、底部發動說明;點卡直選(`selectPartnerAt` 經 `setPickedPartner` 唯一管道寫入、實際切換播確認 SE)、底部鈕=選定並返回,兩畫面皆無獨立返回鈕)。 |
| `inspector.js` | ✅ | 規則制評價(rank + EXP 顯示)、監察官結算演出(`showResultSequence` 分階段 + 處決台詞分支 + 打字機)、好感雙軌查表(`pickByThreshold`,currentFavor 固定 0)、Boss 優先 `bossDialogues`、S 解鎖 → `onRematchBtn` 迎擊分流、最佳成績雙存檔(一般 `saint_best_total_v1` / Boss `saint_best_total_boss_v1`)。**不搬 legacy `inspectorPanelHtml`**。 |
| `main.js` | ✅ | composition root:注入、按鈕/手勢綁定、開機閒置。rematchBtn 綁 `inspector.onRematchBtn`。 |

> **CLAUDE.md §6 開發順序第 1~5 步全部完成。** 一般戰、聖徒化、雙槍、監察官結算、Boss/亂入整條龍皆已接上並驗收。

---

## 二之二、全站通用系統(戰鬥／飛行共用,通則寫在 CLAUDE.md)

這幾套**不屬於任何單一模組**,兩個頁面都吃。改之前先讀對應的通則章節,
不要在其中一邊各寫一份。

| 系統 | 通則 | 實作 | 一句話 |
|---|---|---|---|
| 對白演出 | `CLAUDE.md` §6.5 | `modules/tutorial.js`(DOM)／`flight/index.html` 的 `castEnter`/`drawTalk`(canvas) | 站位、明暗、點擊推進、輪轉換卡全部共用一套語彙 |
| 立繪取景 | `CLAUDE.md` §6.5「取景」 | 同上 | **縮放鎖單眼寬、站位鎖身高**,兩個獨立旋鈕 |
| 畫質 | `CLAUDE.md` §6.5「畫質」 | 同上 | 畫布開到裝置像素(DPR≤2);縮小超過 2 倍要走逐次減半的預縮圖 |
| 音訊分層 | `CLAUDE.md` §6.6 | `config.js` 的 `tuning.*Gain`／`audio.js` | 語音 −18／音效 −22／音樂 −28 LUFS |
| 語音鏈 | `CLAUDE.md` §6.6「耳機對了不代表手機對了」 | `tuning.voiceChain` → `SFX.playVoice` | 手機喇叭 600Hz 以下不發聲,語音層要**量兩次**才知道對不對 |
| 讀取畫面 | — | `main.js` 的 `#assetLoader`／`flight/index.html` 的 `bootUI` | 圈內字樣一律 SAINT INSTALL;監察官對話框＋Hint 輪播**有**;底部「載入中／點擊繼續」**沒有** |

⚠ 讀取畫面有**兩份**(`#assetLoader` 與 `#boot`),`-229` 撤字、`-251` 放回的時候
都各改過一次 —— 只改一邊的話 Ray 會在另一邊看到舊行為(這次就是這樣被抓到的)。

⚠ 量測工具:`tools/audio_probe.html`(響度,含手機喇叭模型)。瀏覽器開,
WebAudio 解碼所以 mp3/m4a/wav 通吃。改 `voiceChain` 或增益就要重跑一次。

---

## 三、Git · 關鍵 commit

**近期(全站/飛行)**
- **HEAD**:`52c07d8` fix(flight):聖王廳地板的水平切線、遭遇不再看得見才算、探索改一次 30 秒 — `-252`。
- `36da648` feat(flight):立繪放大、讀取畫面的監察官說明放回、**畫質自適應** — `-251`。
- `96f10c8` fix(audio):語音在手機外放上「糊」— **語音鏈** + 增益改對耳機／手機的平均響度 — `-250`。
- `8fa967d` fix(flight):立繪的尺與畫質 — **眼寬定縮放、身高定站位**;畫布開到裝置像素 — `-249`。
- `c90c94e` refactor(audio):全域響度重訂三層 — 語音 −18／音效 −22／音樂 −28 LUFS — `-243`。
- `3586b34` refactor:全站音訊集中到 `resources/audio/{bgm,se,vo}` 並統一命名 — `-240`。

**戰鬥模組(第 1~5 步)**
- `9dc6db6` enemy:Boss/亂入接實體—triggerIntruder(S 解鎖→迎擊→槍之魔女 witch)。
- `2f244e4` docs(HANDOFF):回寫進度(partner/weapon/inspector 三輪完成)。
- `3ddaece` docs(SPEC):§4 修正 rank 規則制、tiers 為休眠 config。
- `8af9453` inspector:評價(規則制 rank+EXP)/監察官結算演出/最佳成績雙存檔/迎擊分流。
- `772d0be` weapon:雙槍破防獎勵窗口 + 換裝面板收尾。
- `a58fe81` partner:主動技通用框架(單槽 + 情境標註)+ 被動 cut-in 讀 config。
- `c3e1879` partner:即死防禦(deathGuard)接上致死鏈,一場一次鎖 1 HP。
- `090ba5a` balance(saint):saintComboStep 0.5→1.0(D4)。

### 備註 · DECISIONS 與 SPEC 修正
- `DECISIONS.md` 已含 **D1~D4**(刻意偏離)。
- **SPEC §4 評價已修正為規則制 rank**(逆向誤述修正,非刻意偏離,不入 DECISIONS)。
- **SPEC §三 已補「戰鬥層級模型(場/局/敵/盤)」**:結束一敵唯一靠 `hp` 歸零→overkill→清盤即 win;`boards[0..4]` 是前五盤參數模板、非結束條件,越界循環沿用第 5 盤參數。舊「一場=五盤」為誤述,已修正。
- **Boss 亂入＝重開新場**(場/局/敵/盤 模型):`startIntruderFight` 做完整重置(playerHp 滿血、deathGuardUsed 歸零)。觀察上與 reference 等價(S 解鎖前提無傷),**照原意加程式註解即可,不記 DECISIONS(無 D5)**。

---

## 四、下一輪:CLAUDE.md §6 第 6 步 — 全流程 ACCEPTANCE 對照 reference 收尾

前 1~5 步功能皆已接上。第 6 步為**收尾驗收**:建 `ACCEPTANCE.md` 把 CLAUDE.md §6 驗收流程的關鍵手感點列成勾選清單,於 390×844 對照 `reference/index.html` 逐項打勾。重點覆蓋:
- 聖徒化推進手感(受擊 +1s / 格擋 +0.5s / 免傷不推進 / 無受擊 10s 回滿)。
- 三級防禦門檻(0.35 / 0.12 兩界線)、散彈 Perfect 改傷、狙擊無 Perfect 帶。
- cut-in 後敵照常發動(v18c 取消緩衝)、聖徒化期間大絕更密集。
- Boss 雙發大絕(間隔 1s、2~4s 頻率)、延時懲罰半傷減 1 秒、全彈痕。
- combo 傷害斜率、MaxBurst 追加 20% 總傷(D4/D2 差異)、評價 S 解鎖亂入。
- 場/局/敵/盤:一敵靠 hp 歸零結束、越界循環出盤、Boss=重開新場。

> 連戰 `lineup`(局層,一場多敵)目前預留未接;若日後要接,屬 combat/enemy 的「敵死→下一敵」串接,與本次 Boss 亂入(重開新場)不同層級。

---

## 五、線上功能(本次不實作,state 已預留備忘)

- **Ghost 對戰**:只記每盤點擊頻率(5 數字),對戰生成槍聲+隨機彈孔,無聖徒化/無雙槍,純舒爾特五戰三勝取兩盤,不重播輸入軌跡。
- **排行榜**:各關卡完成秒數(有聖徒化有雙槍),秒數決勝。
- 共用零維運後端(Supabase 類),不做防作弊。順序:前端假榜 → 自我 ghost → 接後端,每階段可獨立出貨。

---

## 六、工作慣例(照做)

- **驗收**:app 內建 Browser(`mcp__Claude_Browser__*`)在 **390×844** 跑;模組化須經 http(`python -m http.server <port> --bind 127.0.0.1`,在 `TIVOT/` 下)。ES module 有快取 → **每次驗收換新 port**。
- **測試 hook**:驗收時可在 `main.js` 暫掛 `window.__T = {…}` 供 `javascript_tool` 驅動內部函式 / 讀 state;**commit 前務必移除**、乾淨重載確認 `typeof window.__T==='undefined'`。
- 敵立繪那幾個 404 是既有外部路徑 fallback,正常、與改動無關。
- **版本號**:**每個 commit 都升** `config.js` 的 `VERSION`(`ver YYYY.MM.DD-NN`,NN 連號),
  包含只動 `flight/` 的 commit。序號與 commit 訊息末尾的 `— ver -NN` 對齊。
  ⚠ -128~-170 那段 flight-only 的 commit 沒升,HUD 版本號卡在 -127,Ray 在手機上
  無從判斷拿到的是不是新版;-171 起補齊對齊。
- **commit**:每模組完成即 commit,訊息末尾加
  `Co-Authored-By: Claude Opus <n> <noreply@anthropic.com>`(`<n>` 寫**當下實際在跑的
  模型版本**,不要照抄本檔 —— `-249` 起是 Opus 5)。
- 刻意偏離 reference 才寫 `DECISIONS.md`;照 reference 一致的不寫;逆向誤述的修正(如 SPEC §4、戰鬥層級模型)不入 DECISIONS。
- 語言:與使用者用**繁體中文**溝通。

---

# 交接 · `ver -288~-295`（主線腳本系統、存讀檔、立繪站位；飛行頁見 `flight/HANDOFF.md`）

## 一、新增的子系統：主線 scene 播放器 ＋ 劇情層存讀檔

依 `docs/TIVOT_SCRIPT_ARCHITECTURE.md`（Ray 提供，ver -326 歸檔）建的第一層。

| 檔案 | 內容 |
|---|---|
| `script/speakers.js` | 角色表（§2）。監察官正名前後當兩個 id：`OFFICER` / `RENNA`。附立繪取景實測值，與 `flight` 的 `PORTRAIT` **同一組數字，改一邊要改兩邊**。 |
| `script/progress.js` | stage / flags / 好感 / 玩家名。**與 flight 共用同一組 localStorage 鑰匙**。 |
| `script/mainScript.js` | scene 鏈。⚠ 目前是**佔位示範內容**，正式開稿時整段刪掉重寫。 |
| `modules/story.js` | scene 播放器（首頁 `story` 鈕，管理人限定）。 |
| `modules/save.js` | F4 即時存／F7 即時讀／F5 選欄存／F8 選欄讀，10 列一頁、超出增頁。 |

⚠ **`RENNA` 不是 `REGINE`**：ver -262 已全面更名，`Regine` 是本名、隊伍中喊 `Renna`。
⚠⚠ **蕾娜 Renna ≠ 蕾妮 Renee**。Renee 是戰鬥搭檔（`config.js` 的 `partners.renee`）。
⚠ F5 是瀏覽器重新整理鍵，必須 `preventDefault`；F 鍵只在 `body.testmode` 下受理。
⚠ macOS 預設把 F 鍵當系統功能鍵，要壓 fn。手機沒有 F 鍵 —— 面板本身可觸控，
  之後在 UI 上補入口即可，不必另寫一套。

**取景踩到的兩個坑**（都照 CLAUDE.md §6.5 修正，flight 版也適用）：
- 輪廓**只能量看得見的那一段**。整張圖量會把散髮/裙襬算進去，夾中線時直接把人
  推出畫面。
- 縮小後不該再把頭頂釘在頂線，改成**腳落地平線**（那本來就是 §6.5 的目標）。

表情差分與 CG 素材尚不存在 → 自動回退基本立繪並在 console 記一筆，
**那串紀錄就是「還缺哪些圖」的清單**。

## 二、立繪站位：發起位制試過又退回，定案固定 2/2 分邊

    右　索拉娜・安雅　　　左　蕾娜・諾薇兒

`side` 寫在角色資料裡，不隨台詞變動。⚠ ver -288 曾改成「發起人站右」，**已退回**——
退回的原因不是規則不好，是**素材做不到**：立繪朝向是畫死的，換邊必須水平翻轉，
而翻轉會把髮旋、配件、持物全部左右顛倒（實測蕾娜的板夾會換手）。

正解是**同一個角色畫左右兩版圖**（Ray 判斷）。欄位已備妥：`ART[].alt` / `PORTRAIT[].alt`，
四人皆 `null`。⚠⚠ 補圖時 **eye/fx/top/bot 四個值全部要重量** —— 那是那一張圖的
數字，不同的畫不可能沿用。目前沒有程式路徑會用到 `alt`，它是留給日後的接口。

⚠ **已知後果**：stage 1 只有蕾娜＋諾薇兒，兩人都在左邊 → **整個第一章的對話畫面上
只會有一個人**（實測確認）。現有 24 組閒聊不受影響（都是三、四人）。
要解就是先補其中一人的 `alt` 圖，或把諾薇兒改到右邊（但右側就變三人輪）。

## 三、飛行頁「進度」鈕（管理人限定）

操作盤右上，顯示 stage、主角名、四人好感與 tier（含「未入隊」）、本章可播閒聊組數。
⚠ 管理人限定是**刻意的**：ARCHITECTURE §0.5 明訂**好感度不可視化**，
玩家端要靠獨坐出場順序、敲門安全感、稱呼變化去感知。
⚠ 唯讀。改值走既有的 `setStage` / `setAff` —— 好感是棘輪制，隨手可點會弄髒測試狀態。

## 四、⚠ 工作方法上的修正（這一輪最貴的教訓）

聖王廳廣場破圖我連改十幾次沒有一次改善，原因是**用靜止截圖找一個移動才出現的問題**。
細節見 `flight/HANDOFF.md` 的 `-290~-295` A/B 兩節。兩條規則：

1. **使用者說「破圖」時先問「靜止時有嗎？」** 靜態破洞與移動抖動是兩類病，修法沒有交集。
2. **不要拿糊掉的小圖下結論。** 390×844 的截圖裡城只有一兩百像素寬還被面板遮住，
   據此說「看起來正常」等於唬人。要看細節先 `resize_window` 開到 1000×1450。
3. **只認量測。** 任何改動都要有改前／改後的數字；量不出改善的**退掉**，不要留在
   程式裡。這一輪就是這樣退掉了三條無效的修法（見 flight/HANDOFF.md D 節）。

## 五、待辦（明天）

1. ~~`TIVOT_SCRIPT_ARCHITECTURE.md` 歸檔到 `docs/`~~ —— **ver -326 完成**。
   內文一字未改，檔頭補了「已實裝／還沒接」對照表。
2. **`TALKS[1]→[3]` 搬遷** ＋ `PARTY` 填真正的入隊章節（安雅 `from:2`、索拉娜 `from:3`）。
   ⚠ Ray 已確認：**stage 1 的閒聊只有蕾娜＋諾薇兒且還沒寫，現有 24 組全部是 stage 3 的**。
   ⚠ 語義保留現行的「**起始章節**」（第 N 組＝第 N 章起可播），不要改成精確匹配 ——
   Ray：「第 1 章的日常閒聊後面還是可以聊，不要做死，因為 stage 本身也連動角色間的
   熟悉度與化學作用」。而且他是**依 stage 分組寫稿**的，群組結構要留著。
3. `stash@{0}`（launch.json，本機 `python3` vs 遠端 `python`）—— 確認哪個可用後 drop。
4. 未追蹤的 `NPC/`、`resources/illustration/` —— 進版控還是進 `.gitignore`。
5. 抖動的殘量（見 `flight/HANDOFF.md` D 節「還沒查的」）。

---

# 交接 · `ver -315`（劇情播放器擴充：背景／CG／暗調 CI／音效／震動／平移；地宮第一幕）

## A. story.js 新增的「演出層」

line 上的演出欄位，全部**只寫變化**（省略＝沿用上一句）：

    bg:'HolyseeDungeonWhole'   背景（resources/background/*.webp），bg:null 清掉
    cg:'001_Nouvelle_Fell'     全屏插圖（resources/illustration/*.webp），cg:null 清掉
    cgPan:'up'                 這一句的 CG 由下往上平移
    ci:'Lunaria_SI_Armed'      暗調 CI 插入（resources/SI/*.webp），ci:null 收掉
    bgm:'crisis'               背景音樂（BGM_SRC 查表），bgm:null 停掉
    se:'se_steps'              音效；多發寫成 [{n:'x'},{n:'x',delay:500}]
    shake:true                 畫面抖一下
    fx:'gunfire'               在 CG 上灑一串槍擊命中點

- ⚠ **持續**（bg/cg/ci）與**一次性**（se/shake/fx）分成 `applyPersist` 與
  `fireOneShot` 兩支處理。混在一起寫會很難讀，而且一次性的容易被誤沿用。
- ⚠ **平移每次都要先移除 class 再加**：同一個 class 還在的話 animation 不會
  重新開始，第二次就不會播。`void el.offsetWidth` 那一行是刻意的。
- ⚠ **音效逐支列出實際路徑**（`SE_SRC`），不要拼副檔名 —— `resources/audio/se/`
  裡 wav／mp3／m4a 三種都有，拼錯會**靜默 404**（audio.js 載不到只 resolve(null)，
  不會報錯，你會以為是音量問題）。
- ⚠ CG 用 `object-fit:cover` 不是 contain：插圖與畫面都是直幅，contain 會上下
  留黑邊，讀起來像「貼了一張圖」而不是「鏡頭切過去」。
- ⚠ 暗調 CI 的壓暗用 `filter` 不用 `opacity`（CLAUDE.md §6.5：壓暗必須不透明）。

## B. 地宮第一幕（`dungeon_chase` → `dungeon_lunaria`）

Ray 的第一段正式稿。逐句驗過：背景／跑姿／腳步聲 → 跌倒插圖 → 上膛 ×2（隔
0.5 秒）→ 戰鬥插入點（跳過）→ 聖徒插圖 → 暗調 CI「讓開。」→ 槍擊 26 發＋抖動
→ 回地宮 → 璐娜莉亞插畫由下往上平移 → 結束交還控制權。

## C. ⚠ 三個待補，不要當成完成品

1. **諾薇兒的五張表情差分是「不同姿勢」不是換臉** —— `top/bot/fx` 還沒逐張量，
   目前沿用 front 那一組，人會偏。要照 CLAUDE.md §6.5 逐張重量，而且 `ART` 現在
   一個角色只有一組取景，屆時要擴成 **expr 各自帶自己的取景**。
2. **彈殼落地音沒有這個素材**。原稿指定了，`resources/audio/se/` 裡沒有。
   我在密集槍擊那一拍暫用 `se_mg_squall`（重機槍連射），那是**我加的**，待確認。
3. **戰鬥沒接線**。`{battle:'tutorial'}` 目前由 story.js 跳過並記 console。
4. ⚠ `Nouvelle_SI_front.webp` 這次被**重新轉檔**了 —— Ray 放了一張新的
   `Nouvelle_SI_front.png` 進來。如果那張畫有改構圖，`speakers.js` 與
   `flight/index.html` 兩邊的 `top/bot/fx` 都要重量（同一組數字、兩個地方）。

⚠ `MAIN_ENTRY` 暫時指到 `dungeon_chase` 方便驗這一幕，正式串主線要改回
  `prologue_audience` 或把地宮段接進鏈裡。

---

# 交接 · `ver -316`（劇情版面定案；重進卡圖修掉）

## A. 版面：上半立繪、下半固定戰鬥盤面（Ray 指定）

    #storyStage{ --story-top:56%; }      ← 比例只寫在這一個地方

所有**場景層**（背景／CG／暗調 CI／特效／情境卡／立繪）都只佔上半；下半是
`#storyBoard`，無戰鬥時放團徽當佔位。

⚠ **版面不能因為有沒有戰鬥而變高變矮** —— 那會讓立繪的大小與站位每一幕都跳。
  接真的戰鬥盤面時只改 `--story-top` 與 `#storyBoard` 的內容，其餘不動。
⚠ `layout()` 的高度要取 **`#storyCast` 的高**，不是整個舞台 —— 拿舞台高去算的話
  人會被畫到盤面底下，而且「腳落地平線」那條規則會落在錯的地方。
⚠ 對話框錨在**立繪區下緣**（盤面之上），不是畫面底：盤面是固定的，框壓上去會
  擋住戰鬥資訊。

## B. 立繪規則（與飛行畫面同一套）

- **單人也站自己那一側**，不置中（Ray 指定「同一人物立繪需一直在同一側」）。
  置中的話同一個人會因為場上有幾個人而左右跳，那正是固定站位要解決的事。
  單人時錨點往中間讓一點（0.38／0.62），畫面才不會太偏；且**單人不夾中線**
  （夾了會把她壓回半邊，人變小又擠在角落）。
- **同一個人換表情／換圖 → 淡入淡出**；**同側換人 → 滑出滑入**（輪轉換卡，
  比照飛行畫面）。⚠ 只換表情卻走滑動的話，讀起來是「她走掉又走回來」。
- **插圖之間也淡入淡出**。第一次上圖直接顯示 —— 先淡出會有一段莫名的空白。
- `CAST_SHOW` 0.52 → **0.44**（Ray：「人物高度高放一點」）。這個值是「最高的人
  露出身體的幾成」，**越小＝鏡頭越近＝人越大、頭頂越高**。上半舞台變矮之後
  不調的話人會顯得又小又低。

## C. ⚠ 重進劇情卡在上一次的畫面（已修）

Bug：第二次點 story 會卡在上一輪的結束畫面。原因是 `stageBg/stageCg/stageCi/
stageBgm` 是**模組級狀態**，關閉時沒清；重開時第一句只寫了 `bg` 沒寫 `cg`，
於是上一輪最後那張插圖一直蓋在上面。

修法：`resetStage()`，在 **`open()`** 呼叫。
⚠ **不能放在 `playScene()`** —— scene 之間是接續的，`bg` 要能跨場沿用，
  每次 playScene 都清的話換場會閃一下黑。

## D. 新增：情境卡 `card:'…'`

背景之上蓋半透黑、文字置中；有 `card` 的那一句**不顯示對話框**（它不是台詞）。
⚠ 腳本裡用 `\n` 斷行，CSS 要 `white-space:pre-line`，否則擠成一行。
⚠ 卡片那一句要明寫 `portrait:{show:false}` —— 立繪預設是 `show:true`，
  不寫的話人會先用 base 立繪站在卡片後面，下一句才換成該有的姿勢，看起來像閃了一下。

## E. 其他（Ray 指定）

- 槍擊改成**機關槍掃射**：沿一條斜線由一端掃到另一端、44 發、**兩秒**，火花放大
  （16px＋外光暈，逐發大小有差）。⚠ 不是隨機灑點 —— 隨機讀起來是一片斑點，
  沿線推進才讀得出「掃過去」。音效改用 `se_lunaMG`。
- **離開劇情要把主畫面的 BGM 接回來**。劇情有自己的曲子，而首頁的播放邏輯只在
  「進首頁」那一刻跑一次，不會自己修正。

---

# 交接 · `ver -317`（抖動只抖場景層；「啊！」加音效）

## ⚠ 抖動的作用對象：**場景各層**，不是整個舞台

Ray：「這一禎要畫面抖動，**對話框不要抖**」。抖整個 `#storyStage` 會連對話框
一起晃，字在跳的框裡很難讀。

改成逐層指定：

    #storyStage.shake #storyBg,  #storyCast, #storyCg,
                      #storyCi,  #storyFx,   #storyCard, #storyBoard { animation: storyShake … }

⚠ **新增場景層時要記得加進這個選擇器清單。** 漏了那一層就不會跟著抖，畫面會
分成「抖的」與「不抖的」兩半 —— 比整個不抖還糟。

## 這一輪順手修掉的

`-316` 把 `se_lunaMG` 加進 `SE_SRC` 了，但**腳本那一行忘了改**，掃射還在放
`se_mg_squall`。現在照 Ray 指定改成 `se_lunaMG`。
⚠ 教訓：加素材與**用素材**是兩個地方，加完要回頭確認腳本真的指過去。

新增 `se_Fall`（跌倒），掛在「啊！」那一拍，與抖動同一句。

---

# 交接 · `ver -318`（插圖平移雙向；抖動殘留修掉）

- `cgPan` 支援 `'up'`／`'down'`。插圖一號改成由上往下平移（Ray 指定）。
- 「那就是......聖約第四騎士團的......」台詞照 Ray 的新稿。

## ⚠⚠ 抖動的 class **跑完要拿掉**（Ray：「最後一格不要抖」）

`-317` 把抖動改成逐層之後，`shake` 這個 class 一直留在 `#storyStage` 上沒清。
那些場景層只要**重新顯示**（插圖換圖是 `display:none→block`）animation 就會
**再跑一次** —— 所以最後一格明明沒寫 `shake`，插圖一上來還是抖了一下。

修法：觸發後 460ms 用 timer 把 class 移除（`st.__shakeT`，重觸發時先 clear）。

⚠ 這是「一次性效果用 class 實作」的通病：**class 是狀態，動畫是事件**。
  用 class 觸發動畫就一定要自己負責把它清掉，否則任何會讓元素重新進入排版的
  操作都會把動畫再放一次。日後加新的一次性效果照這條辦。

---

# 交接 · `ver -319`（璐娜莉亞改走正規立繪；插圖蓋住立繪的坑；平移／延遲）

## A. ⚠⚠ 「立繪一直沒出來」的真因：**插圖蓋在立繪上面**

Ray 回報「『對不起，我已經……！』的立繪一直沒出來」。查下來**不是位置問題**：
`#storyCg`（z-index 2）在 `#storyCast`（z-index 1）**之上**，那一句插圖 002 還在，
人整個被蓋住。

⚠ **凡是要看到立繪的句子，插圖都得先 `cg:null` 讓開。** 這件事在腳本上不明顯
（你只會看到「我明明指定了立繪」），所以每一句都要想一下「現在畫面上有沒有插圖」。

## B. 璐娜莉亞：從「全屏 CI」改成**正規立繪**

Ray：「她的比例明顯與諾薇兒不同，戰鬥中的對話立繪版面分配一概比照飛行畫面」。

CI 那條路是自己一套縮放（object-fit cover 到一個框），**比例對不上是必然的**。
改走立繪系統才會鎖身高、與諾薇兒同一把尺。實測 168cm vs 165cm → 417px vs 406px。

取景值是**量出來的**（照 CLAUDE.md §6.5 / HANDOFF F）：
- `cm 168`（Ray 指定）
- `top 9 / bot 1528` ＝ alpha 上下緣。⚠ 先確認過**四角 alpha 是 0、逐列輪廓寬由
  21% 變到 97%** —— 是去背立繪不是滿版插圖，所以 alpha 邊界就是頭頂與腳底。
  （這一步不能跳：滿版插圖照量會把人放大好幾倍。）
- `fx 0.496` —— 量**頭部那一段**（頭頂往下 8% 圖高）的 alpha 中心得 0.483，
  再拿諾薇兒校準這把尺（同法量她得 0.551、表上 0.564，偏移 +0.013）。
- `eye` 沒量（`CAST_EYE_MIX=0` 不參與運算）。要改回混合模式前必須先量。

⚠ 順帶修掉一個坑：**沒有立繪資料的角色不准碰立繪槽**。`UNKNOWN` 原本 art 是
null，`artOf` 回 null 之後 side 退回 'L'，於是它去佔了左邊那個槽，把站在那裡的
諾薇兒**整個清掉**。現在 `ensureOn` 直接擋掉，`highlight` 也改成「說話者沒有
立繪時誰都不亮」。

## C. 新欄位

- `dark:true` —— 這一句的說話者立繪壓成暗調（剪影感，還沒表明身分）。
  ⚠ 每一句都要清一次：它是**句子屬性**不是角色屬性。
- `delay:2600` —— 對話框**先不出**，等這麼久再打字（等平移跑完，Ray 指定）。
  ⚠ 等待中點畫面要**跳過等待**而不是推到下一句 —— 不然玩家會覺得「點了沒反應」
    然後連點兩下，一次跳掉兩句。（同「還在打字時點一下先補完」的規矩。）
  ⚠ 2600 與 CSS 的平移時間同值，改一邊要改另一邊。

## D. 立繪位置

- 身高差的縱向讓位**只在兩人同台時**才做。它的用意是「腳落同一條地平線」，
  單人時沒有對象可對齊，那一段只是在頭頂空出一塊（實測空掉 62px）。
  改後頂端由 124 → 57。
- `cgPan` 方向要看構圖：001 是跌倒的圖，**由上往下會停在裙底**，往上才收在臉上。

---

# 交接 · `ver -320`（立繪尺寸定案：**一張圖只有一個大小**）

## A. 拿掉「依人數縮放」

Ray：「同一張立繪不可有兩個大小」「多少有些交疊沒關係」「立繪不要出全身，
以膝部以上為原則，不然細節看不清」。

舊作法：兩人同台時每人只有半屏，輪廓超出就**整體等比縮小**。三個後果：
1. 同一張圖**單人時大、兩人時小**，換場就跳一下。
2. 縮到最後**全身都出來**，臉只剩幾十像素 —— 就是「細節看不清」。
3. 還有一道「把輪廓拉回畫面內」的夾制排在「夾中線」**之後**，方向相反、
   後者會贏 —— 兩個人被一起推回中間**疊在一起**（實測重疊 197px，
   璐娜莉亞的頭髮蓋掉諾薇兒半個人）。

現在：`pxCm` 只由 `CAST_SHOW`（0.56）與畫面高決定，是個**常數**。
→ 同一張圖永遠同一個大小（實測諾薇兒每一幀都是 731px，以前在 406~568 之間跳）。
外緣讓它出畫面（`#storyCast` 有 `overflow:hidden`，裁掉的是裙襬與頭髮的邊，不是臉）。

## B. ⚠ 不要改成鎖「耳朵／眼睛」

Ray 提過用耳朵大小鎖。**不要走這條** —— 專案已經踩過同一個坑（`flight/HANDOFF.md`
F 節）：鎖眼寬會把**畫風差異放大成體型差異**。實測索拉娜眼睛被畫小 → 整個人放大
13%（螢幕身高比 1.249 vs 真實身高比 1.107），Ray 當時回報「索拉娜太巨大」。
耳朵是同一類臉部特徵，會重演。

**尺要鎖身高** —— 那是角色的客觀屬性，不隨畫風跑。真正的病根不是「鎖什麼」，
是「會依場上人數動態縮放」，那已經拿掉了。

## C. 新欄位 `hide`

`portrait` 一句只能指定一個人，要**同時**讓另一個人退場就用 `hide:'UNKNOWN'`
（或陣列）。立繪是**持續狀態**，不明寫的話上一句站上來的人會一直留著。
⚠ 要在 `ensureOn` 之前處理：同側換人時先清掉舊的，新的才會走「首次上場滑入」
而不是「輪轉換卡」。

---

# 交接 · `ver -321`（劇情插入戰鬥；劇情專用預載頁；兩種教學分開）

## A. 劇情 → 戰鬥 → 回劇情

`{battle:'tutorial'}` 現在真的會發動教學戰，打完接回劇情的下一句。

- ⚠ **story.js 不 import 戰鬥模組**（單向資料流：劇情不該知道戰鬥怎麼跑）。
  改由 `story.setBattleHandler(fn)` 讓 main.js 注入發動器，回程也由 main.js 負責。
- ⚠ 交棒前要**先收掉舞台**，否則劇情層（z-index 8300）會蓋住戰鬥畫面。
- ⚠ 收掉時**不要接回首頁 BGM**（`close({keepBgm:true})`）—— 戰鬥有自己的曲子。
- **「什麼時候算打完」不看戰鬥模組的內部狀態**，看**首頁重新出現**（`#home.on`）：
  勝、敗、跳過、退出確認回主選單，所有出口都經過那裡，一個訊號涵蓋全部。
- ⚠⚠ 觀察器要**先離開過首頁才武裝**。發動的當下首頁本來就是 `.on`，不設這道的話
  任何一次 class 變動都會被當成「打完回來了」，劇情會在戰鬥還沒開始就蓋回去
  （實測就是這樣，第一版直接跳過整場戰鬥）。
- 回來時 `open(resume)` 不再擋預載頁 —— 圖上一輪已經抓過，再擋只是多一個黑畫面。

## B. 劇情專用預載頁（Ray 指定）

點 story → 自己的預載頁 → 播。
- ⚠ **不併進遊戲開機那個預載**：劇情素材是插圖與背景，一張上百 KB，全塞進開機
  會把首頁等待拉長，而多數玩家點進去是要出陣不是看劇情。代價只落在要看劇情的人身上。
- ⚠ 走**整條 scene 鏈**掃（跟著 `next` 走），不是只掃第一段 —— 中途才換的插圖
  若沒先抓，切過去那一刻會是空白（圖是 `display:block` 之後才開始下載）。
- ⚠ 保底 8 秒：慢網不要把人卡在預載頁，沒載完的用到時自己補。

## C. 兩種教學分開（Ray 指定）

`tutorial.requestReplay({story:true})` → `tutorial.isStoryRun()`。
**ver -323 起台詞也分**（見下方 -323 交接）。
⚠ 兩者都不動「已看過」旗標（`requestReplay` 本來就不動），所以劇情跑過教學
不會讓首次出陣的自動教學消失。

---

# 交接 · `ver -322`（教學打完回到故事開頭；同角色換圖會疊影）

## A. ⚠⚠ 打完教學回到**故事開頭**（Ray 回報）

`resume` 是 `{scene, line: lineIdx+1}`。而戰鬥那一句是 `dungeon_chase` 的
**最後一句**，所以 resume 的 line **剛好等於 `lines.length`**。
舊寫法用 `pos.line < cur.lines.length` 擋掉越界 → 條件不成立 → 停在第 0 句。

正解：**超出就走 `next` 接下一段**（那本來就是 `endScene` 的行為）。

⚠ 這一類「剛好等於長度」的邊界很容易漏：`{battle:…}` 放在段中間時完全正常，
只有放在**段尾**才會炸。日後任何「存位置／回位置」的機制都要想一下段尾那一格。

## B. ⚠⚠ 同一角色換圖會**疊影**（Ray：「淡入淡出時不可重疊」）

淡出是 CSS transition（180ms），但**載圖是非同步的**。舊寫法在 190ms 時
`apply(); el.classList.remove('fading');` —— 移除 class 的那一瞬間元素上還是
**舊圖**（新圖還沒載完），於是**舊圖先淡回來、新圖才蓋上去**，看起來就是兩張疊在一起。

正解：`.fading` 要等**新圖 onload 之後**才拿掉。
⚠ 還要處理「新圖已在快取」的情況 —— 那時 `onload` 不會再觸發，要靠
`complete && naturalWidth` 這條退路（同 `ensureOn` 既有的作法）。


---

# 交接 · `ver -323`（劇情版教學：諾薇兒帶完整段）

Ray 手改過的稿在 `script/TUTORIAL_LINES_NOUVELLE.md`（**那份是母本**，
逐句附了表情差分）。這一版把它接進遊戲。

## A. 只換台詞，不換流程

`config.tutorial.story`（＝ `i18n/zh.js` 的 `tutorial.story`）是**整份平行的台詞**：
`steps` / `script` / `scold` / `result`。程式端只多了四個查表器（`modules/tutorial.js`）：

    storyCfg()               storyRun 且有 story 那份才回它，否則 null
    linesForStep(trigger)    steps 的 trigger → story.steps 的鍵
    scriptLines(key, 原本)   script.* 段落
    scoldCfg() / scoldLine() 插話（原版是監察官，劇情版是諾薇兒）

⚠ **觸發點、節奏、閘門、教的東西全部共用同一套程式碼**。教學的手感已經校過，
另寫一份必然走鐘 —— 日後要再加第三個版本（別的角色帶）也照這個方式加一份資料就好。

⚠ ja/en **沒有** story 那一份（中文母本層先定案，見 `flight/script/STYLE.md`）→
`storyCfg()` 回 null → 自動退回芙蕾雅／蕾妮那一份。是預期行為，不是漏翻。

## B. ⚠⚠ 左槽被搶：諾薇兒與芙蕾雅**同站左側**

`config.tutorial.cast` 現在有三個人，但畫面上只有**左右兩個 `<img>`**。
舊的 `openStep` / `syncCast` 是**逐角色**掃全表：

- `openStep` 逐角色寫 `el.src` → 字典順序在後的那個把真正的說話者蓋掉（連取景一起蓋錯）。
- `syncCast` 逐角色 `toggle('in')` → 沒講話的那位把講話那位的 `.in` 關掉。

**兩個都改成以「槽」為單位算**：先算出本段有誰講話（`used`），
`openStep` 只套 used 的人；`syncCast` 先把每個槽要不要亮算完再一次 toggle。
⚠ 這類 bug 的味道是「**結果取決於物件的鍵順序**」—— 看到這種就要停下來。

## C. 逐句表情差分 `img` 與全畫面 cut-in `cutin`

台詞多兩個選用欄位（都是 ASSETS 鍵）：

    { who:'nouvelle', img:'tut_nouvelle_cringe', text:'…' }
    { who:'nouvelle', img:'tut_nouvelle_saint', cutin:'cutin_nouvelle_saint', text:'SAINT INSTALL......！' }

- `img`：**直接換 src，不做淡入淡出**。同一角色同一槽的表情切換，淡出會讓她整個人
  消失一拍 —— 那是「換人」的語彙，不是「換表情」的（劇情頁的淡入淡出是**換人**用的）。
- `cutin`：**先演完 cut-in 再打字**。`#cutin` 是 z8100、對話框 z8000 ——
  同時跑的話字被蓋住白打了一整句。走 `api.playCutin(done,label,imgKey)` 的 done 回呼接打字機。
  ⚠ 用 `cutinLine` 記住已播過的索引：點擊「跳完整句」會重跑 `showLine` 的分支，
  沒這個游標會**重播一次 cut-in**。

## D. 劇情版**不出結算頁**（Ray 指定，對應表第八節整段刪除）

擋在 `inspector.settle` 的**最前面**（`state.tutorialStoryRun`），不是擋在
`applyTutorialResult` —— 擋在後面的話畫面會先閃一下評價再跳走。
擋掉之後 500ms `goHome()`，由 `main.js` 那個觀察器把劇情續下去。

⚠ **不能只是「不講台詞」**：結算頁停在畫面上就沒有人把劇情叫回來，玩家卡在那裡。

⚠ `tutorialStoryRun` 是新的共享狀態（§3.9，擁有者 tutorial），與 `tutorialRun` 同壽命：
`combat.startGame` 歸零、`maybeStart` 設回、`skip()` 清掉。
`maybeStart` 另外多一道 `if(!replayRequested) storyRun = false;` ——
不是被劇情叫起來的那一場（首次出陣自動教學）一定要是原版，否則旗標會漏到下一場。

## E. 實測（390×844，走完整條）

自動走了兩趟完整流程（劇情 → 教學 → 回劇情），逐句比對：

- 開場三句 → board1 → threat → defended → strike → dualReady/dualGo →
  **saintCall（`resources/CI/Nouvelle_SAINTINSTALL.webp` 全畫面 cut-in 確實播了）**
  → saintStart ×2 → finishMB，全部是諾薇兒、差分與對應表一致。
- 故意點錯格 → 插話「別慌……順序，慢慢來就好。」＋ Cringe 差分（不是監察官）。
- 打完**沒有出結算頁**，直接回首頁 → 劇情從「對不起，我已經……！」續下去到收場。

## F. ⚠ 還沒做：諾薇兒的差分**取景值沒有逐張量**

五張差分是**不同姿勢**（跑／畏縮／驚恐／絕望／驚訝／SAINT INSTALL），
現在共用 `config.tutorial.cast.nouvelle` 的一組 `fit`，換圖時人會上下跑一點。
正解照 CLAUDE.md §6.5：逐張量 top/bot/fx，`cast` 擴成「每張差分各帶自己的取景」。
劇情頁的 `script/speakers.js` 也有同一個 TODO（`ART.nouvelle.expr` 共用 front 的數字）。


---

# 交接 · `ver -324`（獨腳戲的說明立繪放大）

Ray：「把戰鬥中的說明立繪調大」。劇情版教學全程只有諾薇兒一個人，
卻還照著「兩個人要並排塞進 390 寬」那組尺寸縮 —— 全身入鏡、臉只剩四十幾像素。

## 作法：同一組取景放大，**頭頂釘在原處**

`modules/tutorial.js` 的 `applyPortraitFit()`。`config.tutorial.cast.fit`
（zoom/drop）一個字都沒動，只是獨腳戲時多乘一個 `portraitSoloScale`（現行 **1.8**），
並重算 `bottom` 讓頭頂 y 不變 —— 放大多出來的全部從**下面**溢出（裁腿，不裁頭）。

實測 390×844：立繪高 342 → **612 px**，頭頂 y 維持 105，畫面上看到的是頭到腰胯。

⚠ 算式走**像素**不走 %：`object-fit:contain` 下「元素高」與「圖高」只有在寬度
不吃緊時才相等，`height:%` 疊 `max-width:62%` 兩個限制同時在跑，很難算準頭頂落在哪。
放大時也要一併把 `max-width` 鬆綁（那是雙人版怕撞在一起的護欄），否則寬度先吃到上限、
高度就長不上去。

## ⚠⚠ solo 是「整場」的屬性，不是「這一段台上幾個人」

`computeSoloRun()` 在 `maybeStart` 掃**整份台詞**（steps＋script）的 `who`，
只有一個人才算獨腳戲。

逐段判會炸在原版教學：它的插話段（罵人）只有芙蕾雅一個人 → 她會在插話時忽然
放大 1.8 倍再縮回去，**同一張立繪出現兩個大小**（Ray 在劇情頁定過同一條規矩）。

⚠ 判定看**資料**不看 `storyRun` 旗標 —— 哪天再加第三份台詞（別的角色帶）也自動吃到。

## 沒動到的

原版教學（首頁「教學」鈕，芙蕾雅＋蕾妮）**尺寸完全不變** —— 實測比對過。
那組數字是 ver -45 手調到「兩人五官等大、身高差看得出來」的。


---

# 交接 · `ver -325`（劇情教學收短；戰鬥交棒切乾淨；插圖／背景淡入淡出）

Ray 一次交代六件事，逐項記。

## A. 劇情教學**不可跳過**、**教到破防為止**

- 跳過鈕只在**非劇情場**掛 `.on`（`maybeStart`）。劇情教學是主線的一段，
  跳掉之後下一幕接的是「打完了」，接不下去。
- `maybeStart` 在劇情場**濾掉 `strike` 這一步**。整條
  「劇情殺三連擊 → 即死防禦 → 聖徒化 → MB／生命歸還」都是掛在
  `onStepClosed('strike')` 上的，拿掉那一步等於整條鏈都不會發生。
  ⚠ 不要改成「不講 saintCall 的台詞」—— 流程照跑，玩家會卡在沒有引導的閘門。
- 收尾改成：`dualGo` 收段 → 旗標 `awaitDualEnd` → **下一盤載入**那一刻
  封頂敵血 ＋ 講 `finishLR`「撐過來了……收拾他吧！」→ `endTutorial`。
  ⚠ 掛「下一盤載入」不掛「雙槍窗口關閉」：窗口會因清盤／敵死／逾時好幾種原因關掉，
    只有載新盤才真的代表這一輪打完。
  ⚠ 用 `finishLR` 不用 `finishMB` —— MB 那句寫的是「體力也回來一些了」，
    沒有聖徒化就沒有回血，講出來對不上畫面。
- ⚠ `ultSuppressed` 裡「第四盤 && 沒用過聖徒化 → 壓制大絕」那條要加 `!storyRun`：
  劇情版永遠沒用過聖徒化，不加的話第四盤起敵人再也不出手。

## B. ⚠⚠ 劇情插入的戰鬥**不再走收尾流程**

Ray：「教學打完會先進評價一瞬間（BGM）才繼續跑劇情，切乾淨。」

舊作法是 `main.js` 用 MutationObserver 盯 `#home.on`，等於
**先走完整條收尾流程回到首頁，再把劇情蓋上去** —— 那條路上有「驅逐完成」過渡禎
（要點一下）、結算 BGM、評價頁、主選單，全部會閃過去。

現在：`combat` 的 `win()` / `lose()` **第一件事**就是 `storyBattleEnd()` ——
`state.tutorialStoryRun` 為真就直接回呼 `setStoryReturn` 註冊的函式並 return，
整條結算流程根本不執行。`main.js` 收到回呼後走
`combat.goHome(()=>story.open(resume), {noBgm:true})`：

- 首頁確實會被還原，但那是在 `fadeTransition` 的**黑幕全蓋瞬間**，
  劇情在同一刻蓋上去，畫面上看不到首頁。
- `noBgm` 讓主選單 BGM 不起播 —— 不然交棒那一秒會漏出半句主選單的曲子。

⚠ **勝負都要接**。教學的即死防禦讓戰敗幾乎不可能，但收尾台詞之後盤面是交還玩家的，
那一段真的會死 —— 沒接的話會卡在戰敗結算頁，劇情永遠回不來。

⚠ `inspector.settle` 開頭那道 `tutorialStoryRun` 現在**正常走不到**，留著當保險：
哪天多開一條通往 settle 的路，也不會突然冒出一頁評價。

## C. 插圖可以當背景用；背景／插圖都淡入淡出

- `story.js` 的 `imgSrc()`：名字是 `NNN_` 開頭就去 `illustration/`，
  否則去 `background/`。腳本裡照樣只寫一個名字。
  （Ray：「『對不起，我已經…』的背景是插圖002」—— 插圖收掉不等於回地宮，
  聖徒撲上來那張要留在她背後當場景，只是要退到立繪之下才看得到她。）
- `swapImg()` 取代原本的 `setImg`＋手寫淡出：淡出 → **等新圖 onload** → 淡入。
  ⚠ 等 onload 是關鍵，理由同 ver -322 的立繪：不等的話舊圖會先淡回來再被蓋掉，
    看起來是兩張疊在一起。快取命中時 onload 不觸發，要靠 `complete && naturalWidth`。
  ⚠ `#storyBg` 的 CSS transition 時間要與 `FADE_MS` 同值，改一邊要改另一邊。

## D. ⚠⚠ 兩人同台不再夾中線；諾薇兒的差分**逐張量了取景**

Ray：「璐娜登場時兩人的對話立繪都太靠畫面邊緣。」兩個原因疊在一起：

1. **夾中線**（輪廓不准越過中線，越了往外推）。這些立繪的輪廓都比半個畫面寬，
   於是每次都推到底，兩人各自貼著左右邊緣，臉有一半被 `#storyCast` 裁掉。
   → 拿掉，錨點由 0.26／0.74 收到 **0.34／0.66**。臉的位置從此可預期。
   代價是輪廓會在中間交疊 —— Ray 早就定過「多少有些交疊沒關係」，
   交疊的是裙襬與頭髮的邊，臉各自在自己那 1/3 處。
   ⚠ 不要再加「把輪廓拉回畫面內」那道（ver -320 踩過）：方向與錨點相反，
     排在後面會贏，兩個人會被一起推回中間疊成一團（實測重疊 197px）。
2. **差分沿用 front 的取景**（-323 就記過的 TODO）。諾薇兒的差分是不同姿勢，
   臉的橫向位置差很多：front 0.564、Scared **0.397**。照 front 擺會把她往左推
   77px —— 那才是她貼著左緣的主因。

現在 `speakers.js` 的 `expr` 可以寫成 `{src, top, bot, fx}` 自帶取景
（字串形式仍支援），排版一律走新的 `frameOf(id, expr)`。
`story.js` 多記一個 `slotExpr`，因為排版要知道**這一槽現在用的是哪張圖**。

量法（可重跑，見 `speakers.js` 的註解）：`top/bot` = alpha>24 的上下緣；
`fx` = 頭頂往下 8% 身高那一段的 alpha 加權橫向重心 ÷ 圖寬。
校準：同法量 front 得 0.571（表上 0.564）、璐娜莉亞得 0.494（表上 0.496）——
兩個獨立校準都落在 ±0.007 內，所以直接採用量到的值。

## E. 實測（390×844）

自動走完整條：劇情 → 教學（諾薇兒帶、跳過鈕不出現）→ 破防那一盤打完 →
「撐過來了……收拾他吧！」→ 殺敵 → **結算頁完全沒出現**（程式監看 `#result.on`
全程為空）→ 劇情從「對不起，我已經……！」續下去（背景是插圖 002）→
璐娜登場兩人的臉各在 1/3 與 2/3 處 → 跑到收場。


---

# 交接 · `ver -326`（教學立繪逐張取景；雜項歸位）

## A. ⚠⚠ 教學的說明立繪：把**臉**釘住，不是把**圖框**釘住

CSS 只會把圖框貼齊左緣（`left:-2%`）。但諾薇兒的表情差分是不同姿勢，
臉在圖上的橫向位置從 **0.397（Scared）到 0.564（front）** 都有 ——
圖框對齊 ≠ 臉對齊，實測相鄰兩句臉會位移 **71px**（390 寬、立繪 408 寬）。
讀起來像鏡頭在抖，不像換表情。

`modules/tutorial.js` 的 `placePortraitX()`：依取景值算出 `width` 與 `left`，
讓臉一定落在 `portraitFaceX`（0.44×框寬）。換圖時呼叫兩次 ——
先用規格比例（1024×1536）排一次不等載入（不閃），`onload` 後再排一次修正真實比例。

⚠ 查不到取景值就把 `width/left/right` 還原給 CSS（芙蕾雅／蕾妮沒量過，行為不變）。
⚠ `.center`（引導箭頭讓位那個模式）不碰 `left`：那個 class 靠
  `left:50% + translateX(-50%)` 置中，寫死 inline left 會把它推歪半個身寬。

## B. ⚠ 取景值**只有一份**：`script/speakers.js` 的 `ART`

教學的說明立繪與劇情的立繪是同一批圖，量出來當然是同一組數字。
`config.js` 直接 `import { ART }` 再組 `tutorial.portraitFrames`，**沒有抄第二份**。

這個專案被「同一組數字寫在兩個地方」咬過好幾次（`speakers.js` 與 flight 的
`PORTRAIT` 至今仍是「改一邊要改兩邊」，那是因為隔著資料夾邊界不好共用，
不是因為抄一份比較好）。能 import 就 import。
⚠ `speakers.js` 不 import 任何東西，所以這條相依不會成環。

## C. 雜項歸位

- `TIVOT_SCRIPT_ARCHITECTURE.md` → `docs/`（CLAUDE.md §8 的待辦）。內文一字未改，
  檔頭補了「哪些已經照它做出來了／哪些還沒接」的對照表。
- `LunariaOffice.png`（2.4 MB）與 `HolyseeDungeonLow.jpeg` 轉 WebP
  （251 KB／87 KB），原圖進 `resources/_originals/`（不入版控）。
  ⚠ `TIVOT_Emblem.png` 是**例外**，團徽維持 PNG。
  ⚠ 這兩張目前**沒有任何程式引用** —— 是先備著的素材，不是漏接。


---

# 交接 · `ver -327`（預載真的等完；演出拍不出對話框；掃射抖到底；說明立繪分左右）

## A. 預載頁：**decode 完**才算載完

`onload` 只代表「下載完」—— 1024×1536 的 webp 真正解碼是在第一次要畫的時候，
而那一刻剛好是立繪滑入／插圖切換，於是第一格會頓一下或空一拍。
現在每張圖 `onload` 之後再 `decode()`，解碼也搬進預載頁裡做完。

- 保底上限 8 秒 → **25 秒**。Ray 要的是「載完才開始」，8 秒在慢網下常常是
  「還沒載完就開演」。完全不設上限的話一個卡住的請求會把人鎖死，所以保留上限。
- 最短顯示 **500ms**：快取全中時預載只要一百多毫秒，畫面會「閃一下」，
  讀起來像破圖不像在載入。實測本機 787ms。
- 每張圖的 promise 都不 reject（`onerror` 也 resolve）：缺一張圖不該擋住整場。

## B. ⚠ **空台詞的那一拍不出對話框**

Ray：「插圖002出來的時候不要先出空白的諾薇兒對話框」。
`renderLine` 看到 `text` 為空就把框藏起來直接收工 —— 那一拍是演出（咆哮、掃射），
不是對白，掛一個只有名字的空框讀起來像「她有話要說但沒說出來」。

配套加了 `auto:<ms>`：**沒有台詞的演出拍自己走完**，不等玩家點。
咆哮那一拍 `auto:3500`（`Se_enemy_Saintroar` 實測 3.34 秒），
於是「聲音放完 → 對話框與立繪一同出現」。
⚠ `auto` **只給沒有台詞的演出拍用** —— 有台詞的一律點擊推進（CLAUDE.md §6.5
「不自動跳拍」）。玩家想快轉照樣可以點，點了就提前推進。
⚠ 沒有框也沒有 ▼ 提示的一拍如果不自己走，畫面會停在那裡看起來像卡住 ——
所以掃射那一拍也給了 `auto:2200`（略長於 2 秒的掃射）。

## C. 掃射：更密、抖到底、換句就收

- 發數 44 → **96**（約 48 發/秒），火花尺寸 0.8~1.7 → **1.0~2.3**（Ray：「大量密集」「火花大一點」）。
- ⚠ 抖動要**跟著演出的長度**（Ray：「畫面抖動要連續直到射擊效果停止」）。
  單發的 `.shake` 是 0.42 秒跑完就停，配上兩秒的掃射會變成「槍還在打、畫面已經定住」。
  `.shake.hold` 把同一組 keyframes 改成無限循環，由 JS 依演出長度決定何時拿掉。
- ⚠⚠ **換句要收掉上一拍的演出**（`stopFx`，renderLine 開頭呼叫）。
  玩家可以在演出跑完之前就點掉那一拍，殘留的持續抖動會蓋到下一句上 ——
  在會抖的畫面上讀字很難受。掃射的每一發與抖動的收尾都記在 `fxTimers` 裡，一起清。

## D. 說明立繪分左右（不再置中）

Ray：「戰鬥中的說明立繪太靠中了，跟飛行畫面立繪一樣分左右邊」。
`portraitFaceX` 由單一數字 0.44 改成 `{left:0.38, right:0.62}`，
與劇情頁 solo 的錨點同值。實測諾薇兒的臉由 x=172 移到 **x=148**（390 寬）。
站哪一邊看 `cast[key].side`，不隨台詞變動（CLAUDE.md §6.5）。

## E. 璐娜莉亞的立繪比例（ver -328 解，見下）

實測兩人的身高鎖是**準的**：諾薇兒 695px/165cm、璐娜莉亞 707px/168cm，
都是 4.21 px/cm。所以問題不在身高鎖，在兩張插畫**自己的頭身比**：
量頭寬諾薇兒約 122 源像素、璐娜莉亞約 106 —— 她的臉小 13%
（正是 ver -266 索拉娜那件事的同一個量級，Ray 當時就看得出來）。

另外找到一個**可能的量測錯誤**：她穿厚底細跟鞋，`bot` 量的是**鞋底**。
如果 168cm 是赤腳身高，那 1519px 對應的其實是 168＋鞋高（估 11~14cm）≈ 180cm，
現在等於把她放大了 7~8%。

兩個方向相反（「臉太小」要放大她、「鞋高沒算」要縮小她），
所以**先問 Ray 他看到的是太大還是太小**再動，不要憑猜改。


---

# 交接 · `ver -328`（璐娜莉亞的臉太小 → 逐張的畫風補償 `faceAdj`）

Ray 選的方向：**臉太小，人不用動**。

## 為什麼不是身高鎖壞掉

實測身高鎖是**準的**：諾薇兒 695px/165cm、璐娜莉亞 707px/168cm，都是 4.21 px/cm。
問題在兩張插畫**自己的頭身比**：璐娜莉亞被畫成八頭身、諾薇兒接近六頭半 ——
同樣的身高之下她的臉小約一成。這是畫風差異，不是排版錯誤。

## 作法：`ART.lunaria.faceAdj = 1.10`

`speakers.js` 的取景值多一個選用欄位，`story.js` 的縮放乘上去
（`s = pxCm*cm/(bot-top) * (faceAdj||1)`）。沒寫就是 1，其他人不受影響。

⚠ 這個值是**看出來的，不是量出來的** —— 它補的是畫風差異，沒有客觀基準可量。
⚠ 代價寫在資料旁邊：她在畫面上會比 168cm 該有的高度**大一成**，
  也就是拿「身高的真實性」換「臉的可讀性」。Ray 定案要後者。
⚠ **不要改成鎖眼寬／鎖臉寬來自動解**（CLAUDE.md §6.5 踩過）：那會把畫風差異
  **全額**放大成體型差異，四個人的腳就不會落在同一條地平線上。
  逐張補一個係數是有上限、可控的做法 —— 而且哪一張被補過、補多少，看資料就知道。

## 實測（390×844，「！！」那一格）

璐娜莉亞的圖框由 477×715 變 525×787，臉的落點不變（錨的是 fx）。
兩人的頭在畫面上由「55 vs 48」拉近到接近等大。

⚠ 還想再大一點就調 `faceAdj`（1.10 → 1.15 之類），**一個數字**，不要動別的。

## 順帶記一筆：量臉的大小為什麼沒有自動化

試過三種都不可靠：
1. **alpha 輪廓**在頭部那一段 —— 頭髮會算進去（璐娜莉亞的雙馬尾橫向比臉寬三倍）。
2. **膚色連通區**在眼線那一列 —— 起點落在眼睛或鼻影上就整條斷掉（諾薇兒實測回傳 5px）。
3. **頭頂→眼線距離** —— 受頭部俯仰影響太大（同一個人不同差分從 118 到 216 都有）。

所以 `faceAdj` 用眼睛看、用一個數字記下來，是目前最誠實的做法。


---

# 交接 · `ver -329`（Kerberos 之門：劇情進戰鬥的轉場）

Ray 指定的演出：下方盤面區換成 Kerberos 之門（紋章保持在面板正中）→
進戰鬥時門由下往上升、觸頂震動 → 四箭向外彈開 → 紋章浮起、旋轉 180° →
**門由中縫拉開，縫裡露出的就是戰鬥畫面**。

## A. ⚠⚠ 素材：來源是**一張合成好的平面圖**，分件是我切的

`Kerberos.png` 853×1844，**不透明、沒有 alpha、沒有圖層**。門上的紋章與四支箭
都是畫死在上面的。`tools/kerberos_cut.py` 切出三種零件：

    kerberos_door.webp        門（紋章挖掉、補成凹槽）
    kerberos_crest.webp       紋章圓盤（圓形羽化遮罩）
    kerberos_arm_{n,e,s,w}    四支箭（銅色遮罩去背）

⚠ **凹槽是合成的**（把該區壓暗、往石材色靠、邊緣留一圈亮＝內壁受光）。
  拿到分層檔的話直接換掉輸出檔，座標常數不用動。
⚠ 原圖放 `resources/_originals/`（不入版控，同專案慣例）。要重切就放回去再跑腳本。

### ⚠⚠ 試過而失敗：把四支箭從圓盤上「挖掉」

原本想做成「箭飛走之後圓盤上就沒有箭了」。**做不到**：
上下那兩支箭是**同一支十字架的兩端**，貫穿整個紋章 —— 挖掉會把十字架切斷；
遮罩一膨脹又把周圍的浮雕一起吃掉，看起來像紋章壞了，不像箭飛出去。

正解：飛出去的是**複製品**，圓盤保持完整。
「彈開」讀起來是「從四個尖端射出去的東西」，不是「紋章少了四塊」——
而且圓盤完整，後面那段浮起旋轉才成立。

## B. 幾何是**解出來的**，不是調出來的

`layoutKerberos()`：兩個條件同時要成立 ——
①紋章圓心落在下半面板正中 ②升到頂時蓋滿整個畫面。

    令 cy＝紋章圓心佔門高的比例、AR＝門的高寬比
    門頂 = 面板中心y − cy·AR·Wd ，要求 ≤ 面板頂  →  Wd ≥ (面板高/2)/(cy·AR)
    再與「至少要有畫面那麼寬」取大的；上升距離就是門頂那個值。

實測 390×844：門 413×893、頂在 y=472.6（＝面板頂）、上升 472.6 → 升完門頂貼齊 0。
⚠ 每次開場與 resize 都要重算 —— 面板高吃 safe-area，寫死在瀏海機上會錯位。
⚠ 要在舞台已經 `.on` 之後才量，`display:none` 的元素量出來全是 0。

## C. ⚠⚠ 門縫裡要露出戰鬥畫面 → 交棒順序**反過來**

舊的順序是「先 close 劇情、再交棒戰鬥」。那樣門一開只會露出黑幕。
現在：`playKerberos(onGap, onDone)` ——
`onGap` 在門要拉開之前呼叫（讓底下先開戰），`onDone` 在門全開之後才 `close()`。

配套兩件事：
1. `#storyStage.kerb-open` 把舞台背景改成透明、其餘場景層全部 `display:none`，
   只留 `#kerb` —— 縫裡才看得到底下。
2. `launchBattle({instant:true})`：劇情那一場**跳過櫻花過渡禎**，直接 `startGame()`。
   ⚠ 不跳的話門一開露出的是櫻花，兩段轉場疊在一起。

## D. 其他要記的

- 演出期間 `kerbPlaying` 鎖住 `advance()` —— 不然玩家一點就跳過整段演出。
- 四支箭用 **keyframes** 不用 transition：它們平常 `opacity:0`（免得與圓盤上原本的
  箭疊出毛邊），要「先現形再飛出去」；transition 得先把 opacity 設成 1 再改回 0，
  中間會閃一幀，keyframes 的 from/to 一次寫完就沒這問題。
- 門的素材要進**劇情預載**（`preloadStory` 有加）—— 它是進戰鬥那一刻才動的，
  沒先抓的話升上去是一片空白。
- **音效還沒有素材**（Ray：先不配音）。接點留在 `story.js` 的 `KERB_SFX`
  （thud / pop / spin / open 四個鍵），填路徑就會響，程式不用再動。
- 時長：升 700ms → 撞頂 420ms → 彈開 420ms → 浮起旋轉 900ms → 開門 900ms，
  含間隔約 3.3 秒。要縮就動 `KERB_T`。

## E. 實測（390×844，完整跑一遍）

平時：門在下半、紋章正中、全亮。
進戰鬥：升到頂蓋滿全畫面 → 四箭各自飛出畫面（實測 n 到 y=−114、s 到 395、
e 到 339、w 到 −37，opacity 收到 0）→ 紋章 rotate(180deg) scale(1.11) 浮起 →
門左右拉開 → 縫裡是**已經在跑的教學戰**（諾薇兒帶）。劇情層在門全開後才收掉。


---

# 交接 · `ver -330`（換上 Ray 畫的分件；紋章不位移、跟左扇一起退場）

## A. 紋章：**圓心不動**，只放大＋旋轉；退場跟著左半扇

Ray：「圓盤不要位移，保持圓心位置略為放大就好，然後跟著左側的門一起退出。」

- `lift` 那一拍拿掉 `translateY`，只留 `scale(1.11) rotate(180deg)`。
- `open` 那一拍它與左半扇走**同一個 px 位移**（`--kerb-open-x`）—— 它們是同一個剛體。
  ⚠ 不能各自寫 `%`：`%` 是相對**自己**的寬度，紋章與半扇門寬度不同，
    當場就會走不同距離、脫節。
  ⚠ 位移量取兩者的大者：紋章比半扇門寬（67.5% vs 50%），只按門寬走的話
    門都出畫面了紋章還留在縫裡（實測右緣停在 x=133）。
  ⚠ 還要算**放大之後**的右緣（lift 會 scale 1.11），照原尺寸算會短一截。
    現行係數 1.14＝1.11 再留一點餘裕。實測右緣收在 x=−4，與左扇一起乾淨出場。

## B. 換上 Ray 畫的分件（門／紋章）

Ray 給了三張透明圖，放 `resources/_originals/kerberos/`（不入版控）：

    door.png    門，**紋章已經挖掉**（853×1844，不透明）
    crest.png   去背的紋章圓盤（1226×1283）
    arm.png     去背的箭（1214×1295）

門與紋章已換上：**門的凹槽不再是程式合成的**，是畫出來的，質感差很多。
`tools/kerberos_cut.py` 現在只做「轉檔＋縮到門上的實際大小」。

### ⚠⚠ 三個檔**各自是各自的畫布尺寸**，不是同一張門的座標系

所以位置與縮放是**對位算出來的**：拿最早那張「紋章還在門上」的合成圖當基準，
掃縮放與位移取邊緣相關性最大的那組（`python3 tools/kerberos_cut.py align`）。
紋章實測 `CREST_BOX = (136, 80, 576, 603)`，score 0.173，疊上去逐一吻合。

⚠ **箭對不了**：它太細長（447×1256），在滿是浮雕的門上相關性會收斂到最小尺寸、
  落在花押中間 —— 那是雜訊不是解。所以四支箭**目前仍由最早那張合成圖切**。

### 要一勞永逸：請 Ray 把每一件都輸出成**與門同尺寸（853×1844）的透明圖**

那樣全部貼 (0,0) 就對，不必再猜、也不必再跑對位。這是後續要素材時的標準格式。

## C. 還缺的

- **卯釘**：Ray 說做了，但只收到三個檔，沒有卯釘那一張。
- **沒有箭的紋章**：要讓四支箭**真的離開圓盤**（而不是飛複製品）就需要這一張。
  現行作法與理由見 ver -329 B 節。
- **音效**：接點仍留在 `KERB_SFX`（thud/pop/spin/open），素材到了填路徑即可。


---

# 交接 · `ver -331`（Kerberos 換成 Ray 的完整分層：楣／圓盤／箭／鉚釘＋冒煙）

Ray 補齊了分層，整組換掉。**程式端不再合成任何素材**，全部是畫出來的。

    Kerberos_base.png   門本體（紋章整組拿掉）      → kerberos_door.webp
    Kerberos_plate.png  圓盤（**不含箭與鉚釘**）    → kerberos_plate.webp
    Kerberos_arrow.png  箭（一支，四向靠旋轉）      → kerberos_arrow.webp
    Kerberos_revet.png  鉚釘（一顆，四向靠旋轉）    → kerberos_rivet.webp
    Kerberos_top.png    楣（槍棺頂端的橫飾條）      → kerberos_top.webp
    Kerberos_All.png    完整的門 —— **只當對位基準**，不輸出

原始分層放 `resources/_originals/kerberos/`（8.5 MB，不入版控）；上線的五個 webp 約 480 KB。

## A. ⚠⚠ 對位：分件各自是各自的畫布，位置是**算出來**的

`python3 tools/kerberos_cut.py align` —— 拿 `Kerberos_All` 做邊緣相關性搜尋。

- **圓盤**：搜出來很穩（score 0.165）→ `PLATE_BOX = (96, 76, 666, 624)`。疊回去逐一吻合。
- **箭**：**要限制在中軸那條窄帶**才搜得到（score 0.162）→ `ARROW_TOP = (395, 76, 66, 185)`。
  ⚠ 不限制範圍的話它會收斂到最小尺寸、落在花押中間 —— 那是雜訊不是解。
- **鉚釘**：**放棄搜尋**。46px 的東西在滿是浮雕的門上，四個方向搜出來的尺寸與位置
  互相矛盾（全是雜訊）。改成以**圓盤中心為原點、按半徑對稱擺位** ——
  它在畫面上只有 22px、彈開 0.4 秒就沒了，對稱擺位比「搜出來的位置」更可信也更好看。

⚠ 箭與鉚釘都是以圓盤中心按半徑擺的，所以**只要 PLATE_BOX 對，它們就一定對稱**。
  改圖只要重跑 align 更新 PLATE_BOX 與 ARROW_TOP 兩行。
⚠ 幾何 JSON 給的是**中心點＋未旋轉的尺寸＋旋轉角**，不是「左上角＋已旋轉的寬高」——
  CSS 的 rotate 繞元素中心轉，只要中心對，轉幾度都落在該落的地方；
  給已旋轉的寬高的話前端會把旋轉再做一次（等於轉兩次）。

## B. ⚠ rot 決定的不只是外觀，也決定**彈開的方向**

CSS 是 `rotate(--kerb-rot) translateY(-x)` ＝「往自己的上方推」。要往外推，
rot 必須讓自己的上方指向外側。順時針轉，(0,−1) 轉 90° 變成 (1,0)＝右 →
**北 0／東 90／南 180／西 270**。（一開始寫成東 270／西 90，兩支箭是往內縮的。）

⚠ 旋轉與位移要寫在**同一個 transform** 裡。分成兩條規則的話，後面那條會整個蓋掉
  前面那條 —— transform 不會疊加。

## C. 楣（Kerberos_top）：與槍棺同一剛體

Ray：「加到劇情播放器的最下方，與下方控制區無縫貼合，槍棺往上推時一起被推上去。」

作法：楣是 `#kerbDoor` 的**子元素** —— 門往上推它自然跟著走，不必另外做動畫。
橫跨整個畫面寬，**下緣貼齊門的上緣**（往下壓 1px 免得留一條髮絲縫）。

⚠⚠ 平常的裁切要**往上多留一條楣的高度**（`--kerb-top-h`，JS 依實際圖高寫入）：
  `clip-path: inset(calc(var(--story-top) - var(--kerb-top-h)) 0 0 0)`。
  只夾到面板頂的話，掛在門上緣之外的楣會被整條裁掉，畫面上完全看不到。

## D. 解鎖那一拍

`unlock` 一個 class 同時做兩件事：
- **鉚釘依次彈開**：每顆帶自己的 `--kerb-d`（0/90/180/270ms，順時針），
  keyframes 先往內壓 6% 再彈出去 —— 有蓄力才像「鎖被頂開」。
- **箭微幅外推 8%**：⚠ 它們大半藏在圓盤底下，只有尖端露在盤外。
  推太遠會整支從盤底下抽出來，那是「零件掉了」不是「鎖開了」。

**冒煙**：每顆鉚釘彈開處生 4 顆煙團（`kerbPuff`）。
⚠ 掛在 `#kerbSmoke`（**門的子元素**）—— 掛在舞台上的話門在升、煙站著不動。
⚠ 顏色要**灰塵**不要白光：第一版用 rgba(226,220,208,.55)，在深色門上讀起來是
  「閃光」不是「冒煙」，改成偏灰褐、整體不透明度也降下來。

## E. 實測（390×844）

平時：楣壓在場景與控制區的交界、下緣齊平；圓盤在面板正中。
進戰鬥：槍棺（含楣）上推蓋滿全畫面 → 撞頂震動 → 四顆鉚釘依序彈開＋冒煙、
四支箭各推 7px → 圓盤原地放大旋轉 180° → 門與圓盤一起向左右拉開 →
縫裡是已經在跑的教學戰。全程約 3.6 秒。


---

# 交接 · `ver -332`（Kerberos 修四件事；Credit 補兩首）

## A. ⚠⚠ 圓盤會位移 → 素材要裁到**內容邊界**

Ray：「圓盤位移了，效果比上一版還差。」

原因不在動畫（`lift` 只有 scale＋rotate，沒有 translate），在**素材的圖框**：
`Kerberos_plate.png` 是 1295×1214，但內容（alpha>12）只佔 (114,33)–(1173,1127) ——
**圖框中心不等於圓心**。轉 180° 是繞圖框中心轉的，於是整個盤跳一下。

修法：`abox()` 先裁到內容邊界再用，圖框中心＝圓心，原地轉就真的原地。
實測 `plateMoved: [0,0]`。

⚠⚠ **不能用 `im.getbbox()`** 做這件事 —— 它算 alpha>0，而這些圖的邊緣有一圈很淡的
光暈。楣實測 alpha>0 是 2112×492、alpha>12 只有 2105×234，差了一倍。

## B. 楣：對齊的是**中段的下緣**，不是圖的下緣

Ray：「楣還是有縫，是楣本身結構造成的，讓楣往控制面盤壓，中間不要漏。」

楣的下緣不是一條直線：兩端的鉚接塊比中間的橫桿低 28px／235（`top_dip` 量出來的）。
照圖的下緣對齊控制列，中間就露一條縫。改成按**中段**下緣對齊，兩端順勢壓進控制列 ——
`#kerb`(z2) 在 `#storyBoard`(z1) 之上，壓進去看不出來。

## C. ⚠⚠ 鉚釘是**鏡射**不是旋轉；位移要在父座標系算

Ray：「卯釘的角度都不對，參考 reference/Kerberos。」

實測：鉚釘素材**原封不動**（不轉不翻）在 10 點鐘那顆的邊緣相關性 **0.281**
（整個紋章區最高），2 點鐘那顆的最佳解是**水平鏡射**（0.204）。
美術是照**兩軸鏡射**鋪的，不是繞中心旋轉 —— 照旋轉擺四顆全歪。
現在四顆走 `scale(±1, ±1)`：r10(1,1) / r2(−1,1) / r8(1,−1) / r4(−1,−1)。

⚠⚠ 因此**位移一律在父座標系算好**（`--kerb-px/py`，由各自的外向單位向量算），
不要寫 `rotate(...) translateY(-x)` —— 鏡射會把 translateY 的方向弄反，
上面兩顆往外、下面兩顆就往內。實測修正後 r10 走 (−8,−8)、r4 走 (+8,+6)，四向都對。

## D. 箭與鉚釘：疊在圓盤**之上**，彈完不消失，開門跟著走

Ray：「箭頭一開始就要在圓盤上，最上層」「移動完成前後都是剛體，與背景同步，
只有移動時獨立」「門打開的時候箭跟釘都是附著於門的剛體，要一起向外移」。

- DOM 疊序改成 門 → 圓盤 → 箭 → 鉚釘 → 煙 → 楣。
- 彈開改用 **transition** 不用 keyframes，而且**不淡出** —— 舊版飛出去再消失，
  那是「零件掉了」不是「鎖鬆開了」。
- `#kerb.open` 時箭與鉚釘各自加上 `translateX(-var(--kerb-open-x))`，
  與左半扇、圓盤同一個位移量 → 整組一起退場。

## E. ⚠ 還沒解決：箭的尺寸與 reference 對不上

`reference/Kerberos.png`（＝`Kerberos_All.png`，同一張）的紋章**比新分層複雜**：
外圈多一層齒狀環、橫軸有大型矛頭。用 base＋plate＋arrow＋rivet 合成出來的
與它並不一致（見 scratchpad 的 kerb_side2.png 對照）。

判斷：**Ray 的新分層是新設計，不是舊合成圖的拆件**，所以不必也不該對得起來。
箭的尺寸目前照長寬比 1:2.80 推出 66×185（門像素），視覺上合理。
⚠ 若之後發現箭該更大／更長，只要改 `ARROW_TOP` 一行，其他（含四向對稱）自動跟著走。

## F. Credit 補兩首

- **Obsession / PeriTune / Lunaria Theme**（Ray 指定）
- **Crisis / PeriTune / 地宮（劇情）** —— 早就在用卻一直沒列。加素材時要順手加，
  不要等想起來。


---

# 交接 · `ver -333`（戰鬥立繪改吃飛行畫面那一套；紅點定位；璐娜莉亞再放大）

## A. 戰鬥中的說明立繪＝**飛行畫面的那一套規則**

Ray：「戰鬥時的對話立繪要像飛行畫面一樣，角色分左右站，只有一名角色就固定佔一邊」
「人物同一張立繪不可改變大小，立繪原則以飛行畫面原則為準，分左右，可略覆蓋」。

`modules/tutorial.js` 的 `placePortraitX()` 現在照 CLAUDE.md §6.5 算：

    縮放  鎖**身高**：每公分像素 × 角色身高 ÷ 這張圖裡人物的像素身高。
          每公分像素只由「頂線到框底」與 castShow×castTall 決定 —— 是個常數，
          所以**同一張立繪永遠同一個大小**，不再隨場合變。
    站位  臉錨在 portraitFaceX 的左／右（cast[key].side 決定，不隨台詞變動）；
          只有一個人也固定佔自己那一邊，不置中。
    交疊  允許。

參數在 `config.tutorial`：`castShow` 0.56 / `castTall` 176 / `portraitTopPct` 3
（與 `modules/story.js`、flight 同義同值）。
實測 390×844：諾薇兒立繪 457×686、頭頂 y=10、臉落在 x=148（＝0.38×390）。

⚠ **舊立繪（芙蕾雅／蕾妮）沒有取景值** → `hasFrame()` 為假就整段跳過，
  退回原本的 zoom/drop 百分比算法，原版教學的版面完全不變。
⚠ `portraitFrames` 每一筆都要帶 `cm` —— speakers.js 的 expr 只帶 top/bot/fx，
  身高在角色那一層，config 組表時要合進來，否則鎖身高沒有分母。
⚠ 紅點在對話期間本來就壓在立繪之上（`body.dlg-pause #redDots{z-index:21}` > `#tutCast` 20），
  所以立繪變大不會把教學要講的光圈蓋掉 —— 這條先前就寫好了，別拿掉。

## B. ⚠ 防禦教學的第一顆紅點：**固定位置，失敗也不變**

`firstThreatPending()` 的條件由「threat 這一步還沒觸發」改成 **`!defendedDone`**。

舊寫法一觸發就不算數了 —— 於是「太早格擋 → 罵完重放」的那一顆會跳到隨機位置
（Ray：「失敗也不要變」）。教學階段的紅點位置是教材的一部分：位置一直換，
玩家會以為是自己記錯了。

## C. 璐娜莉亞 `faceAdj` 1.10 → 1.22

Ray：「頭明顯比諾薇兒小，臉要差不多大小。」

量法（可重跑）：把兩張圖各自照鎖身高的縮放算好（pxCm 4.21 時 s＝0.454／0.4656），
裁出頭部並排比對 —— 臉寬約 **60 : 50**，缺約兩成。1.10 只補了一半。

⚠ 代價照舊寫在資料旁邊：她在畫面上會比 168cm 該有的高度大兩成。
  這是拿身高的真實性換臉的可讀性，Ray 定案要後者。


---

# 交接 · `ver -334`（站位抄對飛行畫面；昂立的人頭要高；楣浮到最上；門縫十字光）

## A. ⚠⚠ 「同飛行畫面」＝去抄**飛行畫面那一行**，不要抄近似值

Ray 連續三次回報「戰鬥中的說明立繪太靠中間」，最後一次是「為什麼講不聽」。
原因：我抄的是 `story.js` **單人**時的 0.38／0.62，而飛行畫面實際用的是

    flight/index.html: const anchor = (c.side==='L') ? 0.24 : 0.76;

差了 0.14 個畫面寬（390 上是 55px）—— 難怪一直被說靠中間。
現在 `config.tutorial.portraitFaceX = {left:0.24, right:0.76}`，
實測諾薇兒的臉落在 x=94（＝0.24×390），敵人整個讓出來。

**教訓**：需求寫「同某某畫面」時，去讀**那個畫面的程式碼**，別拿記憶中的近似值。

## B. ⚠ 頭頂落點用 `standCm`，不要動 `cm`

Ray：「諾薇兒彎腰站位較低沒關係，但是璐娜是昂立，頭頂高應該較高。」

鎖身高假設的是**站直的人**。諾薇兒的絕望差分是彎腰的，畫出來的身體被壓短，
頭頂落得低 —— 那沒問題。但璐娜莉亞是昂立的，頭頂就該明顯比她高。

`speakers.js` 多一個選用欄位 `standCm`（站姿身高），**只用來算頭頂落點**：

    headY = 頂線 + (CAST_TALL − (standCm || cm)) × 每公分像素

璐娜莉亞給 `standCm:176`（＝全場最高的基準）→ 頭頂貼頂線。
實測頭頂差距由 13px 拉開到 **46px**（諾薇兒 104 / 璐娜莉亞 58）。

⚠ **不要改 `cm` 去達成這件事** —— `cm` 是縮放的分子，一改人就跟著變大變小。

## C. 楣的圖層：z-index 2 → **5**

Ray：「楣被遮住了，在沒有 cut in 的狀況下楣的圖層應該只低於對話框。」
原本 `#kerb` 是 2，被插圖(2)／情境卡(4)蓋掉。抬到 5：低於預載頁(6)與對話框(8000)，
高於所有場景層。⚠ 場景層都只佔上半（`height:--story-top`），所以抬高不會蓋到
它們該顯示的內容 —— 只有位在交界處的楣會浮上去，那正是要的。

## D. 門縫透出的十字亮光（Ray：「初版那個不錯，加回去，光用偏青的藍色」）

`#kerbGlow`：豎的沿門的中縫（吃 `KERB_META.seam` 的 0.506，不是寫死 50%）、
橫的沿紋章的橫軸（＝左右兩支箭的中心線）。撞頂那一下猛亮，再收成微弱常亮留到開門。

⚠ 用 `mix-blend-mode:screen` —— 加色混合才像**透出來的光**；一般疊加在深色門上
  只會變成一條灰白的貼紙。
⚠ 顏色偏青 `rgb(120,220,255)`：純白會讀成反光，青藍才像門後有東西亮著。


---

# 交接 · `ver -335`（換新圓盤；戰鬥立繪縮小；對話框讓開控制列；劇情站位也抄飛行畫面）

## A. 換 `Kerberos_plate`（新版：THE IVth ORDER OF TESTAMENT）

⚠ **換圖不必重跑 align**：圓盤坐的是門上**同一個圓槽**，直徑不變。
只要量新圖的內容框（實測 1171×1165，最寬那一列就是圓盤本身），
讓直徑仍是 545、圓心仍在 (427.5, 374)，高度按新的長寬比換算 →
`PLATE_BOX = (155, 103, 545, 542)`。箭與鉚釘是以圓盤中心按半徑擺的，自動跟著更新。

（align 跑一次要兩分鐘以上，而且對這種「同一個槽換一張圖」的情況根本不需要。）

## B. 戰鬥立繪縮小：`castShow` 0.56 → **0.68**

Ray：「戰鬥立繪太滿，縮一下。」
戰鬥框裡還有敵人、血條、紅點要看，立繪不能像劇情頁那樣壓滿。
值越大＝鏡頭越遠＝人越小。實測 457×686 → **376×565**，敵人整個露出來。
⚠ 0.68 約是頭到膝，仍在 §6.5 的「膝部以上」原則內。

## C. ⚠⚠ 對話框：z-index 4 → **7**，底邊錨在楣的上緣

Ray：「對話框不要壓操作面板，對話框圖層在楣之上。」

兩件事一起壞的：
1. ver -334 把 `#kerb` 抬到 5 之後，z-index 4 的對話框**被楣壓住**了。→ 抬到 7。
   （連帶把預載頁 6 → 9，它本來就該蓋住全部。）
2. 對話框原本 `top: calc(var(--story-top) - 78px)`，台詞一多就往下長到控制列上。
   → 改成 `bottom` 錨定在**楣的上緣**：`calc(100% - var(--story-top) + var(--kerb-top-h))`。
   台詞增行時框往上長，底邊不動。實測框底 429 < 楣頂 435。

⚠ `--kerb-top-h` 要**同時寫在 `#storyStage` 上**（原本只寫在 `#kerb`）——
  CSS 變數只往子孫繼承，對話框不是 `#kerb` 的子孫，讀不到。

## D. 劇情頁的站位也抄飛行畫面（0.24／0.76）

Ray：「劇情播放器的立繪配置邏輯同戰鬥中對話播放立繪配置邏輯，說好幾次了你還是往中間放。」

`modules/story.js` 原本是 0.38／0.62（單人）與 0.34／0.66（兩人），比飛行畫面
靠中間 0.10~0.14 個畫面寬。現在**單人兩人都用 0.24／0.76**，與 flight、教學同一組值。

⚠ **單人也用同一組**：站位是角色的屬性，不該因為台上有幾個人而改變。


---

# 交接 · `ver -336`（門的音效／光只走邊緣／立繪不再兩種呈現／新圓盤）

## A. ⚠⚠ 三支門音效：時間點是**由音檔反推**的，不是對齊動畫起點

用瀏覽器 `decodeAudioData` 量出每支的峰值與首尾過門檻點：

| 音檔 | 長度 | 撞擊峰值 | 可聞段 |
|---|---|---|---|
| `se_Kerberos_pop`  | 2.088s | **1.002s** | 0.206–1.674 |
| `se_Kerberos_gear` | 6.864s | 2.601s | 0.023–6.856 |
| `se_Kerberos_open` | 2.376s | 1.153s | 0.346–**1.921** |

- **pop**：演出第 0 毫秒就播，並把「上推」的長度定成 **1000ms** ——
  門撞到頂的那一瞬正好是撞擊峰值（Ray：「以撞頂那一瞬為撞擊音回推播放時間」）。
- **gear**：旋轉開始播、**旋轉結束就收掉**（Ray：「轉動開始，停轉結束」）。
  素材 6.9 秒遠長於動畫，不收的話門開完了齒輪還在轉。旋轉也由 900→**1600ms**。
  ⚠ 收要**斜降再 stop**，直接 stop 有一聲喀 → 為此在 `audio.js` 加了 `SFX.playCue()`，
    回傳可中止的把手（`.stop(fadeMs)`）。這是 SFX 第一支「演出要收尾」的音。
- **open**：從「門全開的時刻」往回推 **1921ms**（可聞段長度）開始播，
  於是它收在門全開那一刻。通常會與 gear 重疊 —— Ray 說可以。

⚠ 三支都要進**劇情預載**：撞擊音在第 0 毫秒就要響，現抓一定遲到
（`audio.js` 的 `LATE_PLAY_MS` 是 1.5 秒，遲到就乾脆不播）。
⚠ **換音檔要重量這三個數字**。

## B. 光只走圓盤邊緣 → 把光層排在圓盤**之前**

Ray：「圓盤區域內不要有光，只有邊緣透光。」
`#kerbGlow` 在 DOM 上移到 `#kerbPlate` 之前＝畫在圓盤之下。圓盤是不透明的，
自然把中段擋住，畫面上只剩「從圓盤邊緣透出來的光」。
⚠ 用遮罩去挖也做得到，但那要跟著圓盤的縮放與旋轉一起維護；靠疊序是**零維護**。

## C. ⚠⚠ 同一張立繪不可有兩種呈現（已寫進 CLAUDE.md §6.5）

Ray：「『讓開』出來的時候諾薇兒立繪縮小了，同一立繪不可以有兩種大小，寫入原則。」

實測**尺寸其實一樣**（697 = 697）—— 變的是位置：身高讓位原本只在兩人同台時做，
單人那一格不讓，於是她在兩格之間**下沉 46px**，讀起來就是變小。
改成**一律套用**（`headY = 頂線 + (CAST_TALL − standCm) × 每公分像素`）。
代價是單人時頭頂上方會空一塊 —— 那是身高的誠實表現，接受它。

## D. 其他

- 箭的彈開幅度 0.016 → **0.045**（佔門寬）。
- 立繪**頂線不得高於左上角那顆鈕的下緣**（`#testClearBtn`，量實際位置不寫死）——
  再高會被動態島吃掉（Ray 指定）。實測頭頂 56、鈕底 54。
- 對話期間 `#barsBlock` 與 `#hud` 抬到 z21（同紅點）：立繪是裝飾，
  血條與破防計量表是玩家要讀的東西，不能被蓋（Ray 指定）。
- 插圖 002 改成**背景**、且「對不起，我已經」那一句**什麼背景都不寫** ——
  上一拍已經是 002，再寫一次會觸發一次淡出淡入（Ray：「背景不要動」）。
- 「！！」那一拍 `hide:'UNKNOWN'`：立繪是持續狀態，不明寫她會一直站著。
- `Kerberos_plate` 再更新一次（內容 1086×1110）→ `PLATE_BOX=(155,95,545,557)`。
  同樣不必重跑 align：直徑仍鎖 545、圓心仍在 (427.5,374)。


---

# 交接 · `ver -337`（門的音效收尾；對話框讓開血條；立繪與背景融合）

## A. 音效三件

- **神楽鈴不再響**：`launchBattle({instant:true})` 不播 `sfx_startbt`。
  劇情場次的轉場是 Kerberos 之門，門有自己的撞擊／齒輪／開門三支；
  再疊一聲神楽鈴等於兩套儀式撞在一起（Ray 指定移除）。
- **`se_Kerberos_steam`**：第一顆鉚釘跳開時播（Ray 指定）。
  ⚠ 只在**第一顆**播 —— 四顆各播一次會糊成一片，聽起來像雜訊不像噴氣。
  ⚠ 這支是 **.wav**，其餘是 .mp3 → `KERB_SFX_EXT` 記例外，不要假設副檔名。
- **`se_Kerberos_gear` 提前到撞頂**（Ray 指定）：機關是撞到頂才被頂開的，
  聲音比畫面早一步起來才像「裡面的東西動起來了」。收在旋轉結束（不變）。

## B. 對話框讓開血條（戰鬥）

`placeBubble()` 的底邊由「貼 `#grid` 上緣」改成「貼 `#barsBlock` 上緣」。
⚠ 血條與破防計量表就在盤面與敵人框之間，貼盤面等於把它們整條蓋掉。
⚠ ver -336 把它們的 z-index 抬到 21 是為了壓過**立繪**（z20）；
  對話框是 z-8000，抬 z 救不了 —— **位置本身要讓開**。
實測：對話框 289–375、血條 379–414，中間留 4px。

## C. 立繪與背景融合（新檔 `modules/tone.js`）

Ray：「人物立繪根據背景做小幅亮度調整，使人物與背景融合度高一點。」

量背景的平均亮度（縮到 16×16 再讀，整張讀 getImageData 在手機上是幾十毫秒的
同步阻塞），換算成 `brightness(0.88~1.12) saturate(0.94~1.04)`，套在**立繪的容器**上。

⚠ 幅度鎖在 ±12%：立繪是玩家要看清楚的東西，壓太暗就變成「這個人在陰影裡」——
  那是 §6.5「非說話者壓暗」在講的事，不是這裡要做的。
⚠ 套**容器**不是逐張立繪：容器的濾鏡與立繪自己的 `dim`/`dark` 自然疊加，
  不必去合併字串，也不會互相覆蓋。
⚠ 判「背景看不看得見」要用**實際尺寸**（offsetWidth），不要用 `.on` class ——
  劇情的背景靠 .on 控制顯示，戰鬥的敵人底圖沒有那個 class（第一版這樣寫，
  戰鬥裡整段不生效）。
⚠ 同一張圖只算一次（快取）；跨網域圖會污染 canvas → 回 null，當作「不調」。

實測：地宮背景 → `brightness(0.922) saturate(0.958)`（劇情與戰鬥都套到了）。

## D. 掃射那一拍畫面上不留人

`hide:['NOUVELLE','UNKNOWN']`（Ray 指定）。立繪是持續狀態，不明寫會一直站著。
那是「子彈打在牆上」的鏡頭，不是誰在講話。


---

# 交接 · `ver -338`（門的光收乾淨／插圖轉場／音檔統一／場景間讀取頁）

## A. 門的光

- **開門後不留殘光**（Ray 指定）：`#kerb.open #kerbGlow{opacity:0}` 並把 `animation:none`
  ⚠ 光的 keyframes 是 `forwards`，會把末影格留著 —— 只調 opacity 不夠，動畫也要停。
- **圓盤轉動前的環狀溢光**：`#kerbGlow` 多一顆 `.kg-r`（圓形 radial-gradient），
  比圓盤大一圈（1.16），圓心對齊。它與十字光一樣排在圓盤**之下** ——
  內側被盤擋住，畫面上只剩一圈邊緣的光。
  ⚠ 半徑由 JS 依圓盤實際大小寫入，不要寫死 %：圓盤換圖會變大小。

## B. 插圖轉場

- **進場「對焦落定」**（Ray：「插圖插入轉場不自然」）：`.settle` —— 淡入的同時
  由 1.05 收到 1.0、blur 6→0、亮度 1.15→1.0，像鏡頭對焦落定。
  ⚠ 跑完要**把 class 拿掉**：同一個屬性只有一個 animation 生效，留著會把
    平移／推近整個蓋掉。
- **以臉為中心緩慢推近**（`cgZoom:{x,y}`，取代 003 原本的由下往上平移）：
  ⚠⚠ `cgZoom` 給的是**圖上**的位置，要換算成**框上**的 `transform-origin` ——
    `object-fit:cover` 會把圖等比放大再從中央裁掉一圈，圖上的 0.09 不等於框上的 0.09。
    換算在 `coverOrigin()`。實測 003 的臉在圖上 (0.53, 0.09)。
  ⚠ 用 transform 不用 object-position：推近要的是「鏡頭往前」，
    object-position 只會平移裁切窗，畫面大小不變。

## C. ⚠ 音檔統一：**只把 wav 轉掉，mp3 原封不動**

Ray：「se 有 wav mp3 等不同格式，考慮統一，以不失真省資源為前提。」

用 macOS 內建的 `afconvert` 把所有 wav 轉成 **AAC 160k m4a**（wav 是無損源 → 一次有損編碼）：

| 檔 | 前 | 後 |
|---|---|---|
| `se_steps` | 12.7 MB | 113 KB |
| `se_lunaMG` | 1.4 MB | 54 KB |
| `se_weapon_pistol_03` | 704 KB | 26 KB |
| `se_Kerberos_steam` | 557 KB | 18 KB |
| vo × 7 | 1.0 MB | 128 KB |

`resources/audio/se` 由 **18 MB → 3.6 MB**。原始 wav 移到 `resources/_originals/audio/`（不入版控）。

⚠⚠ **mp3 沒有轉成 m4a，這是刻意的**：mp3→m4a 是**有損轉有損**，一定掉品質，
  而省下的空間微乎其微（它們本來就已經是壓縮檔）。「統一格式」若要做到底，
  正解是**從原始母帶重新輸出**，不是拿發布檔再壓一次。
  現況＝壓縮格式兩種（mp3／m4a），兩種瀏覽器都吃，沒有相容性問題。

⚠ 路徑上的 `?v=3` 快取破壞參數順手拿掉了：檔名已經改了，不需要它。

## D. 場景之間的標準讀取頁（`{ load:'sceneId' }`）

與開機那一頁**同一個外觀**（同 id、同 CSS：金色進度圈＋百分比＋SAINT INSTALL）。
腳本裡插一行 `{ load:'下一個場景' }` 就會擋畫面把該場景的素材抓完再往下演。

⚠ 重建同 id 的元素而不是留著開機那顆：開機那顆在揭幕後被 main.js 移除了
  （它掛著語言鈕與提示輪播，留著一直吃資源）。這裡只要外觀，CSS 已經在 style.css。
⚠ 不掛語言鈕與提示輪播 —— 那是開機頁的職責，這裡停留通常一兩秒，掛上去只會閃。
⚠ 停留下限 600ms（同 ver -327 的理由：快取全中時閃一下讀起來像破圖）。
⚠ 進度圈周長 301.59 與 main.js 的 `RING_C` 是同一個數字（r=48 的 viewBox 100），
  改一邊要改兩邊 —— 它是 SVG 幾何不是設定值。

目前示範插在**戰鬥之前**（預載 `dungeon_lunaria`）：戰鬥打完回來就要立刻演，
那時再抓會卡住。其餘位置等 Ray 指定。

## E. 「那就是……」那一句要 `show:true`

⚠ `show` 是**沿用**的：上一拍（掃射）把兩位都 hide 了，不明寫 `show:true`
  她永遠不會回來。這是「持續狀態」設計的代價，加 hide 的時候要順手想下一拍。


---

# 交接 · `ver -339`（插圖斜切揭幕／003 推近＋上移／讀取頁挪到開場／槍棺前收框）

## A. ⚠⚠ 插圖進場改成**斜切揭幕**

Ray：「轉場特效不好，或許應該用斜叉遮罩之類更有視覺效果的方式，讓人知道
這不是立繪，是插畫。」—— 說中了問題的本質：**淡入與立繪的淡入淡出長得一樣**，
玩家分不出「換表情」與「切插畫」。要的是一個**只有插畫才會出現**的動作。

作法：`clip-path` 的斜邊多邊形由左下往右上掃過（約 20°），
同時一道青藍亮線（`#storyCgSlash`）以同角度掃過去。

⚠ 不用 `-webkit-mask`：Safari 在動畫中會整層重繪，滿版插圖上會頓。
⚠ **一定要有那道亮線**：只有裁切邊界的話看起來像圖沒載完，不像被劃開。
⚠ 跑完要把 class 拿掉（同一個屬性只有一個 animation 生效，留著會蓋掉推近）。

## B. 003：推近**＋**上移

Ray：「一邊往上移一邊以臉為中心放大。」
⚠ 兩個都寫在**同一個 transform** 裡：分成兩條規則的話後面那條會整個蓋掉前面。
實測跑到一半是 `matrix(1.06, 0, 0, 1.06, 0, 6.2)`，`transform-origin: 53% 4.8%`
（圖上的臉在 0.53/0.09，經 `coverOrigin` 換算成框上的 4.8%）。

## C. 讀取頁挪到「按下 story 的當下」

Ray：「story 讀取頁放錯地方，應該在 story 按下時就要跑讀取。」
`open()` 改用**開機那一頁的標準外觀**（`showLoader()`），劇情自己那顆簡版
（`#storyLoad`）停用（DOM 與 CSS 先留著，之後確定不用再刪）。
戰鬥前那一行示範用的 `{ load:… }` 也拿掉了 —— `preloadStory` 本來就跟著 `next`
走完整條 scene 鏈，開場那一次已經把後面的場景都抓了。

⚠ `{ load:'sceneId' }` 這個機制**保留**：日後場景鏈變長、或要在中途換一批素材時，
  在腳本插一行就會擋畫面預載。

## D. ⚠ 槍棺上推前先收掉對話框（Ray：「以後推槍棺都要這樣處理」）

`playKerberos()` 一開頭就把 `#storyBubble` 藏起來，並停掉打字機 ——
門推上來會蓋掉整個畫面，框留著等於一句台詞被門壓在底下；不停打字機的話
框收了字還在跑。

## E. 楣「又被遮住」：**沒有重現**，改成加大讓位

逐句掃過 `dungeon_chase` 與 `dungeon_lunaria`，比對所有 z-index 比 `#kerb`(5) 高、
且覆蓋楣那塊矩形的元素 —— **一個都沒有**。截圖也看得到楣壓在插圖與情境卡之上。

推測 Ray 看到的是「對話框緊貼在楣上面」讀成被壓住（框底 429 / 楣頂 435，只差 6px，
框的陰影會糊到楣的上緣）。所以把對話框的底邊再抬 10px（實測改後 419 vs 435）。

⚠ 若還是有被蓋住的情況，**請指出是哪一句** —— 目前的量測顯示沒有元素蓋在它上面，
  沒有線索就只能繼續猜。


---

# 交接 · `ver -340`（插圖換圖改成 1 秒黑幕）

Ray：「插圖為了跑轉場多跳了一下，更糟，換成 1 秒的黑色淡入淡出看看。」

## 三種作法都試過了，結論記在這裡

| 作法 | 版本 | 結果 |
|---|---|---|
| 純淡入淡出 | -321 | 與立繪換表情長得一樣，玩家分不出「換表情」與「切插畫」 |
| 對焦落定（scale 1.05→1 ＋ blur） | -338 | 插圖會縮一下＝在動 |
| 斜切揭幕（clip-path ＋ 亮線） | -339 | **更糟**：掃過去那一下讓插圖「多跳一下」 |
| **黑幕淡出淡入 1 秒** | -340 | 插圖本身完全不動，黑一瞬＝一次剪接 |

**教訓**：滿版插圖上**任何會動的遮罩／縮放都會被讀成畫面在抖**。要「這是插畫不是立繪」
的訊號，訊號本身不能碰到插圖 —— 黑幕是碰不到它的那一種。

## 實作

`#storyFade`：只蓋**場景區**（`height:--story-top`），z-index 4 排在 `#storyCard` 之後。
⚠ 不要蓋楣（`#kerb` 是 5）與控制列 —— 那兩塊在黑幕期間仍然要在，
  否則整個畫面都黑掉，讀起來像當機不像剪接。

`cgFade()`：黑幕淡出 500ms → **在全黑那一刻換圖** → 淡入 500ms。
⚠ 換圖一定要壓在全黑那一刻：早一點或晚一點都會被看見，那就是「多跳了一下」。
⚠ 收黑幕前要等新圖真的畫上去（`onload` ／ `complete && naturalWidth`，900ms 保底）——
  沒載完就收會先看到一格舊圖或空白（同 -322 立繪、-325 背景踩過的坑）。
⚠⚠ **插圖一出現就要走黑幕**，不是只有「插圖換插圖」才走。第一版寫成「場上已有插圖
  才淡」，於是最常見的「沒有插圖 → 插入插圖」完全沒吃到，等於沒改。
⚠ 平移／推近要**等新圖上去才開始**（延後 `CG_FADE_MS`）—— 立刻開始的話動的是還沒被
  換掉的舊圖，黑幕收起來時動畫已經跑掉一截，看起來又是「跳了一下」。

實測（390×844）：opacity 0 → 0.73 → 0.997（此刻 src 換成 001）→ 0.19 → 0，整段 1 秒。


---

# 交接 · `ver -341`（002／003 都要由下往上平移）

- **002**：它是當**背景**用的（立繪要疊在它前面），所以平移要做在背景層 →
  新增 `bgPan:'up'|'down'`，吃與插圖同一組 `storyPanUp/Down` keyframes。
  規則與 `cgPan` 一致：寫了就重播、換了圖就清掉、其餘不動。
  ⚠ 背景換圖走的是淡入淡出（等 `onload`），所以平移要**等新圖上去才開始**（延後 `FADE_MS`）——
    立刻開始的話動的是還沒換掉的舊背景。實測 object-position y：100%→64%→31%→7%→0%。
- **003**：本來就是「推近＋上移」，上移幅度由 ±4% 加大到 **±7%**，讀得出來在往上帶。

## ⚠ 順手清掉一段重複的 CSS

ver -340 用「找起點、找終點、把中間換掉」的方式改 style.css 時，終點抓錯，
於是 `#storyCg.pan-up` / `@keyframes storyCgZoom` **各留下兩份**（後面那份生效，
所以看起來正常，但其中一份還帶著已經移除的斜切揭幕註解）。已合併成一份。

**教訓**：用程式改 CSS 區塊時，改完要 `grep -c` 確認關鍵選擇器**只有一份**。
重複的規則不會報錯，只會讓下一個人改到不生效的那一份。


---

# 交接 · `ver -342`（餵稿規格立起來：格式文件＋音效表全覆蓋＋驗稿工具）

這一版**沒有動演出**，動的是「Ray 交稿 → 我轉成可播的劇情」這條路上的三個卡點。

## A. `script/SCRIPT_FORMAT.md` — 餵稿規格（新檔）

給 Ray 看的：稿子怎麼寫、寫到什麼程度我就不必回頭問。重點是**第 5 節那六件事**
（畫面上有誰／插圖何時收／平移的終點／空拍要不要自己走／對話框要不要等演出／新素材檔名）——
那六件正是 `-318`~`-341` 反覆來回問的東西，寫進稿裡就省一趟。

- 稿子的最小單位是**拍**，不是句：對白拍／演出拍／情境卡拍／閘門拍四種。
- 演出詞彙表把「中文說法 → 引擎欄位」一一對上（`背景`/`插圖`/`平移 上`/`推近 臉`/
  `抖`/`掃射`/`暗調`/`退場`/`等…再出框`/`自動…秒`/`讀取`/`戰鬥`）。
  表上沒有的＝目前做不到，要開工才有（語音、立繪走動、選項分歧、左右平移）。
- 也明寫**不要寫**的東西：站位、明暗、立繪大小、路徑副檔名、預載、`▼` 提示。
- ⚠ 這份與 `modules/story.js` 的「演出層」註解是同一件事的兩面，**改一邊要改另一邊**。

## B. 音效／BGM 表改成「整個資料夾都在表裡」

原本 `SE_SRC` 只列了 6 支、`BGM_SRC` 只列了 1 首 —— 稿子裡想用別的音效，得先等我改程式。
現在 `SE_FILES` / `BGM_FILES` 列出資料夾裡的**每一支**，鍵＝檔名去副檔名**轉小寫**，
查表也轉小寫（`se_Fall` 與 `se_fall` 都找得到），另有 `SE_ALIAS`／`BGM_ALIAS` 收短名
（`crisis`→`PerituneMaterial_Crisis_loop`、`se_saintroar`→`Se_enemy_Saintroar`…）。

- ⚠ 還是**逐支列出檔名**不拼副檔名 —— 這兩個資料夾 mp3/m4a 混用，拼出來的路徑會靜默 404。
- ⚠ 加了新音檔要同步這兩張表；漏了 `script_lint.py` 會報「表裡沒有這個檔案」。
- 順手可用的東西變多了：`Bgm_Lunaria`（`lunaria`）先前根本叫不出來。

## C. `tools/script_lint.py` — 驗稿（新檔）

    python3 tools/script_lint.py

檢查 scene 鏈（`next`／`load`／`MAIN_ENTRY`／孤兒場景）、角色 id、表情差分、
背景／插圖／CI 的檔案、音效／BGM 的鍵、`cgPan`/`bgPan`/`fx`/`cgZoom` 的合法值，
以及「空台詞又沒有 `auto`」這種會讓玩家以為當機的拍。

- ⚠ 資料**不是用 regex 猜的**：借 macOS 內建的 `jsc` 把 `mainScript.js` 與 `speakers.js`
  真的跑一次再 dump 成 JSON（兩支都是純資料）。regex 在巢狀物件與註解裡假陽性太多。
- 目前實跑結果：地宮那兩幕**全綠**；`prologue_*`（示範用佔位稿）有 1 個錯誤
  （`noue_fall` 這張插圖不存在）＋差分回退的提醒 —— 那段本來就是要被正式稿換掉的。

## ⚠ 這一版沒有 JS runtime 可用

這台機器上沒有 node/bun/deno。語法檢查改走 macOS 內建的
`/System/Library/Frameworks/JavaScriptCore.framework/Versions/A/Helpers/jsc`
（去掉 import/export 後丟 `new Function` 解析）。實跑驗收照舊走瀏覽器。

## 順手記一筆：開機時的兩個 console 錯誤與這一版無關

`404 favicon.ico`（沒有這個檔，無害）與一支 `EncodingError: Unable to decode audio data`。
把 `se/`、`vo/`、`bgm/` 三個資料夾的每一支都抓下來 `decodeAudioData` 過一遍 —— **全部解得開**，
所以那支解不開的東西不在這三個資料夾裡（推測是某條帶查詢字串或動態組出來的路徑）。
還沒查到源頭，先記著。


---

# 交接 · `ver -343`（楣與槍棺同速；璐娜莉亞插圖改「直接放大＋上移到頂」）

## A. ⚠⚠ 楣被槍棺吃掉的真因：**clip 與門走的距離不一樣**

Ray：「楣在槍棺往上推的時候要跟槍棺一樣的速度，同一剛體，不要被下面的槍棺吃掉。」

楣（`#kerbTop`）**本來就是 `#kerbDoor` 的子元素**＝真的同一剛體，transform 一分不差。
被吃掉的不是它，是**看得到它的那個窗**：

| | 起點 | 終點 | 行程 |
|---|---|---|---|
| 門 `#kerbDoor` | 0 | `-(--kerb-rise)` | **454.7px** |
| clip 上緣 `#kerb` | `--story-top − --kerb-top-h` | 0 | **412.9px** |

兩者吃**同一段 0.7s、同一條曲線**，但距離差了 41.8px —— 正好是楣的高度。
於是門每上推一分，clip 的上緣就少跟一分，楣**從上緣被一路切掉**，到頂時剛好切光。
實測：楣在畫面上永遠只填滿「clip 上緣到門頂」那條逐漸變窄的縫，看起來就是被下面的門吃掉。

**修法**：把 `.full` 的終點由 `0` 改成 **`起點 − 門的行程`**：

    #kerb.full{clip-path:inset(calc(var(--story-top) - var(--kerb-top-h) - var(--kerb-rise)) 0 0 0);}

行程恆等於門的行程 → 同速。實測（0～960ms 取樣九次）楣的上緣與 clip 上緣**始終差 6px**、
高度始終 41.8px，整片跟著出畫面。

- ⚠ 負的 inset 是合法的（往外擴＝那一側不裁）。`#kerb` 的框本來就是整個舞台，
  擴出去的部分由舞台自己的 overflow 收掉。
- ⚠ **不要寫死成 `-41.8px`／`−楣高`**：那只在「門頂剛好等於面板頂」時成立
  （目前的幾何確實如此，但門多大是 `layoutKerberos` 依實際尺寸解出來的）。
- 教訓：**兩個東西要「同速」，看的是行程÷時間，不是有沒有寫同一條 transition。**
  同時長同曲線但距離不同 ＝ 不同速。

## B. 璐娜莉亞插圖：`cgZoom`（推近動畫）→ `cgScale`（直接放大）＋ `cgPan:'up'`

Ray：「不要做放大效果，直接放大，然後從下上移到頂。」
（與 `-340` 把換圖改成黑幕同一個道理：滿版插圖上**會動的縮放**會被讀成畫面在抖。）

⚠⚠ **第一版寫成 `transform: scale(1.18)` 是錯的** —— 放大了，但**平移幾乎沒有變長**：
`object-fit:cover` 能平移的量是「圖被裁掉的那一截」，transform 只是把裁好的窗整個放大，
裁掉多少不變。實測 003（1085×1450）在 375×455 的框裡只裁掉 **46px**，
推到頂與推到底兩張截圖幾乎一模一樣 —— 等於 Ray 要的位移沒有發生。

**正解是改幾何不是改 transform**：把元素**加寬** `k×100%`、左邊往外拉 `(k−1)/2`，
cover 就以更大的寬去鋪圖 → 圖等比放大 k 倍，被裁掉的高度變成 `k×501 − 455`
（k=1.18 時 **137px**，約三倍行程），`storyPanUp` 那組 keyframes 不必改就一路推到頂。
橫向多出來的往兩側溢出，由舞台 overflow 收掉 —— 那正是「放大＝裁掉兩側」。

- ⚠ 不要順手改高度：高度是場景區的高（`--story-top`），一改對話框與楣的錨點全歪。
- `cgScale` 與 `cgZoom` **互斥**（一個是固定放大、一個是推近的過程），lint 會擋。
- 腳本寫法：`{ cg:'003_Lunaria_Armed', cgScale:1.18, cgPan:'up' }`；
  餵稿的說法是「`放大 1.18` ＋ `平移 上`」（見 `script/SCRIPT_FORMAT.md`）。


---

# 交接 · `ver -344`（手機讀取慢：開機預載由 7.8 MB 砍到 2.4 MB；門的假音效 404）

Ray 回報「手機讀非常慢」。查下來不是「卡住」，是**擋在讀取畫面前面的東西太多**。

## A. 冷啟動第一段 7.81 MB → **2.37 MB**（實測轉輸 2.87 MB，含 HTML 自己抓的四張）

`preloadAll` 原本掃 `ASSETS` **全部**的圖：29 張、5.30 MB，其中大半是 cut-in 與敵人立繪
—— 那些最快也要**進戰鬥**才看得到，卻擋在「還沒到主選單」前面。加上音效 0.76 MB
與兩首 BGM 1.75 MB，冷啟動要先吞 7.81 MB 才點得下去；手機上就是 12 秒保底跑滿還沒好。

改法（`main.js`）：

- 新增 `BOOT_IMG_KEYS`（目前只有 `home_emblem`）—— **只有表上的圖進第一段**，
  其餘丟 `_restImgs`，在 `go()`（進主選單那一刻）由 `preloadRestImgs()` 背景開載，
  出陣時再補一次保險（與 `preloadLateBgm` 同一套路）。
- `bgm_battle`（836 KB）併入 `LATE_BGM_PATHS`：最快也要**點過出陣**才用得到，
  而出陣前還有選武器／選搭檔／櫻花過場。
- 進度圈只算第一段 → 誠實跑完，不再靠 12 秒保底放行。
- ⚠ **音效維持在第一段**：27 支只有 0.76 MB，而它們要解碼（不是抓下來就能響），
  現抓會遲到（`audio.js` 的 `LATE_PLAY_MS` 1.5 秒，遲到就乾脆不播）。圖沒這個問題。
- ⚠ 還有 **502 KB 的圖不走這條路**：`Renee_SI_01`／`MG_Squall`／`Luna_CI_saint`／
  `Saint_UG_CI` 是 `applyConfigToDOM` 在模組初始化時就寫進 `<img src>` 的預設戰鬥配置，
  DOM 一掛就抓。要再瘦一層得改那裡，這一版沒動。

## B. 團徽 845 KB → 542 KB（尺寸 1254 → 900，**格式不變**）

版面是 `min(64vw,300px)`，DPR 3 也只要 900px —— 1254 是白給的。
⚠ 照舊維持 PNG（`resources/_originals/background/TIVOT_Emblem_1254.png` 留了原圖）：
金色細線＋透明轉 WebP 容易糊，而且 manifest／apple-touch-icon 需要 PNG。
（若要再省：900px WebP q90 是 **251 KB** —— 那是換編碼，要 Ray 看過才算數。）

## C. ⚠ 門的 `se('arrow')` 沒有素材 → 每次進戰鬥打一次 `undefined.mp3`

`src(k)` 直接拼 `KERB_SFX[k]`，而 `arrow` 根本不在表裡 → 抓 `resources/audio/se/undefined.mp3`
→ 404 拿回一頁 HTML → `decodeAudioData` 丟 `EncodingError`（console 那個「Uncaught (in promise)」
就是它）。改成查不到就安靜跳過並記一次。**缺素材是常態，拼路徑前一定要先查表。**

## D. 還沒動、需要 Ray 定的三件

1. **三首 BGM 的位元率是別人的兩倍**：`Bgm_Lunaria` 195k／`PerituneMaterial_Crisis_loop` 174k／
   `bgm_flight.mp3` 192k，其餘一律 96k。轉成 96k AAC 約各省一半（合計 −3.2 MB，
   其中 Crisis 直接讓劇情那道門少 0.9 MB）。⚠ 這是**有損轉有損**，且沒有母帶
   （`_master/` 只有那五首舊的），所以先問。
2. **劇情預載跟著整條 `next` 鏈走**：地宮那條是 5.19 MB。只載第一幕＋門是 3.74 MB，
   第二幕那 1.45 MB 可以用現成的 `{ load:'sceneId' }` 閘門延後。插在哪是腳本的事。
3. **躺在 deploy 裡但沒人載的檔**：`resources/audio/bgm/_master/*.mp3`（6.9 MB，有進版控）、
   `resources/illustration/Kerberos.png`（2.1 MB，是美術對位用的合成圖）、
   `resources/SI/Luna_SI_angry.png`（2.4 MB，未入版控，要入的話照 §5 轉 WebP）。
   不影響載入速度，只影響 repo 與靜態空間的大小。


---

# 交接 · `ver -345`（三首 BGM 降到 96k；新立繪轉 WebP）

## A. BGM 位元率統一 96 kbps CBR（Ray 同意轉）

| | 前 | 後 |
|---|---|---|
| `Bgm_Lunaria.m4a` | 2255 K / 195 kbps | **1120 K** / 96 kbps |
| `PerituneMaterial_Crisis_loop.m4a` | 1863 K / 174 kbps | **1037 K** / 96 kbps |
| `bgm_flight.mp3` → **`bgm_flight.m4a`** | 2190 K / 192 kbps | **1111 K** / 96 kbps |

合計 −3.0 MB。八首 BGM 現在全部落在 96 kbps，與既有那五首一致。

- 工具是 macOS 內建的 `afconvert`（這台沒有 ffmpeg）：
  `afconvert -f m4af -d aac -b 96000 -s 0 in out`
  ⚠⚠ **`-s 0`（CBR）不可省**。預設的 `-s 3`（真 VBR）會把 `-b` 當成寬鬆的品質目標，
    實測同一首吐出 1726 K ≈ 150 kbps —— 看起來「有轉」但只省了兩成。
    `-s 0` 出來 1120 K / 96058 bps，正中目標。
- **沒有裁短**：Ray 說可以裁到 1:30 左右，但三首本來就是 93.8 / 86.7 / 93.1 秒 ——
  已經在 1:30 上下，裁只會動到既有的循環點，不划算。
- 原始檔存 `resources/_originals/audio/bgm/`（不入版控，要回滾從那裡拿）。
- ⚠ `bgm_flight` 換了副檔名，三處引用一起改：`flight/index.html` 的 `makeLoop`、
  `modules/story.js` 的 `BGM_FILES`、`tools/audio_probe.html`。
  （§6.6 說改名走 `tools/audio_reorg.py`，那支只管 `config.js` 與 `flight/index.html`
  兩邊；這次還多一處探針頁，所以是手改後 grep 驗證沒有殘留 `.mp3`。）
- 飛行頁實測 `bgm_flight.m4a` 200、循環照舊（`makeLoop` 是**交叉淡入**兩個 Audio 元素，
  不吃 gapless，所以 AAC 的編碼器 padding 無所謂）。

## B. 兩張新立繪轉 WebP

`Luna_SI_angry` / `Luna_SI_taunt`：2410 K + 2407 K（PNG）→ **303 K + 276 K**（`-q 85 -alpha_q 90`），
原 PNG 移入 `resources/_originals/SI/`。

⚠⚠ **檔名寫 Luna，畫的看起來是璐娜莉亞** —— 金髮雙馬尾＋眼罩＋同一套黑白洋裝，
與 `Lunaria_SI_Armed` 是同一個人（`Luna_SI_Front` 也是）。目前**沒有任何程式引用它們**，
所以先只轉檔不接線。要接進 `speakers.js` 之前得先定「這是誰」——
⚠ 提醒：`speakers.js` 現在 `luna`（搭檔）指的是 `partner/Luna_CI_exc.webp`，是修女那位。

量好的取景值（照 §6.5，方法用 `Lunaria_SI_Armed` 校準過：量得 top=9/bot=1529/fx=0.495，
表上是 9/1528/0.496）：

    Luna_SI_angry   1024×1536  top=0  bot=1536  fx=0.477
    Luna_SI_taunt   1024×1536  top=0  bot=1536  fx=0.510

⚠ 兩張都**碰到畫布上下緣**（第 0 列有 12px 寬的髮絲、末列有鞋襬），所以 `top/bot`
就是 0/1536；真正的「頭殼頂」在 y≈28 / y≈10。要是接上去覺得人偏高，改的是這一項。

## C. 順手記下：飛行頁有兩個 404

`flight/ship/rigging.png`、`flight/ship/flag.png` —— 與這一版無關，還沒查。

## 現況數字（給下一輪對照）

    開機第一段        2.37 MB（-344 之前是 7.81）
    劇情第一道門      4.39 MB（-344 之前是 5.19；只載第一幕的話 3.0 MB）
    resources 總量    37.2 MB（不含 _originals）


---

# 交接 · `ver -346`（手機立繪忽大忽小／站太低；劇情音效先載）

Ray（手機實機）：「角色立繪在戰鬥中忽大忽小、且站太低，再往上一個頭身」＋「腳步聲跟跌倒音沒載到」。

## A. ⚠⚠ 忽大忽小：相機（pxCm）是**每一句**重算的，而手機的視口會變

`pxCm = (框高 − 頂線) / (castShow × castTall)`，框高＝`#top` 的 `height:50%`。
手機瀏覽器的工具列一收合，視口就變幾十像素 → `#top` 跟著變 → **同一張立繪在相鄰
兩句之間大小不同**。桌面沒有這件事，所以在開發機上永遠看不到。

修法（`modules/tutorial.js` 與 `modules/story.js` **同一套**）：相機快取 ——
寬度沒變、框高變化在 **18% 以內**一律沿用上一次；真的轉向（寬度變）才重量。
進戰鬥／進劇情各 `resetCamera()` 一次。

第二個來源（比較小，但同一個病）：**縮放的分母用了「這一張差分自己的像素身高」**。
差分是不同姿勢，alpha 上下緣會差 1%（諾薇兒 cringe 1528 / surprise 1519）——
每換一次表情就縮放一次。改成一律用**該角色基本立繪**的像素身高當分母
（`el.dataset.baseKey`）；位置照樣吃這一張自己的 `top/fx`（那是取景，本來就該逐張算）。
實測：換表情前後高度 479.5 / 482.4 → **477.6 / 477.6**。

## B. 站太低：**相機的頂線**與**頭頂的落點**本來就不該是同一條線

原本兩者共用 `max(3%框高, 清盤鈕下緣+4)` ＝ 把整顆鈕的高度（44px）讓出來。
但要閃開的只有瀏海／動態島，而 `#top` 已被 `#app` 的 padding 推到瀏海之下，
**鈕的上緣就是安全線**；何況立繪站左（臉錨 0.24W）、退出鈕在右上，本來就不重疊。

現在拆成兩個值：

    camTop  = max(3%, 鈕下緣+4)   → 只餵 pxCm（維持 Ray 對著這個框調出來的 castShow 0.68）
    headTop = max(3%, 鈕上緣)     → 頭頂真正擺哪

實測：**大小不變（477.6）**，頭頂由 56.4px 上移到 **10.6px**（往上 46px ≈ 0.7 個頭身）。
⚠ 一個頭身是 68px，但 12px 已是這個版面的上限 —— 再上去頭會被 `#top` 的 overflow 裁掉。
要再高只能改版面（例如把 `#top` 的 50% 加大），那會連帶動到盤面，先不碰。

## C. 「腳步聲跟跌倒音沒載到」：劇情預載**音效與圖片一起搶頻寬**

`preloadStory` 原本二十幾個請求一起開跑。手機頻寬被 3.3 MB 的圖吃光，只要整包沒在
`PRELOAD_CAP_MS`（25 秒）內載完，閘門照樣放行 —— 這時圖已經在快取裡、**音效還沒解碼完**，
於是開場那兩支（`se_steps` / `se_Fall`）一播就落進 `LATE_PLAY_MS`（1.5 秒）的
「遲到不播」規則，整個消失。

改成 **音效／BGM 先跑完（自己 6 秒上限）再抓圖**。實測順序：SE 在 t=1ms 開始、
t=18ms 完成；圖片 t=150ms 才開始。音效總共幾百 KB，代價是零。
⚠ 原則：**圖沒載完最多晚一拍淡進來，音效沒載完是直接不響** —— 兩者的失敗代價不對等，
排隊順序要照這個排。

## D. 順手：`se_punch.mp3` / `se_walk.mp3` 進表了

`tools/script_lint.py` 報「SE_FILES 表裡沒有這個檔案（遊戲載不到）」——
Ray 新丟進 `se/` 的兩支還沒進表。已補（現在腳本寫 `se_walk` / `se_punch` 就會響）。
❓ 地宮開場的腳步聲目前仍是 `se_steps`（4.06 秒的 `se_walk` 是新的）—— 要換的話說一聲。


---

# 交接 · `ver -347`（會客廳那一幕的素材轉檔＋取景實測；**腳本尚未寫進 mainScript**）

Ray 交了「第四騎士團駐地會客廳」整幕稿（背景 `LunariaOffice`、BGM `bgm_result`，
璐娜莉亞／諾薇兒／蕾娜三人 50 餘拍）。這一版**只完成素材端**，腳本還沒接。

## A. 素材轉檔（11 張，21.4 MB → 1.97 MB）

    Luna_SI_seat_angry / _hand / _smirk       2397/2415/2490K → 247/228/267K
    Nouvelle_SI_Awkwerd / Gossip1 / Gossip2 / Shy   1701/1660/1646/1692K → 161/144/145/140K
    Renna_SI_awkwerd / bow / smile            1462/1572/1431K → 128/147/124K
    illustration/004_Renna_intro              2430K → 243K

`-q 85 -alpha_q 90`（插圖 `-q 82`），原 PNG 移入 `resources/_originals/`（不入版控）。
`Luna_SI_seat_N.webp` 是 Ray 自己給的 WebP，未動。

## B. 取景實測（照 §6.5，量法與 `speakers.js` 同一支）

| 檔 | top | bot | fx |
|---|---|---|---|
| `Luna_SI_seat_N` | 7 | 1536 | 0.448 |
| `Luna_SI_seat_smirk` | 0 | 1536 | 0.504 |
| `Luna_SI_seat_angry` | 0 | 1536 | 0.521 |
| `Luna_SI_seat_hand` | 3 | 1536 | 0.465 |
| `Nouvelle_SI_Awkwerd` | 2 | 1534 | 0.468 |
| `Nouvelle_SI_Gossip1` | 0 | 1536 | 0.710 |
| `Nouvelle_SI_Gossip2` | 2 | 1536 | 0.603 |
| `Nouvelle_SI_Shy` | 4 | 1533 | 0.592 |
| `Renna_SI_awkwerd` | 6 | 1526 | 0.519 |
| `Renna_SI_bow` | 0 | 1530 | 0.507 |
| `Renna_SI_smile` | 5 | 1530 | 0.518 |

⚠ `Nouvelle_SI_Gossip1` 的臉在 **0.710**（其他差分落在 0.39~0.60）—— 這張她整個人偏右，
  沿用別張的 fx 會把她推出畫面。這正是「每張差分都要自己量」的例子。

## C. ⚠⚠ 沒解決的關鍵問題：**坐姿立繪不能照身高鎖**

`Luna_SI_seat_*` 是**坐著**的璐娜莉亞，四張都畫滿整張畫布（1529~1536px）。
§6.5 的縮放是 `pxCm × 身高cm ÷ 圖裡的像素身高` —— 對坐姿而言那個像素跨距**不是身高**
（坐著的人縱向短，畫師為了填滿畫布把她畫大了），照算會讓她的頭比站著的人大一大截。

試過三種自動量法都不可靠，**都不要再走一次**：
- 膚色偵測臉的高度 → 手臂、胸口、腿一起被算進去（璐娜莉亞量到「臉寬 785px」）。
- 窄帶（臉中心 ±70px）取膚色縱向跨距 → 髮絲與陰影混進來，諾薇兒 518 / 蕾娜 493，
  兩個人的臉不可能差這麼少又這麼不穩。
- 眼罩當剛體尺標（暗塊連通域）→ 髮帶、衣服也是暗塊，分不出來。

**該走的路是 §6.5 允許的「看出來的補償」**：先給坐姿一個 `cm` 估計值
（頭頂→座面的跨距大約是站姿身高的 0.7~0.8，即 `cm≈118~134`），
接到場景裡渲染一次，與同台的諾薇兒（165cm 站姿）並排比頭的大小，再調一次。
比較用的合成圖已經產過（`站姿 vs 坐姿×1.0/0.85/0.75/0.68/0.62`），
但**沒有給 Ray 確認過**，所以不要當定案。

## D. 待辦（下一輪直接接著做）

1. **把會客廳那一幕寫進 `script/mainScript.js`**（Ray 的稿在對話紀錄裡，逐拍已標好立繪）。
   需要的新東西：
   - `speakers.js`：蕾娜的 `expr`（smile / bow / awkward）、諾薇兒的四張新差分、
     璐娜莉亞的坐姿四張（**卡在 C 的 cm 校準**）。
   - 「？？？」講第一句：她自報家門那一拍要用**另一個未表明身分的 id**
     （現行 `UNKNOWN` 的 art 指向璐娜莉亞），例如 `UNKNOWN_OFFICER`（art: renna）。
   - **主角的空白對話框**（稿裡兩處「主角說話空白頓點」）：目前 `text` 為空就不出框，
     要另加一個「出框但沒有字」的表達（例如 `blank:true`），名字取 `progress.getPlayerName()`。
2. **場景接鏈**：這一幕接在 `dungeon_lunaria` 之後（台詞提到「剛剛要不是這傢伙」）。
   接上之後整條鏈的預載會變大 → 建議在交界插 `{ load:'…' }` 閘門，
   並讓 `collectAssets` 遇到「next 已被 load 閘門守住」時就停，不要預先算進第一道門。
3. `Nouvelle_SI_Gossip2` 這一版稿子沒用到（Gossip1 用了兩次），先留著。


---

# 交接 · `ver -348`（會客廳那一幕接上：56 拍、坐姿取景定案、空白對話框、讀取閘門）

## A. 監察官這一幕**全程叫「監察官」**（Ray 定案）

第一句那個「？？？」不另立 id —— 直接用 `OFFICER`（顯示名「監察官」），
下一幕才換成 `RENNA`（蕾娜）。Ray：「當作兩個角色就好」，正是規格 §2 的作法，
`speakers.js` 的 `OFFICER` / `RENNA` 本來就共用同一組 art。

## B. ⚠⚠ 坐姿的璐娜莉亞：兩個補償各司其職，不要混

`Luna_SI_seat_*` 四張都畫滿整張畫布，那個縱向跨距**不是身高**（坐著的人縱向短，
畫師為了填滿畫面把她畫大了）。逐張帶兩個覆寫值：

    faceAdj 0.92   大小 —— 讓她的臉與諾薇兒差不多大（Ray 對站姿定的標準）。
                   先試 1.05：實測螢幕上頭高 80px vs 諾薇兒 70px，**大了 14%**，收到 0.92。
    standCm 155    高度 —— 坐著的人頭頂本來就比站著的低，低約 46px。
                   先試 145：**太低**，人幾乎整個沉到對話框後面。

實測（390×844）：監察官頭高 ≈78px、璐娜莉亞 ≈75px，頭頂差 57px —— 讀得出來她坐著。
⚠ 這兩個是**看出來的**（§6.5 允許），不是量出來的。三種自動量法都失敗（膚色偵測臉高、
窄帶膚色跨距、眼罩當剛體尺標），細節見 `-347` 那一節，**不要再走一次**。

## C. 新增的兩個腳本能力

- **`blank:true`** ＝ **出框、但框裡沒有字**（Ray 的稿：「主角說話空白頓點，空白對話框」）。
  ⚠ 與「空台詞」是兩回事：空台詞是演出拍（咆哮／掃射），那一拍畫面上**不該有框**；
    `blank` 是「有人在說話，只是我們聽不到」。名字取 `progress.getPlayerName()`。
  ⚠ `speakers.js` 補了一筆 `PLAYER`（`art:null`）—— 只為了讓驗稿工具查得到，
    顯示名不走查表。
- **`{ load:'sceneId' }` 現在真的會切斷預載**：`collectAssets` 看到「這一段有 load 指向
  自己的 `next`」就不再往下算。於是開場那一道門只載地宮，會客廳那 2.3 MB 留給交界那一頁。

## D. 這一幕的實作筆記

- 56 拍。站位：璐娜莉亞在右（坐姿），監察官與諾薇兒同在左 —— 兩人輪流上場是
  「舊的滑出、新的滑入」，那是固定站位的既定行為，不是 bug。
- ⚠⚠ 第 0 拍**一定要明寫 `portrait:{ show:false }`** —— 立繪預設 `show:true`，
  漏寫的話監察官的立繪會跟著上場站在左邊，一路留到後面幾拍（實測踩到，
  與 `dungeon_chase` 開場那一拍是同一個坑）。這一拍要的只有暗調 CI。
- 稿上「只給立繪、沒有台詞」的反應拍（璐/諾對看、監察官無聲一笑）給 `auto:900` ——
  沒有對話框就沒有 ▼，不自己走會讀成當機。要改成點擊推進的話，得先讓那種拍也出框。
- 腳步聲那一拍：`se_walk` 4.06 秒但只停 1.4 秒就推走，聲音會蓋到下一句 —— 那是要的
  （她們正走出去，璐娜莉亞從背後喊）。同一拍 `hide:['OFFICER','NOUVELLE']` 讓兩人退場。
- `Nouvelle_SI_Gossip2` 這一版稿子沒用到（Gossip1 用了兩次）。

## E. 驗稿工具跟著補

`load` 那一行是閘門不是演出拍（不必有 speaker）；`blank` 的拍不算「空台詞又沒有 auto」。


---

# 交接 · `ver -349`（地宮收尾補一段；坐姿再縮 20%；舞台指示不進台詞）

## A. 地宮那一幕補上團長登場（Ray 補稿）

`dungeon_lunaria` 在「璐娜莉亞團長……！」（003 插圖）之後接七拍：換 BGM 到 `Bgm_Lunaria`、
背景回地宮、插圖收掉，璐娜莉亞**本人站到畫面上**（`angry` / `taunt` 兩張差分），
講完「13th 來人了」才進讀取閘門 → 會客廳。

⚠⚠ **`Luna_SI_angry` / `Luna_SI_taunt` 是璐娜莉亞的站姿差分**（`-345` 轉檔時只知道
「檔名寫 Luna、畫的像璐娜莉亞」，這一版由 Ray 的稿確認）。全身站姿，所以
cm / standCm / faceAdj 全部沿用她站姿那一組（1.22），只覆寫 `top/bot/fx`。
→ `resources/SI/` 裡的 `Luna_*` 一律是**璐娜莉亞**；搭檔璐娜是 `partner/Luna_CI_*`。

## B. 坐姿璐娜莉亞：`faceAdj` 0.92 → **0.74**（Ray 實機看過後指定「再縮小 20%」）

⚠ 所以坐姿**不是**「臉與站著的人一樣大」—— 她坐在遠處的椅子上，比同台站著的人小一圈
才是對的。這是 Ray 的鏡頭判斷，**不要**用「臉要等大」的理由改回去。
歷程留在 `speakers.js` 的註解裡：1.05（大 14%）→ 0.92（頭高齊平）→ 0.74（定案）。

## C. 舞台指示不進台詞

原稿的「（舉起義肢）」「（乾笑）」是 Ray 給自己看的註解，不是台詞（Ray 指定）。
已從 `text` 拿掉，改寫進程式註解 —— 動作交給 `seat_hand` / `awkward` 那兩張差分演。
**通則：稿子裡括號的動作描述一律當舞台指示，除非 Ray 明說要唸出來。**

## D. `004_Renna_intro` 又換了一版

⚠ Ray 這次給的檔名是 `.png`，**內容其實是 WebP**（`sips` 報 format: webp，magic 是 RIFF/WEBP）。
直接改副檔名即可，**不要再 cwebp 轉一次** —— 那是第二次有損編碼。
判斷方法：`sips -g format` 或看前 16 個位元組。


---

# 交接 · `ver -350`（場景間黑幕；監察官改走正規立繪；說話者疊在前；手機忽大忽小第二刀）

## A. ⚠⚠ 手機忽大忽小：`-346` 只修了一半

`-346` 快取了 `pxCm`，但**頂線與頭頂落點還是每一句重量**（去量那顆角落鈕的實際位置）。
iOS 的 `env(safe-area-inset-top)` 會隨網址列收合而變（47px ↔ 0）→ `#app` 的 padding 變 →
鈕動 → 頂線動。**尺寸沒變、整個人上下跳**，讀起來一樣是「忽大忽小」
（§6.5 早就記過：位移會被讀成縮放，ver -336 那次也是）。

現在 `tutorial.js` / `story.js` 兩邊都改成**整組取景一起快取**
（`pxCm` ＋ `camTop`／`headTop`／`topLine`）：一場之內是常數，**寬度變了（轉向）才重量**。
教訓：要「同一張立繪＝同一個結果」，快取的必須是**整組幾何**，不是其中一個數字。

## B. 場景之間黑幕淡入淡出（Ray 指定）

`runLoadGate` 前後包一層 `#storyFade`：黑幕淡入 → 讀取頁 → 收讀取頁 →
**在全黑之下**演出新場景的第一拍 → 黑幕淡出。
⚠ 第一拍要在黑幕還蓋著時演，否則會看到舊畫面殘留一格才換（同 -340 換插圖那個坑）。

## C. 監察官登場改走正規立繪（不是全屏 CI）

Ray：「這張立繪要在左邊滑入，不用全身，比照對話播放規則縮放」——
與 `-319` 把璐娜莉亞從 CI 改成正規立繪是同一個決定：走立繪系統才鎖身高、才與同台的人
同一把尺；CI 是自己一套縮放，比例對不上是必然的。
`dark:true` 給暗調（還沒表明身分），它是句子屬性，下一句自動清掉。

其餘照 Ray 的修訂稿：
- 會客廳開場加**情境卡**（「第四騎士團駐地／會客廳」），與地宮開場同一套黑透罩。
- 自我介紹**斷成兩句**：前半配暗調立繪；插圖（撥髮）自己走 1 秒；
  後半「蕾姬娜˙馮˙海森伯格。」**對話框壓在插畫上**（框 z-7 > 插圖 z-2，不必特別處理）。
- 無台詞的立繪切換一律 `auto:1000`（Ray：「停一秒再往下走，或者點擊」）。

## D. 說話的人一定疊在另一個人之上

`highlight()`（story）與 `showLine()`（tutorial）都改寫 inline `z-index`（說話者 2、另一位 1）。
⚠ 用 inline 不用 class：兩個槽是兄弟元素、DOM 序固定（L 在前 R 在後），
光靠 class 沒辦法讓左邊蓋過右邊。

## E. `Luna_SI_angry` 再縮 10%

逐張的 `faceAdj`：1.22 × 0.9 ＝ **1.10**（Ray 實機指定）。`taunt` 維持 1.22。
⚠ 同一角色的兩張站姿差分吃不同補償是正常的 —— 補的是**那一張畫**的畫風／構圖差異。
要調就調這裡，不要回頭動角色的 `cm`（那會連基本立繪一起變）。


---

# 交接 · `ver -351`（演出原則寫進憲法；黑幕卡死的 bug；G2 入口情境卡）

## A. ⚠⚠ 原則類的東西**寫進 `CLAUDE.md` §6.5**（Ray 指定）

Ray：「原則類的東西都要寫進去，確保每個 session 都遵守。」新增兩組：

**演出的節奏**
- 插圖轉回一般對話 → **等畫面全亮才彈出角色與對話框**。
- 無台詞的立繪拍 → **停一秒或點擊推進，而且那一秒從立繪站定才起算**。
- 說話者的立繪圖層**一定在另一個人之前**。
- 場景之間走**黑幕**；新場景開場先用**情境卡報地點**。
- 取景要**整組快取**，一場之內是常數。

**腳本的寫法**
- 稿子裡括號的動作描述是**舞台指示，不進台詞**。
- 立繪／插圖／背景／BGM 都是**持續狀態**，開場不要某人上場一定要明寫 `show:false`。

## B. ⚠⚠ 黑幕會卡死在畫面上（真 bug，已修）

`cgFade` 的計時器原本掛在 `fxTimers`，而 `renderLine` 一開頭 `stopFx()` 把它全清 ——
玩家在 500ms 的淡黑期間點了下一句，「換圖並收黑幕」那個計時器就**被取消**，
黑幕**永遠留在畫面上**。實測：G2 情境卡那一拍點快一點，之後整段都是黑的。

修法：黑幕有自己的 `cgFadeT` / `cgFinish`，`renderLine` 進來時**先把它做完**（`flushCgFade`）
而不是取消 —— 玩家想快轉就立刻換好圖、收黑幕。
另加 `fadeOwner`（`'cg'` / `'gate'`）分辨黑幕是誰掛的：場景之間的讀取閘門用同一塊黑幕，
**那一塊不能被 flush 收掉**（它要蓋到新場景第一拍演完）。

**教訓：一次性演出的計時器可以清；「還沒做完的狀態轉換」不能清，要 flush。**

## C. 這一輪的演出調整

- `dungeon_lunaria`：「璐娜莉亞團長……！」之後插一拍**情境卡**「聖王廳地宮　G2入口」，
  收插圖／換背景／換 BGM 全放在卡片這一拍（黑幕正好把這些一起藏起來）。
- G2 入口的兩張站姿再縮 15%：`angry` 1.10 → **0.94**、`taunt` 1.22 → **1.04**。
  ⚠ 兩張數字不同是刻意的（angry 多縮過一輪），要調就調 `expr` 上的 `faceAdj`，
    不要動角色的 `cm`。
- `reveal()`：`renderLine` 把「放人與對話框出來」整段包起來，走過黑幕就延後
  `CG_FADE_MS×2 + 140` 再執行。
- 無台詞拍的 `auto` 加上 `SLIDE_MS`（立繪滑入 450ms）——Ray：「監察官立繪撤太快」。


---

# 交接 · `ver -352`（立繪往上一個臉；戰鬥與劇情同一把尺）

## A. 手機上立繪太低 → 頂線改夾退出鈕的**上緣**

`#storyExit` 是 `top: env(safe-area-inset-top) + 10px`，瀏海機上鈕的**下緣**落在 ~101px
（桌機只有 ~58px）。頂線夾下緣＝把鈕高（44px，約一個臉）再讓一次，手機上尤其明顯。
要閃開的是瀏海本身，而版面已被 `#app` 的 padding 推到瀏海之下 —— **鈕的上緣就是安全線**。

⚠ 但**相機的頂線仍用下緣**：`CAST_SHOW` 是對著那個框調出來的，一起改人就變大變小。
所以 `topLines()` 回傳兩個值：`camTop`（下緣＋4，餵 pxCm）與 `headTop`（上緣，頭頂落點）。
實測劇情頁：立繪的圖框上緣 97px → **49px**（往上 48px ≈ 一個臉）。
（tutorial.js 的同一個修法在 `-350` 就做過，這一版補上 story.js。）

## B. ⚠⚠ 戰鬥的對話立繪與劇情頁**同一把尺**（Ray：「同比例同高度」）

差在三處，全部對齊：

| | 之前（戰鬥） | 現在 |
|---|---|---|
| 參考框（`pxCm` 的分母） | `#tutCast`＝`#top`，`#app` 的 **50%** | **`#app` 高 × 56%**（＝劇情頁的 `--story-top`） |
| `castShow` | 0.68 | **0.56**（與劇情頁同值） |
| 身高讓位 | 沒有 | **有**（`(176 − cm) × pxCm`，同 §6.5） |

實測同一個角色：劇情 h=672／頭頂 49，戰鬥 h=665／頭頂 56 —— 對得上（1% 的差來自
戰鬥那邊縮放鎖的是**基本立繪**的像素身高，劇情鎖的是那一張差分自己的）。
戰鬥立繪因此比原本大 39%（477→665），會多蓋住一點敵人框 —— 那是「同比例」的代價，Ray 指定。

⚠ **要調大小就調 `castShow`，不要回頭去改分母** —— 分母一改，「同一把尺」就又斷了。
⚠ 量到的是 `#app` 座標，最後要換算回 `#tutCast` 座標（減掉兩者的 top 差）。

兩條都寫進 `CLAUDE.md` §6.5 了。


---

# 交接 · `ver -353`（G2 卡片清場；三張立繪再縮；13th 那句換圖）

- **G2 情境卡那一拍要清場**（Ray：「G2入口時諾薇兒的立繪還在，撤掉，說話才出來」）。
  立繪是持續狀態，卡片這一拍加 `hide:['NOUVELLE']`；她在自己那句 `show:true` 才回來。
  ⚠ 通則：**情境卡是「地點介紹」，台上不該站著人** —— 新場景的卡片拍都要記得清場。
- 逐張 `faceAdj` 又調（Ray 對著實機一輪一輪收）：

      lunaria.taunt        1.04 → **0.83**（−20%）   實測立繪高 705 → 562px
      lunaria.seat_angry   0.74 → **0.63**（−15%）   同組坐姿裡只有這一張再縮
      （lunaria.angry 維持 0.94、其餘坐姿維持 0.74）

  ⚠ 同一個角色的差分吃不同的補償是**刻意**的 —— 補的是那一張畫的構圖差異
    （seat_angry 她往前傾、臉畫得比較大）。要調就調 `expr` 上的 `faceAdj`，
    **不要動角色的 `cm`**（那會連其他所有差分一起變）。
- 「 13th……第十三騎士團嗎！？」的立繪由 `cringe` 改 `surprise`。


---

# 交接 · `ver -354`（立繪忽高忽低的真因；音效一定要等到；撞頂才抖；黑幕下先清場）

## A. ⚠⚠ 「人物站位忽高忽低」的真因有兩個，都與手機無關

Ray 回報「從『很好…你做得到的』那格開始變低，到『來了！擋下來！』才恢復」。
在桌機重現後量出來：**同一段教學跑兩次，立繪高度 665 vs 732（差 10%）**。

1. **量了那顆鈕的即時 rect。** 頂線原本去量 `#exitBtn` 的位置，而那一刻鈕可能正好
   被藏起來（Kerberos 門還在開、結算 banner 開著）或版面還在轉場 → `height` 是 0 →
   頂線退回 3% → 整個人放大一成。更糟的是這個值**被快取一整場**。
   → 改成**由 CSS 常數推**：`.corner-btn` 44px、`top:10px`，再加 `--notch-bar-h`。
     不管什麼時候算，結果都一樣。
2. **參考框拿了 `#app` 的高。** `#app` 是 `height:100%`，而 main.js 會取 100vh／100dvh
   裡**較大**的那個來閃開 iOS 幽靈工具列 —— 實測 `#app` 883、視口 812（差 8.7%），
   而且那個值在開機過程中還會變一次。
   → 改成 **`window.innerHeight × 56%`**，與劇情頁的 `#storyStage`（`fixed; inset:0`）
     那個框完全同源。

實測修後：劇情 h=738／頭頂 52，戰鬥 h=732／頭頂 56（1% 的差來自戰鬥鎖基本立繪的身高），
**連跑兩次都是 732/56**。兩條都寫進 `CLAUDE.md` §6.5。

## B. ⚠⚠ 音效還是沒載到：`AUDIO_FIRST_MS` 被當成「等這麼久就算了」

`-346` 寫成 `race(audio, 6s).then(圖片)`，整個 gate 只等**圖片** —— 慢一點的手機上
6 秒一到就去抓圖，音效還沒解碼完就開演，開場那兩支照樣落進 `LATE_PLAY_MS` 被丟掉。
正解：6 秒只是「圖片最多讓路這麼久」，**最後一定要等音效與圖片都好**
（只有 25 秒總上限能中斷）。

另加一道保險：`SFX.ready(src)` 為 false 時，劇情層改用 **`HTMLAudio` 串流**播那一支
（`playSeFallback`）。Web Audio 那條的補播有 1.5 秒時限，超過就不播；HTMLAudio 馬上出聲，
而且那一下是使用者點擊推進的，還在手勢的同步區間裡，iOS 不會擋。

## C. 槍棺：CSS 的上推時間與撞擊時刻對不上

門的震動與撞擊音都排在 `KERB_T.rise`＝1000ms（`se_Kerberos_pop` 的峰值實測 1002ms），
但 CSS 的 transition 只有 **0.7 秒** —— 門 700ms 就到頂、靜止 300ms 才抖才響。
clip 與門一起改成 **1s**（同 -343：要同速，看的是行程÷時間）。

## D. 黑幕下要先清場

`hide` 原本寫在 `reveal()` 裡，而 `reveal` 走過黑幕時會延後到畫面全亮才跑 ——
於是要撤的人是**在觀眾眼前滑出去**的（Ray：「G2 場景切換時仍有諾薇兒立繪殘留」）。
改成 `hide` 在 `renderLine` 一開頭就做，黑幕蓋著時就撤乾淨。

## E. 監察官第一句不再加 `dark`

立繪滑進來時本來就經過一次壓暗（`tone.js` 的背景調色 ＋ 非說話者的 `.dim`），
再疊一層 `.dark` 會黑到看不出是誰（Ray：「已經有一次暗階，不用再塗黑」）。


---

# 交接 · `ver -355`（改照飛行畫面那一套：一個 rect 都不逐句量；戰鬥音樂在門上推那一瞬起播）

## A. Ray：「你就不能按照飛行畫面的原則處理嗎？」—— 對，而且差別就在最後一項

飛行頁的做法（`flight/index.html` 的 `castMeasure()`）：**resize 時把取景算成全域常數**
（`CAST_TOP` / `CAST_PX_CM`），之後 `castGeom()` 只是查表 —— 畫面上**沒有任何逐句量測**。

我這幾版一直在往那個方向收，但每次都留了一項還在逐句量：

| 版 | 那時還在逐句量的東西 |
|---|---|
| -346 | 頂線（`topLine()` 量退出鈕） |
| -350 | 頭頂落點（量鈕的上緣） |
| -354 | `#tutCast` 的螢幕位置（`wr0.top`）、`#storyCast` 的即時高度 |
| **-355** | **沒有了** |

現在每一項都是常數：視口高（`innerHeight`）、瀏海高（`--notch-bar-h`）、
鈕的幾何（`.corner-btn` 44px、`top:10px`）。
`#tutCast` 的頂 ＝ `#app` 的 padding-top ＝ 瀏海高，所以「框內座標的頭頂」
就是 `BTN_TOP + 身高讓位`，連換算都不必量。

實測：劇情 h=738／頭頂 52，戰鬥 h=732／頭頂 56，跑幾次都一樣。
**教訓：DOM 版要跟 canvas 版一樣穩，唯一的路是「所有輸入都不許是即時 rect」。**
（已寫進 `CLAUDE.md` §6.5，連飛行頁的基準實作一起指名。）

## B. 戰鬥音樂：門**開始上推**那一瞬起播

新增 `story.setBattleCue(fn)`，`playKerberos` 一開頭就呼叫；main.js 注入
`SFX.playBgm(bgm_battle, {fadeOutMs:600})`。
⚠ 不能等 `setBattleHandler` —— 那是門開到**縫**（onGap）才呼叫的，距離開始上推
有 3 秒多（rise 1000 ＋ 撞頂 ＋ 解鎖 ＋ 紋章浮起 1600）。
⚠ `launchBattle` 裡原本那一行照留（帶 `delayMs:1000`，給櫻花過渡禎用）——
`playBgm` 對「同曲播放中」直接 return，不會打架。


---

# 交接 · `ver -356`（戰鬥音樂改到撞頂那一刻；璐娜莉亞站姿下移半張臉）

- **戰鬥音樂：撞頂之後才進**（-355 曾放在「開始上推」那一瞬，Ray 改口）。
  `riseCue()` 由 `playKerberos` 開頭移到 `at(KERB_T.rise, …)` 那個回呼裡。
  實測 `bgm_battle` 在 **t=1003ms** 被呼叫（門的上推正好 1000ms）。
  理由記著：上推那一秒還是劇情的餘韻，音樂壓在撞擊上會把那一下的重量分掉。
- **璐娜莉亞站姿往下半張臉**：`angry` / `taunt` 兩張加 `standCm:171`（角色預設 176）。
  頭頂 y ＝ `頂線 + (176 − standCm) × pxCm`，調小 5cm ＝ 下移 5×4.43 ≈ **22px**
  （一個臉 44px，取一半）。實測 `topInCast` 10 → **32**。
  ⚠ 只寫在 `expr` 上，不動角色的 `standCm` —— 那是基本立繪（`Lunaria_SI_Armed`）的值。
  ⚠ 也不要用 `cm` 去達成：`cm` 是縮放的分子，一改人就跟著變大變小。
- Ray 確認：**戰鬥對話的尺寸與高度已經對了**（-355 的常數化生效）。


---

# 交接 · `ver -357`（三張立繪換新版；原圖備份的規矩寫進憲法）

## A. Ray 換掉三張，轉檔＋**重量取景**

    Luna_SI_seat_angry   2658K → 328K   top 0→0    bot 1536→1526  fx 0.521→0.539
    Luna_SI_seat_hand    2497K → 275K   top 3→20   bot 1536→1489  fx 0.465→0.546
    Nouvelle_SI_front    1618K → 137K   top 1→3    bot 1535→1536  fx 0.564→0.582

⚠⚠ **同名換圖一定要重量**：`seat_hand` 這一張新版的人物在畫布裡內縮了
（`bot` 少 47px、`fx` 右移 0.08）—— 沿用舊值會歪掉一大截。
⚠ `Nouvelle_SI_front` 是**基本立繪**：它同時是 `config.js` 的 `portraitFrames.tut_nouvelle`
（自動跟著 ART 走，不必另改）與戰鬥那邊「鎖縮放」的分母，所以它的 `bot-top` 一變
會影響全部諾薇兒差分的大小 —— 這次只差 1px，可忽略。
⚠ `faceAdj` 不跟著改：那是畫風補償，與換圖無關（新版的內縮由 `top/bot` 吸收）。

## B. 原圖備份的規矩（Ray 指定，已寫進 `CLAUDE.md` §5）

固定三步：`cwebp` → 改 `config.js`/`speakers.js` 指 `.webp` → **原 PNG 移進
`resources/_originals/<同層資料夾>/`**，絕不留在會被載入的目錄。
底線開頭的資料夾不會被遊戲載入（同 `_master`/`_unused`），且已在 `.gitignore`。

⚠ **`_originals` 目前 133 MB、只在本機**。它是「可回滾」不是「異地備份」——
要防硬碟壞掉得另外複製到雲端／外接。**要不要改成入版控由 Ray 決定**
（repo 會多百餘 MB，push 會變慢；`http.postBuffer` 之前已設過）。


---

# 交接 · `ver -358`（道具系統／好感度計數／教學結算＋掉落）

## A. 道具（`script/inventory.js` ＋ `config.js` 的 `items`）

- 定義在 `config.items`：`catOrder`（道具/武器/素材/裝備/特殊，Ray 指定的五類）、
  `catName`、`defs`（`id → {name, cat, desc}`）。
- 存檔 `tivot_inventory_v1`，形狀是 **`{id:數量}`** —— 存 id 不存名稱，改名不動存檔。
- API：`add/addMany/remove/count/all/grouped/defOf/nameOf/snapshot/restore/clear`。
- ⚠ 三條來源（戰鬥後獲取、商店購買、劇情取得）**一律走 `add()`** —— 不要有第二個地方
  記持有量。未定義的 id 照收（素材是資料），只在 console 記一筆讓漏填現形。
- ⚠ 放 `script/` 不放 `modules/`：這是**存檔資料**，與 `progress.js` 同一層；
  `modules/` 放的是戰鬥與演出邏輯。

## B. 好感度計數（`script/progress.js`）

`addAffection(who, delta)` / `affectionOf(who)` / `tierOfChar(who)`，四人（renna/nouvelle/
sorana/anya）。三條規矩：

1. **棘輪**：可以扣，但不跌破**已達 tier 的下限**（`tierFloor`）。地板另存
   `tivot_aff_floor_v1` —— ⚠ 不能塞進 `tivot_affection_v1`，那支的形狀被
   `flight/index.html` 讀著。
2. **小數要留住**：蕾娜只吃 S 且一次 **+0.25**，所以一律存實數並對齊 1/4
   （`Math.round(v*4)/4`，避免 10.249999）。
   ⚠ `flight/index.html` 的 `setAffection` 有 `v|0` —— **讀**沒問題，哪天要在飛行頁
     加減好感，那一行也得改。
3. 夾在 0~50。

實測：10 →（+0.25 ×2）10.5 →（+20）30.5 →（−99）**21**（tier 3 的地板，棘輪生效）。

## C. 教學結算：**沒有監察官、不評等級**，收尾彈拾得道具

- `inspector.settle` 一開頭改成：`state.tutorialRun` → `tutorialSettle()`。
  該頁 `showResultSequence(..., { noInspector:true })`，戰績照列、**EXP 照算照顯示**、
  但不畫 S/A/B 那個大字。⚠ 「不評等級」是不顯示，不是不算 —— 等級之後要拿來解隱藏關，
  教學場不該污染那條線。
- 掉落 `config.tutorial.loot`＝聖徒之爪（低品質）×1、碎鐵片×2，結算頁站定 800ms 後彈
  `modules/loot.js` 的視窗。⚠ **道具是在 `showLoot` 裡入袋的**，呼叫端不要自己再寫一次。
- ⚠⚠ 劇情版教學以前是**整頁不出**（`storyBattleEnd` 直接把場子交還劇情）。現在勝利改走
  結算頁，**按鈕按下去才回劇情**（`inspector.init` 多注入一支 `storyReturn`）。
  戰敗維持原樣（不給結算與掉落）。劇情版不播「驅逐完成」過渡禎（同 -329「切乾淨」）。
- 首頁多一顆 **「道具」** 鈕 → `loot.showBag()`（五類分組，空的分類也列標題）。

## D. 教學的聖徒血量

- 新增 `tutorial.storyFinishEnemyHp: 90`：劇情版（教到破防為止）在破防那一盤打完時
  壓到這個值 —— Ray：「破防結束以後可以一盤內收拾的血量」。
  ⚠ 與 `finishEnemyHp`（70）分開：那是聖徒化＋MB 之後的殘血，劇情版沒有那一段。
- 「破防前敵 HP 掉到 50% 以下 → 強制灌滿破防值進入破防教學」**本來就有**
  （`dualForceHpRatio: 0.5` → `onEnemyHp` → `fillEnergy`），這一版確認沒被別的條件擋住。

## E. 還沒做（Ray 的規格裡提到、這一版沒動）

- **商店**：`inventory.add()` 已備妥，但沒有商店 UI 與價格資料。
- **好感度的來源**：`addAffection` 只是計數器；四人各自的加減規則
  （docs/TIVOT_AFFECTION_RULES.md：諾薇兒聖徒化 +1／安雅不用聖徒化 +1／索拉娜評價越爛越加／
  蕾娜只吃 S +0.25）**還沒接到戰鬥結算上**。
- **裝備/使用道具**：只有持有量，沒有裝備欄位與使用效果。


---

# 交接 · `ver -359`（帝都 攝政王廣場：正名蕾娜；好感度起算；素材批次轉檔）

## A. 新場景 `capital_square`（接在 `hq_audience` 之後，交界有讀取閘門）

- 她**這一幕起改叫蕾娜**（speaker `RENNA`）；上一幕是 `OFFICER`（顯示「監察官」）。
  ⚠ 諾薇兒這一幕台詞裡仍叫「監察官大人」—— 那是**台詞內容**，與 speaker id 無關。
- **好感度起算**：scene 上新增 `initAffection:{renna:0, nouvelle:5, anya:0, sorana:0}`，
  由 `story.js` 在 scene 收尾寫一次。
  ⚠ 設的是**絕對值**（走 `setAffection` 不走 `addAffection`）—— 起算點就是地板本身，
    不該被上一輪的棘輪擋住。
  ⚠ 用 flag `aff_init_<sceneId>` 擋住重複執行，否則重看這一幕會把玩家累積的好感洗掉。
  ⚠ 這組值與 `progress.AFFECTION_DEFAULT`（10，flight 頁也在用）**不一致**是刻意的：
    預設 10 是還沒接劇情時的暫定值，正式起算以這一幕為準。

## B. ⚠ 稿子裡有一處欄位錯位，我改了

「確實……像監察官大人說的那樣。」稿上標「蕾」＋`Renna_SI_awkwerd`，但那句是**諾薇兒**
在附和（她口中的「監察官大人」就是蕾娜本人，蕾娜不會這樣稱呼自己），下一拍蕾娜才接
「叫監察官好生硬耶！」。已改為 `NOUVELLE` ＋ 她的 `awkward`。**若原意真是蕾娜，說一聲改回去。**

## C. ⚠ 這一幕兩個人都在左邊 → 每一句都在換卡

固定站位是「左 蕾娜・諾薇兒／右 索拉娜・安雅」（§6.5）。上一幕有璐娜莉亞站右所以還好；
這一幕**全程只有她們兩個**，於是每換一個說話者就是一次「舊的滑出、新的滑入」。
規則上正確，但讀起來很忙。要改的話有兩條路，都要 Ray 定：
①這一幕讓蕾娜暫時站右（破壞「同一個人永遠同一邊」）；
②同側換人改成**不滑走**、只切換明暗（要新的演出規則）。

## D. 素材批次（16 張圖 ＋ 1 首 BGM）

    SI：GuildCounterCA / GuildHunter ×4 / Nouvelle_SI_whisper / Renna_SI_stare
        ＋ Luna_SI_seat_smirk（換新版）        1523~2507K → 119~249K
    background：Capital_{Square,Church,Cityhall,Downtown,Midtown,Uptown}_Day
        ＋ Captal_Guild_Day                    2469~3011K → 191~347K
    enemy：Kidd_CI                              2120K → 227K
    bgm_Capital_Day.m4a                         174k → **96k CBR**（1453K → 809K，Ray：「壓縮作業」）

取景值（新的三張）：

    Renna_SI_stare       top=0  bot=1527 fx=0.515
    Nouvelle_SI_whisper  top=8  bot=1530 fx=0.697   ⚠ 偏右，與 gossip1（0.710）同類構圖
    Luna_SI_seat_smirk   top=2  bot=1519 fx=0.539   （換新版，舊值 0/1536/0.504）

⚠ `GuildHunter_*` / `GuildCounterCA` / `Kidd_CI` / 其餘六張 Capital 背景**尚未接線**——
  轉好放著，等後面的場景用。


---

# 交接 · `ver -360`（蕾娜這一幕站右：scene 級站位覆寫；帝都那一拍的 se 節奏）

## A. scene 級站位覆寫 `sides:{RENNA:'R'}`

Ray 對 `-359` 那個「兩個人都在左邊、每句都在換卡」的回答是「蕾娜站畫面右側」。
做成**整幕覆寫**而不是改角色的 `side`：

- `story.js` 新增 `sideOf(id)`：先看 `cur.sides`，沒有才用 `ART[].side`。
  `playScene` 進場時把 `cur.sides` 收進 `sideOverride`。
- `capital_square` 加 `sides:{ RENNA:'R' }`。實測：諾薇兒左、蕾娜右，**兩人一直在台上**，
  不再每句滑進滑出。
- ⚠ 為什麼不直接改 `ART.renna.side='R'`：會客廳那一幕璐娜莉亞**坐在右邊**，
  蕾娜若也變右就會把她擠掉（坐著的人滑走特別怪）。要全域改的話那一幕得一起重排。
- ⚠ 覆寫只能到「整幕」為止，**不可以逐句換邊** —— 那是 ver -288 被退回的發起位制
  （立繪朝向畫死，換邊要水平翻轉，髮旋與持物會左右顛倒）。已寫進 `CLAUDE.md` §6.5。

## B. 那一記悶響的節奏（Ray 修正）

    （主角空白框）        ← **不再配 se_punch**
    蕾：「……你倒是叫得挺順口的。」stare
    諾：（無台詞）surprise ＋ 畫面震動 ＋ se_punch   ← 揍下去在這一拍
    諾：「（那是侯爵的千金欸！你莊重一點！）」whisper

原本 se_punch 掛在空白框那一拍、震動另成一拍且沒有立繪；現在悶響與震動同拍，
並帶諾薇兒的驚訝立繪 —— 讀起來才是「她動手了」。


---

# 交接 · `ver -361`（拾得道具改點擊才彈；教學結算用 crisis；坐姿「用椅子鎖」）

## A. ⚠⚠ 坐姿大小一直跳：**四張坐姿的椅子本身就畫得不一樣大**

Ray：「璐娜坐姿的大小一直跳，用椅子來鎖。」

先確認過一件事：**這四張是各自生成的，椅子不是同一張素材** —— 同座標比對 RGB，
左右椅背區的平均差 50~96（0 才是同一張圖）。所以「每張自己的 alpha 跨距」根本
不是同一把尺，引擎照它算當然會跳。

量法（可重跑，寫進 `speakers.js` 的註解）：把每張的 alpha 輪廓對 `seat_N` 做
**只比椅子帶**的縮放對位（左右各 22% 欄、縱向 20~75%，避開中間會動的身體與手臂），
求最佳縮放 k：

    seat_N 1.000（基準） / seat_smirk 0.996 / seat_angry 1.140 / seat_hand 1.122

再套公式（⚠ 要連 alpha 跨距一起算，那是引擎的分母）：

    faceAdj_i = 0.74 × (bot_i−top_i)/(1536−7) ÷ k_i
    → seat 0.740 / smirk 0.737 / angry 0.648 / hand 0.634

實測四張的「椅子在螢幕上的大小」：0.36019 / 0.36013 / 0.36028 / 0.36039 —— 差 **0.07%**。

- ⚠ **交叉驗證**：Ray 先前憑眼睛把 `seat_angry` 收到 0.63（−15%），公式算出來 0.648 ——
  兩條獨立的路落在 3% 內。四張一律改用公式值，方法才一致。
- ⚠ **不要改用「整張輪廓」對位**：`hand` 那張手臂舉起來、輪廓變了，量出來 1.089
  （與椅子帶差 3%），`angry` 更會量成 1.027（差 10%，明顯錯）。
  **身體會動，椅子不會** —— 那正是「用椅子來鎖」的意思。
- ⚠ 這是**第四種**量法了。前三種（膚色偵測臉高／窄帶膚色跨距／眼罩當剛體）都失敗，
  記在 `-347`。差別在於：椅子夠大、對比夠強，而且**是這一組圖裡唯一不動的東西**。

## B. 拾得道具改「點畫面才跳出」

原本結算頁站定 800ms 自動彈。現在掛一次性的 `pointerup`（capture、once）——
玩家把戰績看完，想看下一頁再點，與對話推進同一個手感。
⚠ 「繼續」那顆鈕也會**先彈道具再說**（`_lootPending` 擋著）：不能讓玩家一按就走，
那樣掉落等於沒發生（東西其實已經入袋，但他不知道拿到什麼）。

## C. 教學結算的按鈕與 BGM

- 按鈕文案 `回到主畫面` → **`繼續`**（三語一起改）。教學結算是「往下走」不是「離場」。
- 結算 BGM 由 `bgm_result` 改成 **crisis**（Ray 指定）：那首 result 是「一場驅逐打完」的
  收束感，而教學是劇情中間的一段。
  ⚠ `config.ASSETS` 新增 `bgm_crisis`，並**併入第二段預載**（`LATE_BGM_PATHS`）—— 別擋開機。
  ⚠ `bgmVol.bgm_crisis` 設 **0.62**，與 `story.js` 播它時寫的 0.62 同值：
    `playBgm` 對「同曲播放中」直接 return，誰先起播誰的音量就定了 ——
    不一致的話同一首會因為「是誰起播」而大小聲。


---

# 交接 · `ver -362`（訓練用聖徒血量 500 → 300）

Ray 指定。血量只影響**節奏**不影響流程 —— 教學段落未播完前敵血夾底 1 不可被殺
（`combat.enemyDamage` 的教學夾傷），所以調低不會提前結束教學。

連帶的門檻是**跟著血量走的比例**，不必另外改：

    dualForceHpRatio  0.5 → 150 以下強制灌滿破防值進破防教學
    strikeForceHpRatio 0.3 → 90 以下強制進劇情殺
    storyFinishEnemyHp     → 破防那一盤打完壓到 90（劇情版收尾，一盤內可收拾）

⚠ `storyFinishEnemyHp`（90）現在正好等於 `strikeForceHpRatio` 的門檻（90）——
巧合，兩者互不干涉（劇情版沒有劇情殺那一段，那條在 `storyRun` 時已被濾掉）。


---

# 交接 · `ver -363`（seat_angry +20%；開發者跳段鈕；楣的圖層僅次於對話框）

- **`seat_angry` 加大 20%**：0.648 → **0.778**（Ray 指定）。
  ⚠ 它**刻意脫離椅子鎖**（-361 建立的那把尺）—— 那一張是她前傾發火的構圖，
    鎖住椅子反而讓人顯得縮在後面。其餘三張仍在鎖上（0.740 / 0.737 / 0.634）。
- **跳段鈕 `#storySkip`（開發者限定）**：`story.skipToNextGate()` 從目前這一句往後找
  第一個 `{ load:… }` 直接演它（那一行就是讀取閘門）；找不到就 `endScene()` 走 `next`。
  ⚠ 只在 `body.testmode` 出現（CSS 控制），是開發用的梯子不是玩家功能。
  ⚠ 跳之前要收乾淨：打字機、`delay`、`auto`、演出計時器、**還沒收的黑幕**（`flushCgFade`）——
    不收的話上一句的殘留會蓋到新段落上。
  實測：地宮 line 9 → 一按 → `hq_audience` line 0。
- **楣的圖層**：`#kerb` z-5 → **6**（僅次於對話框 7）；退出鈕與跳段鈕抬到 8（要一直點得到）。
  規則寫進 `CLAUDE.md` §6.5：楣是**畫框**，背景／插圖／立繪／情境卡／黑幕全在它之下。
  ⚠ 但我**沒能重現**「楣被遮住」的畫面：劇情層裡本來就沒有 5~6 之間的東西，
    立繪(1)、插圖(2)、情境卡(4)、黑幕(4) 都在它之下。會蓋住它的只有全螢幕的東西
    （cut-in 8100/8200、讀取頁、拾得道具 9600）—— 那些是刻意的。
    **下次看到請說是哪一拍**（哪一幕、哪一句、當下畫面上有什麼），我才知道要抬誰。


---

# 交接 · `ver -364`（「楣被遮」的真因：它是**被自己的容器裁掉**，不是被立繪蓋住）

Ray 給了截圖，量出來就清楚了：

    楣的實際上緣  393px
    clip 的起點   432px  ＝ 面板頂(494) − 楣高(62)
    → 楣的上緣 **39px 被 `#kerb` 自己的 clip-path 切掉**，露出後面的立繪與背景

`-363` 把 `#kerb` 抬到 z-6 沒有用，因為根本不是圖層問題。

**真因**：clip 的起點寫成「面板頂 − 楣高」，那個式子**只有在「門頂＝面板頂」時才成立**。
門要多高是 `layoutKerberos` 依實際尺寸**解出來**的（見那支函式的幾何說明）——
這一版的視窗比例下實測**門頂 447、面板頂 494，差了 47px**。
（`-343` 當時量到兩者相等，所以那個式子矇對了；比例一變就露餡。）

**修法**：`layoutKerberos` 把**楣的實際上緣**寫進 `--kerb-clip-top`，CSS 的 clip 直接吃它
（舊式子留作退路）；`.full` 的終點跟著改成 `--kerb-clip-top − --kerb-rise`，
所以 `-343` 那條「clip 與門同速」仍然成立。

實測（上推全程取樣八次）：`clipTop` 與楣的上緣**完全同步**
（393/393 → 351/351 → 180/180 → −23/−23），楣高全程 62 不變。

⚠ 教訓：**凡是「由幾何解出來」的量，都不要在別處用另一條式子重算一次**。
要嘛把解出來的值寫成 CSS 變數給大家用，要嘛就都去問同一支函式。


---

# 交接 · `ver -365`（把「一個量只有一個計算點」升格成鐵律）

Ray：「重覆發生，低級錯誤，寫進原則確保不會再犯。」

同一類錯誤這個 session 內犯了三次（`-343`、`-352`/`-354`、`-364`），共同結構完全一樣：

> **同一個量在兩個地方各算一次，用的是「看起來等價」的兩條式子 ——
> 而它們只在某個特定比例下才真的相等。比例一變就露餡。**

已寫進 `CLAUDE.md` **§0 鐵律第 7 條**（不可違反）：

- 任何解出來／量出來的量（幾何、尺寸、時間、音量、門檻）**只准有一處計算**；
  其他地方一律**讀**它 —— CSS 變數、匯出常數、快取物件。
- **不得用「等價的式子」重算一次。**
- 改動時的自檢：動任何幾何／時間／音量常數之前，先 `grep` 那個數字或那條式子，
  **確認全專案只有一處**。找到第二處就是 bug，不是巧合。
- 真的必須出現在兩邊時（CSS 與 JS 都要用），**兩邊註解互指**，寫明「改一邊要改另一邊」。

表格（三次的兩個計算點與「何時才相等」）也列在鐵律裡，下一輪照著查就能認出同型的坑。

## 立刻照規則掃了一遍，把還在重複的收乾淨

規則寫了不落地就只是口號。`grep` 找出**同型的重複計算點**，全部收進 `config.castStage`：

    topRatio 0.56  ← 原本 story.js 與 tutorial.js **各寫一份**
    btnTop   10    ← 同上（角落鈕距容器頂）
    btnH     44    ← 同上（角落鈕高，立繪頂線靠它算）

兩支模組現在都只是**讀** `GAME_CONFIG.castStage`，計算點只有 config 一處。
⚠ CSS 那邊（`--story-top:56%`、`.corner-btn{44px}`、`#storyExit{top:…+10px}`）是
**鐵律 7 但書允許的第二處**（CSS 讀不到 JS）—— 已在兩邊註解互指「改一邊要改另一邊」。

重構後實測不變：劇情 h=738／頭頂 52、戰鬥 h=732／頭頂 56、clip 393.24 ＝ 楣上緣 393。



---

# 交接 · `ver -366`（關門演出＝進場的倒放；教學戰的開發者跳關鈕）

## A. `story.playKerberosClose(done)` —— 進場那一套倒著演

Ray：「戰鬥結束後讓槍棺從兩邊閉合…倒置的團徽轉回縮小…箭頭與卯釘依次扣回，
每一步金屬磨擦火花，不要有蒸氣，原蒸氣音改為 `se_metalclip`；關閉完成後上一層
黑透遮罩再跑結算。簡單來說就是戰鬥入場畫面的倒放。」

    ① remove('open')   兩扇合上          + 中縫火花 + se_metalclip     0.9s
    ② remove('lift')   紋章轉回並縮小     + 圓盤外緣火花 + 齒輪聲       1.6s
    ③ remove('unlock') 箭與鉚釘依次扣回   + 逐顆火花 + se_metalclip     0.46s+
    ④ kerb-veil        黑透遮罩淡入                                    0.46s
    ⑤ 收舞台 → 交給結算

實測時間軸：0.35s 門合上 → 1.05s 紋章轉回 → 2.81s 箭與鉚釘 → 3.51s 遮罩 → 3.9s 收場。

- ⚠ 進場結束時舞台已被 `close({keepBgm:true})` 收掉，所以這裡要**重新開場**：
  `on` ＋ `kerb-open`（那個 class 把場景各層與對話框藏起來，只留門）。
- ⚠ 擺「門是開著的」起始狀態時要先關掉過場（`kerb-instant`），否則一掛上 class
  就會從關著的狀態演一次開門給你看。
- ⚠ 收尾**一定要把舞台收掉再叫結算**：劇情層 z-8300 > 結算頁 banner z-30，
  不收的話門會蓋在結算上面。
- ⚠ 火花與煙**共用 `#kerbSmoke` 容器但形狀完全不同**：煙是 0.78s 的灰團往上飄（`<i>`），
  火花是 0.5s 的亮粒先噴後落（`<b>`）。不拉開差別會讀成同一種東西。
- ⚠ 進場那一套**沒有動**（仍有蒸氣與蒸氣音）—— Ray 只要求關門這一套改。
- ⚠ combat **不 import story**（模組邊界）：關門由 main.js 用 `combat.setStoryClose()` 注入，
  同 `setStoryReturn` 的作法。

## B. 教學戰的開發者跳關鈕 `#tutDevSkip`

`combat.devSkipBattle()`：**不演關門、不上結算、不給掉落**，直接走既有的
`storyBattleEnd()`（不結算交還劇情）。非劇情教學則回首頁。
顯示條件 `body.testmode.tut-on` —— `tut-on` 由 tutorial 的 start/endTutorial 掛上與清掉。
⚠ `tut-on` **只給 CSS 用**，不要拿它當狀態判斷；狀態的真相在 `state.tutorialActive`（鐵律 2）。


---

# 交接 · `ver -367`（面盤操作：跳段鈕移位、點擊推進、三種手勢）

Ray：「把跳過鈕放在控制面盤左下，面盤點擊也可推進對話、畫面，面盤手勢按住下拉加速對話
且自動點擊，往右滑是自動播放，往左滑開啟此場景已播腳本。」

| 操作 | 行為 | 實測 |
|---|---|---|
| 面盤左下的 `SKIP` | 跳到下一個讀取頁（開發者限定） | left 12 / bottom 12 |
| 點面盤 | 推進一句（與點上半場景區同義） | 20 → 21 |
| 按住**下拉** | `fastMode`：打字 4ms/字 ＋ 每 120ms 自動推進，**放開即停** | 0.9 秒推了 2 句 |
| **往右**滑 | `autoPlay`：唸完停 1.1 秒自動推進，再滑一次關掉 | 4 秒推了 3 句 |
| **往左**滑 | 開啟本場「已播腳本」（含說話者、捲到最底） | 17 列 |

- ⚠ 手勢**起點在面盤才算**（`--story-top` 之下），起點在上半就是單純的點擊推進 ——
  兩者共用同一個 `#storyTouch`，靠起點分流。滑過就不算點（不然放開手會多推一句）。
- ⚠ 兩種模式**不跨場**（`playScene` 時關掉）：那是「玩家現在的操作意圖」，不是劇本狀態。
- ⚠ 自動推進掛在**「這一句唸完」**（打字機的 `onTyped` 回呼），不是固定秒數。
- ⚠⚠ **`setInterval` 回傳的是 number**，往上面掛屬性（`typing.__ms`）會直接 TypeError ——
  實測踩到。間隔要記在另一個變數（`typingMs`）。
- 已播腳本只留**這一場**；跨場回顧要另一套（存檔級）資料，還沒做。


---

# 交接 · `ver -368`（金錢：戰鬥掉落、商店買賣、道具欄顯示）

## A. 金錢

- 存 `tivot_money_v1`，**與道具分開**（道具是 `{id:數量}`；硬把錢塞成假 id 會讓
  「走訪全部道具」多一個特例，還要為它開一個假分類）。
- API：`getMoney / addMoney / spendMoney`（扣到負數夾在 0，沒有欠款）。
- 單位 **G**（Ray 指定），寫在 `config.items.moneyName`。

## B. 戰鬥掉落

`config.battleLoot.money = { chance:0.7, min:12, max:48, bossMul:3 }` —— 機率與範圍在 config，
程式只擲骰（鐵律 1）。一般戰鬥勝利時擲一次，**點畫面才彈**（與教學掉落同一套手感，
不自動蓋掉戰績）。金錢與道具**同一個視窗**（Ray：「併入道具欄顯示」）。
⚠ 教學戰不吃這張表：那一場的掉落是腳本寫死的教材（`tutorial.loot`）。

## C. ⚠ 買賣**只在商店**（Ray 修正）

- **道具欄純顯示**：只有餘額與清單，沒有賣鈕。
- `modules/loot.js` 的 `showShop()`：目前只有「收購」。
  賣價 ＝ `items.defs[].sell × config.shop.sellRate`（1 ＝ 照定價收）。
  ⚠ **沒寫 `sell` 的道具不出現在收購清單**（劇情道具／任務物品賣不掉；「不能賣」是預設）。
  ⚠ 兩段式確認（第一下變「確定？」，2 秒還原）—— 賣東西不可逆，但也不值得另開確認視窗。
- ⚠⚠ **`config.shop.buy` 還是空的**：要賣什麼、賣多少是內容資料，等 Ray 給貨單，
  給了就填 config，這一頁不必改。
- ⚠⚠ 首頁那顆「商店」鈕是**管理人模式限定的臨時入口**。正式入口是城鎮節點
  （docs/TIVOT_IMPL_SPEC.md §1），那個做出來就要把首頁這一條拿掉 ——
  不要讓玩家從首頁直接開商店。

實測：道具欄顯示「G 20」且無賣鈕；商店列出兩項可賣、兩段式確認、賣一個碎鐵片
→ 20→23 G、數量 4→3。


---

# 交接 · `ver -369`（城鎮探索：節點、時鐘、時段背景、商店貨單、路人閒聊）

## A. 新的三支檔

    script/clock.js   遊戲內時間（起點 1908-06-13 14:00）＋時段 → 背景差分名
    script/town.js    城鎮節點（資料）：背景、出口、進場對白、商店、路人閒聊
    modules/town.js   城鎮執行：箭頭（按住 0.5 秒）、移動、對白、商店鈕、閒聊

⚠ 對白**不另寫播放器**：走 `story.playAdhoc(lines, done)` —— 立繪取景、明暗、打字機、
對話框、面盤手勢全部與主線同一套（§6.5「同一把尺」）。story 為此開了三個接口
（`stageEl` / `setSceneBg` / `playAdhoc` / `showPanel` / `isPlaying`），
`endScene` 認得 `__adhoc` 段落（不寫進度、不收舞台）。

## B. 時間

- 起點 **1908-06-13 14:00**；存的是**從起點算起的分鐘數**（存絕對日期會讓改起點要改存檔）。
- 每移動一次 **+10 分鐘**（⚠ Ray 沒指定，這是我先給的值 —— 要改就改 `STEP_MIN`）。
- 時段 → 背景差分（命名規約全城鎮通用，大小寫照 Ray 給的原樣）：
  `Dawn 5-8 / Day 8-17 / Dusk 17-20 / night 20-24 / midnight 0-5`。
- ⚠ 背景走**候選鏈**：`<地點>_<時段>` → `_Day` → 無尾巴的原名（室內用）。
  用 `Image` 逐個試而不是 HEAD 請求（一次移動等一個 round-trip 太貴），**載到才換**。
  退回會在 console 留一筆 —— 否則「為什麼晚上還是白天」會查很久。

## C. 箭頭：按住 0.5 秒才走（Ray 指定，防誤觸）

蓄能圈是 SVG 描邊（`strokeDashoffset` 由滿到空），放開／移出就取消歸零。
走的時候 `se_walk` ＋ 時鐘前進 ＋ 淡出換節點。箭頭旁邊標**目的地名**。

## D. 商店（貨單、買賣、店主）

- 物價模型改成 **`defs[].price` ＝市價**，收購價 ＝ `price × shop.sellRate`（0.5，
  Ray：「買收價為物價 50%」）。⚠ 折扣寫在**店家**不寫在道具 —— 一個道具只有一個數字。
- 貨單 `config.shop.stock.grocery = ['milk','cheese','lime_rum']`（50/100/200 G）。
  ⚠ 恢復體力的數值放在 `defs[].use.hp`，但**戰鬥外的使用系統還沒做**，資料先放著。
- 店主台詞是**輪播**（每按一次「與店主交談」換下一句）。
- ⚠ 錢不夠的商品**按鈕直接淡掉**，不要等按下去才說「錢不夠」。

## E. ⚠ 缺口與待確認（下一輪要先解掉）

1. **`Nouvelle_SI_pray` 沒有**（酒館第一句的禱告）—— 現在會回退基本立繪。
2. **時段背景只有 `_Day`**：其餘四個時段全城鎮都沒有，晚上看起來會像白天。
3. **酒館、旅店沒有背景**，暫借 `Capital_Downtown`；**上街區與旅店還沒有稿**。
4. `resources/SI/28e1e4e3-….webp` 是**蕾娜掩嘴**的差分（不是諾薇兒禱告）—— 要 Ray 命名。
5. ⚠ 稿子裡「廣場的**左**＝舊街區」但小節標題是「二、**西區街道**」——
   我採用「西區街道」（背景 `Capital_Downtown`）。哪個是正式名稱請定案。
6. 稿子有兩處**欄位錯位**（與 `-359` 同型），我按語意改了並在註解裡標明：
   教堂的「我沒事啦」標成 `Renna_SI_awkwerd`（應為諾薇兒）。
7. 移動一次 10 分鐘是**我給的值**，Ray 沒指定。


---

# 交接 · `ver -370`（清場改成引擎規矩＝鐵律 8；城鎮改用手勢移動；閒聊改單句）

## A. ⚠⚠ 「換場沒清立繪」為什麼會重犯 —— 規矩寫錯層級了

Ray：「不是寫入原則了嗎？怎麼還是犯錯？」

原因誠實記著：**那條規矩是寫給「腳本」的**（§6.5：第一拍要 `show:false` / `hide`），
而城鎮探索是**新的程式路徑** —— 它不經過 scene 渲染，所以整條規矩被繞過去。

修法有兩層：

1. **程式**：新增 `story.clearCast()`（清兩個立繪槽 ＋ 收對話框），
   城鎮 `enter()` 進節點時呼叫、`playAdhoc()` 開場時呼叫、對白演完再呼叫一次
   （Ray：「人物對話結束就把立繪全撤，只留背景跟移動用的箭頭」）。
2. **憲法**：升格成**鐵律 8** ——「規矩寫成『腳本要怎麼寫』是不夠的，要寫成**引擎的一支
   函式**，然後所有路徑都呼叫它」。並要求：**新增任何一條進入／切換畫面的路徑時，
   先問既有的演出規矩有哪幾條、我呼叫了嗎**。
   ⚠ 與鐵律 7 是同一件事的兩面：7 是「一個**量**只有一個計算點」，8 是「一個**動作**
     只有一個實作」。

## B. 移動改成手勢（Ray：「箭頭太醜了」）

常駐箭頭鈕整組拿掉。現在是**按住 → 往某個方向滑 → 那個方向的邊上浮出提示與蓄能圈
→ 滿 0.5 秒才走**；中途放開或轉向就取消。
⚠ 提示浮在**該方向的邊**，不是畫面中央 —— 玩家的手正往那邊去，訊息要出現在他看的地方。
⚠ 室內（只有 `back` 一個出口）往任何方向滑都回去。
⚠⚠ 實作踩到一個坑：`go()` 原本收「方向」，但手勢那一段已經把方向換算成**目的地 id**，
  傳進去再查一次 `exits[dir]` 只會查到 undefined —— **提示出得來、時間也滿了，就是不會走**。

## C. 路人閒聊改成單句（Ray 指定）

原本走 `playAdhoc`（會接管推進，等於進了對話模式）。改成 `story.flashLine(text)`：
**只把一句話放進對話框，不進對話模式**，再點一下換下一句。
⚠ 兩者的差別寫在 story.js 的註解裡：`playAdhoc` 接管推進，`flashLine` 只是浮一句話。

實測：對白演完立繪全撤（`castOn:false`、對話框 hidden）；往左滑並按住 → 走到西區街道
（14:00→14:10）；滑一下就放開 → **不會走**；往右滑 → 提示顯示「餐酒館」。


---

# 交接 · `ver -371 / -372`（商店改版、店主對白、羅盤移動）

## A. 移動改成「槍棺的四支箭＝羅盤」（`-372`，Ray 指定）

有目的地的方向 → 那支箭**浮起 ＋ 像指南針一樣輕輕晃 ＋ 從下面發光**；
**長按**該箭 → 在箭的位置浮出目的地名與蓄能圈 → 滿 0.5 秒才走，放開就取消。

- ⚠⚠ 晃動與浮起**必須與箭原本的 `rotate(var(--kerb-rot))` 寫在同一個 transform 裡** ——
  transform 不會疊加，分兩條規則的話後面那條會整個蓋掉前面（-343／-350 踩過兩次）。
- ⚠ 浮起的方向沿用 `--kerb-px/--kerb-py`（解鎖時往外彈的那個向量）—— 那已經是
  「這支箭朝外的方向」，不必另外算（鐵律 7）。
- ⚠ `#kerb` 是 `pointer-events:none`，箭要**自己打開**才點得到（子元素可以覆寫父層）。
  實測 `elementFromPoint` 打在箭上會命中 `kerb-arrow w avail` ✓。
- ⚠ 箭的座標問 `getBoundingClientRect`，不要自己算 —— 那組位置是 `layoutKerberos`
  解出來的（鐵律 7）。
- ⚠ **對白播放中羅盤要滅**（跟著 `showNav` 開關）：不然玩家讀台詞時底下有東西在發光晃動，
  按下去還會與推進台詞打架。實測對白中亮的箭＝0，對白後＝n/e/w。
- 室內只有 `back` 一個出口時掛在**下**那一支 —— 「退回」讀起來就是往下。

## B. 商店改版（`-371`，Ray 改稿）

- **分頁：買／賣**；**店主立繪在一旁**；**點到的品項出說明**。
- ⚠ **特殊物品不能賣**：擋在 `inv.sellPrice()`（**價錢層**）而不是 UI ——
  日後有別的賣出入口（委託、熔解…）也自動吃到。
- 店主對話鈕 → **一段對白**（店主與諾薇兒輪流講，含新差分 `bigsmile`），
  ⚠ 按下去先收商店 → 交給 `story.playAdhoc` 演 → 演完再把商店開回來。
  這樣立繪與明暗與別處一致，不必在商店頁裡另做一套對話框。
- **雜貨舖點畫面就開買賣選單**（`shopOnTap`，Ray 指定），不做成按鈕。

## C. 背景與素材

- 西區街道 `Capital_Downtown` → **`Capital_Uptown`**（Ray 指定）。
  ⚠ 連帶：**上街區（`uptown`）現在沒有自己的圖了**，還在用同一張。
- 酒館 → **`Capital_Bistro_Day`**（新素材，已轉檔）。
- 新增 `Nouvelle_SI_bigsmile`（top 4 / bot 1534 / fx 0.565）。
- ⚠ **`Nouvelle_SI_pray` 仍然缺**（酒館第一句禱告會回退基本立繪）。


---

# 交接 · `ver -373`（抵達停一秒、對話只播一次、目的地字格、場景名、指南針改繞軸、藍光）

| Ray 指定 | 作法 |
|---|---|
| 走到新地方先停一秒再放立繪 | `ARRIVE_MS=1000`，抵達 → 停 → 才 `playAdhoc` |
| 對話只觸發一次 | **不再看節點的 `once` 欄位**，一律用 flag 擋（預設值站對邊） |
| 上/左/右加目的地字格，按住蓄能滿了才走 | `.town-dest`，蓄能畫在字格本身（由左往右填），與羅盤共用 `HOLD_MS` |
| 場景名顯示在上方、注意動態島 | `#townInfo` 置中，`top: safe-area + 62px`（讓到角落鈕下緣之下） |
| 箭頭要繞軸左右偏、不要亂飄 | `kerbCompass` 的位移改成**常數**，只有 `rotate` 在振盪 |
| 箭頭下的光改淡藍 | 金光被深銅金的門吃掉 → 改 `--holy` 那個偏青的藍（與門縫透光同色系） |

## ⚠ 換節點要收乾淨的**三件事**（都是實測踩出來的）

`enter()` 一開頭現在做三件事，缺一件就會有東西跑到新地點上：

1. `clearTimeout(arriveT)` —— **抵達停頓的計時器**。不取消的話上一個地點的對白會在
   一秒後**於新地點開演**（實測：從西區立刻回廣場，「肚子餓」那段跑到廣場上演）。
2. `story.endAdhoc()` —— **還在播的臨時段落**。不中止的話它的推進與計時器還活著，
   會在新的地點把上一段演完。
3. `story.clearCast()` —— 清場（鐵律 8）。

⚠ 這正是鐵律 8 的實例：「每次換節點都要做的事」要**列成一組、由一個地方負責**，
而不是散在各處靠記得。


---

# 交接 · `ver -374`（立繪與框一起出、擺幅錯開、地名貼著箭、兩人分左右）

| Ray 指定 | 作法 |
|---|---|
| 立繪與對話框要一起出來（現在是先出框） | 給**第一句**加 `delay: SLIDE_MS`（450）—— 框等人站定。實測立繪 1.2s 上、字 1.45s 起 |
| 箭頭擺幅錯開，不要太整齊 | 四支各自的週期／相位／幅度（2.3~3.1s、錯開的負延遲、3~5°） |
| 地名放在箭頭左右上方 | 字格改由 JS 依**那一支箭的 rect** 定位；⚠ 量完自己的寬度再夾回畫面內（實測左邊那格跑到 −33） |
| 兩個角色要分左右 | `playAdhoc(lines, done, {sides})`，節點可寫 `sides:{RENNA:'R'}`；行政廳已套用 |

## ⚠⚠ 「這不是也寫進原則了嗎？」—— 第三次了，所以加了一張檢查表

清場（-370）、站位（-374）、抵達停頓，三次都是同一個形狀：
**規矩寫在「腳本層」，而新的程式路徑沒有吃到**。

`CLAUDE.md` §6.5 現在有一張「**新畫面／新路徑上線前的檢查表**」，每加一條進入或切換
畫面的路徑就逐條確認：

1. 清場（`story.clearCast()`）
2. 中止上一段對白（`story.endAdhoc()`）
3. 取消上一段的計時器（抵達停頓、auto、演出）
4. 兩人同台分左右（`sides` 覆寫）
5. 立繪與對話框一起出現（`delay: SLIDE_MS`）
6. 同一段對白只播一次（用 flag 擋）
7. 取景走同一把尺（`castStage`）

⚠ 這張表就是鐵律 8 的落地：**規矩要有實作、而且要有人在上線前逐條核對**。

---

# 交接 · `ver -375`（舊街區改名、賞金獵人公會、劇情插入戰、敵人標準卡、懸賞榜）

## A. 這一版做了什麼

Ray 交了「二、舊街區（原西區街道）」整段稿，裡面第一次出現**劇情插入戰**與
**敵人資訊標準卡**。做出來的東西分四層：

| 層 | 東西 |
|---|---|
| 資料 | `config.enemies.guild_hunter`（標準卡）、`config.battles`（這一場的規則）、`config.bounties`（懸賞）、道具 `brass_casing` |
| 引擎 | `combat.startScriptBattle()`、`storyFramed()`、絕對值懲罰三支、盤面 `boardLoop`、`state.noSaint/noPartner` |
| 演出 | 敵人卡的 `bg`＋`fit.mode:'contain'`（去背立繪要背景）、新的 `blunt` 受擊特效 |
| 內容 | `script/town.js` 的 `oldtown`／`guild` 兩個節點；`modules/loot.js` 的懸賞榜 |

## B. 敵人資訊標準卡 → 程式的對照

Ray 的卡是**絕對值**寫法，所以資料就存絕對值（鐵律 7：不要換算成倍率再存）：

| 卡上 | config 欄位 | 讀它的地方 |
|---|---|---|
| HP：200 | `hp` | `enemy.setEnemy` |
| 延時懲罰 5 秒，攻擊力 5 | `delayPenalty:{seconds,damage}` | `combat.effIntervalLimit()` / `delayDamage()` |
| 點錯懲罰攻擊力 5 | `wrongPenalty:{damage}` | `combat.wrongDamage()` |
| 蓄力攻擊：10 | `attack` | `defense`（大絕） |
| 盤面配置 33344, loop | `boardGrids` ＋ `boardLoop` | `combat.boardSeqIdx()` |
| 背景／戰鬥立繪 | `bg` ＋ `image`＋`fit.mode` | `enemy.setEnemy` |
| 掉落物／金錢 | `loot` ＋ `money.hpRatio` | `inspector.scriptSettle()` |
| 抗性／弱點 | `resist`/`weak` | ⚠ **還沒實作**，資料先存著 |

⚠ 三支計算函式（`delayDamage`/`wrongDamage`/`effIntervalLimit`）是**唯一**算懲罰的地方，
呼叫端不准自己再乘一次倍率。

## C. 實測（390×844）

- 公會整段照順序演完：立繪換人、說話者 z-index 在前、空畫面拍沒有框。
- 戰鬥實測值：`ULT_DAMAGE 10 / DELAY_SECONDS 5 / DELAY_DAMAGE 5 / WRONG_DAMAGE 5 /
  N=9（第三盤仍是 9，符合 33344）/ noSaint,noPartner true / HP 200`。
- 打完 → 關門 → 結算頁「沒有監察官、沒有等級、EXP 415」→ 鈕是「繼續」→
  點畫面彈拾得：**黃銅彈殼 ×6 ＋ 133G**（200×0.665，落在 6~8 成內）→
  再按「繼續」回到公會，接著演「服了！我服了！」→ 演完清場、旗標
  `town_capital_guild` 與 `guild_registered` 都寫進去 → 點畫面開懸賞榜（黑船洛爾夫 500G）。
- 舊街區：長按下方箭頭提示「舊街區」、時鐘 14:00→14:10、三個目的地字格
  （賞金獵人公會／雜貨舖／餐酒館）都在。

## D. ⚠ 我改了 Ray 寫法的地方（要確認）

1. **「西區街道」整段進場對白被換掉了** —— Ray 這次給的是「這地方……有點可怕」那一段。
   原本那段**肚子餓**（`Se_Tummy` ＋「剛剛才經歷一場死鬥」）現在**沒有地方演**。
   要留的話請說要接在哪裡（我建議接在餐酒館之前，那一段禱告詞才有前因）。
2. **旅店暫時接不到**：舊街區的「上」原本是旅店，被公會頂掉了。節點與背景都還在，
   Ray 指定掛哪個方向就接回去。
3. **結算頁的字**是沿用驅逐任務那一套：「聖裁／賞金獵人**已淨化**」。
   對一個街頭獵人來說「淨化」怪怪的，要不要給插入戰另一組字？
4. **打輸的去向**：現在走一般的失敗流程（回主畫面）。要改成「當場重來」請說。
5. **移動一次 10 分鐘**、獵人與櫃台的**身高**（178/168）都是我給的值。

## E. ⚠ 缺口（沿用上一版，加一條）

1. `Nouvelle_SI_pray` 沒有（酒館第一句）。
2. 時段背景只有 `_Day`。
3. 旅店沒有背景；**上街區與旅店還沒有稿**；上街區的背景被舊街區借走了。
4. `resources/SI/28e1e4e3-….webp` 是蕾娜掩嘴的差分，要 Ray 命名。
5. `se_ginclick.mp3` 是新丟進來的，**還沒照 §6.6 量響度**（其餘音效都量過）。
6. `NPC_Capital_Gunstore_SI`（槍店店主）已轉 WebP 並量好取景（top 6 / bot 1531 / fx 0.476），
   但**還沒接**：Ray 還沒給槍店的稿與節點位置（舊街區的對白提到「要去保養武器嗎」，
   看起來就是它）。

---

# 交接 · `ver -376`（帝都地圖定案、戰敗一律 Game Over、首頁收掉兩顆鈕）

## A. 帝都的地圖（Ray 定案）

```
                    ┌ 行政廳 ─ 中心區 ─ 教堂 ┐
                              │
   船塢                  攝政王廣場                  旅店
    │                    │   │   │                   │
  舊街區 ── 槍店      左 ─┘   │   └─ 右            上街區 ── 雜貨舖
    │  └ 賞金獵人公會        （下＝入口）              │  └ 餐酒館
```

- **舊街區**（`Capital_Downtown`）：左＝槍店、右＝賞金獵人公會、上＝船塢、下＝廣場。
- **上街區**（`Capital_Uptown`）：左＝雜貨舖、右＝餐酒館、上＝旅店、下＝廣場。
- ⚠ 三家店（餐廳／旅店／商店）**從舊街區搬到上街區**（Ray：「餐廳旅店跟商店放上街區」）。
  那一段「肚子餓」的進場對白**跟著搬** —— 它是餐酒館那頓飯的前因，留在舊街區會沒頭沒尾。
- ⚠ `Capital_Uptown` 還給上街區了（-371 那次兩張圖被對調過，現在對回來）。
- ⚠ **槍店／船塢／旅店三個節點沒有背景**（Ray 還沒給）。節點與名字先做著，
  `script_lint.py` 會把三張缺圖列成錯誤 —— 那正是我們要的提醒，不要為了讓 lint 全綠而拿掉。
  槍店的店主立繪已經有了（`NPC_Capital_Gunstore_SI`，取景量好）。

## B. 戰敗一律 Game Over（Ray 定案）

> 「除標明劇情殺／可戰敗之外，戰敗一律要接 Game Over 畫面回主選單。」

- 實作在 `combat.lose()`：唯一的例外是那一場的卡上寫 `allowLose:true`。
  其餘（含劇情帶起來的教學）一律往下走「死亡定格 → 驅逐失敗 → 結算 → 首頁」。
- ⚠ 連帶改了 `inspector.settle`：教學／插入戰的結算頁加 `!isLose` 守門 ——
  那一頁是**給打贏用的**（戰績＋拾得），輸了看到一頁戰績是錯的。
- ⚠ 教學真正的「劇情殺」不受影響：`tutorial.onPlayerDead()` 在傷害落地那一刻就攔下來，
  根本走不到 `lose()`。
- 實測：插入戰打輸 → 「聖光黯滅／HUND 倒下了…」→ 鈕回主選單；
  **道具與金錢一毛都沒進帳**（實測 before/after 都是 0），公會那一段的旗標也沒寫 →
  回頭再走一次還打得到。

## C. 首頁收掉「道具」與「城鎮」兩顆鈕（Ray 指定）

- ⚠ **道具欄因此沒有入口了**（`loot.showBag` 還在，只是沒人叫它）。
  之後要接在城鎮或別的選單裡 —— 不要因為「沒人叫」就把那支函式刪掉。
- 「商店」那顆本來就是管理人模式限定，維持原樣（正式入口是雜貨舖節點）。

## D. ⚠ 缺口

1. 槍店／船塢／旅店：**沒有背景、沒有稿**。
2. `Nouvelle_SI_pray`（酒館第一句）沒有。
3. 時段背景只有 `_Day`。
4. 道具欄沒有入口（見 C）。
5. `se_ginclick.mp3` 還沒照 §6.6 量響度。
6. 結算頁對插入戰仍寫「賞金獵人**已淨化**」，字要不要換還沒定。


---

# 交接 · `ver -377`（武器店：打靶分歧戰、三頁商店、武器卡與持有制）

## A. 這一版做了什麼

Ray 交了武器店整段稿（含**可戰敗**的打靶、三張武器卡、三頁商店）。

| 層 | 東西 |
|---|---|
| 資料 | `weapons` 六把（三把新貨＋三把原有補齊 `cat`/`critRate`/`maxMod`）、`shop.shops`（每家店長什麼樣）、`shop.stock.gunstore`、`battles.range_trainee` |
| 引擎 | 劇情分歧（`label` / `goto` / `battle.onLose`）＋ 勝負一起交還；逐武器暴擊率；橘圈絕對值傷害；武器持有制；逐場最佳紀錄 |
| UI | 商店改成**由 config 驅動**（頁籤／店主圖／只收哪一類／要不要比數值），武器規格表＋「對比現有」 |
| 內容 | `gunstore` 節點（背景 `Capital_Firearm_Day`，Ray 當場交件）、店主隨機知識 8 句 |

## B. 兩條「只有一份」的整理（鐵律 7）

1. **武器規格文字**原本手寫在 `desc` 裡（「反擊：8發×6傷害」），與 `hits`/`dmgPerHit`
   是同一組數字寫兩次。現在由 `config.weaponStatRows()` 產生，`flavor` 只留一句風味。
   出擊整備的卡與槍店的規格表**共用**那一支。
2. **橘圈改打傷害**原本存倍率（`perfectDamageScale:0.5`），但 Ray 的卡寫的是絕對值
   （「橘圈 6 發 ×4」）。改存 `perfectDmgPerHit`，倍率由 `defense.js` 現算（只有一處）。

## C. 分歧的機制（可戰敗）

- 腳本：`{ battle:'range_trainee', onLose:'range_lose' }` ＋ 目標拍的 `label`
  ＋ 贏的那一支結尾 `{ goto:'range_merge' }`。
- 引擎：`combat.storyBattleEnd(lost)` 把勝負一起交還 → `story.resumeFrom(pos, res)`
  依 `res.lost` 決定接哪一支。
- `script_lint.py` 會檢查：`goto`/`onLose` 的 label 存不存在、
  寫了 `onLose` 但那一場沒有 `allowLose`（＝分歧演不到）、
  標了 `allowLose` 卻沒有 `onLose`（＝輸了會照著贏的那一支演）。

## D. 實測（375×812）

- 武器店整段演完，**輸了**接到「果然調校不太行呢。」→ 合流 → 修理音 → 收尾清場、旗標寫入。
- **贏了**：結算頁多一列「最佳紀錄 0.5 秒」＋★ NEW RECORD ★，存進 `tivot_best_range_v1`。
- 商店：三頁（買武器／賣武器／武器改裝）、龍息的規格表＋「對比現有：鐵拳」（壓暗那張）、
  買下 3000（9000→6000）→ 鈕變「已持有」→ 賣出頁只有龍息（1500G），
  **開局那三把不在賣出清單裡**（它們沒有價格）→ 出擊整備的卡疊變成四張。
- 店主對話鈕：隨機一句，講完自動開回商店。

## E. 順手修掉的既有 bug

- `modules/town.js` 的 `lastChat` **從來沒有宣告**。ES module 是嚴格模式，
  所以酒館的路人閒聊一按就丟 ReferenceError ——**一句都放不出來**。

## F. ⚠ 要 Ray 定案

1. **打靶的「最佳紀錄」記的是通關用時**（越短越好）。要記別的（命中數／得分）請說。
2. **打靶用的訓練用聖徒是原始數值**（HP 500、大絕 45），不是教學那個被鎖過的版本 ——
   所以很容易輸。這樣可以嗎？（分歧存在，輸了也演得下去。）
3. 打靶**沒有禁**聖徒化與搭檔技（稿上沒說）。要禁就加 `noSaint`/`noPartner`。
4. 「絞肉機 改」的**暴擊率 10%**，比原版 20% 低 —— 卡上就是這麼寫的，其餘完全相同。
   確認一下是不是筆誤。
5. **（修理音）先借 `se_metalclip`**（金屬扣件）。要專門的音效請給。
6. **店主的 8 句知識是我寫的**（黃圈橘圈、暴擊怎麼擲、改裝的限制），要換直接改
   `script/town.js` 的 `keeperRandom`。
7. 三把新槍**沒有自己的立繪與音效**，先借同類那一把的。
8. **武器改裝頁留空**（照你說的）。合成表（素材→武器）還沒有資料。

## G. ⚠ 缺口

1. 船塢、旅店：沒有背景、沒有稿。
2. 道具欄仍然沒有入口（-376 移掉首頁那顆鈕之後）。
3. `Nouvelle_SI_pray`（酒館第一句）沒有；時段背景只有 `_Day`。
4. `se_ginclick.mp3` 還沒照 §6.6 量響度。

---

# 交接 · `ver -378`（初始武器分兩套數值、地名白字、修理音）

## A. ⚠⚠ 武器數值分「本篇」與「試玩版」兩套

Ray：「修改初始特殊武器數值，但是跟試玩版的『出陣』那個分開，**試玩版的不要動**。」

作法：`config.weapons[key]` **本體維持試玩版的值一個字不動**，本篇要調的欄位寫在
`story:{…}` 覆寫裡。讀取一律走 **`weaponOf(key, story?)`**（鐵律 8 —— 不要在各模組
自己 merge 一次），`story` 與否由 `state.storyMode()` 決定
（＝`scriptRun || tutorialStoryRun`，也就是城鎮那條線）。

| 武器 | 試玩版（不動） | 本篇（Ray 的卡） |
|---|---|---|
| 絞肉機 | 8發×6・黃50%・橘完全防禦・爆20% | **8發×3**・黃50%・橘完全防禦・**爆10%** |
| 鐵拳 | 6發×4・**黃75%**・橘6發×2・爆20% | **6發×3**・**黃50%**・橘6發×2・爆20% |
| 嗜心者 | 1發72・黃無・橘無・爆20% | **1發56**・黃無・橘無・爆20% |

- 對照槍店的貨：龍息 6發×6／絞肉機改 8發×6・爆10%／遊隼 1發72 —— 三把都成了清楚的升級。
- 顯示：**商店與道具欄顯示本篇數值**（那些地方只存在於城鎮）；
  **出擊整備頁顯示試玩版數值**（那一頁就是試玩版的出陣流程）。
  ⚠ 本篇日後若有自己的裝備畫面，那一頁要傳 `story=true`。
- `分類`：**步槍 → 萊福槍**（Ray 改正），嗜心者與遊隼兩把都改。
- ⚠ Ray 的初始武器卡上有價格（3000/4000/5000），但他先前指定「這四把不能賣」——
  所以存成 **`value`（市價）而不是 `price`**：`price` 一寫下去 `sellPrice` 就會有值。
  卡上的數字留著給改裝／估價用，但賣不掉。

## B. 其他

- （修理音）改用 `se_ginclick`（Ray 指定）。
- **城鎮上方的地名與時間改白字＋不模糊的黑描邊**（Ray 指定）：原本是金字＋6px 模糊陰影，
  壓在白天的天空或雜貨舖的暖光上會整個糊掉。描邊用八方向 1px 實心（0 模糊）＋一層很淡的
  擴散 —— 模糊半徑大等於把字自己也糊掉，那正是原本看不清的原因。

## C. ⚠ 還沒做：道具欄的入口

Ray：「道具欄只在飛行畫面有入口。」飛行頁目前**沒有**這顆鈕，而且它是非 module 的
獨立頁面、也沒有載 `style.css`（載了會把那一頁的版面洗掉）——
`#lootSheet` 那一套樣式現在只在 `style.css` 裡。兩條路：
  ① 把那段樣式抽成 `css/lootsheet.css`，兩個頁面共用（乾淨，但要動 style.css）；
  ② 飛行頁自己複製一份樣式（不動 style.css，但同一份樣式有兩份＝鐵律 7 的病）。
**等 Ray 決定**再做。

---

# 交接 · `ver -379`（船塢）

- `Capital_Dock_Day`（Ray 交件）轉 WebP；`se_SailorShout` 登記進 SE 表。
- `capital.dock` 節點的七拍照稿演完（實測）：
  `happy` ×2 → **空畫面＋水手吆喝**（無框無立繪）→ `shocked2` 無台詞停一秒 →
  `cringe` → 主角空白框 → 基本立繪收尾。
- ⚠ 「明天要搭的船」是下一段主線的伏筆，這一段沒有機能（不能上船、沒有商店）。
- 剩下的缺口：**旅店**（沒有背景、沒有稿）、`Nouvelle_SI_pray`、時段背景只有 `_Day`、
  以及**道具欄的入口**（見 -378 §C，等 Ray 決定樣式要怎麼共用）。

---

# 交接 · `ver -380`（道具欄搬到飛行畫面；樣式抽成兩頁共用）

## A. 作法

- `#lootSheet` 那一整套樣式（拾得／道具欄／商店／懸賞榜）抽成 **`css/lootsheet.css`**。
  `style.css` 是主遊戲專用的（它連 `html/body/#app` 的版面都管），整份掛到飛行頁會把
  那一頁洗掉 —— 所以只抽這一段，兩頁共用同一份（鐵律 7）。
- **由 `modules/loot.js` 自己掛**（`ensureCss()`，路徑用 `import.meta.url` 推，所以
  `/` 與 `/flight/` 兩層都對）。任何頁面只要 import 它就有樣式，不必逐頁記得加 `<link>`。
  `index.html` 另外靜態掛一份（帶 `data-lootsheet`，`ensureCss` 看到就不重掛）——
  避免第一次開視窗時樣式還在載、閃一下沒樣式的畫面。
- 飛行頁加了「道具」鈕（右上那一排，`right:248px`）＋一段 `<script type="module">`。
  那一頁其餘的 script 是非 module 的，兩者並存沒問題。

## B. ⚠⚠ 抽樣式時最容易漏的一件事：CSS 變數

抽出來的那一份用到 6 個變數，但**宿主頁面不一定有**（飛行頁的 `:root` 只有 `--gold`，
而且色碼不同）。所以調色盤要**掛在 `#lootSheet` 自己身上**。
實測漏了 `--panel`，飛行頁的面板整個變透明、舵輪從後面透出來。

自檢法（改這一份之後跑一次）：

```
grep -o 'var(--[a-z-]*)' css/lootsheet.css | sort -u
```

對照檔頭 `#lootSheet{}` 那一行，少一個就補一個。

## C. 連續性（Ray：「就算出城→飛行→戰鬥→再入城都還是連續的」）

**資料層已經是連續的**（實測：城鎮打完架拿到的 133G 與黃銅彈殼×6，
在飛行頁的道具欄裡看得到）：

| 東西 | 鑰匙 | 誰寫 |
|---|---|---|
| 道具 | `tivot_inventory_v1` | `script/inventory.js`（唯一入口） |
| 金錢 | `tivot_money_v1` | 同上 |
| 好感 | `tivot_affection_v1` | `script/progress.js` |
| 時鐘 | `tivot_clock_v1` | `script/clock.js` |
| 旗標 | `tivot_flags_v1` | `script/progress.js` |

⚠ **但三個「轉場」還不存在**（要不要做請 Ray 決定）：

1. **出城 → 飛行**：城鎮裡沒有任何出口通往飛行頁（船塢有背景與對白，但沒有登船）。
2. **飛行 → 戰鬥**：飛行頁的「生怪」只是在那一頁生模型，沒有接到舒爾特盤那一套。
3. **再入城**：`town.open()` 一律從 `entry`（攝政王廣場）開始，**沒有記住玩家離開時
   站在哪個節點**。要做的話是一個 `tivot_town_pos_v1`，但現在沒有任何路徑會用到它，
   所以先不寫（不寫沒人叫的程式）。

---

# 交接 · `ver -381`（「一輪遊戲」的邊界：從頭開始要重跑劇情）

## A. Ray 回報的 bug

> 「從頭開始遊戲的話城鎮探索的劇情要重新出現。現在就算從頭開始也沒有劇情了。
>   所謂劇情只跑一次是**指一輪遊戲內只跑一次**，從頭開始、或者從之前的存檔開始的話就要跑劇情。」

原因：旗標（`tivot_flags_v1`）是**永久**的，沒有任何地方清它 —— 首頁的 story 鈕
（＝從頭開始）只是 `story.open(null)`，旗標還留著，城鎮那些「只播一次」的段落就整個消失。

## B. 修法：把「一輪遊戲」寫成一支函式

`progress.newRun()` —— **一輪遊戲的邊界只定義在這一支**（鐵律 8）。它清掉：

    旗標 / 階段 / 好感 / 好感地板 / 玩家名 / 時鐘 / 道具 / 金錢 / 教學看過沒

**不清**跨輪的東西：靜音、語言、管理人模式、最佳紀錄、武器與搭檔的選擇
（那些是偏好與成績，不是劇情進度）。

- 首頁的 story 鈕改成 `prog.newRun(); story.open(null);`
- ⚠ **讀檔不走這一支**：讀檔是 `runRestore()`，把那個存檔的旗標放回去 ——
  該演的劇情自然會演，因為那個存檔本來就還沒演過。
- ⚠ 教學的 `storageKey` **問 config**，不要在 progress 裡抄一份字串（鐵律 7）。

## C. 存檔一起改成「整輪」（v2）

`prog.runSnapshot()/runRestore()` ＝ 進度 ＋ **時鐘** ＋ **道具與金錢**。
與 `newRun()` 清單是**同一張清單的兩面**：newRun 清掉的，存檔就要存得起來、讀得回去。
⚠ 加了新的「一輪內」存檔（例如日後城鎮的所在節點），**兩支都要加**——
漏一支的下場是「讀了舊存檔卻還帶著新一輪的錢」。
舊的 v1 存檔照樣讀得開（`apply()` 認得只有 `progress` 那一層的舊格式）。

## D. 實測

- 船塢演完 → 旗標 `town_capital_dock` 寫入 → 再進去**不播**；
  `newRun()` 之後再進去 → **又播了**。
- 存檔 → `newRun()` 清空 → 讀檔：旗標／金錢 777／時鐘 15:30／黃銅彈殼×3 全部回來。

## E. 下一步（Ray 指定）

- **飛行 →戰鬥**：「本來就是針對飛行頁的，**怪碰到船以後進入舒爾特盤**。」→ 要接。
- **出城 → 飛行**：Ray：「還沒做到那邊。」

---

# 交接 · `ver -382`（飛行 → 舒爾特盤 → 飛行：遭遇戰接起來了）

## A. Ray 指定

> 「本來就是針對飛行頁的，**怪碰到船以後進入舒爾特盤**。」

飛行頁本來就有 `engage` 狀態（距離 ≤210 → 停住、跳出「遭遇　◯◯／— 進入戰鬥 —」、
停 3 秒消失）。那一格字是**開了空頭支票**的佔位，現在把它兌現。

## B. 兩個頁面怎麼交棒

飛行頁與主遊戲是**兩個 HTML**，所以交棒走 localStorage（同好感／時鐘那一套慣例）：

| 鑰匙 | 誰寫 | 誰讀 |
|---|---|---|
| `tivot_battle_req_v1` | 飛行頁遭遇時 | `main.js` 讀取頁被點掉之後（**讀了就清**） |
| `tivot_flight_ret_v1` | 飛行頁遭遇時（座標／航向／高度） | 飛行頁自己開頁時（**讀了就清**） |

流程：遭遇 → 停 1.1 秒讓那一格看得到 → 跳 `../index.html` → 讀取頁點掉之後，
**在聖光蓋滿那一刻直接開打**（不經主選單）→ 打贏 → 結算「繼續」→ 拾得 →
`location.href='flight/index.html'` → 船回到離開時的座標。

- ⚠ **打輸不回去**：走一般的失敗流程（Game Over → 主選單，-376 的規矩）。
- ⚠ **退出也不回去**：退出確認那顆鈕字面上是「回主選單」，所以按下去先把
  `flightBack` 關掉 —— 不關的話按退出反而被送回天上。
- ⚠ 交棒只做一次（`battleGoing` 鎖）：`engage` 是**每幀**都會進來的。

## C. ⚠ 三隻怪的敵人卡還沒給

`config.battles.flight_centipede / flight_serpent / flight_pirate` 現在**一律先借
巨型聖徒**（`facelessgiant`）跑流程 —— 同打靶先用訓練用聖徒的作法（Ray：「暫放那個跑流程」）。
卡到位之後**只要改 `enemy` 那一欄**。

素材現況：`flight/enemy/FLH_Pirate/viewex.png`（1536×1024，2.1MB，**還沒轉 webp**）
可以當空賊的戰鬥立繪；蜈蚣與羽蛇只有 489×291 的方位圖，當立繪太小。
另外**沒有天空背景**可以當戰鬥背景。

## D. 實測（完整來回）

飛行頁生怪 → 把牠移到船邊 → `engage` → 寫入交棒紀錄 → 跳主遊戲 →
點掉讀取頁 → 直接開打（`flight_centipede` / 巨型聖徒 HP300，主選單沒出現）→
打贏 → 「繼續」→ 拾得 → 跳回飛行頁，`cam` 回到 (20100, 15200, alt 700)、交棒紀錄已清空。

## E. 順帶確認（Ray 追問的那件事）

> 「我是說，從頭開始遊戲 **story** 的話城鎮探索的劇情要重新出現。story 才是正式版，出陣是試玩版。」

-381 做的正是這件事，實測：首頁的 **story 鈕**按下去 → `progress.newRun()` →
旗標與金錢歸零 → 從 `MAIN_ENTRY` 演起 → 城鎮那些「只播一次」的段落會再播。
**出陣（試玩版）不碰這些**。

⚠ 但首頁的版面還停在試玩版的樣子：正中央那顆「開始遊戲」＝**出陣**，
而 `story` 只是右下角一顆**管理人模式限定**的小鈕。要不要把正式版的入口換成 story
（出陣退成小鈕或收進管理人模式），等 Ray 指定。

---

# 交接 · `ver -383`（飛行頁「免戰」開關）

Ray：「做一個開關在飛行頁。打開的話碰到怪不會進戰鬥。**地圖設計時一直被怪打會煩死**。」

- 鈕在**生怪鈕右邊**（同一排，操作盤左下）。開著是綠的，狀態存 `tivot_flight_peace_v1`。
- 擋在 **`toBattle()` 這唯一的發動點**（鐵律 8）—— 日後多一條進戰鬥的路
  （例如空賊主動撞船）也自動吃到，不必再擋一次。
- 遭遇**照演**（怪會停下、跳那一格、3 秒後消失），只是不跳頁。
- ⚠ 「遭遇」那一格的第二行跟著改字：`— 進 入 戰 鬥 —` → `— 免 戰 中 —`。
  不改的話下次自己測會以為交棒壞了。
- ⚠ 狀態**會一直記著**（畫地圖常重整，每次重按很煩）。唯一的提醒就是鈕是綠的。
  `progress.newRun()` **不清它** —— 那是工具開關，不是一輪遊戲的進度。
- ⚠ `left` 用 `calc(12px + 5.4em)` 讓開生怪鈕，不寫死像素（那顆是中文字，寬度隨字型變）。

---

# 交接 · `ver -384`（預載改順序、音效瘦身、對白中收 HUD、城鎮起點 11:00）

## A. 預載順序：音效 → 圖 → 音樂（Ray 定案）

原本是「立繪 → MainMenu 音樂 → 三支關鍵音效 → 整批（圖與音效併行）」。
現在是**三段串接**：全部音效 → 圖（監察官立繪排第一）→ 音樂。

實測（Resource Timing）：`SFX 168~284ms ×27 → IMG 298~335ms ×6 → BGM 351ms`，
三段乾淨不重疊。

⚠ 為什麼：音效**要解碼**，慢網下最容易「點進去了還沒好」（開場那兩支消失過好幾次）；
圖沒載完頂多晚一拍；音樂最不急。併行會搶頻寬，那正是老問題的成因。

## B. 音效瘦身：256 kbps mp3 → AAC

26 支重轉（立體聲 96k／單聲道 64k），**2.26 MB → 1.07 MB（省 53%）**；
`resources/audio/se` 整個資料夾 3.9 MB → 2.7 MB。原始檔進 `_originals/audio/se/`。

⚠ 轉完要改的引用有**四處**：`config.js`、`modules/story.js` 的 `SE_FILES`、
`modules/town.js`、`flight/index.html`；門的音效另有 `KERB_SFX_EXT` 的**預設副檔名**
與 `playKerberosClose` 裡**一行寫死 `.mp3`** 的齒輪音（那一行以前就不一致過）。
驗證：逐一 `fetch` + `decodeAudioData`，七支抽驗全部通過（長度與聲道數都對）。

## C. 對白播放中不放時間地點

**飛行頁**的左上（大陸曆／日期／時刻）與中央（地名）在對白播放中淡出。
- 真相來源就是 `sayQueue`（drawTalk 用的那一個），不另記一份旗標。
- 用淡出不是 `display:none` —— 對白一段接一段，硬切會閃。
- 右上那排鈕**不收**（地圖／返回／道具要隨時按得到）。
- ⚠ 城鎮頁**本來就沒有這個問題**：`#townInfo` 在 `#townNav` 裡，對白期間整層 `display:none`
  （實測確認）。唯一的例外是酒館的路人閒聊（`flashLine`，導覽是開著的）——
  那一句沒有立繪，而且收了再開會閃，所以維持原樣。

## D. 城鎮探索的起點時刻 11:00

`clock.js` 的 `EPOCH` 由 14:00 改 **11:00**（Ray 指定）。
⚠ 在此之前的劇情不推進時鐘，所以「開局時刻」＝「踏進城鎮那一刻」，改這一個數字就是改那件事。
要讓劇情段落各有時刻是另一件事（要在 scene 上加 `time:`），不要拿這個常數去湊。

---

# 交接 · `ver -385`（城鎮的地名／時刻：對白中一律讓開）

Ray：「**城鎮頁才有問題，時間地點都壓人物臉上了**，飛行頁反而沒問題。」

## A. 我上一版判斷錯了，錯在哪

-384 我只驗了「節點進場對白」那一條路（`enter()` 前後 `showNav(false/true)`），
量到導覽層整層 `display:none`，就下結論說城鎮沒問題。**那是只驗了一條路**。

導覽層**留著開**的路徑至少有兩條：
  · 酒館的**路人閒聊**（`flashLine`）—— 實測 -384 之前：`nav block`、地名 `opacity 1`。
  · 任何沒經過那兩個 `showNav` 呼叫點的路徑（店主對話收尾、戰鬥交棒回來…）。

## B. 修法：規矩交給**演出層**宣告，不由城鎮在呼叫點決定

`story.markTalking(true/false)`：
  · `renderLine()` → true（每一拍開始演）
  · `flashLine()` → true（路人閒聊也是對白）
  · `clearCast()` / `close()` → false（收場）
CSS：`body.story-talking #townInfo{opacity:0}`（0.22s 淡出，不硬切）。

⚠ 這與飛行頁那一版是**同一個形狀**：誰在演，誰負責說「現在在演」——
城鎮那邊原本是「誰開關導覽，誰順便決定地名要不要出現」，於是漏了兩條路（鐵律 8）。

## C. ⚠⚠ 踩過的坑：**不要去「量 DOM」判斷有沒有在講話**

第一版寫成「對話框看得見 or 台上有人」現場量 —— 立繪的 `.on` 與對話框的 `visibility`
都是**這一拍稍後**才套上／撤掉的（滑入 450ms、延遲打字、滑出動畫），當場量會量到
**上一個狀態**。實測結果整個顛倒：對白中 `false`、結束後 `true`。
正解是演出層自己宣告布林值，不要從畫面反推。

## D. 實測

| 情境 | 導覽層 | 地名 |
|---|---|---|
| 抵達停頓 | none | （看不到） |
| 進場對白中 | none | opacity 0 |
| 對白結束 | block | opacity 1 |
| 酒館路人閒聊中 | **block** | **opacity 0** ← 這條以前是亮的 |
| 走去下一個節點 | none | opacity 0 |


---

# 交接 · `ver -387`（槍棺跨頁交棒、遭遇距離、城鎮的路人單句／櫃台鈕／出航）

Ray 六條修正，逐條記在下面。

## A. 飛行畫面：楣常駐、遭遇時槍棺彈出（item 1）

> 「飛行畫面時將楣常置於操控面板底部，遭遇敵人時槍棺彈出，進入戰鬥畫面，
>   彈出瞬間優先加載敵立繪、其次音效、最後音樂。戰鬥結束後以標準閉棺畫面進入結算，
>   結算完畢回到遇敵時的畫面。」

畫面上是**一扇門、兩個頁面**：

| | 飛行頁（`flight/index.html`） | 主遊戲（`main.js` + `modules/story.js`） |
|---|---|---|
| 平常 | 楣貼在操控面板底緣（整扇棺沉在畫面之下） | — |
| 遭遇 | 停 700ms 讓「遭遇　◯◯」讀得到 → 整扇推上來（1000ms，`se_Kerberos_pop`）→ 跳頁 | — |
| 接手 | — | `story.showKerbGate()`：門直接擺成「已推到頂」 |
| 讀取 | — | **敵立繪 → 音效 → 音樂**（三段串接），藏在門背後跑 |
| 開門 | — | 點一下 → `playKerberosFromRisen()`（撞頂 → 解鎖 → 圓盤 → 開門） |
| 打完 | — | `playKerberosClose()` → 結算 → `flightBack` 跳回飛行頁 |

- 幾何：`layoutFKerb()` 解出來的 —— 門寬 `max(畫面寬, 畫面高/高寬比)`（推到頂要蓋得滿），
  門頂由「**楣的下緣貼齊畫面底**」反推。楣的高度發佈成 `--fkerb-top-h`，
  生怪／免戰兩顆鈕靠它讓開（不寫死像素）。
- `z-index` **兩段**：平常 45（壓過 HUD 鈕 40，但低於開機頁 50 與地圖 60 —— 楣不該蓋在
  讀取頁或地圖上）；`.rise` 時抬到 90。
- ⚠ 上推的 class **不要掛在 `requestAnimationFrame` 裡**：分頁在背景時 rAF 完全不跑，
  於是門不推、時間到了直接跳頁（實測踩過）。門的起始狀態早就在 DOM 裡落地了，直接加 class。
- ⚠⚠ **那一顆「開棺」不能省**：跳頁＝新的 document，音訊要**這一頁的**使用者手勢才解得開
  （iOS 一定要）。門的齒輪／開門音與戰鬥 BGM 全靠它。所以這條路徑**不走讀取遮罩**，
  改成「蓋滿畫面的槍棺 ＋ 一行『開棺』」，讀取藏在門背後（`bootBattleGate`）。
- ⚠ `KERB_META.top` 與飛行頁的 `FKERB.top` 是**同一組數字的兩份**（非 module 頁面 import 不到）。
  改圖重跑 `tools/kerberos_cut.py` 之後**兩邊都要貼**，兩邊註解已互指。
- 實測（Resource Timing，交棒過來那一頁）：
  `Saint_GT_CI.webp 162ms → se_* 184~187ms ×26 → bgm_battle 409ms`，順序如 Ray 指定。
  關門那一段實測 900~4200ms 逐步收回，4500ms 才上結算頁。
- 打完回程實測：cam 回到離開時的座標，`tivot_flight_ret_v1` 已清空。

## B. 遭遇距離：不可讓怪撞到船（item 2）

`ENEMY_CFG.engage` 210 → **480**。但**光加大距離不夠** —— 真正讓牠貼到臉上的是那段等待：
敵人一進 `engage` 就停住了（`updateEnemy` 提早 return），**船卻還在全速前進**，
`MAX_SPD 6.0 × dt × 0.06` ≈ 360 世界單位/秒，1.1 秒就走掉近 400，比整個 engage 半徑還遠。

所以 `update()` 多一行：`if(battleGoing) mv=0;` —— 停的是**位移**不是 `cam.speed`
（改 speed 會讓速度錶／螺旋槳／尾流一起說謊，同 `SPD_BOOST`／Sturm 的作法）。
⚠ 只在**真的要進戰鬥**時停；免戰模式下遭遇照演但不跳頁，那時停船只會在畫地圖時擋路。

## C. 路人單句推廣到四處（item 3）

酒館＝市井、教堂＝**教廷**、行政廳＝**政治**、船塢＝**地理情報**，各七句（`script/town.js` 的 `chatter`）。
互動改成「**點一下出一句、再點一下收掉**」（`chatterOn`）—— 收掉之前不出下一句，
一直點就一直換句的話玩家永遠讀不完。
⚠ 順帶把 `story.hideBubble()` 補上 `markTalking(false)`：`flashLine` 開場時宣告了在演，
收場就該由**同一個層**關掉（§6.5），不然城鎮的地名／時刻會一直讓開。

## D. 買賣視窗不放店主立繪（item 4）

`.shop-keeper-art` 整個撤掉（`modules/loot.js` ＋ `css/lootsheet.css`）。
店主本來就畫在背景的櫃台後面，買賣視窗是疊在他前面的一張單子 —— 再放一次同一個人，
等於同一個角色在畫面上有兩份，而且把清單擠成半寬。
⚠ `config.shop.shops[].art` 這個欄位**留著**（別家店日後可能用得到），只是這一頁不再讀它。

## E. 櫃台鈕（item 5）

商店／武器店／公會共用**一顆** `#townCounter`（差別只在開哪個選單，鐵律 8）。
座標寫在節點的 `counter:{x,y,label}`，而那是**背景圖上的比例**，由 `placeCounter()`
依 `object-fit:cover` 的裁切換算成螢幕座標（原則見 CLAUDE.md §6.5.4）。
原本「點畫面就開商店」（`shopOnTap`）已移除 —— 兩個入口做同一件事，總有一個會被忘記維護。

目前三家的座標（實測貼在櫃台上）：雜貨舖 `.51/.53`、武器店 `.51/.55`、公會 `.52/.58`。
⚠ 公會要**登記過**（`guild_registered`）才出現。

## F. 出航（item 6）

城鎮入口（`capital.square`）的**下方**固定是「出航」，走的是同一套方向出口
（長按那一支箭／字格），不另做一顆鈕。
⚠ 目的地字格原本**下方不做**（那通常是「退回上一層」）—— 現在下方只在「是出航」時才出現，
而且擺在箭的**正上方**：那支箭本來就快貼到畫面底了（實測箭心 y=742、箭底 787，舞台只有 812），
擺下面會被切掉一半。順手把字格的**縱向夾制**補上（原本只夾橫向）。

`sail:{ flag:'got_ship', blocked:[…] }`：旗標還沒立起來就由諾薇兒攔下來
（「蕾娜小姐還沒回來」「船也還沒整備好」）。
⚠⚠ **`got_ship` 目前沒有任何地方會立起來** —— 拿到船的那一幕還沒寫。主線寫到那裡時，
在那個 scene 加 `flags:['got_ship']` 就會通。在那之前出航一律被攔（那正是 Ray 要的）。

## G. 已知的缺口／待辦

- `got_ship` 沒人立（見 F）。立了之後出航會直接 `location.href='flight/index.html'`，
  **城鎮的所在節點目前不存** —— 從飛行頁回來時是回主選單那一套。要做「回到原本那個城鎮節點」
  的話，那是 §6.9 那張清單要一起加的東西（`newRun` 清、存讀檔存）。
- `script_lint.py` 仍有兩個既有錯誤（`noue_fall` 缺圖、`Capital_Inn` 缺背景），與本次無關。

---

# 交接 · `ver -388`（飛行頁改內嵌 iframe：拿掉「開棺」那一顆、剛體推棺、回程不重載）

Ray 三條：
> 「⚠ 有一顆『開棺』擋在中間，不能省…**想辦法克服**，而且棺往上推的時候只有底圖，這不對，
>   推上的時候要連紋章，箭、釘、楣一起剛體推上，戰鬥結束不要另跑預載頁，用結算頁來跑。」

## A. 克服的辦法：**飛行頁不再是「另一頁」，而是主遊戲的滿版 iframe**

-387 那顆「開棺」是**跳頁**逼出來的：跳頁＝新的 document，音訊要那一頁自己的使用者手勢
才解得開（iOS 一定要），否則門的齒輪／開門音與戰鬥 BGM 整段是靜音的。
把飛行頁塞進 `#flightFrame`（`position:fixed;inset:0;z-index:8200`）之後：

- 戰鬥跑在**父頁**，而父頁在開機那一點就解鎖過了 → **那顆鈕整個不見了**（實測：遭遇 →
  自動演完撞頂／解鎖／圓盤／開門 → 開打，中間沒有任何提示，`gateTip` 根本沒建出來）。
- 同源 iframe 的使用者手勢會**往上傳給父頁**（HTML 規範的 activation notification
  會傳給 ancestors 與同源 descendants），所以玩家在飛行畫面上的每一次操作也在幫父頁續命。
- 交棒改成呼叫父頁的掛鉤 `window.__tivotFlight.battle({battle, geom})`／`.close()`；
  父頁回程直接 `openFlight()`（顯示 iframe ＋ `__flightResume()`）。

⚠ **獨立開啟仍然要能用**（開發時直接開 `flight/index.html`）：查不到父頁掛鉤（`EMBED` 為 false）
就退回舊的 localStorage ＋ 跳頁那一套，`bootBattleGate` 與「開棺」那一顆**留著不要刪** ——
那條路徑上它是真的需要的。

⚠ **飛行頁的音樂與環境音要明確收掉**（`__flightPause` / `__flightResume`）：
只把 iframe 藏起來不夠 —— rAF 停了它的 `updateBgm` 也停了，那幾個 `<audio>`
會**維持當時的音量繼續播**。

⚠ `flightBack`（打完要不要回飛行頁）是個旗標，所以**別的戰鬥開場時要清掉**
（`launchBattle` 與 `setBattleHandler` 各加一行）—— 不清的話「飛行遭遇打輸 → 回主選單 →
再打一場劇情戰打贏」會被錯誤地送去飛行頁。

## B. 一扇門、兩個文件：幾何只能有一處

飛行頁的槍棺加上**紋章／四支箭／四顆鉚釘**（全部是 `#fkerbDoor` 的子元素＝同一剛體，
與主遊戲的疊序相同：門 → 紋章 → 箭 → 鉚釘 → 楣）。只推底圖的話上推的是一塊鐵板。

⚠⚠ 兩邊的門寬**不能各算一次**：主遊戲的條件是「紋章圓心落在下半面板正中」，飛行頁是
「推到頂要蓋得滿畫面」—— 實測 375×812 下是 **375.6 vs 407.9（差 8.6%）**，交棒那一格
紋章會忽然變大。作法照鐵律 7：**算的那一支發佈出去**（`fkerbGeom{Wd,Hd,top}`），
交棒時帶著，主遊戲 `showKerbGate(geom)` → `layoutKerberos` 的 `kerbForce` 覆寫。
用完在 `playKerberosFromRisen` 的收尾清掉，否則之後的劇情場次會沿用飛行頁那組尺寸。

實測交棒後主遊戲的 `#kerbDoor` 是 376×812、紋章寬 240（＝0.63892×375.6）—— 與飛行頁同值。

## C. 戰鬥結束不再跑預載頁

iframe 從頭到尾沒有卸載過，所以打完「繼續」之後是**直接把它顯示回來**：沒有第二次讀取頁、
船還在原座標、時鐘與航向都沒動。實作是既有的 `combat.goHome(openFlight, {noBgm:true})` ——
在黑幕**全蓋**的那一刻才換（同劇情交棒 `resumeFrom` 的作法），所以看不到中間那一格。
⚠ 戰鬥／結算的曲子要 `SFX.stopBgm(600)`，不然回到飛行畫面還在放；飛行頁自己的曲子
由 `__flightResume` 接回去。

## D. 踩到的坑

- ⚠ **classic script 的頂層 `const`/`let` 會撞 TDZ**：`resize()` 在指令碼求值期間就會呼叫
  `layoutFKerb()`，而 `FKERB` 與 `fkerbGeom` 原本宣告在檔案下半 → `Cannot access … before
  initialization`，整個飛行頁死在那裡（**踩了兩次**）。凡是 `resize()` 用得到的常數，
  一律宣告在檔頭那一區（`VIEW_F` 旁邊）。
- ⚠ **上推的 class 不要掛在 `requestAnimationFrame` 裡**：分頁在背景時 rAF 完全不跑，
  於是門不推、時間到了直接交棒。門的起始狀態早就在 DOM 裡落地了，直接加 class 就有過場。
- ⚠ 瀏覽器分頁在背景時 `setInterval` 會被夾到 1 秒 —— 用它取樣量動畫時序會得到
  「一瞬間就演完了」的錯誤結論（我一度以為關門演出被跳過，實測其實是 900~4200ms 正常演完）。

## E. 已知的缺口

- 首次進飛行頁（試飛／出航）**還是會跑一次飛行頁自己的讀取頁** —— 那是「進入」不是「結束」，
  Ray 這次要的是後者。要連進入也免掉的話，得在主遊戲讀取那一段就把 iframe 掛起來預熱。
- 城鎮出航之後**所在節點仍然不存**（見 -387 的交接 G 節），從飛行頁按「返回」會落回城鎮舞台
  （因為它一直在 iframe 底下開著），但重整就沒了。

---

# 交接 · `ver -389`（拿掉遭遇提示、立刻彈棺、閉棺剛體、預載分流）

Ray 三則追加：
> 「飛行畫面的羽蛇『進入戰鬥』這個對話框可以完全移除了，**在敵撞上玩家空氣壁之前就要彈槍棺**」
> 「閉棺的時候記得**紋章是跟左側的門一起剛體進入**」
> 「首次進飛行頁仍會跑是正確的，留著，**從城鎮回到飛行畫面也要跑，做預載分流**」

## A. 遭遇提示移除 ＋ 立刻彈棺

- `drawEngagePrompt` 只在**免戰模式**才畫。正常遭遇一律不畫 —— 現在棺是立刻推上來的，
  那一格字只會被門壓在底下閃一眼。
  ⚠ 免戰那一版**留著**：那是開發用的狀態（不會進戰鬥），沒有這一格就完全看不出遭遇有沒有
  觸發 —— ver -383 加那行字正是為了這件事。字也只剩「— 免 戰 中 —」那一版。
- `FKERB_HOLD_MS`（700ms）**整個拿掉**：那 700ms 原本是給提示讀秒用的，提示沒了就只剩
  「怪停在船前面給你看個仔細」。現在「碰到遭遇距離」與「棺開始上推」是**同一拍**。
- 撞不到的保證有兩層（-387 已經做好，這裡只是把等待拿掉）：
  敵人一進 `engage` 就 `return`（不再前進），船由 `battleGoing` 讓 `mv=0`。
  所以推棺那一秒雙方都是靜止的。

## B. 閉棺：紋章與左半扇是同一個剛體

⚠⚠ **CSS 的過場吃的是「新狀態」的 transition。** 關門第一拍是 `remove('open')`，
於是 `#kerb.open #kerbPlate`（借了門的曲線 `cubic-bezier(.7,0,.3,1)`）**不再適用**，
紋章掉回自己的基準 `cubic-bezier(.34,1.2,.5,1)`（帶回彈的浮起曲線）——
與左半扇走不同節奏，兩者當場脫節。開門沒事是因為那一拍 `.open` 正好在生效。

修法：關門第一拍另外掛 `kerb-shut` 把門的曲線借過來，第二拍（紋章自己轉回並縮小）
與 `.lift` 一起拿掉 —— 那一拍本來就該用回彈曲線。

實測（每 100ms 取 `translateX`，左半扇 vs 紋章）：
`-323/-323 → -315/-315 → -294/-294 → -245/-245 → -143/-143 → -68/-68 → -22/-22 → -6/-6 → 0/0`
—— 逐格相同。

## C. 預載分流

進飛行畫面有**兩條路**，讀取頁只有其中一條要跑：

| 路徑 | 讀取頁 | 作法 |
|---|---|---|
| 主選單「試飛」／城鎮「出航」＝**進入** | **跑** | `contentWindow.location.reload()` |
| 戰鬥結束回來（`openFlight({resume:true})`） | **不跑** | 只是把 iframe 顯示回來＋`__flightResume()` |

⚠ 重載一定要走 `contentWindow.location.reload()` —— 把 `src` 設成**同一個字串不會重載**。
⚠ 呼叫端要注意別把參數餵錯：`bindBtn` 會把事件物件當第一個參數傳進去，所以
  `flightBtn` 與 `town.setFlightOpener` 都包成 `()=>openFlight()`；
  回程那條包成 `()=>openFlight({resume:true})`（`combat.goHome` 也會帶參數）。

實測：試飛 → 返回 → 再試飛 → 讀取頁**有跑**；遭遇 → 打贏 → 繼續 → 回飛行畫面**沒跑**，
船還在原座標。

---

# 交接 · `ver -390`（閉棺：箭與鉚釘也一起剛體移入）

Ray：「**釘跟箭也要跟門一同剛體移入**」——與 -389 的紋章是同一個病。

- 箭與鉚釘的**基準**過場是 `.40s cubic-bezier(.2,1.5,.4,1)` **再加上逐顆的 `--kerb-d` 延遲**
  （那是「解鎖時四顆依次彈開」用的）。開門那一拍它們有自己的 `.9s` 覆寫，所以沒事；
  **關門一拿掉 `.open` 就掉回基準** → 四顆各帶不同延遲、用 0.4 秒彈回原位，門卻走 0.9 秒
  ——看起來是零件先飛回去、門才關上。
- 修法同 -389：把 `kerb-shut` 的規則從「只有紋章」擴到 `#kerbPlate` ＋ `.kerb-rivet`
  ＋ `.kerb-arrow`。⚠ **借曲線時不要寫延遲**（＝延遲歸零）：同一剛體上的零件不能有時間差。
- ⚠ `kerb-shut` 仍然只罩第一拍。後面兩拍（紋章轉回並縮小、箭與鉚釘依次扣回）各自該用
  本來那條帶回彈、帶延遲的曲線 —— 那兩拍就是要看到「零件一顆一顆扣回去」。

實測（每 100ms 取 `translateX`）：
```
0    L=-336 P=-336 r10=-344 r4=-328 aN=-336 aE=-317
300  L=-266 P=-266 r10=-274 r4=-258 aN=-266 aE=-247
600  L=-27  P=-27  r10=-35  r4=-19  aN=-27  aE=-9
900  L=0    P=0    r10=-8   r4=8    aN=0    aE=18
```
—— 每一格與左半扇的差值都是常數（紋章 0、r10 −8、r4 +8、箭 n 0／e +18），
那個常數就是它們各自的解鎖彈開量（`--kerb-px`），還留著是對的：下一拍才扣回去。

⚠ 順帶記一個**測試用的坑**：`playKerberosClose` 會先掛 `kerb-open`，而那個 class 把
`#storyBoard` 設成 `display:none` → `layoutKerberos` 量到 `panelH=0` 直接 return。
真實流程沒問題（開門那一套早就把幾何算好寫成 inline 了），但**單獨呼叫關門來測**
會量到一整排 0。要單測就先 `showKerbGate()` 把幾何建起來。

---

# 交接 · `ver -391`（城鎮背景糊化、火車像素化、營業時間、BGM 不重疊）

## A. 城鎮背景「些微糊化」

Ray 第一版回退（「糊化太多了」），第二版說明了目的：
> 「稍微糊化讓 **AI 製圖的顆粒**不那麼明顯，還是你有其他方案？」

那不是景深，是**磨掉高頻雜訊**。試過三個量：

| 量 | 結果 |
|---|---|
| `blur(2.2px)` | 明顯淺景深，廣場的人與窗全化開 —— 退回 |
| `blur(1.2px)` | 還是看得出在糊 —— 退回 |
| **`blur(0.6px) contrast(1.03)`** | 顆粒沒了，畫面仍讀成「對焦的」 ← 現行 |

- 0.6 CSS px ＝ DPR 2 下 1.2 裝置像素，**剛好是顆粒的尺度**（AI 生成的雜訊 ＋ 1536px 的圖
  被縮到約 750 裝置像素時的取樣鋸齒），卻碰不到輪廓。
- `contrast(1.03)`：任何低通都會連帶吃掉一點對比，補回來才不會讀成失焦。3% 看不出色偏。
  **不想要就拿掉，是獨立的一項。**
- 配 `transform:scale(1.01)`：blur 會往框外取樣到透明，不放大四邊會有一圈半透明軟邊。
  ⚠ 縮放對 `placeCounter` 是安全的（cover 是線性映射，`getBoundingClientRect` 也吃 transform）。
- **其他方案**（如果 0.6px 還是覺得多）：真正的根治是**出一張接近顯示尺寸的圖** ——
  現在是 1536 縮到 750，少縮一半雜訊就少一半，而且不必付任何模糊的代價。

## B. 火車像素化

火車原本是**直接畫在主畫布上的向量圖形**（抗鋸齒、全解析度），旁邊的地形卻是低解析緩衝
放大出來的像素塊 —— 一列邊緣乾乾淨淨的車貼在馬賽克山上，讀起來像貼紙。

作法與怪同一套（`enemyFB`）：畫進與地形同解析度的離屏緩衝（`pk=clamp(BW/W,0.15,1)`），
關掉平滑 1:1 放大貼回來。實測交棒的 blit：來源 155×279 → 目的 364×655（放大 2.34×，
與當下的地形倍率同源）。

- ⚠ 外框要把**煙**一起框進去（煙拖得比車身長很多）。
- ⚠ 透明度留在離屏那一層（煙每團自己的 alpha、車身的 fade），貼回來 `globalAlpha` 是 1。
- ⚠ 緩衝**只長不縮**（縮到剩一半以下才重配）：外框每幀都會變一點，火車可能好幾列。

## C. 營業時間

> 「城鎮的店鋪都在早上8點開門晚上8點關門，旅店24小時開門，酒吧開到00時。」

`hours:[開,關]` 寫在節點上（24 制，**上界不含**）：雜貨舖／武器店 `[8,20]`、餐酒館 `[8,24]`、
旅店不寫（＝全天）。打烊時：櫃台鈕不出現、地名後面加「已打烊」、**進場對白也不播**
（在關著的店裡讓店主開口是錯的；旗標不會記，下次營業時間內進來才播）。

⚠⚠ **賞金獵人公會沒有設時間** —— Ray 那一句列的是「店鋪／旅店／酒吧」，公會不在其中，
所以維持全天。要給它時間就在 `script/town.js` 加一行 `hours`，程式端不必動。

實測：21:00 雜貨舖「已打烊」＋點畫面出「櫥窗裡的燈熄了…」；21:00 餐酒館開著；
00:30 餐酒館「已打烊」；旅店與公會全天。

## D. BGM 不重疊

> 「BGM 播下一首就停上一首，不要重疊」

**根因是 ver -388 的內嵌 iframe**：飛行頁有**自己的一套 BGM**（另一個 document、另一組
HTMLAudio）。跳頁的時代舊頁一走音樂自然沒了；內嵌之後主選單／城鎮的曲子會繼續播 ——
兩首疊在一起。

- 修法：**進飛行畫面時收掉主遊戲的 BGM**（`openFlight` 裡 `SFX.stopBgm(600)`，唯一的入口 ——
  試飛／出航／打完回來三條路都經過它，鐵律 8）；回來時把底下那一頁的曲子接上
  （城鎮 → 新增的 `town.resumeBgm()`；首頁 → `bgm_home`）。
- ⚠ 環境音（航行／停船那種循環床）與音樂是**堆疊**的，那不算重疊（§6.6）。

**順帶修掉一個一直都在的 `playBgm` bug**（會造成「上一首停了、下一首沒播」的靜音）：
同曲判斷本來看 `_bgmSrc`（＝**要播哪一首**），但換歌的那 0.9~1.9 秒之間它早就是新的一首、
元素上還是舊的（`el.paused` 仍是 false）。於是換歌途中收到**同一首**的重複請求
（`ensureBgm`／`riseCue`／場景切換都會）就被當成「已經在播」直接 return，
還順手 `clearTimeout(_bgmTimer)` —— 而那個 timer 正是「等一下把新的掛上去」那一步。
現在改看 `_bgmPlaying`（真的掛上元素的那一首），並加 `_bgmSwitching`（換歌在路上就別打斷）。
blob 抓不到也改成**退回直接串流**，不再 `return`。

## E. 順手修的：城鎮背景載得到 Ray 新交的時段差分

Ray 在這一輪同時交了一批時段背景（`_Dusk/_night/_midnight`），但都是 **PNG**，而載入器
寫死 `.webp` —— 一張都載不到。候選鏈改成：
`這個時段 → 時段的大小寫變體 → _Day → 無時段`，每個名字再試 `.webp → .png`。

- ⚠ **大小寫變體是必要的**：`clock.band()` 出的是 `Dawn/Day/Dusk/night/midnight`，
  但檔名兩種都出現（`Capital_Church_Night` vs `Capital_Cityhall_night`）。
  **macOS 不分大小寫、靜態空間分** —— 在本機測不出來，上線才 404。
- `script_lint.py`：有 PNG 沒 WebP 從**錯誤**降成**提醒**（載得到，只是還沒照 §5 轉檔）。
- 實測：21:00 舊街區 → `Capital_Downtown_night.png`；00:30 船塢 → `Capital_Dock_midnight.png`；
  18:00 餐酒館 → `Capital_Bistro_Dusk.png`；廣場仍優先吃 `Capital_Square_Day.webp`。

⚠⚠ **兩件事要 Ray 決定／處理**：
1. 那批 PNG 照 §5 應該轉成 WebP（`_originals` 留原檔）。目前能跑，但體積是好幾倍。
2. **`Capital_Dock_duskpng.png` 檔名有誤**（"dusk" 後面多了 "png"）—— 黃昏的船塢會退回白天。
   另外 `Capital_Hotel_*` 已經有三張，但旅店節點的 `bg` 是 `Capital_Inn`，兩邊對不上。

---

# 交接 · `ver -392`（公會營業時間、傍晚的提醒、旅店那一幕與旅店大廳）

## A. 公會營業時間

`hours:[8,20]`（Ray：「賞金獵人公會一樣8-20」）＋打烊那一句。

## B. 傍晚的提醒

> 「拜訪過所有地點後走到下一個場景時**或**時間抵或過 18:00：（現有地點，優先所有事件）
>   諾：『天色不早了，我們先回廣場旅店吧？』」

資料在 `TOWNS.capital.evening`（`hour` / `flag` / `lines`），判定在 `modules/town.js` 的
`eveningDue()`。

- ⚠ **優先所有事件**＝它**取代**那一次抵達原本要演的進場對白，而那一段的旗標**不會記** ——
  下次再進來還是會演（與「打烊不播」同一個作法）。
- ⚠ **只演一次**（`town_evening_nudge`）：它是一句提醒，不是每走一步都要唸的。
- ⚠ **在旅店裡不演**：站在旅店裡說「我們先回旅店吧」是錯的。
- ⚠ 「走過了沒」另開一組旗標 `seen_<城>_<節點>` —— **不能借用進場對白的旗標**：
  中心區根本沒有對白、被打烊擋掉的也沒記，那些都算走到過。
- 實測：11:00 起跳、`setElapsed(7h)`＝18:00 → 走到中心區時演了那一句；再走到廣場就不再演，
  廣場自己的對白正常。

⚠⚠ **稿子寫的是「回廣場旅店」，但旅店目前掛在上街區**（Ray 於 -376 指定「餐廳旅店跟商店
放上街區」）。要嘛把旅店移到廣場、要嘛改那句台詞 —— 我沒有自作主張動任何一邊。

## C. 旅店初訪那一幕

節點 `inn` 的 `bg` 由不存在的 `Capital_Inn` 改成 **`Capital_Hotel`**（Ray 交件的檔名，
晨／黃昏／夜的差分由 `bgFor` 的候選鏈自己挑）。台詞照稿，中間三拍是**演出不是台詞**：
畫面抖＋`se_kerberos_drop`（無立繪）→ 插圖 `005_Kerberos` 由下往上平移（無立繪）→ 收圖回大廳。

新素材照 §5 的流程處理完了：
- `NPC_Hotel_Capital` → webp，原 PNG 進 `_originals/SI/`；`speakers.js` 加 `CLERK` ／ `clerk`。
  取景 `fx:0.494 top:0 bot:1535`（量出來的）。⚠ `top:0/bot:1535` 不是漏量 ——
  這張是**滿版取景**（髮髻碰上緣、靴子碰下緣，實測 row 0 與 row 1535 都有不透明像素）。
  身高 **168 是估的**（同公會櫃台），有設定再改。
- `005_Kerberos` / `005-1_Kerberos` → webp，原 PNG 進 `_originals/illustration/`。
- `se_kerberos_drop.mp3` → **AAC m4a**（§6.6：音效一律 AAC），原檔進 `_originals/audio/se/`，
  並加進 `story.js` 的 `SE_FILES`。

⚠ 稿上第二句蕾娜（「是嗎？她今天也很累了呢。」）**補了一個主角的空框**在前面 ——
稿子沒寫那一拍，但沒有它這句話沒有對象（§6.5 的慣例）。

## D. 旅店大廳（新模組 `modules/inn.js`）

| 進度 | 由什麼推出來 | 畫面 |
|---|---|---|
| `none` | 還沒住下 | 沒有大廳（分支一：「時間還早，我想去城裡逛逛呢。」） |
| `wait` | `inn_wait`（分支二演完） | 諾薇兒的門亮著（醒）＋「獨自坐坐」 |
| `slept` | `inn_renna`（蕾娜道過晚安） | 兩扇門都暗（睡）＋「回房睡覺」 |

- **分支每次進來都判一次**（走完城裡所有地點了沒），不是只判一次。
- **敲門**：單句、沒有立繪（Ray：「未開門無立繪」），可以一直敲；內容在資料上（`innKnock`）。
- **獨自坐坐**：暗去 → **時鐘 +2 小時** → 亮起 → 蕾娜回來那一幕 → `inn_renna`。
- **回房睡覺**：開既有的存檔面板（F5 那一套），不另做一個。
- 四格門位固定（Ray：「共有四人，留出適當空間。目前只有一人」）；蕾娜那一格**要等她回來才亮**。

### 踩到的
- ⚠ **大廳要跟著導覽一起開關**（`body.town-nav`）：第一版沒接，蕾娜那一幕的立繪與對話框
  上面壓著「獨自坐坐」與提示。與羅盤／目的地字格**同一個開關**（`showNav`），不要另寫一套。
- ⚠ **元件要離場景區的下緣遠一點**：那一截被**楣**壓住（楣的上緣在場景區底往上約 60px）。
  第一版 `bottom:12px` 的提示整行看不見。現在是 150/108/86。
- ⚠ 提示用**白字＋不模糊的黑描邊**：旅店白天的大廳很亮，金字讀不出來（同 `#townInfo`）。
- ⚠ `modules/inn.js` **不 import `town.js`**（會循環）：`say`／`lock`／`play` 走 `setup()` 注入。
  `say` 除了放那一句，還要把城鎮的 `chatterOn` 打開 —— 不打開的話那一句收不掉。
- ⚠ 頭像**不用新素材**：拿立繪 ＋ `speakers.js` 量好的 `fx`（`background-size:260%`）。

## E. 還沒解決的

- **旅店在上街區、台詞說「廣場旅店」**（見 B 的警告）。
- 旅店的 `Capital_Hotel_*` 只有 Day/Dusk/Night 三張（沒有 Dawn/midnight），會退回 `_Day`；
  而且都是 PNG（`bgFor` 載得到，但沒照 §5 轉檔，lint 會提醒）。
- `Capital_Dock_duskpng.png` 檔名仍有誤（"dusk" 後面多了 "png"），黃昏的船塢會退回白天。

## F. 順手做完的兩件

1. **背景全部轉成 WebP**（§5 的規約）：Ray 這一輪交的 15 張時段差分都是 PNG ——
   `resources/background` **48 MB → 7.5 MB**，原檔進 `_originals/background/`（gitignore，不入版控）。
   ⚠ **檔名一個字都沒改**（含 `Capital_Dock_duskpng` 那個 typo）—— 只換副檔名。
2. **修掉背景載入的競態**：背景是非同步載的，快速連走兩個節點時前一個的 `onload`
   可能**晚於**後一個才回來，把已經換好的背景又蓋回舊的那一張
   （實測：開城 → 立刻進大教堂，畫面停在廣場）。加了流水號 `bgSeq`，載完先確認自己還是最新的。
   實測修好後：大教堂／船塢／餐酒館在 21:00 各自吃到自己的 `_night`。

---

# 交接 · `ver -393`（傍晚那句改「回旅店」、伙伴房間改右側直排）

- 「天色不早了，我們先回**廣場**旅店吧？」→「我們先回**旅店**吧？」（Ray 定案）。
  旅店掛在上街區不在廣場，那三個字會把玩家指錯方向。
- 伙伴房間由「底部橫排」改成**右側直排**。
  ⚠ **還沒入隊的伙伴：留空間、不留空格**（Ray 指定）—— `visibility:hidden` 不是
    `display:none`：位子還在（人到了就往下長，已在的人不會跳位），但畫面上沒有空盒子。
  ⚠ 起點 `top:104px` 是**讓開右上角那兩顆鈕**（✕ 與靜音，實測靜音那顆到 y=92）——
    第一版 78 的最上面那格被靜音鈕壓住。
  ⚠ 直排四格 4×62＋3×7 ＝ 269，104 排到 373，停在楣（約 395）之上。
  ⚠ 橫向擺右邊之後與置中的行動鈕、提示不會互相壓到（直排 x 313~365，鈕與提示在 x 280 以內）。
- 實測：一人時只有諾薇兒那一格看得到；蕾娜回來之後兩格由上往下排（104 / 173），
  下面兩格佔位但看不見。

---

# 交接 · `ver -394`（「選單」金屬鈕、靜音移左上、旅店的鈕擺到家具上）

## A. 「選單」（原右上角的 ✕）

- 文字 `✕` → **「選單」**，樣式由圓形的 `.corner-btn` 換成 `.kerb-btn`：
  深槍鐵漸層底 ＋ 古銅邊 ＋ 上緣高光／下緣陰影（沖壓件的斷面）＋ 凹刻字。
  與門的鉚釘、箭同一個材質語言。
- 位置錨在**楣的下緣**：`top: var(--kerb-clip-top) + var(--kerb-top-h) + 9px`，`right:12px`。
  ⚠ 那兩個變數是 `layoutKerberos` **解出來**寫在 `#storyStage` 上的（鐵律 7：問算的那一支），
    CSS 猜不到槍棺多高。沒量到之前用與 `#kerb` 同一條退路，免得第一幀跳一下。
- ⚠ **順手修掉一個一直都在的漏收**：按下去只呼叫 `story.close()`，城鎮完全沒收 ——
  `town.isOpen()` 還是 true、`body.town-nav` 還掛著（羅盤的箭會留在下一次開啟的舞台上發光）。
  加了 `story.setTownCloser(town.close)`（注入，劇情層不 import 城鎮）。
  實測按下去：`storyStage` 收乾淨、`body` 只剩 testmode、`town.isOpen()` false、首頁回來。
- ⚠ `#storyExit` 不再是 `.corner-btn` —— 但 `castStage` 的頂線本來就是**讀 config 常數**
  （`btnTop:10 / btnH:44`），不量那顆鈕，所以立繪取景不受影響（那正是 ver -354 改成
  讀常數的理由）。

## B. 靜音移到整個畫面左上

`right:12px` → `left:12px`。⚠ 縱向留在 `notch + 56`：正上方是管理人模式的「清盤」鈕
（`#testClearBtn`，top:10 高 44 → 下緣 54），兩顆都是 testmode 限定，擺同一格會疊在一起。

## C. 旅店的兩顆鈕擺到背景的家具上

「獨自坐坐」→ **茶桌**、「回房睡覺」→ **櫃台桌面上方**（Ray 指定）。
座標寫在節點的 `innSpots`（**背景圖上的比例**），換算走**同一支** `town.bgPoint()` ——
櫃台鈕原本把那段 cover 數學寫在 `placeCounter` 裡，這一版抽出來成為唯一的計算點（鐵律 7）。

⚠⚠ **這張背景是 3:2 橫幅、框是直幅，cover 會把左右各裁掉約 22%——只有 x∈[0.225,0.775]
看得到**。茶桌的圓心在 x≈0.22，正好落在裁切線上，所以錨的是桌面**看得見的那半邊**
（0.315），不是圓心。改座標時要記得這件事，不要照著原圖的目測填。

實測（375×455 的場景框）：獨自坐坐 (60,335)、回房睡覺 (181,204)、選單 (334,485)、靜音 (30,74)。

---

# 交接 · `ver -395`（蕾娜插圖平移、取名那一段、蕾娜的回店時刻）

## A. 蕾娜插圖由下往上平移

`script/mainScript.js` 那一拍加 `cgPan:'up'`（`004_Renna_intro` 是全站唯一一張蕾娜插圖）。
平移 2.6 秒、那一拍只停 1 秒 → **平移延續到下一拍**（沒寫 `cgPan` 的句子不碰平移），
她報上名字時鏡頭還在走。

⚠ 我把這一句理解成「那張插圖的顯示方式」——不是「在旅店那一幕插一張蕾娜的圖」。
  若原意是後者，說一聲，那是把 `cg:'004_Renna_intro'` 加進 `innRenna` 的第一拍而已。

## B. 不要寫「敲敲伙伴的門」

旅店的提示改成只留「坐下來消磨時間吧。」——敲門是玩家自己會去試的事，寫出來像在派任務。

## C. 蕾娜的回店時刻（獨自坐坐的機制改寫）

> 「獨自坐坐會自動將時間推至 8 點，如果玩家選擇走出旅店，在 8~9 點時進去仍碰得到蕾娜，
>   有碰到的話好感 +1，沒碰到的話蕾娜就已經在房內了。」

**8 點＝晚上八點（20:00）**（城裡的店也是這個時間打烊，她辦完事回來剛好）。

| | 行為 |
|---|---|
| 獨自坐坐 | 時鐘**推到 20:00**（已經過了就不動 —— 時間只能往前）→ 蕾娜那一幕 |
| 20:00~21:00 走進旅店 | **自動撞見她** → 同一幕 |
| ≥21:00 才進來 | 她已經回房（記 `inn_missed`），沒有那一幕、沒有好感 |
| 碰到她 | 好感 **+1** |

- ⚠ 「錯過」一定要**記旗標**，不能每次看時鐘算 —— 隔天早上時鐘又小於 21，
  不記的話她會「重新出現在大廳等你」。
- ⚠ 好感只在**一支**收尾函式裡加（`finishRenna`）：兩條路都走它（鐵律 8）。
- 實測：17:00 坐下 → 時鐘 20:00 ＋ 那一幕；20:30 走進去 → 直接撞見，好感 10→11；
  21:30 走進去 → `inn_missed`、好感不變、兩扇門都是睡著的。

## D. 取名那一段

⚠⚠ **我把這一段接在「行政廳」的結尾，不是旅店。** 判斷依據：末句是
「**晚上六點回旅店集合。明早出發。**」—— 那正好對上 `TOWNS.capital.evening` 在 **18:00**
的那句提醒（「天色不早了，我們先回旅店吧？」）。在旅店（20:00）演的話，
「六點回旅店集合」已經過去了，講不通。原本的收尾「啊——！」被這一段取代。
**如果原意是別的地方，說一聲，整段搬過去就好（資料而已）。**

- 「（那是侯爵的千金欸！你莊重一點！）」的括號是**台詞本身**（她壓低聲音講的），照留。
- 手刀那一拍：`shake` ＋ `se_punch` ＋ 無台詞的立繪拍（停一秒自己走）。
- `HUND` 照原文不翻。

### 名字與暱稱（新的閘門拍 `{ nameInput:true }`）
- 預設 **凱勞諾斯／凱**。⚠ 西文 `Keraunos`／`Ky` 是**檔名與程式用的 id**，與玩家輸入
  **脫鉤** —— 不要拿玩家輸入的字去拼路徑（Ray 明講）。
- 台詞裡 `{P}`＝名字、`{N}`＝暱稱，**顯示的那一刻才代換**。
- 面板蓋在最上層並吃掉點擊（這一拍是閘門，不能點畫面跳過去）；留空按確定＝沿用預設。
- ⚠ `PLAYER_DEFAULT` 由「托爾斯」改成「凱勞諾斯」，`flight/index.html` 有**第二份**
  （非 module 頁面 import 不到）—— 兩邊都改了，註解互指。
- ⚠ 暱稱是「一輪內」的資料：`newRun()` 清、`snapshot/restore` 帶（§6.9 那張清單的兩面）。
- ⚠ `script_lint.py` 要認得這個閘門拍（沒有 speaker 也正常），已加。

---

# 交接 · `ver -396 / -397`（打靶改計時挑戰、選項、暱稱名牌、旅店睡覺、立繪調色、選單）

## A. 打靶場改成**計時挑戰**（ver -396）

新敵人卡 `dart_target`「固定立靶」（Ray 交件 `Dart_timeattack`，已轉 WebP）：
HP 300、**不攻擊**、點錯 **+3 秒** ＋ `se_dart_fail`（已轉 AAC）、**超過 50 秒**走失敗分支。

⚠⚠ 「不攻擊」是由**戰鬥卡的 `timeAttack`** 關掉整條路徑，**不是**把 `attack` 調成 0 ——
調成 0 的話大絕紅點、蓄力槽、延時懲罰、「太慢了」那一行照樣會演。守門有三處，各守一次：
- `combat.enemyAttack` → 所有扣血路徑的唯一入口（大絕／延時／按錯／格擋）
- `defense.scheduleUlt` → 連排程都不開（不然紅點與蓄力槽還是會演）
- `combat.startIntervalTimer` → **完全不起**（Ray：「不要有延時懲罰」）

⚠ 「超時」借用既有的 `onLose` 分歧路：對腳本而言「太慢」與「打輸」是同一件事 ——
接另一支台詞。判定在 `combat.win()`（`state.timeOver`），由 `inspector` 交棒時帶出去。
`script_lint.py` 跟著放行「有 `timeAttack.parSec` 就可以寫 `onLose`」。

⚠ 最佳紀錄與 ★ NEW RECORD ★ **本來就有**（`record:'range'`，ver -377），不必新寫。
實測：46.4 秒（含一次 +3 秒）→ 顯示「戰鬥用時 46.4秒／最佳紀錄 46.4秒／★ NEW RECORD ★」。

## B. 選項（yes / no）

新的閘門拍 `{ choice:[{text,goto},…] }` ＋ 面板。槍店的試槍邀請用它分兩支：
答應 → 打靶；婉拒 → 「是嗎？真可惜。」（Ray 交稿）→ 接回合流點。
格式寫進 `script/SCRIPT_FORMAT.md` §8.56。⚠ 每一支分歧都要接回合流點 —— 後面通常還有伏筆。

## C. 主角的名牌用**暱稱**（ver -396）

`speakers.js` 的 `PLAYER.name` 由 `{P}` 改 `{N}`，story 顯示時取 `getPlayerNick()`。

## D. 旅店（ver -396）

- 拿掉「大家都睡了。回房休息吧。」，改成**雪鐵龍箭指著鈕 ＋ 一句說明**
  （「推進時間至隔日早上七點，並恢復體力。」）。
- 「回房睡覺」推進到**下一個 07:00**（21:00 睡 → 隔天 07:00；01:00 睡 → 同日 07:00），再開存檔面板。
- ⚠⚠ **「恢復體力」目前沒有實體**：這個專案還沒有體力／疲勞系統（戰鬥 HP 是每場重置的）。
  那句話先照 Ray 的字寫在鈕上，體力系統做出來時**接在 `inn.sleepHere()` 這一支**。

## E. 立繪依背景調整亮度（ver -396，全場景）

⚠⚠ 原本是換背景之後 `setTimeout(…,420)` 去量 —— 但 `swapImg` 是**先預載、載好才換 src**，
慢一點的圖 420ms 還沒換上去，於是量到的是**上一張**，而且用上一張的 src 當快取鍵存起來
→ 那張新背景**從此再也不會被量到**。改成綁 `#storyBg` 的 `load` 事件（換幾次量幾次），
主線／城鎮／旅店一體適用。實測：廣場白天 `brightness(1.01)`、大教堂夜 `0.911`、旅店白天 `0.936`。

## F. 選單（ver -397）

「選單」鈕由「直接離開」改成開一個真的選單：**回到主選單／分軌音量（音樂・音效・語音）／
自動播放間隔**。實作在新的 `modules/settings.js`。

- ⚠ **分軌要在 limiter 之前**：limiter 是所有聲音的共同入口，在它之後只有一顆總音量，切不開軌。
  作法是每一軌各掛一顆 gain 接進 limiter，發聲端改連 `busIn(c, 軌)`（合成音也是）。
  BGM 不走 Web Audio（HTMLAudio），那一軌在 `bgmTargetVol()` 乘進去。
- ⚠ 軌別走**資料夾**判（`/audio/vo/` → 語音），不靠呼叫端記得傳（§6.6 的命名規約）。
- ⚠ 這一層與 config 那份「每支音檔的實測增益」是**兩回事**：那份把三層拉齊（§6.6 的 LUFS 表），
  這一層讓玩家再調。**兩者相乘**。
- ⚠ 這些是**玩家的偏好**，`progress.newRun()` **不清**（同靜音、語言，§6.9）。
- ⚠ 自動播放間隔由選單決定（400~2000ms，預設 1100）；**加速模式（按住下拉）維持 120ms** ——
  那是「一路衝過去」，不是節奏偏好。

---

# 交接 · `ver -398`（回到主選單、確認、射擊挑戰、震動、HUND／監察官、璐娜立繪）

## A. 「回到主選單」變成戰鬥盤面 —— 修好了

⚠⚠ 根因：那顆鈕只呼叫 `story.close()`，而劇情層底下是 `#app`，`#home` 早就被別的路徑
關掉了（`combat.startGame`／`openFlight`／飛行頁交棒都會 `home.classList.remove('on')`）——
掀開來當然是上一場戰鬥的盤面。

修法：走**唯一那支**回主選單（`combat.goHome`，注入進 story）—— 它會把 banner／過渡禎／
獎勵層收乾淨、把 `#home` 開回來、接回主選單 BGM，而且自帶黑幕；劇情層在**黑幕全蓋那一刻**
才收，所以看不到中間那一格。順帶把飛行 iframe 也收掉、`flightBack` 歸零。
實測：故意把 `#home` 關掉再按 → 首頁回來、舞台收乾淨、`town.isOpen()` false。

## B. 回到主選單前的確認

「尚未儲存的進度將會遺失。確定要回到主選單嗎？」＋ 返回／確定。
⚠ 面板用**換頁**不疊第二層（疊兩層很難點得準）；⚠ 「返回」不能 `close()` 再 `open()` ——
`open()` 開頭那道「已經開著就不重開」的守門會擋掉（移除是 200ms 之後才發生的）。

## C. 槍店的「射擊挑戰」

櫃台的商店選單多一顆（`config.shop.shops.gunstore.challenge`）。按下去收商店 → 交給劇情
播放器演 `challengeLines`（含 `{battle:'range_trainee'}`）→ 打完接回來 → 回到櫃台。
走的是**同一場**（同一份最佳紀錄）、同一條 `onLose` 分歧（超過 50 秒）。
⚠⚠ 那三句台詞是**我寫的**（Ray 只說要加那個選項），要換掉直接改 `script/town.js`。

## D. 震動（新模組 `modules/haptics.js`）

| 何時 | 長度 |
|---|---|
| 受到敵人主動攻擊（大絕／延時／按錯／格擋） | 45ms |
| 反擊：狙擊／散彈 | 18ms |
| 反擊：**機槍** | **一條連續震動，長度＝整串的持續時間**（`(發數−1)×90 + 60`） |
| 破防窗口**每一發** | 18ms |
| 槍棺撞頂 | 90ms（全場最重） |
| 劇情畫面震動（`shake`）／腳本備註（`vibrate:true`） | 60ms |

- ⚠ 機槍**不是每發各震一下**（Ray 指定「連續震動同持續時間」）：手機會把密集的短震
  合併成一串黏在一起的抖動，讀起來比一條長震還糊。
- ⚠⚠ **iOS Safari 沒有 `navigator.vibrate`** —— 整支在 iPhone 上是 no-op，不是壞了。
  網頁拿不到 Taptic，只有原生殼才有。**測的時候請用 Android。**
- ⚠ 開關在選單裡（預設開）。打開時會震一下當試用 —— 沒有回饋的話玩家不知道這台震不震得動。
- ⚠ 受擊那一下守在 `combat.enemyAttack`（所有扣血路徑的唯一入口），一處就涵蓋全部。

## E. 取名之前叫 `HUND`

`{P}` 與 `{N}` 在取名之前都代換成 `HUND`。⚠ 「還沒取名」與「取了名」是**兩個顯示**，
不是「預設值」—— 預設值（凱勞諾斯／凱）是**輸入框裡的預填**，玩家按確定才成立
（`isNamed()` 判鑰匙存不存在，所以玩家真的取名叫「凱勞諾斯」也算取過名）。
⚠ `flight/index.html` 那一份也改了（非 module 頁面，兩邊註解互指）。

## F. 蕾娜正名之前叫「監察官」

⚠⚠ **正名前後是兩個 speaker id**（Ray：「可以視作不同角色」）：`OFFICER` 用到她說出
**「我以海森伯格之名請求妳，喔。」**那一句為止（含），下一句起改 `RENNA`。
帝都廣場那一幕原本整幕都寫 RENNA，已改掉前半 13 處。
⚠ 那一幕的 `sides` 覆寫**兩個 id 都要寫**（`{ RENNA:'R', OFFICER:'R' }`）——
只寫一個的話前半段會跑回她的固定站位（左），與諾薇兒疊在一起。
⚠ `prologue_audience` / `prologue_fall` 兩個**孤兒場景**仍用 OFFICER，沒動它們。

## G. 璐娜生氣立繪更新

`Luna_SI_seat_angry` 換新（同名覆蓋）→ 轉 WebP、原 PNG 進 `_originals/SI/`，
並照 §5 **重量取景值**：`bot 1526→1518`、`fx 0.539→0.531`。
⚠ `faceAdj`／`standCm` 是那一組坐姿的補償（推導在 speakers.js 上方），**不是量出來的**，換圖不動。

---

# 交接 · `ver -399`（兩個回歸：音效全沒了、打完架被關在店裡）

兩個都是我自己在 -397／-394 弄出來的，記在這裡當教訓。

## A. 音效全部不見（-397 的分軌改動）

`playSrc` → `playWhenRunning` → **`playBuffer`**。我把 `src` 一路傳到 `playWhenRunning`，
卻**漏了最後一段**：`playBuffer(c, buf, vol, voice, handle)` 沒有 `src` 這個參數，
而函式裡寫了 `busIn(c, layerOf(src, voice))` → `src` 是未宣告的識別字 → ReferenceError。

⚠⚠ **它被 `playBuffer` 外層那個空的 `catch(e){}` 靜靜吞掉了** —— console 一行都沒有，
音效就是全部不響。那個 catch 是刻意的（音效壞掉不該讓遊戲停），但它也會把這種低級錯誤藏起來。
**動 `audio.js` 之後一定要真的聽一次，不要只看 console。**
實測修好：攔 `AudioBufferSourceNode.start` 有被呼叫（修之前是 0 次）。

## B. 打完靶／賞金獵人之後，商店與公會的返回鍵不見，人被關在裡面（-394 的漏收）

⚠⚠ 根因：-394 為了「按選單離開時城鎮也要收」，把 `townCloser()` 放進了 `story.close()`。
但**交棒給戰鬥走的也是 `close({keepBgm:true})`**（`playKerberos` 的 `onDone`）——
於是推槍棺那一刻 `townId` 就被清成 null。打完回來 `resumeFrom` 雖然把舞台開回來，
但 `showNav()`／`refreshArrows()`／`placeCounter()` 全部查不到節點 →
**羅盤的箭與櫃台鈕都不出現**，玩家沒有任何出口。

修法：收城鎮是「**離開這一切**」才做的事 → 從 `close()` 移到 `goHomeNow()`（選單那條路）。
實測：櫃台「射擊挑戰」→ 打完 → `town.isOpen()` 仍為 true、`body.town-nav` 回來、
`.kerb-arrow.s` 有 `avail`（返回鍵回來了）。

⚠ 教訓（與鐵律 8 同一個形狀的反面）：**同一支函式被兩種語意的路徑共用時，
不要在它裡面加「只有其中一種語意才該做」的事**。`close()` 同時是「離開」與「交棒」，
所以收城鎮不能放在它裡面。

---

# 交接 · `ver -400`（雜貨舖的背景圖不見了）

⚠⚠ 根因：Ray 把雜貨舖換成**有時段差分**的檔案（`Capital_Grocerie_day` / `_dusk`），
基底檔 `Capital_Grocerie` 就沒了 —— 但節點上還寫著 `noTime:true`，
而 `noTime` 的候選鏈**只找沒有時段尾巴的那個名字**，`_Day` 那一級根本不會試。
→ 一張都載不到，整片沒有背景。

修了三件：
1. **節點拿掉 `noTime`**（它現在有時段差分了）。
2. **`_Day` 那一級也要試大小寫變體**：交件的檔名 `_Day`/`_day` 兩種都出現過，
   而那一級是「這個時段沒有圖」時的退路 —— 它自己再漏一次，夜裡就整片沒有背景。
3. **`script_lint.py` 補上守門**：`noTime` 的節點要驗**基底檔本身**，
   不能拿 `_Day` 當通過條件。⚠ 這次 lint 之所以沒抓到，正是因為它用 `_Day` 放行了
   （而且 macOS 檔案系統不分大小寫，`_Day` 對到了 `_day`）。
4. `Capital_Grocerie_dusk.png` 照 §5 轉 WebP，原檔進 `_originals/background/`。

實測：13:00 → `_Day`、18:00 → `_Dusk`、21:00 → 退回 `_Day`（雜貨舖還沒有夜景）。

⚠⚠ **這一類問題在本機測不出來**：macOS 檔案系統不分大小寫，`_Day` 的請求會對到 `_day`；
上了靜態空間（Linux）才會 404 再走變體。目前靠候選鏈接住，但**檔名不一致這件事本身沒解決** ——
要根治就把所有時段尾巴統一成 `clock.band()` 的輸出（`Dawn/Day/Dusk/night/midnight`）。
那是改 Ray 的檔名，我沒有自作主張動（§5：大小寫照他給的原樣）。

---

# 交接 · `ver -401`（旅店的一次性說明、獨自坐坐改規則、初見劇情改成「同質」）

## A. 城鎮對話在哪裡改

**`script/town.js`** —— 純資料（節點、進場對白、店主台詞、路人單句、旅店那幾段）。
播放與互動的程式在 `modules/town.js`（＋旅店大廳 `modules/inn.js`），**不要把台詞寫進那兩支**。
主線的對白在 `script/mainScript.js`；角色與立繪在 `script/speakers.js`。

## B. 獨自坐坐：初入對白演完就能用、一次兩小時

- 出場時機由 `stage()` 改成 **`introDone()`**（旅店的初見對白演過了沒）——
  ⚠ 連 `sitAlone()` 的守門也要一起改成看 `wantSit`：舊守門寫的是「只有在等蕾娜時才能坐」，
    不改的話按下去什麼都不會發生（實測踩到）。
- 一次 **+2 小時**（Ray：「消磨時間：2小時」）。
  ⚠⚠ **還在等蕾娜時不准跨過 20:00**：從 19:00 一口氣坐到 21:00 就整個錯過她了。
    所以那一段最多只坐到 20:00，坐完再走同一組窗口判斷（碰到／錯過）。
- 坐完 **換時段背景**（Ray 指定）：`host.refreshBg()` → 走城鎮那一支候選鏈（鐵律 7）。
  實測 16:00 →（坐）18:00 →（坐）20:00，背景換成 `Capital_Hotel_night`。
- ⚠ 大廳可能在**還沒住下**時就開了 → 四扇門要全部是空的（不然諾薇兒的門會在她還站在旁邊時亮著）。

## C. 一次性說明（遮罩＋箭頭）

三個：**獨自坐坐 / 敲伙伴的門 / 回房睡覺**。遮罩壓暗全場、被說明的那顆抬到遮罩之上
（`.spot`，鑲一圈光）、箭頭指著它、點一下收掉並記旗標（`inn_tip_*`）。
- ⚠ **一次只排一個**：三個提示會在不同時機到齊，排隊比疊在一起清楚。
- ⚠⚠ **量不到就先不要演**：被說明的那顆若還沒被 `relayout()` 擺好（背景還沒載完 →
  `bgPoint` 回 null → 鈕是隱藏的），rect 是 0×0，說明文字會被算到畫面外
  （實測 `top:-74px`，整行看不見）。所以 `relayout()` 收尾要再排一次。
- ⚠ 常駐在鈕下方那一行說明**撤掉了**（Ray：「說明都是一次性的」），
  但**雪鐵龍箭留著** —— 它不是說明，是「現在該按這個」的指示。

## D. 初見劇情改成「同質」共用

> 「城內其他地方的初見劇情保留，下次回來或**進入他城同質店**時觸發初見劇情。」

節點加 `kind`（`tavern` / `grocery` / `gunstore` / `guild` / `church` / …），
初見旗標由 `town_<城>_<節點>` 改成 **`town_kind_<kind>`**。於是：
- 在帝都沒看到（打烊／被傍晚的提醒插隊／中途離開）→ 下次回來還看得到
- 到了別的城的同一種店 → 那時才第一次看到，照樣演
- 已經看過了 → 別的城的同一種店不再重播

⚠⚠ **這是我對那句話的解讀**：把「初見劇情」當成「第一次走進**這種地方**」的戲，
而不是「第一次走進帝都那一間」。如果原意是別的（例如每一城各有自己的初見戲），
說一聲 —— 拿掉節點上的 `kind` 就退回舊行為，程式端不必動。
⚠ 換旗標名等於**舊存檔的那幾段會再演一次**；上線前若要保留舊存檔，得在 `progress` 做一次搬遷。

---

# 交接 · `ver -402`（雪鐵龍箭也一次性、`kind` 縮到三家店）

## A. 雪鐵龍箭也是一次性說明

常駐在「回房睡覺」上方的那支箭（`.inn-point`）撤掉了 —— 箭與文字現在**都只在
`.inn-guide` 那個一次性遮罩裡出現一次**（Ray：「雪鐵龍箭也都是一次性說明」）。
⚠ 遮罩裡那支箭的定位是 `translate(-50%,-100%)`（掛在目標**上緣**），與舊的常駐箭
（`-50%,-50%`）不同 —— 所以浮動動畫要用**自己的** keyframes（`igPoint`），
沿用舊的 `innPoint` 會被它的 transform 蓋掉、箭直接跳位。舊 keyframes 一併刪了。

## B. `kind` 只留三家店

Ray 指定：**賞金獵人公會／槍店／雜貨舖**才是「這種店」的初見戲（沒看到的話，
到別的城的同一種店還會演）。**餐酒館、船塢、行政廳**（以及大教堂、各街區、旅店）
是**這一座城自己的戲** —— 跳過就跳過了，不會在別的城重現，所以不給 `kind`。

⚠ 連帶：旅店沒有 `kind` 了，`inn.js` 不能再自己拼旗標名（會拼錯城）——
改由 `town.flagOf()` 算好、透過 `arrive(n, {introFlag})` 傳進去（鐵律 7：只有一支在決定）。

## C. 順帶回答：`auto` 要怎麼取消

`renderLine` 判的是 `if(line.auto>0)`，所以 **`auto:0` 與「整個不寫」是同一件事** ——
都變成「等玩家點一下」。**建議整個不寫**（少一個會被誤讀成「有設定」的欄位）。
⚠ 空台詞的那一拍本來就不出對話框，但**點畫面照樣推得動**（推進掛在 `#storyTouch`，
不是掛在對話框上），所以無台詞＋不寫 `auto` ＝「停在這一格，等玩家點」。

---

# 交接 · `ver -403`（主頁全白／圖讀不進去 —— 稿子的語法錯會炸掉整個開機）

## A. 症狀與成因

Ray 回報「主頁壞了，圖讀不進去」。實際狀況是**所有** `<img>` 的 `src` 都是 `null`，
`resource` 清單裡連一筆 404 都沒有 —— 因為**根本沒人去設 src**。

主控台只有一行 `Uncaught SyntaxError: Unexpected token ':'`。
`index.html` 是**單一 module 圖**：`main.js → config.js / modules/* / script/*`。
其中**任何一支**語法錯，整張圖就不會 evaluate，`applyConfigToDOM()` 沒跑過，
於是每一張圖都是空的、開機遮罩也不出現 —— 畫面看起來「圖讀不進去」，
但那只是唯一看得見的症狀，實際上**整個 JS 都沒跑**。

⚠ **不要從「圖」開始查**。判斷順序是：
①`document.images` 的 `src` 是不是 `null`（是 → 不是網路問題，是 JS 沒跑）
②主控台第一行錯誤 ③逐檔驗語法。

錯的是 Ray 手改的兩支稿子：

| 檔 | 寫成 | 應該是 |
|---|---|---|
| `script/town.js` cityhall | `nou('run','啊——！'),se:'se_steps'` | `nou('run','啊——！', { se:'se_steps' })` |
| `script/mainScript.js` capital_square | 貼進了 `ren(...)`／`nou(...)` | 那兩個簡寫**只有 `town.js` 有**；mainScript 要寫 `{ speaker:'RENNA', text:…, portrait:{…} }` |

⚠ `ren()` / `nou()` 是 `script/town.js` 檔頭自己定義的小工具，**mainScript.js 沒有**。
  兩份稿子的寫法不同 —— 從一支複製到另一支要換格式（或哪天把那組工具抽成共用的）。

## B. 取名那一段搬家了（Ray 這次的實際改動）

原本在 `TOWNS.capital.cityhall` 的取名戲整段拿掉，接回 `MAIN_SCRIPT.capital_square`
（那一幕本來就有前半段：「好、好的，蕾娜小姐」→ 手刀 → 「沒關係啦」）。
現在 `capital_square` 是完整的一段：`…沒關係啦 → 不過……我該怎麼稱呼你好呢 →
{nameInput:true} → 那麼，晚上六點回旅店集合`。
cityhall 收在 `nou('run','啊——！', {se:'se_steps'})`。
⚠ 全專案的 `nameInput` 閘門**只剩這一個**（grep 過）。

## C. `script_lint.py` 加了「逐檔驗語法」

以前它把四支檔串成一個暫存檔再交給 jsc，語法錯雖然抓得到，但行號是**暫存檔**的行號，
對不回原檔。現在 `check_syntax()` 先逐檔 `jsc --module-file=` 跑一次，直接印
「哪一支、第幾行」，有錯就停在那裡不往下驗。

**手改完 `mainScript.js` / `town.js` / `speakers.js` / `config.js` 一定要跑：**
```
python3 tools/script_lint.py
```

## D. 順帶

- `se_dart_fail.m4a` 補進 `modules/story.js` 的 `SE_FILES`（ver -397 漏了 → 預載不到）。
- `Capital_Uptown_Dusk/night/midnight` 轉 webp，原 PNG 進 `_originals/background/`
  （8.3 MB → 0.93 MB）。⚠ `-402` 那次 `git add -A` 把兩張 PNG 直接進版控了 ——
  **新圖進來一律先轉檔再 commit**（見 CLAUDE.md §5 的轉檔三步）。

---

# 交接 · `ver -404`（店舖畫面：右店主、左選單）

Ray：「把各商店的櫃台按鈕改成店主立繪，並讓店主常駐對話框…」→ 追加修正兩次：
「**不用點擊，直接右店主左選單**」→「**店主不用放對話框…騰空間出來給選單**」→
「**點擊選單可展開全寬**」。最後的樣子是：

**走進商店／武器店／公會 ＝ 右邊店主立繪 ＋ 左邊選單，不必按任何東西。**

## A. 拿掉的東西

`#townCounter`（ver -387 那顆櫃台鈕）、`placeCounter()`、節點上的 `counter:{x,y,label}`、
`style.css` 的 `.town-btn` 與 `townCounterPulse` —— 全部撤掉。留著就是第二個入口，
其中一個一定會被忘記維護（鐵律 8）。

## B. 立繪

`story.castSolo(id)`：把**一個**角色放上台、保持原色、另一側清空，**不接管推進**。
走的是與對白**同一支** `ensureOn`、同一把尺（§6.5：同一張立繪＝同一個結果）。
⚠ 「右店主」不是寫死的 —— NPC 在 `speakers.js` 的 `side` 本來就是 `'R'`。

## C. 選單

既有的 `showShop`／`showBounty` 多一個 `opts`：
- `dock:'left'` → 靠左停、**不壓黑、不吃掉其他地方的點擊**（右邊站著店主，
  底下的面盤還要能長按箭頭走出店門）。
- `info` → 標題下印地名＋時刻。
- `onClose` → **任何**收掉單子的路徑都通知城鎮（不通知的話玩家按了關閉就再也開不回來）。
- 兩支都**回傳 `close`**，城鎮換節點時收得掉。

**點標題 → 展開全寬**：只是把 `dock-left` 這個 class 加減掉，展開後的版面就是這一份
CSS 原本那張置中視窗（鐵律 8，不另做一套）。標題右邊的 `⤢`／`⤡` 是提示。
⚠ 點的是**標題列**不是整張單子 —— 每一列與那排鈕都有自己的動作。

## D. 為什麼沒有常駐對話框

實測 390×844：一條全寬的對話框佔掉 80px，單子只剩 **225px**，而一張完整的商店單子
（標題＋金額＋頁籤＋清單＋說明＋四顆鈕）要 **306px**，怎麼壓都塞不下。
中間試過「把對話框縮到右下角貼著店主」，Ray 直接說不要那一句 —— 空間給選單。

## E. 地名／時刻要讓開

立繪的臉就在上緣那一行的高度（§6.5 的 -385 同一個理由）。但這是**持續狀態不是對白**，
所以不借用 `story-talking`，另給 `body.town-shop`；開關只有一支
（`modules/town.js` 的 `setShopOn`，鐵律 8）—— 按「交談」進入真正的對白時要先關掉，
那時地名交回 `story-talking` 管。讓開不等於不給：那兩個資訊改印在單子的標題下。

## F. 單子的下界 ＝ 楣的上緣

`--kerb-clip-top` 是 `layoutKerberos` 解出來的**楣的實際上緣**，ver -404 起
**順手發佈到 `:root`** —— 單子是 body 底下的兄弟元素，讀不到 `#storyStage` 上的那一份。
⚠ 不要在 CSS 裡用「56vh − 楣高」再算一次（鐵律 7；那正是 -343／-364 那兩次的形狀）。

## G. NPC 的引號

Ray：「npc講話不要有引號」。`script/town.js` 裡**頭尾就是 `「…」`** 的台詞字串
全部去掉外層那一對（32 條：店主對白 ＋ 四條路人單句）。
⚠ 句子**裡面**的引號（`『永夜』`、`「禍魘」`）是原文的一部分，照留 —— 用的是
「只脫最外層」的規則，不是全刪。
⚠ 商店那顆鈕的字由「與店主交談」縮成「交　談」：窄單子上四顆鈕要排進一列。

## H. 順帶

進場對白演完才擺店舖畫面（`afterArrive`），打烊則整個不出現。
換節點的「六件要收乾淨的事」多了第六件：`shopClose()`（§6.5 那張新路徑檢查表）。

---

# 交接 · `ver -405`（存貨與購買數量／路人／選單鈕撤出／回程方向／旅店直接睡）

## A. 店鋪存貨 ＋ 購買數量

新檔 **`script/shopstock.js`**：只記「現在還剩幾個」，初始貨量在
`config.shop.stock[店] = [{id, n}]`（寫成純字串＝不限量，永遠買得到也不記帳）。

- 槍店三把各 `n:1`（Ray：成品槍不是量產品）；雜貨舖 8／5／3。
- **賣給店家＝入庫**（Ray 指定），之後買得回來；貨單上原本沒有的東西也會擺出來賣。
- 存**絕對值**不存差值 —— 差值在 config 改動之後會算出負數或憑空長貨。
- 買的時候有 `− n ＋` 數量列，`／n` 是**這一次最多能買幾個**
  ＝ min(店裡剩幾個, 錢夠買幾個, 武器的 1)。武器夾 1 是因為 `hasWeapon` 是布林。
- ⚠ **先扣貨再扣錢**，錢不夠就把貨放回去 —— 不會發生「錢扣了東西沒拿到」。
- ⚠ 「售完」與「錢不夠」是兩件事：沒貨才寫售完，買不起是鈕暗掉、字仍是「買下 N」。
- ⚠⚠ **§6.9 的兩面都加了**：`progress.newRun()` 清 `shopStock.KEY`，
  `runSnapshot/runRestore` 帶 `shop`（`restore` 連「沒有這一段」也要清 —— 舊存檔）。
- ⚠ 清單那一列改成**格線**：品名一欄、價錢一欄，標籤（持有／庫存）自己佔第二列。
  flex 的時候長品名一換行，第二行會鑽到價錢底下（實測「持有」的框被拆成兩半夾著 3000 G）。

## B. 路人有名字了

`SPEAKERS.VOICE.name` 由 `''` 改成 `'路人'`，城鎮的 `chatter()` 問它（不寫死）。
ver -404 把 NPC 台詞的外層引號拿掉之後，名字欄是唯一還能標明「這是一句話不是旁白」的地方。

## C. 槍棺推上時「選單」鈕撤出

`#storyStage:has(#kerb.rise) #storyExit` → 往右滑出 ＋ 淡出 ＋ `pointer-events:none`。
⚠ 用 `:has()` 讀門自己的狀態，**不另外掛一個 class**（鐵律 8：「門推上來了沒」只有
`#kerb.rise` 一個真相）。曲線與門同一條（`.7,0,.3,1`），不然兩個東西看起來各走各的。

## D. 回程掛在來時方向的反向

`go(to, dir)` 記 `pendingDir` → `enter()` 換算成 `backDir`（＝反向）→ `exitsOf()` 把
`exits.back` 掛到那一支箭。
- 實測：中心區 →**右**→ 大教堂，回程在**左**；中心區 →**左**→ 行政廳，回程在**右**。
- ⚠ **先擺出航再擺 back**：「下」那一格是出航的既定用途（ver -387 Ray 指定）。
- ⚠ 沒有記錄時（開城第一格、戰鬥交棒回來、讀檔）退回舊行為「掛在下」。

## E. 旅店：初入就能直接去睡

`wantSleep` 由 `stage()==='slept'` 改成 `introDone()`。六點以前按下去 →
諾薇兒 front「再逛一下嘛，還沒六點呢。」（`innEarly`，資料在 `script/town.js`）。
- ⚠ 「太早」**不是靠藏起鈕**擋：鈕不見了玩家只會以為還不能睡。
- ⚠ 那個「六點」＝ `TOWNS[].evening.hour`，由 `town.afterArrive` 傳進 `inn.arrive`
  （鐵律 7 —— 改集合時間只改那一處）。
- ⚠ 蕾娜還沒回來就先睡 → 記 `inn_missed`（她 20:00 才進門）。**這是我的判斷**，
  Ray 沒明說 —— 不記的話 `stage()` 卡在 `wait`／`none`，與「已經過了一夜」對不上。
- 一次性說明的順序改成 坐 → 睡 → 敲門（兩顆鈕現在同時出現，不能再靠「誰在場」分先後）。

## F. Ray 自己改的（不是我動的）

`script/town.js` 的餐酒館路人單句換了幾句，另外**兩拍的 `auto:1000` 被拿掉**
（`capital.guild.lines[5]`、`capital.tavern.lines[2]`）。那兩拍沒有台詞也沒有 ▼，
`script_lint.py` 會各報一個提醒 —— 若那是刻意要「點一下才走」，忽略即可；
若是誤刪，把 `auto:1000` 加回去。

---

# 交接 · `ver -406`（打烊的店走不進去）

Ray：「打烊的店進不去，會直接在頁面跳賞金獵人公會，營業時間…等等」

- `go()` 在移動前先問目的地的 `isOpenNow()`：關著就**不移動、不推進時鐘**，
  就地浮一句 —— 名字欄放店名，內文是「打烊描述　營業時間 08:00 – 20:00」。
  走 `flashLine` ＋ `chatterOn`（路人單句那一套），所以「再點一下收掉」的節奏一致。
- **目的地字格上就標「（已打烊）」**（`nameOfNode`）—— 走過去才發現關門是白走一趟，
  而移動要花 10 分鐘遊戲內時間。
- 營業時間排字只有一支：`hoursText(n)`。

## ⚠ 順帶修掉一個既有的量測 bug

`refreshArrows()` 原本在 `showNav(true)` **之前**被呼叫，而 `#townNav` 沒有 `.on` 時
整層是 `display:none` —— 目的地字格量到 **0×0**，夾回畫面內那一段就退回預設的半寬 40px，
字格被擺在錯的位置（實測「武器店（已打烊）」左緣 −23，第一個字被切掉）。
以前字短剛好塞得進錯誤的位置所以看不出來。現在 `showNav(true)` 自己會呼叫
`refreshArrows()`（＝**顯示之後才量**）。

⚠ 這是量 rect 的通病，與 §6.5 那條「量不到就先不要擺」是同一件事：
**量之前先確認那個東西真的顯示著**。

---

# 交接 · `ver -407`（旅店兩顆行動鈕：擺開＋改樣式）

Ray 回報「回房睡覺跟獨自坐坐兩個會同時存在」，選擇「**兩顆都留，但擺開／改樣式**」。
問題不是「不該同時在」（那是 -405 他自己指定的），是**兩顆長得一模一樣**，
而它們的份量差很多：一個消磨兩小時、一個結束今天並存檔。

三件事：
1. **各帶一行時間成本**（`消磨 2 小時` / `到隔日 7:00・存檔`）。時間是資源，
   按之前就該看得到代價。⚠ 那行字寫在 `inn.js` 而不是資料上 —— 它是 `SIT_MIN` /
   `WAKE_HOUR` 的說明，兩者放在一起才不會改了常數忘了改字。
2. **睡覺那顆走金色實心的「主要動作」樣式**，坐坐維持空心暗底。
3. **位置拉到對角**：坐坐 `0.300/0.760`（左下茶桌）、睡覺 `0.620/0.440`（右上櫃台）。

## ⚠ 順帶修：行動鈕會壓到伙伴門

鈕錨在**背景圖**上（`bgPoint`，隨機器長寬比在圖上滑動），門欄錨在**畫面右緣**（CSS）
—— **兩套座標系**。挪到 0.620 之後，390×844 上睡覺鈕右緣 338、門欄左緣 328，疊了 10px。

修法不是改那個 0.620（換一台機器又會不一樣），是在 `relayout()` 擺好之後**夾一次**：
夾回畫面內 ＋ 不進門欄（門欄左緣現量）。⚠ 夾之前要確認鈕真的顯示著 —— 沒顯示的
元素量到 0×0（同 -406 那個字格的坑）。

---

# 交接 · `ver -408`（睡覺鈕回櫃台上方；坐坐不再消失）

## A. 鈕的位置會被讀成語意

Ray：「把回房睡覺固定櫃台上方，你這樣擺搞得好像要去跟蕾娜睡一樣，害我產生不必要的期待」

-407 為了「擺開」把睡覺鈕從 0.490 移到 0.620 —— 那個位置**正好在右側伙伴門欄的
蕾娜那一格旁邊**，讀起來就變成「進她的房間」。已改回 `0.490/0.450`（櫃台正上方）。

⚠ **教訓**：挪任何一顆鈕之前先看它會落在誰／什麼東西旁邊。兩顆鈕的區別交給
**樣式**（實心／空心）與時間成本那一行就夠了，不要靠拉遠位置。

## B. 「獨自坐坐又不見了」

原本 `wantSit = introDone() && st!=='slept'` —— 睡下之後就收起來。那個限制是 ver -392
寫的（當時它的用途只有「等蕾娜」），但**消磨兩小時本身就是一個行動**：ver -406 之後
城裡的店打烊就走不進去，「在旅店坐到店開門」是玩家真的需要的一步。

現在兩顆都是 `introDone()` —— 初入對白演完就有，之後一直都在。
⚠ 「還不能做」一律**不要靠藏起鈕**擋（鈕不見了玩家只會以為壞了），要像諾薇兒那樣
**開口擋**（`innEarly`）。

## C. 順帶修：一次性說明的鏈斷了

`maybeGuide()` 原本是 `if(wantSit)… else if(wantSleep)… else if(wait)…`。
`showGuide` 在「已經看過」或「量不到位置」時是**靜靜地 return**，而 else-if 只看前一個
**條件**成不成立 —— 坐坐一旦永遠成立（本版起），睡覺與敲門那兩個說明就再也輪不到了。
改成把候選排成佇列**逐個試到真的開起來為止**。

---

# 交接 · `ver -409`（獨自坐坐往上移；夾住不壓對話框）

Ray：「獨自坐坐壓到對話框了，往上移。**以後好感度往上坐一坐會有女角出來跟你互動，
所以一定要留。**」

- 落點由 `0.315/0.735` 改成 `0.345/0.605` —— 在**椅背上緣**，語意也對得上
  （要坐的就是那張椅子）。x 收在 0.345 是因為 0.395 會與上方的睡覺鈕橫向重疊一小段，
  疊成階梯狀又會被讀成一組選單（同 -407 那個坑）。
- ⚠⚠ **真正保證不壓到框的不是那個數字，是 `relayout()` 的夾**：下界＝對話框上緣。
  敲門的回話、路人單句都會把框叫出來，而鈕是常駐的。
  對話框錨在**楣的上緣**（`layoutKerberos` 那組變數），鈕錨在**背景圖**上 ——
  兩套座標系，換一台長寬比不同的機器就會撞。
  ⚠ `visibility:hidden` 的元素照樣量得到 box，所以框沒顯示時也夾得準。

**這是旅店這一區第三次踩「兩套座標系」**（-407 撞門欄、-409 撞對話框）——
現在 `put()` 一次夾三件：畫面內、門欄、對話框。再加任何常駐元件都要進那個夾。

---

# 交接 · `ver -410`（戰鬥中的副武器切換鈕）

Ray：「在敵我血條右側加入副武器切換鈕，大小同破防值，預設為機槍，按一下換霰彈，
兩下換步槍，切換時放音效 se_ui_sortie，發射中可切換，但要等發動中的反擊結束才會生效。
切換用牌卡輪換特效。」

## 輪換的是「類別」不是「武器」

Ray 講的是**機槍／霰彈／步槍**三種，所以按一下就跳到下一個**類別**。
- 類別的順序**由 `config.weapons` 的出現順序推出來**，不在程式裡寫死中文字串（鐵律 1）。
- 每個類別要拿哪一把記在 `lastByCat` —— 玩家在整備頁選過哪一把，這裡就給哪一把
  （買了「絞肉機 改」之後，機槍那一格自然變成它）。只認**持有中**的。
- 只有一個類別有槍時整顆藏起來：一顆按了不會變的鈕比沒有還糟。

## 「發射中可切換，但要等反擊結束才生效」

⚠⚠ 為什麼一定要延後：`critRate()` 是**每一發現查** `state.equippedWeapon` 的，
重機槍那一串是 8 發 × 90ms —— 中途換槍，後半串就用新槍的暴擊率，
**同一次反擊變成兩把槍混出來的傷害**。

所以：`counterBusy` 在重機槍那一串期間為 true，按下去只寫進 `pendingWeapon`，
`flushPending()` 才真的換。單發（狙擊）與齊發（散彈）是一瞬間，打完就直接 flush。
卡面**立刻**翻成新的那一把（要有回饋），邊框呼吸（`.pending`）表示「還沒生效」。
⚠ `startGame()` 要 `resetWeaponSwitch()` —— 排隊中的切換不可以跨場留著。

## 其他

- 音效走 `asset('sfx_start')` ＝ `se_ui_sortie.m4a`（與整備頁換卡同一支，本來就是那個鑰匙）。
- 牌卡翻面：`wsFlip` 340ms，**翻到 90°（卡面看不見）那一格才換內容** —— 才像同一張卡翻過去。
  時間常數 `WS_FLIP_MS` 與 CSS 同源，改一邊要改另一邊。
- **教學戰不出現**：那一場的裝備本來就是鎖死的（`stashTutorialLoadout` 強制機槍），
  而教學正在教機槍那一串反擊。這不是新規矩，是照既有的那個鎖。

---

# 交接 · `ver -411 / -412`（槍棺的吊墜）

Ray：「在槍棺左上方掛上這個吊墜，將吊掛點做成剛體與槍棺鎖死，鐵鍊與吊墜微微晃動，
槍棺上彈、開、閉的時候進行自然的劇烈擺動，在不擺動的狀況下不可碰觸到圓盤」
＋ 後續四條：「再長一點，再往右移一點」「平常移動出腳步聲時就要晃」
「顏色太像了，所以要動一動比較看得到」「左右開的時候也要跟著左邊的棺門移出」。

## 三層（不是一層）

同一個元素疊不了三個 transform 動畫，所以：

    #kerbPend   → 開門時跟著**左半扇**走同一個 px 位移（`--kerb-open-x`）
    .kp-swing   → 劇烈擺動（上彈 9° / 撞頂 22° / 開 15° / 閉 15° / 走路 6°）
    .kp-idle    → 一直在的微微晃動（±3.5°）＋ 偶爾掃過的高光

⚠ 兩層旋轉的 `transform-origin` 都在**吊掛點**（50% 0）—— 不同步的話兩個旋轉互相拉扯，
看起來像鏈子在伸縮。
⚠ 關門那一拍要靠 `kerb-shut` 把門的曲線借過來（ver -389 紋章踩過的同一個坑）。

## 「不可碰觸到圓盤」要對著**圓**，而且要用吊墜的**輪廓**

這個吊墜是 X 形：**最寬的地方在很上面**（y=0.21 半寬 0.49），往下越來越窄，
0.79 以下只剩一根細刺。而它掛在圓盤的**上方**，那一段圓周是縮進去的。

- 拿**外框**去閃**外框**：吊墜被壓成 52px，細節全糊。
- 拿**輪廓**去閃**圓**：89px，Ray 要的「再長一點、再往右」同時成立。

輪廓表（`KERB_PEND.prof`）是逐列量那張圖的 alpha 出來的，只列右半（圓盤在右邊）。
最大寬度是**解出來的**（由大往小試到第一個過關），不是填的數字 —— 換一台長寬比不同的
機器，圓盤的位置會變，答案跟著變。

## 顏色太像 → 靠「動」與「影子」分離

吊墜與槍棺是同一個材質色。所以：微晃振幅由 1.5° 加大到 **3.5°**、加深 drop-shadow、
再加一道**每 9 秒掃過一次**的高光（`kpShine`）。
⚠ 高光要**用吊墜自己的圖當遮罩**（`mask-image` 指同一張圖）——不遮的話那道光會掃在一個
矩形上，看起來像有人拿手電筒照玻璃。用 `screen` 混色：高光是加上去的光，不是白漆。
⚠ 「偶爾」是**比例**撐起來的（9 秒裡只有 1.1 秒在掃），不是把動畫放慢。

## 走路也要晃

`modules/town.js` 的 `stepSfx()`：腳步聲 ＋ `story.kerbPendSwing(6, 1.5)`。
⚠ 一支函式（鐵律 8）—— 走路的地方不只一處（走到隔壁節點、出航），兩件事要一起發生
就不要讓呼叫端各記得一次。幅度小（6°）才分得出「走路的震動」與「上彈那一下」。

## 兩個文件都要掛

飛行頁的 `#fkerbPend`（`FKERB_PEND` 是 `KERB_PEND` 的第二份，兩邊註解已互指）。
遭遇時門是在飛行頁推上來的，交棒之後主遊戲那一半才接手 ——
只有一邊掛的話交棒那一格會憑空多出一個東西。飛行頁沒有「開門」那一段，所以只有兩層。

## 素材

`resources/vfx/kerberos_pendant.webp`（原 PNG 已進 `_originals/kerberos/`）。
⚠ 鏈子的中心實測在 x=511.5/1024 ＝ 正中線，所以 `transform-origin:50% 0` 就是吊掛點。
⚠ 進了 `preloadStory` 的門素材清單 —— 它是進戰鬥那一刻才動起來的，沒先抓會是一片空白。
